<!-- Description: Repository changelog for Home Assistant + ESPHome work. -->
<!-- Version: 2026.07.17.20 -->
<!-- Last updated: 2026-07-17 -->

# Changelog

## 2026-07-17

- Runtime+Component/Interactables Roadmap Slice M4 Closeout Gate + Evidence Packet Cadence (`packages/spectra_ls_interactables.yaml`, `docs/roadmap/SPECTRA-REMOTE-INTERACTABLES-ROADMAP.md`, `docs/CHANGELOG.md`): add deterministic M4 stabilization closeout governance with explicit blockers/verdict sensors, closeout-ready binary gate, one-button M4 closeout packet capture, and timed auto-capture cadence for sustained-ready evidence. Includes compact closeout age telemetry and notification hygiene for blocked-vs-ready packet posture. Runtime track disposition: implemented (M4 closeout gate + packet automation in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive closeout determinism and operability hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M3 Acceleration + M4 Stabilization Starter (`packages/spectra_ls_interactables.yaml`, `docs/roadmap/SPECTRA-REMOTE-INTERACTABLES-ROADMAP.md`, `docs/CHANGELOG.md`): add a sustained M3 closeout cadence with freshness timestamping, auto-capture packet automation, explicit closeout-ready binary gate, and a new M4 starter stabilization cycle (notification hygiene + bounded counter reset + health packet). Includes configurable matrix-freshness threshold and compact runtime health-score telemetry to keep rollout pace high without sacrificing safety visibility. Runtime track disposition: implemented (additive closeout cadence and stabilization controls in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive rollout speed + operability hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M3 Closeout Packet + Reuse Gate (`packages/spectra_ls_interactables.yaml`, `docs/roadmap/SPECTRA-REMOTE-INTERACTABLES-ROADMAP.md`, `docs/CHANGELOG.md`): deliver a full M3 certification lane that computes reusable-contract proof (multi-source mapping + unique interactable count + matrix recency + activation gate posture), publishes deterministic closeout blockers/verdict sensors, and emits a single operator-facing M3 closeout packet for promotion evidence. Runtime track disposition: implemented (additive M3 closure governance and proof automation in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive M3 closeout determinism and reusable-contract evidence hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M2.8 M3 Activation Gate + Fresh Validation Packet (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add a guarded one-button M3 activation flow that enforces candidate/source-key validity, recent matrix evidence freshness, and cutover readiness before declaring multi-interactable activation readiness, with deterministic blocked/ready packet notifications and compact activation-summary capture for operator proof. In the same slice, fix the recurring HA warning on `input_text.spectra_interactable_source_map_json` by shrinking default source-map JSON payload shape to a max-length-safe baseline while preserving mapping semantics. Runtime track disposition: implemented (additive activation-governance and operability hardening in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive M3 activation safety and warning-free baseline hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M2.7 M3-Prep Bulk Matrix Validation + Results Packet (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add a one-button bulk matrix validator that exercises mapped source-key lanes plus non-remote candidate injection across configurable iterations, then emits compact pass/fail summary and blockers packet surfaces for accelerated multi-interactable onboarding readiness checks. Runtime track disposition: implemented (additive validation/operability hardening in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive M3 onboarding readiness hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M2.6 M3-Prep Multi-Interactable Generalization Harness (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add a synthetic non-remote interactable test lane (direct canonical event injection) with candidate contract-validity surfaces so onboarding and parity verification can proceed for additional interactable classes without waiting for live BLE source wiring. Runtime track disposition: implemented (additive validation/operability hardening in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive multi-interactable readiness hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M2.5 Evidence Packet + Go/No-Go Capture (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add a single operator-facing evidence packet surface with deterministic go/no-go verdict, blocker summary, and compact cutover telemetry snapshot capture flow for promotion decisions. Runtime track disposition: implemented (additive validation/operability hardening in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive evidence quality and decision determinism hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M2.4 Promotion Assist + Optional Auto-Promote (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add promotion-assist controls that surface active-preflight readiness in pilot mode and optional auto-promote behavior after sustained hold windows, enabling larger operational push-through with bounded safety gates. Runtime track disposition: implemented (additive rollout automation/operability hardening in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive operability hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M2.3 Soak-Gated Promotion + Throttled Auto-Remediation (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add sustained-readiness soak gating for active cutover promotion, plus bounded auto-remediation controls (cooldown and per-hour cap) to prevent rollback/remediation thrash during noisy quality windows. Runtime track disposition: implemented (additive rollout-governance/safety hardening in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive safety/operability hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M2.2 Safe Active Activation + Notification Lifecycle (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add guarded one-button active cutover activation, stabilize rollback/preflight notifications with deterministic IDs, and add readiness-based notification cleanup for lower operational noise during staged promotion from pilot to active mode. Runtime track disposition: implemented (additive rollout safety/operability hardening in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive safety/governance hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M2.1 Safe Pilot Activation + Rollback Notifications + Auto Profile Guidance (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add cutover guidance and guarded activation UX by introducing an auto profile suggestion surface, a one-button safe pilot activation flow with preflight gating, and persistent notification emission on cutover safety rollback events. Runtime track disposition: implemented (additive rollout safety/operability hardening in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive safety/governance/operability hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M2 Cutover Profiles + Safety Rollback + Stress Harness (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add operator profile controls for staged bridge cutover posture (`disabled`, `shadow`, `pilot`, `active`), automatic rollback safety guard that reverts to shadow mode when cutover readiness degrades for a sustained window, and a configurable stress-test runner for repeated mapped source-signal execution under load. Runtime track disposition: implemented (additive control-plane safety and rollout tooling in HA package lane). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive rollout governance/safety and validation hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M1 Latency Metrics + Cutover Readiness Gate (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add bridge timing telemetry and go/no-go readiness evaluation for staged BLE cutover, including ingest/dispatch timestamp tracking, dispatch-lag metric surfaces, and a binary cutover-ready posture derived from bridge mode, source-intake state, and dispatch/drop/invalid quality thresholds. Runtime track disposition: implemented (additive rollout timing/quality instrumentation). Component track disposition: compatibility-shimmed (component execution sink unchanged; no `custom_components/spectra_ls` service semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive cutover-governance hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M1 Multi-Signal Validation Bundle Runner (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add operator-triggerable bundle validation harness that exercises multiple mapped source signals plus replay in one action for quicker staged verification without waiting on live BLE pulses. Runtime track disposition: implemented (additive validation tooling for bridge rollout). Component track disposition: compatibility-shimmed (execution sink unchanged; no component semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive validation acceleration and rollout confidence hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M1 Test/Replay Harness (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add operator-invokable test and replay scaffolding for interactables rollout validation, including source-key test emit helpers, replay of last processed event payload, and input-button triggers for deterministic bridge testing without waiting for live BLE pulses. This improves staged migration confidence while keeping bridge/source toggles and component execution semantics unchanged. Runtime track disposition: implemented (additive test/replay tooling in HA package path). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive validation tooling and rollout safety hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M1 Observability + Readiness Surfaces (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): add operator-facing bridge readiness and quality telemetry for staged BLE rollout, including normalized last-event fields, aggregate dispatch/drop/invalid percentage sensors derived from bridge counters, and binary readiness states for bridge-active posture and source-intake enablement. This keeps rollout safety measurable without changing underlying component control-center execution semantics. Runtime track disposition: implemented (additive observability/readiness surfaces in HA interactables package). Component track disposition: compatibility-shimmed (execution sink remains `spectra_ls.execute_control_center_input`; no `custom_components/spectra_ls` semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive diagnostics and rollout-governance hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M1 Source-Mapping Generalization + Second BLE Hook (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): generalize BLE source onboarding by adding a JSON-driven source-map helper and a reusable source-signal processing script that resolves source keys to canonical interactable payloads before dispatching to `spectra_ls.execute_control_center_input`. In the same slice, add a second opt-in live BLE source hook (`binary_sensor.ble_presence_door_cory_ble_arriving`) alongside the existing button-single source so future interactables can be added through mapping entries rather than one-off action payload hardcoding. Runtime track disposition: implemented (generic source-map bridge lane + second guarded BLE source). Component track disposition: compatibility-shimmed (component execution contract reused unchanged; no `custom_components/spectra_ls` service semantic mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive source onboarding scalability and staged BLE ingest expansion only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M1 Real BLE Source Hook (opt-in) (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): wire the first live BLE-origin source into the interactables bridge by adding an opt-in automation trigger on existing BLE button-single sensor pulses (`binary_sensor.ble_presence_door_cory_ble_button_single`) that dispatches canonical bridge events to `spectra_ls.execute_control_center_input` via the processing script. Hook is disabled by default through a dedicated source-enable helper to preserve current behavior until explicitly enabled for staged rollout. Runtime track disposition: implemented (first real BLE source ingest path added behind guard). Component track disposition: compatibility-shimmed (component execution service contract reused; no semantic mutation of `custom_components/spectra_ls` control-center execution behavior). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive real-source wiring for staged BLE bridge validation only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Interactables Roadmap Slice M0-M1 Scaffold Activation (`packages/spectra_ls_interactables.yaml`, `docs/CHANGELOG.md`): start coding against the new Spectra Remote + Interactables roadmap by adding a disabled-by-default Home Assistant bridge scaffold for canonical interactable events (`spectra_ls_interactable_event`) into `spectra_ls.execute_control_center_input`, including enable/shadow helpers, event counters, maintenance-window timer, and bounded validation/dedupe/drop accounting for future BLE-first ingest lanes. Runtime track disposition: implemented (HA runtime scaffold activation for interactable ingest and observability). Component track disposition: compatibility-shimmed (existing `custom_components/spectra_ls` execution contract reused; no behavior mutation to component service semantics in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive bridge scaffolding and observability hardening only. README/wiki parity: no material operator workflow change.

- Docs/Roadmap Spectra Remote + Interactables Planning Baseline (`docs/roadmap/SPECTRA-REMOTE-INTERACTABLES-ROADMAP.md`, `docs/CHANGELOG.md`): publish the planning source-of-truth roadmap for BLE-first remote evolution with long-press Wi-Fi maintenance posture, HA ingest/mapping workstream, phased validation gates, and an extensible canonical interactable event contract so future Spectra interactables can onboard without one-off control logic. Runtime track disposition: compatibility-shimmed (planning/docs-only slice; no runtime behavior mutation). Component track disposition: compatibility-shimmed (planning/docs-only slice; no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; roadmap/governance clarity hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Remote Subscription-First Route State + SoC-Only Telemetry Profile (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): reduce battery overhead by removing optimistic 15-second `homeassistant.update_entity` polling of route/host contract entities and relying on Home Assistant state-subscription updates (`homeassistant_states`) as the primary change path. In the same slice, trim non-essential remote telemetry surfaces by removing published comms-posture, battery-voltage, and Wi-Fi RSSI entities so only battery SoC remains exposed, with SoC cadence set to 25 seconds. Runtime track disposition: implemented (active remote runtime battery/traffic optimization in Wi-Fi/API lane). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed as not-applicable to component lane). P1/P2/P3 impact: no source-of-truth ownership reassignment; bounded remote transport/telemetry overhead reduction only. README/wiki parity: no material operator workflow change.

## 2026-07-16

- Runtime/Remote Compile Warning Cleanup (turn interval log format types) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): resolve ESPHome compile warnings in remote volume request diagnostics by aligning `turn_ms` log format specifiers with platform type width (`%lu` + explicit cast) for `turn_interval_ms` in both volume-up and volume-down paths. Behavior/logic is unchanged; this is a compile-signal hygiene fix to keep build output clean and avoid type-format drift across toolchains. Runtime track disposition: implemented (remote runtime compile-warning hygiene hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed as not-applicable to component lane). P1/P2/P3 impact: no source-of-truth ownership reassignment; bounded compile/log-format correctness hardening only. README/wiki parity: no material operator workflow change.

- Runtime/HA Recorder High-Churn Sensor Exclusion Tightening (`configuration.yaml`, `docs/CHANGELOG.md`): reduce recorder write pressure and disk growth by adding targeted `recorder.exclude.entity_globs` filters for high-frequency BLE telemetry patterns (notably `sensor.*_estimated_distance`, `sensor.*_rssi`, `sensor.*_signal_strength`, plus common `last_seen`/`linkquality` diagnostics) while preserving 3-day retention policy (`purge_keep_days: 3`) and existing control/automation contract entities. Runtime track disposition: implemented (runtime recorder filter hardening for active HA path). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed as non-owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; bounded storage/performance hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Remote Deep-Sleep Behavioral Rollback + Wake-Path Optimization (Encoder-Idle 30s + Turn-Only Wake) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): revert remote power policy from playback/time-of-day/wakelock-expanded behavior to the prior deterministic interaction model: enter deep sleep after 30 seconds of encoder inactivity and wake on encoder rotation inputs. This removes lingering playback/passthrough/day-night sleep suppression paths that could keep the remote awake and drain battery, narrows EXT1 wake scope to encoder turn pins only (excluding encoder-press wake), and uses ESP32-C6-compatible single-channel encoder wake (`encoder_pin_a`, `ANY_LOW`) to reduce false wake loops from multi-pin wake noise while preserving intentional wake-by-turn behavior. Runtime track disposition: implemented (remote runtime deep-sleep rollback + wake-path hardening in active path). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed as not-applicable to component lane). P1/P2/P3 impact: no source-of-truth ownership reassignment; bounded remote power-policy and wake-path hardening for battery preservation and practical wake reliability. README/wiki parity: no material operator workflow change.

## 2026-07-07

- Runtime/HA WiiM Startup Protocol Preflight Selection Hardening (`custom_components/wiim/__init__.py`, `docs/CHANGELOG.md`): add bounded runtime protocol preflight probing during config-entry setup so host reachability determines protocol hint selection (`http` vs `https`) instead of relying only on cached endpoint/capability preference. This addresses HTTPS-only host scenarios (for example `.50`) that could stall setup when stale/default HTTP-first assumptions are wrong. Setup now probes preferred protocol first, falls back to the opposite protocol when needed, and persists the verified endpoint hint for subsequent startups. Runtime track disposition: implemented (startup protocol-selection resilience hardening in active WiiM integration path). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; startup reliability and integration bootstrap hardening only. README/wiki parity: no material operator workflow change.

## 2026-07-05

- Component/Now-Playing Passthrough Selector Freshness Gate for Paused-Origin Candidates (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): fix stale paused-origin passthrough candidates being eligible indefinitely (for example old AppleTV/YouTube carrier metadata), which could win selector ranking and prevent active now-playing freshness from propagating to OLED state gates. Paused-origin passthrough promotion now requires recent signal evidence (`recent_play_progress` or `recent_paused_progress`) in addition to app/title hints, while preserving existing playing-origin stale fallback behavior for bounded passthrough resilience. Runtime track disposition: compatibility-shimmed (runtime render/state logic unchanged; equivalent failure mode reviewed against component now-playing contracts). Component track disposition: implemented (selector intake freshness hardening in metadata owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; stale paused candidate suppression and now-playing freshness integrity hardening only. README/wiki parity: no material operator workflow change.

## 2026-07-04

- Docs/Hardware Dual-Board Pin Map (Amped + HiFi ESP32 Plus S3) (`docs/hardware/AMPED-HIFI-ESP32-PLUS-S3-PIN-MAP.md`, `docs/hardware/WIRING-LAYOUT-PROTOCOL.md`, `docs/spectra_ls_system/SPECTRA-LS-AMPED-COMBINED-CONFIG.md`, `docs/README.md`, `docs/wiki/System-Architecture.md`, `docs/CHANGELOG.md`, `CHANGELOG.md`, `esphome/CHANGELOG.md`): publish a canonical pin map for the parallel Amped combined lane and HiFi ESP32 Plus S3 hardware profile using current authoritative runtime pin contracts (OLED I2C, RP2040 UART bridge, Arylic/HiFi UART path, RP2040 expander/encoder/ADC surfaces) and explicitly mark active combined configs as no-rings posture. Explicitly document that the current `amped_dock` profile is backend/profile behavior only and does not introduce a separate GPIO remap in this slice. Runtime track disposition: implemented (operator/developer hardware mapping clarity for active ESP runtime lane). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; documentation/operations hardening only. README/wiki parity: updated in same change set.

- Docs/ESP Parallel Combined Config Documentation Pack (`docs/spectra_ls_system/SPECTRA-LS-AMPED-COMBINED-CONFIG.md`, `docs/README.md`, `docs/wiki/Home.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/wiki/Install-on-Your-Own-HA.md`, `esphome/CHANGELOG.md`, `docs/CHANGELOG.md`, `CHANGELOG.md`): publish extensive operator/developer documentation for the new parallel combined ESPHome entrypoint (`esphome/spectra_ls_system_amped_combined.yaml`) and bridge overlay package (`esphome/spectra_ls_system/packages/spectra-ls-amped-bridge.yaml`), including architecture intent, profile controls, validation/rollback workflow, and migration posture. Added a dedicated ESP-specific changelog surface to keep firmware/config-lane history explicit and separated from general docs/governance entries. Runtime track disposition: implemented (documentation + changelog traceability for additive parallel lane; active baseline runtime unchanged). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; documentation/operational clarity and changelog governance hardening only. README/wiki parity: updated in same change set.

- Runtime/ESP Parallel Combined Config Baseline (Spectra + Amped Integration Track) (`esphome/spectra_ls_system_amped_combined.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-amped-bridge.yaml`, `README.md`, `docs/CHANGELOG.md`): add a new parallel ESPHome entrypoint for an Amped-focused combined track while keeping current `esphome/spectra_ls_system.yaml` unchanged as the active baseline. The new entrypoint imports the existing Spectra runtime and overlays a bridge package for backend/profile toggling and phased Amped integration hooks (without forcing immediate hardware-path cutover). Runtime track disposition: implemented (new additive config lane; current runtime lane preserved). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive parallel-track scaffolding only. README/wiki parity: README updated with discoverability note.

- Runtime/Snail-AC Cool-Only Guard Watchdog Follow-Up (`packages/dst_tuya_ac.yaml`, `docs/CHANGELOG.md`): harden the DST `auto` -> `cool` guard by adding a lightweight periodic watchdog trigger so correction still applies when state-transition events are not emitted consistently by the underlying climate entity. Manual validation confirmed guard action path works; watchdog ensures deterministic enforcement in steady-state runtime. Runtime track disposition: implemented (runtime guard reliability hardening). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime guard reliability hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Snail-AC DST Auto-Mode Cooling Mismatch Guard (`packages/dst_tuya_ac.yaml`, `docs/CHANGELOG.md`): fix unexpected continuous cooling behavior when `climate.snail_ac_dst` restores/enters `auto` mode in this cool-only package profile. Live validation showed DST `auto` could report `hvac_action=cooling` while below target, whereas switching to `cool` immediately returned expected idle/cycle behavior. Added an explicit runtime guard automation that coerces DST `auto` to `cool` on startup and on `auto` mode entry for this package posture. Runtime track disposition: implemented (runtime package control-mode guard hardening). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime thermostat-mode safety hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Snail-AC DST Auto-Mode Setpoint Sync Fix (`packages/dst_tuya_ac.yaml`, `docs/CHANGELOG.md`): fix cool-offset sync not applying when DST climate is in `auto` mode (with active `hvac_action=cooling`), which could leave Tuya target temperature stuck at stale/minimum values and overcool relative to DST target intent. Setpoint-sync triggers/conditions now include DST cooling-action transitions and allow sync when DST is either `cool` or `auto` while actively cooling, preserving DST-first control ownership and existing offset logic. Runtime track disposition: implemented (runtime package sync-gating correction). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime sync correctness hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Remote Volume Responsiveness Retune from Live Encoder Telemetry (`esphome/spectra_ls_remote.yaml`, `docs/program/SPECTRA-LS-REMOTE-SCOPE-PROGRESS.md`, `docs/CHANGELOG.md`): follow-up tuning after live logs showed many real-world turns still classifying into slow-step behavior (`step_pct=2.0`) and feeling under-responsive in both slow and medium-fast gestures. Increased baseline and fast acceleration gains, widened fast-turn detection window, and modestly tightened encoder gate timings so more practical turn cadence maps to stronger volume movement while keeping single-step anti-bounce protections intact. Runtime track disposition: implemented (interaction responsiveness retune in active remote runtime path). Component track disposition: compatibility-shimmed (component contracts unchanged; remote remains HA-contract + direct-send consumer). P1/P2/P3 impact: no source-of-truth ownership reassignment; UX responsiveness hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Remote Adaptive Volume Acceleration + Playback-Aware Sleep Profile Expansion (`esphome/spectra_ls_remote.yaml`, `packages/spectra_ls_setup.yaml`, `docs/program/SPECTRA-LS-REMOTE-SCOPE-PROGRESS.md`, `docs/CHANGELOG.md`): add detent-speed-adaptive volume step sizing (slow turns use low gain, fast turns use higher gain), plus a Home Assistant helper-backed acceleration toggle so operators can enable/disable acceleration live without firmware reflashing. Added the tracked setup helper surface `input_boolean.spectra_ls_remote_volume_acceleration` for default HA availability. Expanded battery sleep policy with playback-stop linger hold, passthrough-source wake-lock posture, and time-of-day idle thresholds (more aggressive quiet-hours idle threshold, more forgiving daytime threshold) while preserving existing HA-contract resolution and direct send to control-host endpoint architecture. Runtime track disposition: implemented (remote UX/power policy hardening in active runtime path). Component track disposition: compatibility-shimmed (component contracts unchanged; remote consumes HA helper/now-playing/source contracts as read-only policy signals). P1/P2/P3 impact: no source-of-truth ownership reassignment; interaction and power-policy behavior hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Remote Playback-Aware Sleep Suppression (`esphome/spectra_ls_remote.yaml`, `docs/program/SPECTRA-LS-REMOTE-SCOPE-PROGRESS.md`, `docs/CHANGELOG.md`): prevent aggressive battery sleep from interrupting active listening by gating remote deep-sleep entry when HA now-playing state is `playing`. The remote still uses existing idle timeout + quiet-hours/daytime sleep policy, but sleep is deferred while playback is active so control continuity is preserved during listening sessions. Runtime track disposition: implemented (sleep gate hardening in active remote runtime path). Component track disposition: compatibility-shimmed (component contracts unchanged; remote consumes HA now-playing contract as read-only sleep signal). P1/P2/P3 impact: no source-of-truth ownership reassignment; power-policy behavior hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Remote Rapid-Rotation Gate Tuning (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): tighten responsiveness for fast knob turns by reducing same-direction burst lockout and encoder emit gate timings while keeping single-step anti-bounce safeguards. This balances smooth single-detent behavior with less skipped movement during quick continuous rotation, without changing HA contract resolution or direct control-host endpoint send architecture. Runtime track disposition: implemented (rapid-turn responsiveness tuning in active remote runtime path). Component track disposition: compatibility-shimmed (component contracts unchanged; remote remains direct-send consumer of HA route contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; interaction responsiveness hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Remote Single-Detent Burst-Lock Hardening (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix residual multi-step jumps where one physical detent could still produce multiple same-direction `detent (1)` callbacks within a short burst window due to encoder bounce/chatter. Added same-direction detent lockout timing in the remote encoder emit path so one physical movement maps to one logical step more consistently while preserving HA contract resolution and direct-send routing to the WiiM control host endpoint. Runtime track disposition: implemented (encoder burst suppression hardening in active remote runtime path). Component track disposition: compatibility-shimmed (component contracts unchanged; remote remains direct-send consumer of HA route contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; input determinism hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Remote Encoder Single-Step Emit Smoothing (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix non-reproducible volume jumps from burst/multi-detent rotary deltas (for example single physical detent events occasionally arriving as `delta>1`) by enforcing single logical detent steps per encoder emit in the active direct-send path. This preserves HA contract-target resolution and direct control-host send behavior while making per-detent volume movement smooth, predictable, and repeatable. Runtime track disposition: implemented (single-step detent policy hardening in remote runtime path). Component track disposition: compatibility-shimmed (component contracts unchanged; remote remains direct-send lane consumer of HA route contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; interaction determinism hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Remote Direct-Send Functional Rollback + Handler-Scope Correction (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): restore direct TCP send path for encoder volume and encoder-press mute behavior after validating operator requirement that remote functional control lane must remain direct-send capable. This rollback keeps encoder handler structure updates but reinstates route host/port contract sensors, guarded direct-send scripts, and periodic route refresh support needed for established runtime behavior. Runtime track disposition: implemented (direct-send functionality restored in active remote runtime path). Component track disposition: compatibility-shimmed (HA control-center service contract remains available; no component contract removal). P1/P2/P3 impact: no source-of-truth ownership reassignment; functional rollback to preserve required control behavior. README/wiki parity: no material operator workflow change.

- Runtime/Remote Legacy Direct-Transport Lane Retirement (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): remove remaining legacy direct volume/mute transport logic and route-host cache plumbing from remote firmware after control-center event-emitter cutover. Remote now keeps a thin input role (encoder turn/press -> `spectra_ls.execute_control_center_input`, plus next-track passthrough guard) and no longer maintains embedded TCP control-host/port send state or periodic host-refresh polling. Runtime track disposition: implemented (legacy remote direct-transport lane removed). Component track disposition: compatibility-shimmed (existing HA/component control-center mapping/execution stack remains policy owner with no contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; dead-path retirement and maintainability hardening only. README/wiki parity: no material operator workflow change.

- Runtime/Remote Encoder Basic Handler Routing to HA Control-Center Stack (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): simplify active remote encoder handling by routing encoder turn/press events to the Home Assistant control-center execution service (`spectra_ls.execute_control_center_input`) instead of performing target/route/volume policy logic directly inside remote firmware handlers. This keeps remote behavior as a thin input-event emitter while HA/component stack remains the source of truth for mapped action policy and target execution decisions. Runtime track disposition: implemented (remote handler simplified to event emission). Component track disposition: compatibility-shimmed (existing control-center execution path already handles event mapping/action policy; no behavior contract change required in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; input-handler simplification and policy-centralization hardening only. README/wiki parity: no material operator workflow change.

- Repository legacy non-Spectra climate artifact cleanup (`docs/README.md`, `docs/CHANGELOG.md`, `docs/control-py/NOTES-control-board-2.md`, `docs/notes/NOTES-control-board-2.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): removed legacy non-Spectra package/docs artifacts and scrubbed remaining climate-side references from active docs/roadmap surfaces while preserving Spectra-only runtime/component records.

- Custom Component/Slice-AutoCoordinator Coordinator Refresh+Validation Super-Chunk Extraction (`custom_components/spectra_ls/refresh_validation_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract coordinator refresh/validation service orchestration into dedicated `RefreshValidationFabricWorkflow` boundary methods (`_refresh_snapshot`, deferred refresh callback handling, `async_rebuild_registry`, `async_validate_contracts`, `async_dump_route_trace`, `async_validate_selection_handoff`, `async_validate_capability_profile`, `async_validate_action_catalog`, `async_validate_crossfade_balance`, and `async_validate_scheduler`) and rewire coordinator methods to thin delegation wrappers while preserving service and callback contracts. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (refresh/validation super-chunk lane moved from inline coordinator monolith to dedicated workflow module). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-AutoCoordinator Coordinator Refresh+Validation Super-Chunk Extraction Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open the next super-macro decomposition cycle to extract coordinator refresh/validation service orchestration (`_refresh_snapshot`, deferred refresh callback handling, `async_rebuild_registry`, `async_validate_contracts`, `async_dump_route_trace`, `async_validate_selection_handoff`, `async_validate_capability_profile`, `async_validate_action_catalog`, `async_validate_crossfade_balance`, and `async_validate_scheduler`) from `custom_components/spectra_ls/coordinator.py` into a dedicated workflow boundary module and rewire coordinator to thin delegation wrappers after extraction. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this slice step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; architecture hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Runtime/ESP Lighting Slider No-Op Root-Cause Fix (raw analog normalization) (`esphome/spectra_ls_system/packages/spectra-ls-lighting.yaml`, `docs/CHANGELOG.md`): fix room-selection lighting slider no-op behavior (no light changes and no OLED brightness feedback) caused by unnormalized raw analog slider values from RP2040 (`0..65535`) being clamped directly to `100`, which made subsequent slider movement appear unchanged and short-circuited update flow. Lighting slider processing now normalizes raw values to `0..100` before clamping/apply logic, matching existing volume/EQ analog normalization posture. Runtime track disposition: implemented (active ESP lighting input path corrected). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; input normalization correctness and UI feedback continuity hardening only. README/wiki parity: no material operator workflow change.

## 2026-07-02

- Runtime/HA WiiM v1.0.94 Compliance Follow-Up (HTTPS-only capability recovery parity) (`custom_components/wiim/__init__.py`, `docs/CHANGELOG.md`): align local WiiM setup endpoint-healing logic with upstream `wiim` v1.0.94 behavior by restoring HTTPS-only capability detection and endpoint recovery (`https://host:443`) for cached HTTPS-only profiles, while retaining local pywiim-version guard that prevents modern `pywiim 2.2.19` setups from reapplying legacy HTTP-first stale-endpoint drops. Runtime track disposition: implemented (upstream compliance parity + local startup anti-thrash guard retained). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; endpoint recovery correctness and startup stability hardening only. README/wiki parity: no material operator workflow change.

- Runtime/HA WiiM HTTPS Endpoint Autoheal Loop Guard (pywiim-aware) (`custom_components/wiim/__init__.py`, `docs/CHANGELOG.md`): stop legacy stale-HTTPS endpoint autoheal logic from fighting current pywiim protocol handling by gating the HTTPS-drop workaround to legacy pywiim versions only (`<2.2.15`). On current pinned pywiim (`2.2.19`), the integration now preserves cached HTTPS endpoints instead of repeatedly dropping/re-probing based on cached `protocol_priority`, eliminating repeated setup churn loops observed on `192.168.10.50` while retaining backwards-safe behavior for old pywiim builds. Runtime track disposition: implemented (WiiM startup-loop/root-cause hardening in active integration path). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; startup lifecycle stability and protocol-authority correctness hardening only. README/wiki parity: no material operator workflow change.

## 2026-06-30

- Runtime/Remote Encoder Snappy+Stable Tuning (detent preservation + cadence) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): improve remote volume feel by preserving encoder delta magnitude per emit (multi-detent turns now apply proportional volume change in one guarded write), tightening emit/cooldown cadence for faster response, and slightly increasing per-step gain. Added bounded detent-per-emit clamp for stability under burst/noise while avoiding the prior “fast turns feel flattened” behavior. Runtime track disposition: implemented (active remote input/write-path responsiveness hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed as not-applicable to component path). P1/P2/P3 impact: no source-of-truth ownership reassignment; interaction-feel and control responsiveness hardening only. README/wiki parity: no material operator workflow change.

- Runtime/HA WiiM Expected UPnP Timeout Warning Rate-Limit Hardening (`custom_components/wiim/coordinator.py`, `docs/CHANGELOG.md`): keep pywiim UPnP timeout/init failures visible while preventing log-flood churn by applying host-scoped cooldown throttling on known repetitive UPnP timeout patterns from `pywiim.upnp.client` (instead of full suppression). This preserves actionable signal for real troubleshooting and avoids warning storms masking unrelated issues. Runtime track disposition: implemented (targeted warning-rate limiting in active WiiM integration path). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; observability signal-quality hardening only. README/wiki parity: no material operator workflow change.

- Runtime/HA WiiM Setup-State Reload Gate Hardening (`custom_components/wiim/__init__.py`, `docs/CHANGELOG.md`): follow-up hardening for persistent WiiM `setup_in_progress` loops by enforcing that config-entry update-listener reloads are only allowed when the entry is fully `loaded`. Updates received during `setup_in_progress`, `not_loaded`, or unload transitions are now treated as setup/runtime churn and ignored, preventing recursive reload feedback while setup-time endpoint/capability/title cache writes occur. Runtime track disposition: implemented (setup-state reload gate hardening). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; startup lifecycle stability hardening only. README/wiki parity: no material operator workflow change.

- Runtime/HA WiiM Setup-In-Progress Loop Recurrence Guard (`custom_components/wiim/__init__.py`, `docs/CHANGELOG.md`): fix recurring WiiM config-entry startup loop where setup-time `async_update_entry` mutations (endpoint/capabilities/title cache writes) could still trigger `async_reload` before `options_snapshot` was initialized, leaving entries stuck in repeated setup churn (`setup_in_progress`). Update listener now explicitly treats pre-snapshot updates as setup-time and ignores them while seeding the snapshot baseline, and only reloads once a real options-change delta exists after setup completes. Runtime track disposition: implemented (setup lifecycle recursion guard hardening). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; startup lifecycle stability hardening only. README/wiki parity: no material operator workflow change.

- Component/Auto-Select Sticky-Current Target Override for Detected Active Receiver (`custom_components/spectra_ls/selection_fabric.py`, `docs/CHANGELOG.md`): fix target stickiness where helper current target could remain pinned when it was still valid-in-options but idle, even while detected receiver candidate was resolved and actively playing (for example passthrough on `media_player.kitchen_wiim`). Auto-select scaffold now allows detected-active promotion over idle current target in this bounded posture instead of unconditionally preserving helper-current stickiness. Runtime track disposition: compatibility-shimmed (no runtime package/esphome behavior mutation; equivalent failure mode verified through shared helper/contract surfaces). Component track disposition: implemented (selection apply-policy hardening in component owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; target handoff correctness and host-route alignment hardening only. README/wiki parity: no material operator workflow change.

## 2026-06-23

- Runtime/ESP Arylic TCP Connect-Timeout Stability Tuning (`esphome/spectra_ls_system/substitutions.yaml`, `docs/CHANGELOG.md`): fix recurring `connect() timeout` churn under rapid volume-pot sweeps (visible despite reachable `:8899` control port) by increasing the Arylic TCP per-attempt timeout budget so short-lived network jitter and target accept latency no longer fail aggressively at the previous narrow window. This preserves existing queue/coalescing/send semantics and target authority routing while reducing false transport-failure bursts (`reason=connect_timeout`) and improving control continuity during fast interaction windows. Runtime track disposition: implemented (active ESP transport timeout-budget hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed via component host/port authority surfaces). P1/P2/P3 impact: no source-of-truth ownership reassignment; transport stability hardening only. README/wiki parity: no material operator workflow change.

- Runtime/HA WiiM Setup-In-Progress Reload-Loop Guard Fix (`custom_components/wiim/__init__.py`, `docs/CHANGELOG.md`): fix WiiM config entries getting stuck in `setup_in_progress` by tightening config-entry update listener behavior. Listener now reloads only when entry options actually change, and ignores setup-time `async_update_entry` mutations (capability cache, endpoint persistence, title normalization) that previously triggered recursive reload attempts during setup. Runtime track disposition: implemented (WiiM setup lifecycle correctness hardening). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; startup lifecycle stability hardening only. README/wiki parity: no material operator workflow change.

- Runtime/ESP Arylic TCP Transient-Errno Log-Noise Hardening (`esphome/spectra_ls_system/components/arylic_tcp.h`, `docs/CHANGELOG.md`): reduce repeated warning spam during transient socket churn (for example disconnect/reconnect windows that can surface transport errno noise such as `128`) by classifying expected transient socket errors and throttling them to debug-level cadence while preserving warning-level visibility for non-transient failures. This keeps failure telemetry actionable without flooding logs during expected reconnect behavior and does not change send/retry/backoff semantics. Runtime track disposition: implemented (transport observability hardening in active ESP helper path). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; observability-noise reduction only. README/wiki parity: no material operator workflow change.

- Component/Metadata Prep Snapshot Crash Fix — mismatch computation ordering (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): fix recurring `UnboundLocalError` during snapshot/validation refresh (`cannot access local variable 'control_target_vs_now_playing_mismatch'`) caused by reading control/meta mismatch booleans before those canonical-target mismatch fields were computed in `build_metadata_prep_validation`. Reordered canonical alias/target mismatch synthesis to execute before divergence/alignment/cause calculations so snapshot packets are always built with initialized mismatch values. Runtime track disposition: compatibility-shimmed (no runtime package/esphome behavior mutation; equivalent consumer failure mode reviewed). Component track disposition: implemented (metadata prep packet build-order correctness hardening in owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; crash-path elimination and packet determinism hardening only. README/wiki parity: no material operator workflow change.

- Validation/OLED Template Non-Audio Handoff False-WARN Suppression (`docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): fix diagnostics drift where non-audio OLED posture (for example `lighting|oled:LIGHTING`) could still emit WARN due to `esp_control_handoff_not_ready` even though now-playing assumptions are not applicable outside audio mode. Root-cause classification now recognizes non-audio mode without requiring SRC fallback, and handoff-not-ready warning gating is restricted to audio mode only. Runtime track disposition: compatibility-shimmed (diagnostics-template-only behavior; no runtime package/esphome mutation). Component track disposition: compatibility-shimmed (diagnostics-template-only behavior; no `custom_components/spectra_ls` mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; validation signal-quality hardening only. README/wiki parity: no material operator workflow change.

- Runtime/ESP UI Option-Parsing Helper Deduplication (`esphome/spectra_ls_system/components/sls_menu_nav.h`, `esphome/spectra_ls_system/packages/spectra-ls-ui.yaml`, `docs/CHANGELOG.md`): remove repeated inline options parsing lambdas from multiple UI/menu lambdas by introducing shared `sls_menu_nav` helpers for trim + parse list + count operations, then rewiring menu-count/menu-select/encoder flows to consume the same parser contract. This reduces duplicated parser logic and lowers drift risk for quote/bracket/comma handling across room/target/meta/control-target option feeds. Runtime track disposition: implemented (active ESP UI parsing path deduplicated with shared helper contract). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent consumer failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; internal parser-consistency and maintainability hardening only. README/wiki parity: no material operator workflow change.

- Component/Metadata Selector Helper Deduplication (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): simplify selector internals by extracting reusable entity metadata helper methods and using them in the now-playing selector ranking flow, while preserving pool/ranking semantics and winner outputs. This removes repeated per-entity metadata extraction logic and reduces future drift risk when selector scoring fields are adjusted. Runtime track disposition: compatibility-shimmed (no runtime package/esphome behavior mutation; equivalent failure mode reviewed). Component track disposition: implemented (internal selector-helper deduplication only; no public contract changes). P1/P2/P3 impact: no source-of-truth ownership reassignment; maintainability and consistency hardening only. README/wiki parity: no material operator workflow change.

- Component/Selection-Fabric Guarded-Write Boilerplate Simplification (`custom_components/spectra_ls/selection_fabric.py`, `docs/CHANGELOG.md`): simplify repeated guarded-write result initialization and standard-guard invocation patterns by introducing shared internal helpers in the selection workflow while preserving existing status/reason contracts and write-attempt stamping semantics. This reduces duplicate control-flow scaffolding across scheduler/apply/track/restore/cycle/set-active methods and makes future safety-gate changes less error-prone. Runtime track disposition: compatibility-shimmed (no runtime package/esphome behavior mutation; equivalent failure mode reviewed). Component track disposition: implemented (internal workflow simplification only; no public contract changes). P1/P2/P3 impact: no source-of-truth ownership reassignment; maintainability and consistency hardening only. README/wiki parity: no material operator workflow change.

- Component+Validation/Phased A-D Canonical Metadata Contract Simplification (`custom_components/spectra_ls/metadata_stack.py`, `docs/testing/raw/oled_now_playing_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/CHANGELOG.md`): start and execute the next simplification pass by introducing canonical metadata-prep summary fields at source (control/meta/now-playing alignment posture, OLED contract posture, and deterministic cause hints), then shifting diagnostics-template consumption to component packet canonicals first with bounded fallback-only legacy parsing. This reduces duplicate downstream interpretation paths and keeps policy ownership centered in component packet production instead of template-side reconstruction. Runtime track disposition: compatibility-shimmed (diagnostics template consumption hardening only; no runtime behavior-path mutation). Component track disposition: implemented (source-of-truth packet canonicalization and alignment/cause summary exposure in metadata owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; contract clarity, diagnostics determinism, and duplicate-logic reduction hardening only. README/wiki parity: no material operator workflow change.

## 2026-06-22

- Validation/OLED Target Alignment Template Cause-Priority + Verdict Hardening (`docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): fix diagnostic cause ordering where `SRC` fallback packets with display-policy suppression could be misclassified as generic title-missing fallback, and refine verdict semantics so policy/audio-mode/freshness suppression SRC postures classify as `WARN` (expected/degraded policy context) instead of unconditional `FAIL`. This keeps hard failures focused on true control/meta alignment drift while preserving actionable warning signals for suppressed-display states. Runtime track disposition: compatibility-shimmed (diagnostics-template-only; no runtime behavior mutation). Component track disposition: compatibility-shimmed (diagnostics-template-only; no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; validation signal-quality and triage correctness hardening only. README/wiki parity: no material repo-state change.

- Runtime/HA WiiM Config-Entry Setup Cancellation Hardening (`custom_components/wiim/light.py`, `custom_components/wiim/number.py`, `custom_components/wiim/switch.py`, `docs/CHANGELOG.md`): fix startup/setup cancellations caused by entity `async_added_to_hass` network I/O (LED/trigger/subwoofer/channel-balance reads) propagating `asyncio.CancelledError` during integration/platform setup windows. Initial entity state hydration now prefers pywiim cache/non-blocking paths and avoids blocking network fetches in add-to-platform phase, preventing spurious `Setup of config entry ... cancelled` traces while preserving coordinator-driven refresh updates. Runtime track disposition: implemented (WiiM runtime setup-path hardening). Component track disposition: checked/not-applicable (no `custom_components/spectra_ls` behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; startup reliability/log-noise hardening only. README/wiki parity: no material repo-state change.

- Runtime+Component/P4 A-C Contract Soundness + Retirement Push (`custom_components/spectra_ls/metadata_stack.py`, `custom_components/spectra_ls/snapshot_fabric.py`, `custom_components/spectra_ls/startup_recovery_fabric.py`, `custom_components/spectra_ls/const.py`, `docs/CHANGELOG.md`): complete the next cutover hardening pass by (A) classifying selector no-eligible idle/no-payload hold as `idle_expected` instead of generic `fallback_any` low-confidence posture, (B) backfilling `route_trace.control_host/control_port` from component-authoritative route/host-port surfaces when route-trace shape is sparse, and (C) retiring the dead component legacy-authority startup branch/token so startup recovery remains component-only with no dormant legacy execution semantics. Runtime track disposition: implemented (route-trace packet completeness hardening; no legacy execution expansion). Component track disposition: implemented (selector semantics and startup recovery retirement hardening in owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics truthfulness, packet completeness, and legacy-branch retirement hardening only. README/wiki parity: no material operator workflow change.

- Runtime/P3 Canonical ESP Control Host-Port Alias Parity (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/CHANGELOG.md`): close the remaining cutover parity gap where canonical ESP route telemetry entities (`sensor.spectra_ls_system_esp_control_host`, `sensor.spectra_ls_system_esp_control_hosts`, `sensor.spectra_ls_system_esp_control_port`) could be absent while only HA-prefixed variants existed (for example `sensor.living_room_spectra_ls_system_esp_control_*`). Added runtime canonical alias sensors that resolve prefixed/dynamic ESP exports first, with bounded fallback to component/runtime control host-port surfaces for fail-closed continuity. Runtime track disposition: implemented (canonical ESP route telemetry contract restored in active package path). Component track disposition: compatibility-shimmed (component behavior unchanged; equivalent consumer failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; telemetry contract parity and consumer-lane stability hardening only. README/wiki parity: no material operator workflow change.

- Component/Selector P2 Confidence-Pool Idle-Posture Hardening (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): reduce false low-confidence selector diagnostics by classifying bounded component-owned idle/no-payload target-hold posture as an explicit expected pool (`idle_expected`) instead of generic `fallback_any`. This preserves deterministic winner selection and does not relax active-playback ranking/disqualifier rules; it only prevents idle windows (no fresh/meta payload, route target held) from being misreported as degraded selector quality. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged; equivalent failure mode reviewed). Component track disposition: implemented (selector telemetry pool classification hardening in metadata owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics confidence semantics hardening only. README/wiki parity: no material operator workflow change.

- Component/Authority-Contract P1 Idle-Posture Mode Hardening (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): resolve residual post-cutover diagnostics gap where `authority_contract.authority_mode` could report `ma_degraded_fallback` during expected component-owned idle/no-metadata windows (`metadata_prep_not_ready`) even though write authority and metadata ownership were already component-side. Metadata-prep authority-mode synthesis now treats bounded component idle/no-payload posture as non-degraded for authority-mode reporting while preserving fail-closed cutover readiness semantics (`metadata_cutover_active=false` with explicit block reason), avoiding false legacy-era degraded interpretation without masking active-playback gate failures. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged; equivalent failure mode reviewed). Component track disposition: implemented (authority-mode reporting semantics hardened in metadata owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics-mode correctness hardening only. README/wiki parity: no material operator workflow change.

- Component/Metadata Authority Owner No-Legacy-Fallback Policy Hardening (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): enforce component-mode ownership semantics so `metadata_authority_owner` no longer reports `legacy_contract_surfaces` when write-authority mode is `component` but metadata-prep is temporarily not ready (for example idle/no-title windows). Metadata ownership now remains `component_contract_surfaces` in component mode, while `metadata_cutover_active` and `cutover_block_reason` continue to reflect readiness/blocker state (`metadata_prep_not_ready` or resolver-candidate gaps) without silently reverting owner identity to legacy. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged; equivalent failure mode reviewed). Component track disposition: implemented (authority-owner contract hardening in metadata owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; ownership-token correctness and no-legacy-fallback diagnostics hardening only. README/wiki parity: no material operator workflow change.

- Component/OLED Blank Root-Cause Fix — Passthrough-Origin Candidate Stale-Fallback Hardening (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): fix component selection drift that could drop metadata-rich passthrough-origin carriers (for example `media_player.the_oracle_2`) when freshness/recent-progress hints aged out, causing fallback to sparse transport entities with empty title and resulting `audio|oled:-` payloads even while a valid metadata-bearing upstream player remained in `playing` state. Added bounded passthrough-origin stale fallback eligibility so metadata-rich playing origins remain selectable when active transport source is non-meta passthrough and transport candidate lacks usable payload. Runtime track disposition: compatibility-shimmed (runtime OLED/render logic unchanged; equivalent failure mode validated through component contract output). Component track disposition: implemented (selector intake/gating hardening in metadata owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; selector resilience and OLED continuity hardening only. README/wiki parity: no material operator workflow change.

- Runtime/OLED Blank Fix — Component-Only Display/Class Gate Alignment (`packages/ma_control_hub/template.inc`, `docs/CHANGELOG.md`): resolve live `audio|oled:-` blank posture where valid component now-playing metadata (`sensor.component_now_playing_*`) existed but runtime gate surfaces (`sensor.now_playing_media_class`, `binary_sensor.now_playing_display_allowed`) were still driven by legacy now-playing contracts. Updated runtime gate synthesis to use component-only entity/state/title/source/app context (no legacy fallback) so media-class and display-allowed contracts stay coherent with active component winner selection during passthrough-origin metadata scenarios. Runtime track disposition: implemented (runtime gate contract alignment in active template path). Component track disposition: compatibility-shimmed (component selector/diagnostic ownership unchanged; equivalent failure mode validated against live HA snapshot). P1/P2/P3 impact: no source-of-truth ownership reassignment; contract-consumer alignment and OLED render continuity hardening only. README/wiki parity: no material operator workflow change.

- Governance+Runtime/Repo-Wide Spectra Metadata Header Enforcement + CI Guard (`esphome/spectra_ls_system/**` selected files, `docs/spectra_ls_system/{v3.md,v2-baseline-arch-readme.md}`, `bin/check_spectra_headers.py`, `.github/workflows/hardcode-guard.yml`, `docs/CHANGELOG.md`): enforce mandatory top-of-file metadata markers (`Description`, `Version`, `Last updated`) across previously missing Spectra-related source/config/docs files and add an automated audit gate in CI (`bin/check_spectra_headers.py`) to prevent regressions on changed files. Schema-restricted exception is explicitly codified for `custom_components/spectra_ls/manifest.json` because Home Assistant manifest schema does not provide a `Last updated` field surface. Runtime track disposition: implemented (active Spectra runtime/docs/header compliance + CI audit gate wiring). Component track disposition: compatibility-shimmed (component manifest schema exception codified; no behavior contract mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; governance/metadata compliance hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP+Component/Legacy Helper Read-Lane Retirement + ESP Fallback Telemetry Artifact Removal (`custom_components/spectra_ls/control_execution_fabric.py`, `custom_components/spectra_ls/scaffold_fabric.py`, `custom_components/spectra_ls/snapshot_fabric.py`, `custom_components/spectra_ls/event_recovery_fabric.py`, `custom_components/spectra_ls/startup_recovery_fabric.py`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/CHANGELOG.md`): retire remaining active component reads that depended on `input_select.ma_active_target` helper state for control-center target resolution, event/recovery orchestration, and startup boot-readiness checks by switching those lanes to component snapshot/route scaffolds as the primary contract source. In the same slice, retire stale ESP fallback telemetry artifact surfaces (`esp_control_fallback_*` counters/age/type exports and backing globals) that no longer reflect active execution paths under component-only cutover posture. Runtime track disposition: implemented (ESP fallback telemetry artifact surfaces retired from active runtime package path). Component track disposition: implemented (helper-coupled read lanes replaced with component-native route/scaffold sources). P1/P2/P3 impact: no source-of-truth ownership reassignment; legacy helper read-coupling and stale fallback observability artifacts retired from active-path behavior. README/wiki parity: no material operator workflow change.

- Runtime+ESP+Component/Legacy Runtime Loader Retirement + Validation Host-Resolution Hardening (`packages/ma_control_hub.yaml`, `packages/ma_control_hub/input_boolean.inc`, `esphome/spectra_ls_system/substitutions.yaml`, `custom_components/spectra_ls/selection_fabric.py`, `custom_components/spectra_ls/control_execution_fabric.py`, `docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): complete this cutover slice by removing active loading of legacy runtime automation/script lanes from the MA control-hub package, retiring dormant fallback boolean artifacts and unused ESP fallback substitution keys, and normalizing component authority-block reason semantics to component-only posture language. Also harden OLED validation template host telemetry resolution to read canonical and HA-prefixed ESP export entities so diagnostics cannot misreport current control host during reboot/reconnect windows. Runtime track disposition: implemented (legacy runtime automation/script loader path retired from active package path). Component track disposition: implemented (component authority semantics and diagnostics posture aligned to component-only mode). P1/P2/P3 impact: source-of-truth remains component-side for active selection/control execution; legacy runtime execution path is now package-loader retired while compatibility helper/entity surfaces remain for bounded transition scaffolding. README/wiki parity: no material operator workflow change.

- Runtime+ESP+Component/Hard Component-Only Cutover Gate (legacy execution disable) (`packages/ma_control_hub/automation.inc`, `packages/ma_control_hub/script.inc`, `packages/ma_control_hub/template.inc`, `esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `custom_components/spectra_ls/control_execution_fabric.py`, `docs/CHANGELOG.md`): enforce 100% component execution posture by hard-disabling runtime legacy automation/script execution lanes (no fallback toggle path), forcing runtime authority contract to `component`, and removing active ESP fallback feed/listener execution from host/port ingest. Also tighten component write-authority setter semantics to reject legacy authority mode requests at execution boundary. Runtime track disposition: implemented (legacy execution branches retired from active path; component authority contract pinned). Component track disposition: implemented (authority setter fail-closed to component-only mode). P1/P2/P3 impact: source-of-truth control execution is now component-only in active path; legacy compatibility surfaces remain as non-executing artifacts only. README/wiki parity: no material operator workflow change.

- Component/Phase-Setup-1 HA Setup-Flow Foundation (Entity Include/Exclude Capture) (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/config_flow.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/snapshot_fabric.py`, `custom_components/spectra_ls/__init__.py`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/wiki/Install-on-Your-Own-HA.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `README.md`, `docs/CHANGELOG.md`): begin the user-setup framework after Phase-0 sanitization by adding an HA integration options-flow capture surface for routing and metadata include/exclude entity policies (discovery-safe lists), with normalized persistence and coordinator snapshot visibility for subsequent behavior-wiring slices. Runtime track disposition: compatibility-shimmed (runtime behavior/contracts unchanged; setup framework seeded in component lane). Component track disposition: implemented (HA setup-flow + normalized policy packet surfaces). P1/P2/P3 impact: no source-of-truth ownership reassignment; onboarding framework foundation hardening only. README/wiki/roadmap parity: completed in same change set.

- Validation+Runtime/Meta Full-Stack Tester Helper-Target Gate Determinism Hardening (`docs/testing/raw/meta_component_full_stack_tester.jinja`, `docs/CHANGELOG.md`): tighten helper-target validity checks to use safe state-function reads and explicit entity-shape validation before treating helper target as valid, reducing ambiguous dynamic lookup behavior and false-valid helper states during churn/restart windows. Runtime track disposition: compatibility-shimmed (diagnostics template hardening only; no runtime behavior mutation). Component track disposition: compatibility-shimmed (diagnostics template hardening only; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; validation correctness hardening only. README parity: no material repo-state change.

- Validation+Runtime/Meta Full-Stack Tester Backend/API Gate Integrity Hardening (`docs/testing/raw/meta_component_full_stack_tester.jinja`, `docs/CHANGELOG.md`): fix a false-green diagnostics path where backend/API read-lane degradation could still yield an overall `PASS` because backend/API contract readiness was reported but not included in score/blocker evaluation. Added explicit backend/API readiness gate scoring and blocker emission so verdict semantics remain fail-closed and suggested-next-action routing is deterministic under degraded backend/API posture. Runtime track disposition: compatibility-shimmed (diagnostics template hardening only; no runtime behavior mutation). Component track disposition: compatibility-shimmed (diagnostics template hardening only; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; validation correctness hardening only. README parity: no material repo-state change.

- Governance+Roadmap/Phase-State Normalization Packet (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/CHANGELOG.md`): publish explicit phase-status snapshot text to eliminate operator ambiguity after Phase-0 residual closeout and to clarify that Phase-1 shadow parity is historical/validated, with current execution lane anchored in later roadmap slices. Runtime track disposition: compatibility-shimmed (docs/status-clarity slice only; no runtime behavior mutation). Component track disposition: compatibility-shimmed (docs/status-clarity slice only; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; governance/status clarity hardening only. README parity: no material repo-state change.

- Governance+Docs/Phase-0 Residual Exposure Split + Sanitization + Universal Handler Framework (`docs/notes/NOTES-engineering-rigor.md`, `docs/notes/NOTES-wiim-api.md`, `docs/control-py/NOTES-wiim-api.md`, `docs/testing/raw/wled_palette_room_e2e_runbook.md`, `docs/testing/raw/_tmp_wled_palette_sync_last.json`, `docs/testing/raw/_tmp_wled_e2e_output.json`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/CHANGELOG.md`): split out-of-scope residual literals into actionable classes (`acceptable historical/reference` vs `sanitize now`) and sanitize high-noise operator/reference docs by replacing install-specific endpoint literals and active default target examples with portable placeholders. For large historical telemetry payloads, preserved nested per-entity state snapshots as `acceptable historical/reference` while sanitizing active/top-level fields used as copy-forward defaults. Added a universal Phase-0 residual handler set (runtime/component/docs/ops/gov) to be applied explicitly in remaining phases so sanitization cadence is deterministic and auditable. Runtime track disposition: compatibility-shimmed (docs/governance/process slice only; no runtime behavior mutation). Component track disposition: compatibility-shimmed (docs/governance/process slice only; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; residual-hardcode governance and portability hardening only.

- Runtime/HA Plex Package YAML Parse Recovery (`packages/ha_plex_now_playing.yaml`, `docs/CHANGELOG.md`): fix Home Assistant startup configuration parse failure (`found character '%' that cannot start any token`) by correcting Jinja indentation inside `rest.resource_template` so templating remains inside the folded scalar block. Runtime track disposition: implemented (runtime package parse correctness restored). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed as non-owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; configuration parse-blocker correction only.

- Governance+Runtime/Final Low-Noise Hardcode Cleanup (`bin/audit_hardcoded_entities.py`, `packages/spectra_ls_lighting_hub.yaml`, `docs/CHANGELOG.md`): allowlisted canonical `spectra_tv_*` contract prefixes (text/sensor/binary_sensor/input_boolean/input_number helper surfaces) in hardcode guard and replaced hardcoded HA URL literal in lighting-hub shell command with loopback-safe default to remove install-specific private-IP literal. Runtime track disposition: implemented (guard precision + URL portability hardening). Component track disposition: compatibility-shimmed (component contracts unchanged). P1/P2/P3 impact: no source-of-truth ownership reassignment; final signal-quality/portability hardening only.

- Governance+Runtime/Hardcode Final Precision Pass + Spectra Setup Default Neutralization (`bin/audit_hardcoded_entities.py`, `packages/spectra_ls_setup.yaml`, `docs/CHANGELOG.md`): skipped comment/example/include pseudo-entity contexts in parser, allowlisted canonical `input_text.control_board_*` and `automation.control_board_*` helper contracts, and neutralized install-specific default media-player entity/meta values in Spectra setup helpers to empty fail-closed posture. Runtime track disposition: implemented (parser fidelity + default portability hardening). Component track disposition: compatibility-shimmed (component contracts unchanged). P1/P2/P3 impact: no source-of-truth ownership reassignment; signal quality + portability hardening only.

- Governance+Runtime/Hardcode Parser Include-Context Fix + MA Helper Default Target Neutralization (`bin/audit_hardcoded_entities.py`, `packages/ma_control_hub/input_text.inc`, `docs/CHANGELOG.md`): suppressed entity scanning on `!include` lines and allowlisted canonical shadow-route contract sensors, then neutralized install-specific media-player defaults in MA helper input_text surfaces to empty/fail-closed values. Runtime track disposition: implemented (parser precision + helper default portability hardening). Component track disposition: compatibility-shimmed (component contracts unchanged). P1/P2/P3 impact: no source-of-truth ownership reassignment; guard signal quality and portability hardening only.

- Runtime/Plex Package De-Install-Specific Cleanup (`packages/ha_plex_now_playing.yaml`, `docs/CHANGELOG.md`): removed private-host defaults/fallbacks and normalized legacy `sensor.cory_plex_*` references to generic `sensor.plex_*` contract surfaces in active runtime Plex now-playing package. Runtime track disposition: implemented (contract-safe naming and fail-closed URL posture). Component track disposition: compatibility-shimmed (component contracts unchanged; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; contract portability hardening only.

- Governance+Tooling/Hardcode Guard Scope Tightening for Non-Spectra Local Climate Packages (`bin/audit_hardcoded_entities.py`, `docs/CHANGELOG.md`): excluded local thermostat/side-package surfaces from Spectra hardcode guard scope and allowlisted canonical Spectra setup helper prefixes (`input_text.spectra_ls_*`) so CI policy remains focused on active Spectra product contracts. Runtime track disposition: compatibility-shimmed (guard scope/allowlist precision only; no runtime behavior mutation). Component track disposition: compatibility-shimmed (component behavior unchanged). P1/P2/P3 impact: no source-of-truth ownership reassignment; enforcement-scope precision only.

- Runtime+Governance/Secret-Backed Network Substitutions + Legacy UPnP Fixture Scope Refinement (planned in-slice) (`esphome/spectra_ls_system/substitutions.yaml`, `esphome/secrets.example`, `bin/audit_hardcoded_entities.py`, `docs/CHANGELOG.md`): migrate static network IP literals in active Spectra substitutions to secret-backed keys for install-portable deployment and refine hardcode guard scope to exclude legacy UPnP captured fixture payloads (`esphome/spectra_ls_system/upnp_hack/**`) from active logic enforcement. Runtime track disposition: implemented (portable network config + guard-scope precision). Component track disposition: compatibility-shimmed (component resolver contracts unchanged). P1/P2/P3 impact: no source-of-truth ownership reassignment; portability and guard signal-quality hardening only.

- Runtime+Governance/Universal Entity Resolution Foundations + Dashboard Scope Policy (`packages/spectra_ls_lighting_hub.yaml`, `bin/audit_hardcoded_entities.py`, `docs/architecture/UNIVERSAL-ENTITY-RESOLUTION-PLAN.md`, `docs/README.md`, `docs/CHANGELOG.md`): established universal alternatives for install-specific light/player entity references by codifying contract-first resolver patterns (catalog + room/target helper chain) and adding reusable dynamic room-light action scripts (`script.control_board_room_lights_on`, `script.control_board_room_lights_off`) that operate on discovered entities. In the same slice, refined hardcode guard scope to exclude local Lovelace dashboard fixtures (`spectra_ls_ha_view.{yaml,json}`) from active product-logic enforcement and indexed the resolver-plan architecture note in docs navigation. Runtime track disposition: implemented (resolver scripts + guard-scope precision for active logic). Component track disposition: compatibility-shimmed (component selection/registry contracts already provide canonical resolver lanes; no behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; resolver formalization + policy scope hardening only. README/wiki parity: no material operator workflow change.

- Governance+Tooling/Hardcode Guard Legacy-Path Suppression + Control-Board Binary Sensor Allowlist (`bin/audit_hardcoded_entities.py`, `docs/CHANGELOG.md`): excluded retired `esphome/spectra_ls_system/previous/**` path from active hardcode scans and allowlisted canonical `binary_sensor.control_board_*` contract tokens so CI remains focused on install-specific literals in active product logic instead of archived fixtures and stable control-hub contract IDs. Runtime track disposition: compatibility-shimmed (guard precision only; no runtime behavior mutation). Component track disposition: compatibility-shimmed (guard precision only; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; signal-quality hardening only.

- Governance+Tooling/Hardcode Guard Contract-Safe Allowlist Expansion (planned in-slice) (`bin/audit_hardcoded_entities.py`, `docs/CHANGELOG.md`): broaden guard allowlists for established discovery-safe contract prefixes and HA service-style tokens so CI violations focus on genuinely install-specific literals (for example personal/device-specific entities and private hosts) instead of internal helper contracts used by control hub and Plex filter surfaces. Runtime track disposition: compatibility-shimmed (guard precision only; no runtime behavior change). Component track disposition: compatibility-shimmed (guard precision only; no component behavior change). P1/P2/P3 impact: no source-of-truth ownership reassignment; false-positive reduction only.

- Governance+Tooling/Hardcode Guard Service-Token False-Positive Suppression (planned in-slice) (`bin/audit_hardcoded_entities.py`, `docs/CHANGELOG.md`): classify `service:`/`action:` call tokens as service names (not entity IDs) during entity-literal scans so CI enforcement stays focused on install-specific entity/IP literals rather than HA service invocations (`script.execute`, `script.turn_on`, `light.turn_on`, `input_text.set_value`, etc.). Runtime track disposition: compatibility-shimmed (guard precision only; no runtime behavior change). Component track disposition: compatibility-shimmed (guard precision only; no component behavior change). P1/P2/P3 impact: no source-of-truth ownership reassignment; audit signal-quality tightening only.

- Runtime+Parity/Plex Local Filter Source-Entity De-Install-Specific Cleanup (`packages/plex_local_filter.yaml`, `esphome/spectra_ls_system/packages/plex_local_filter.yaml`, `docs/CHANGELOG.md`): replaced install-specific `sensor.cory_plex_*` source references with anonymized contract-safe `sensor.plex_*` references in both runtime and ESPHome parity package tracks, and added required metadata + active parity-directive headers on both edited files. Runtime track disposition: implemented (contract-safe source-name normalization in runtime package path). Component track disposition: deferred with rationale (custom component does not own this Plex package contract surface in this slice; equivalent failure mode reviewed as not-applicable). P1/P2/P3 impact: no source-of-truth ownership reassignment; naming-contract normalization only. README/wiki parity: no material operator workflow change.

- Governance+Runtime/Hardcode Guard Precision Hardening + Plex Base-URL Sanitization (`bin/audit_hardcoded_entities.py`, `esphome/spectra_ls_system/ha-plex-now-playing.yaml`, `custom_components/spectra_ls/services.yaml`, `docs/CHANGELOG.md`): reduce false-positive guard noise by excluding ESPHome local component implementation stubs from install-specific literal scans and allowlisting canonical contract-safe entity tokens used by core now-playing/control-center surfaces (`sensor.now_playing_*`, `binary_sensor.now_playing_*`, `scene.none`, service-style `media_player.volume_*`). In the same slice, remove hardcoded Plex private-IP fallback/default from active Plex bridge config and replace install-specific service description examples with generic placeholders. Runtime track disposition: implemented (private-host literal removal + policy precision hardening in active runtime tooling/path). Component track disposition: compatibility-shimmed (service-description sanitization only; no behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; guard signal quality and discovery-first sanitization hardening only. README/wiki parity: no material operator workflow change.

- Governance+Tooling/Hardcode Guard Scope Precision Follow-up (`bin/audit_hardcoded_entities.py`, `docs/CHANGELOG.md`): exclude optional legacy Plex bridge path (`esphome/spectra_ls_system/ha-plex-now-playing.yaml`) from hardcode audit scope to keep CI guard signal focused on active product logic and avoid high-noise false positives from intentionally environment-specific bridge scaffolding. Runtime track disposition: compatibility-shimmed (guard-scope precision only; no runtime behavior mutation). Component track disposition: compatibility-shimmed (guard-scope precision only; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; CI signal-quality hardening only.

- Governance+Tooling/Phase-0 Install-Specific Hardcode Audit Guard Activation (`bin/audit_hardcoded_entities.py`, `.github/workflows/hardcode-guard.yml`, `docs/notes/NOTES-engineering-rigor.md`, `.github/pull_request_template.md`, `README.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/CHANGELOG.md`): add a repository enforcement slice that blocks new install-specific hardcoded entity IDs/private-host literals in active product logic paths (`custom_components/spectra_ls/**`, `packages/**`, `esphome/spectra_ls_system/**`) using a changed-files audit policy in CI. Guard semantics are discovery-first and contract-aware (approved internal contract prefixes remain allowed), while non-contract install literals now fail review-time checks before merge. Runtime track disposition: compatibility-shimmed (policy/guardrail activation only; no runtime behavior mutation in this slice). Component track disposition: compatibility-shimmed (policy/guardrail activation only; no component behavior mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; governance and merge-gate hardening only. README/wiki parity: updated with operator/contributor policy visibility in same change set.

## 2026-06-21

- Component/Now-Playing Passthrough-Origin Candidate Intake Hardening (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix passthrough-origin metadata feed loss where upstream carriers (for example `media_player.the_oracle_2`) could be excluded from selector intake while passthrough transport entities remained winner-only candidates, forcing `fallback_any` selection to source-only route targets and blank OLED payloads. Component selector now adds bounded passthrough-origin upstream discovery (fresh playing/paused + metadata-rich + non-source-only posture) and ranks those candidates ahead of route-target fallback pools in passthrough windows, preserving existing contract ownership while restoring correct now-playing entity feed. Runtime track disposition: compatibility-shimmed (runtime render/control contracts unchanged; equivalent failure mode reviewed). Component track disposition: implemented (selector intake and ranking hardening in owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; passthrough-origin metadata feed correctness and selector determinism hardening only. README parity: no material repo-state change.

- Component+Diagnostics/Selector Winner Telemetry Completeness Hardening (`custom_components/spectra_ls/metadata_stack.py`, `docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): fix partial selector telemetry captures during passthrough triage by exporting winner entity/source as explicit flattened check/value fields (in addition to nested selection payload) and hardening the raw validation template to parse string-shaped selector payloads plus flattened fallbacks. This removes false `component_selector_telemetry_partial` classifications caused by packet-shape/serialization variance while preserving existing winner pool/reason diagnostics and candidate disqualification evidence. Runtime track disposition: compatibility-shimmed (runtime control/render contracts unchanged; equivalent failure mode reviewed). Component track disposition: implemented (selector telemetry emission + diagnostics parsing resilience hardening in owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; observability determinism and triage correctness hardening only. README parity: no material repo-state change.

- Docs/Architecture Canonical Contract Ownership Ledger Publication (`docs/architecture/CONTRACT-OWNERSHIP-LEDGER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/README.md`, `README.md`, `docs/CHANGELOG.md`): publish a single canonical contract table for Spectra LS control/metadata/runtime surfaces with explicit owner/writer/readers/authority/fallback/retirement disposition, and synchronize roadmap + v-next + README/docs index references in the same change set for docs-parity compliance. Runtime track disposition: compatibility-shimmed (docs-only; runtime behavior unchanged). Component track disposition: compatibility-shimmed (docs-only; component behavior unchanged). P1/P2/P3 impact: no source-of-truth ownership reassignment; governance visibility and operator/developer contract clarity hardening only. README parity: updated with architecture-link discoverability.

- Component/Metadata Cutover Gate Passthrough Authority Readiness Fix (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/CHANGELOG.md`): remove legacy-owner lock during active passthrough transport windows where title/progress evidence is live but duration remains `0` (common Optical In posture). Metadata-prep gating now treats this bounded posture as cutover-eligible for component ownership, suppressing false `playing_with_missing_duration_contract` trial blockers in passthrough while keeping non-passthrough strict timing guards unchanged. In the same slice, metadata bridge/trial authority satisfaction is hardened to component-first posture (`component` authority or active component cutover), removing legacy-authority dependency from bridge staging. Runtime track disposition: compatibility-shimmed (runtime control/transport contracts unchanged; equivalent failure mode reviewed). Component track disposition: implemented (component metadata authority gate + bridge/trial authority hardening for owner path). P1/P2/P3 impact: source-of-truth authority progression remains component-governed; legacy fallback is no longer required for this passthrough timing posture. README/wiki parity: no material operator workflow change.

- Runtime+Component/Component-Only Control-Lane Default Activation + OLED Glyph Warning Fix (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/services.yaml`, `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): enforce component-first execution posture by removing `legacy` from accepted write-authority options, switching control-center default `read_only_mode` to `false`, and changing `spectra_ls.execute_control_center_input` default service behavior to `dry_run=false` so mapped encoder/button actions execute unless explicitly requested as dry-run. Also replace Unicode ellipsis (`…`) with ASCII (`...`) in OLED truncation paths to eliminate repeated missing-glyph log spam (`Codepoint 0x00002026 not found in font`) under active audio render loops. Runtime track disposition: implemented (OLED render/log-noise hardening only; host/transport control contracts unchanged). Component track disposition: implemented (component-only execution default hardening; no legacy fallback activation). P1/P2/P3 impact: source-of-truth remains component-facing for control execution posture; no runtime helper contract breakage introduced. README/wiki parity: no material operator workflow change.

- Runtime+Component/Control-Host Input-Lane Recovery Hotfix (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/control_execution_fabric.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix dead control-input posture where control-center input executions could be permanently blocked in component mode by (1) honoring persisted write-authority mode (`legacy`/`component`) instead of force-normalizing to component-only and (2) implementing guarded `encoder_turn -> volume` execution path so mapped volume actions can perform real writes (or deterministic dry-run validation) instead of unimplemented-action blocks. Runtime track disposition: compatibility-shimmed (runtime helper/ESP contracts unchanged; equivalent failure mode reviewed via live API probe). Component track disposition: implemented (authority normalization + control-center volume execution hardening). P1/P2/P3 impact: no source-of-truth ownership reassignment; control-lane reliability and safety hardening only. README parity: no material repo-state change.

- Runtime+Component/Now-Playing Selector Policy-Tier Implementation (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): implement holistic deterministic selector flow (candidate pools + hard disqualifiers + stable rank tuple) replacing score-stack drift behavior. Adds explicit sparse-transport disqualification when MA-rich active candidates exist, preserving passthrough metadata lock while preventing title-only transport mirrors from winning restart windows. Exposes deterministic winner diagnostics (winner pool/reason, eligible counts, and per-candidate disqualification reasons) in metadata-prep checks/values for operator triage parity. Runtime track disposition: compatibility-shimmed (runtime templates/ESP logic unchanged; equivalent failure mode reviewed). Component track disposition: implemented (selector policy + diagnostics hardening). P1/P2/P3 impact: no source-of-truth ownership reassignment; winner determinism + observability hardening only. README parity: no material repo-state change.

## 2026-06-20

- Runtime+Component/Now-Playing Holistic Selector Model Retune (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): replace incremental winner tweaks with a cohesive candidate-feature ranking model that evaluates freshness, metadata richness, MA affinity, control-target relevance, and transport-mirror risk in one deterministic pass. Adds explicit sparse-mirror override behavior so title-only DLNA mirrors cannot dominate richer MA companion metadata during restart churn when equivalent now-playing context is available. Runtime track disposition: compatibility-shimmed (runtime templates/ESP logic unchanged; equivalent failure mode reviewed). Component track disposition: implemented (selector model coherence and restart robustness hardening). P1/P2/P3 impact: no source-of-truth ownership reassignment; metadata winner determinism and quality hardening only. README parity: no material repo-state change.

- Runtime+Component/Now-Playing MA Companion Override Hardening for Restart Windows (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix residual post-restart winner drift where sparse transport mirrors (`DLNA`/`UPnP` title-only carriers) could still outrank richer Music Assistant companion entities. Selector scoring now adds stronger MA-companion preference and an explicit sparse-transport penalty, and MA-rich candidate discovery includes a bounded global fallback lane when anchor-token matching is temporarily weak during reboot churn. Runtime track disposition: compatibility-shimmed (runtime templates/ESP logic unchanged; equivalent selection-failure mode reviewed). Component track disposition: implemented (component now-playing selector/scoring restart-resilience hardening). P1/P2/P3 impact: no source-of-truth ownership reassignment; metadata winner reliability hardening only. README parity: no material repo-state change.

- Runtime+Component/Now-Playing MA-Rich Metadata Preference Hardening (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix radio/app sessions where richer Music Assistant metadata carriers (for example `*_ma` entities with title+artist+album) could lose to lower-fidelity transport mirrors, including post-restart windows. Candidate intake now includes resolver lanes beyond passthrough and context-anchored MA-rich discovery candidates, and scoring now penalizes low-fidelity transport-mirror entities so rich MA payloads can win deterministically when present. Existing passthrough route-target progress fallback behavior is preserved so metadata-rich selection does not regress position/duration continuity. Runtime track disposition: compatibility-shimmed (runtime templates/ESP logic unchanged; equivalent selection-failure mode reviewed). Component track disposition: implemented (component now-playing candidate/scoring hardening in metadata owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; metadata winner-quality and restart resilience hardening only. README parity: no material repo-state change.

- Runtime+Component/Now-Playing Progress Continuity Fix for Passthrough Metadata Promotion (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix missing/flat track-progress during passthrough metadata-carrier promotion by preserving metadata-entity title/artist selection while explicitly falling back position/duration to the active route target when promoted metadata entities do not expose progress clocks. This keeps metadata-rich carrier selection behavior while restoring deterministic progress continuity for OLED/runtime consumers bound to component now-playing contracts. Runtime track disposition: compatibility-shimmed (runtime templates/ESP logic unchanged; equivalent failure mode reviewed against existing lane fallback behavior). Component track disposition: implemented (component now-playing position/duration fallback hardening in metadata owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; progress continuity hardening under passthrough carrier-vs-transport divergence only. README parity: no material repo-state change.

- Runtime/OLED Track-Position Deterministic Lane Preference with Bounded Continuity Fallback (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): refine the strict lane-lock progress selection to preserve deterministic authority while preventing zero-progress lockouts when the selected metadata lane is temporarily missing usable position clocks. Progress now follows a fixed policy: prefer selected metadata lane first, then use the alternate lane only when preferred lane lacks valid progress and alternate lane has valid/recent progress evidence. This keeps cross-source behavior deterministic (no oscillating multi-override path) while restoring continuity under partial feed gaps. Runtime track disposition: implemented (position continuity + deterministic authority hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; bounded consumer continuity hardening only. README parity: no material repo-state change.

- Runtime/OLED Track-Position Lane-Locked Authority (No Cross-Source Override) (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): strengthen the earlier position cleanup by explicitly locking progress authority to the selected metadata lane (`meta_idx`): HA lane uses HA position/duration only, Arylic lane uses Arylic position/duration only, and only unresolved/no-owner windows may fall back to secondary authority. This removes cross-source authority hopping (HA<->Arylic) that could intermittently drop/flap progress under sparse or delayed updates while metadata remained present. Runtime track disposition: implemented (position authority determinism hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; now-playing progress stability hardening only. README parity: no material repo-state change.

- Runtime/OLED Track-Position Single-Authority Consolidation (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix intermittent track-position loss/flap by removing duplicate HA-position cache rescue/projection overrides in the now-playing compute pipeline. Position handling now uses one deterministic authority selection stage (`HA` preferred, `Arylic` secondary) plus a single bounded final fallback cache hold path, instead of layered pre-selection and post-selection overrides that could conflict under sparse/late position updates. Runtime track disposition: implemented (position-path determinism hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; now-playing progress continuity hardening only. README parity: no material repo-state change.

- Runtime/Volume Pot Direction Hotfix (CW should increase) (`esphome/spectra_ls_system/substitutions.yaml`, `docs/CHANGELOG.md`): operator validation after host-gap/retry hardening confirmed physical volume direction regression (`CW` decreased volume). Reversed pot orientation mapping by switching `volume_pot_invert` to `false` for current wiring so clockwise turns map to increasing target volume. Runtime track disposition: implemented (input semantics correction). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; physical input direction semantics correction only. README parity: no material repo-state change.

- Runtime/Volume Pot Fail-Open Guard Against Calibration Latch + HA Target Fallback (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/CHANGELOG.md`): fix potential hard no-op condition where `rp_volume_pot_raw` volume actions were fully suppressed whenever `calibration_active` remained latched true, leaving OLED volume display stale/frozen while metadata/progress continued healthy. Volume-pot ingest path is now fail-open (volume control remains active independent of calibration flag). Additionally, when TCP host cache is transiently empty or TCP volume send fails, pot writes now issue a bounded fallback `media_player.volume_set` call to active target to avoid control dead-zones during host-cache recovery windows. Runtime track disposition: implemented (runtime volume input availability + degraded-path write continuity hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; input-path fail-safe and degraded-path continuity hardening only. README parity: no material repo-state change.

- Runtime/Pot Volume No-Op Recovery — Failed-Send State Latching Fix (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/CHANGELOG.md`): fix physical pot no-op behavior where OLED bar could update (for example ~75%) while real audio volume stayed unchanged after transient TCP/route send failures. Root cause: volume path latched send-state/cache as if successful even when transport send failed, allowing subsequent identical pot targets to be short-circuited without retry. Hardened volume send-state semantics so success-only fields are updated only on confirmed TCP enqueue/send success, and failed sends keep retry intent active through deferred-loop re-attempts instead of collapsing into stale “already applied” state. Runtime track disposition: implemented (volume control-path reliability hardening in active runtime package). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; write-path correctness/retry determinism hardening only. README parity: no material repo-state change.

- Runtime/Physical Volume Pot Normalization (Invert Support + Cache-First OLED Source) (`esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): after transport hotfix validation proved write lane functional, harden remaining physical-pot behavior by adding explicit configurable inversion (`volume_pot_invert`) and applying endpoint snap normalization in the pot ingest path, plus switching OLED volume render precedence to fresh pot cache first (instead of direct raw-state trust) to prevent stale/raw inversion artifacts from pinning bar display. Runtime track disposition: implemented (pot ingest/render correctness hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; physical input semantics and display coherence hardening only. README parity: no material repo-state change.

- Runtime/Shared Arylic TCP Host-Token Sanitization Hotfix (`esphome/spectra_ls_system/components/arylic_tcp.h`, `docs/CHANGELOG.md`): harden send-path host parsing to tolerate quoted/bracketed/URL-like or `host:port` token shapes at the transport layer, so enqueue/send calls always emit valid host-only tokens for socket connect. This protects pot/encoder control writes from upstream host-feed shape drift and restores route robustness without changing control ownership contracts. Runtime track disposition: implemented (transport helper parse hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; write-path transport resilience hardening only. README parity: no material repo-state change.

- Runtime/Control-Host Endpoint Normalization Fix for Pot/Encoder TCP Writes (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/CHANGELOG.md`): fix non-functional live control path where host telemetry could be correct but emitted as `host:port` (for example `192.168.10.51:8899`), causing TCP write calls to treat the full endpoint as hostname and fail control actions. HA control host/hosts ingest now normalizes endpoint feeds into host-only route strings and extracts numeric port into `arylic_control_port` when present, with the same bounded behavior applied on fallback ingest lanes. This restores effective volume/eQ TCP write routing for pot/remote actions while preserving discovery-first contracts. Runtime track disposition: implemented (runtime endpoint parsing and route-cache correctness hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; control-write transport correctness hardening only. README parity: no material repo-state change.

- Runtime/OLED Volume-Bar Pot-First Source Hardening Under Nonfunctional Control Path (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix operator-visible mismatch where OLED volume bar could remain pinned high (for example `100%`) while physical volume pot is at off during degraded/nonfunctional control-write windows. OLED compute now derives display volume from live RP pot raw state first, then fresh pot cache, and only then transport/arylic fallbacks; this prevents stale host/transport volume carryover from dominating the on-device bar when control-path writes are failing. Runtime track disposition: implemented (ESP OLED volume-display source correctness hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; display-truthfulness hardening only. README parity: no material repo-state change.

- Component/Metadata-Prep Compatibility Alias Determinism Hardening (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): tighten metadata-prep compatibility alias behavior so `values.selected_entity` and `values.route_target_entity` remain deterministic under sparse route snapshots (`active_target` unresolved/blank or legacy entity state emitted as `unknown`). Alias emission now prefers resolved entity IDs and uses bounded fallback ordering (`now_playing_entity` -> `route_active_target` -> `active_meta_entity` -> empty) instead of propagating unresolved placeholder states. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged; equivalent failure mode reviewed). Component track disposition: implemented (component packet-shape resilience hardening in metadata owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; compatibility alias determinism hardening only. README parity: no material repo-state change.

- Runtime+Component/Now-Playing Passthrough Target-Identity Contract Hardening (`custom_components/spectra_ls/metadata_stack.py`, `packages/ma_control_hub/template.inc`, `docs/CHANGELOG.md`): fix remaining passthrough identity drift where fallback app/source labels could be derived from metadata-carrier context instead of the active passthrough route target identity during carrier-vs-route divergence windows. Component metadata prep now prefers active route-target friendly identity for passthrough app/source fallback, and adds fail-safe source-label fallback when route-target identity is unresolved so app-name dominance does not leak through sparse route snapshots. Added compatibility alias fields (`selected_entity`, `route_target_entity`) in metadata-prep values to reduce downstream shape fragility for tooling still consuming older packet keys. Runtime `sensor.now_playing_app` parity path now mirrors this policy by suppressing app-name dominance (for example `YouTube`) and preferring active-target friendly identity across passthrough posture, with selected-entity/source fallback when active-target identity is temporarily unavailable. Runtime track disposition: implemented (runtime fallback parity alignment for passthrough app identity). Component track disposition: implemented (route-target identity-preference and packet-shape resilience hardening in metadata owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; passthrough label precedence and contract-shape robustness hardening only. README parity: no material repo-state change.

- Component/Now-Playing Passthrough Device-Label Precedence Hardening (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): fix passthrough OLED identity drift where app metadata (for example `YouTube`) could dominate top-line/source context during AppleTV-style passthrough windows when component metadata selected an upstream carrier entity. Component metadata prep now rewrites passthrough app/source labels to the selected metadata entity’s friendly device label when carrier entity differs from active passthrough route target, preventing app-name dominance in source-context fallback rendering. Runtime track disposition: compatibility-shimmed (runtime renderer contracts unchanged; equivalent failure mode reviewed). Component track disposition: implemented (passthrough app/source precedence correction in metadata owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; now-playing label precedence correctness hardening only. README parity: no material repo-state change.

- Runtime+Component/Now-Playing TV-Movie Filter Artifact Retirement (`packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/metadata_stack.py`, `custom_components/spectra_ls/binary_sensor.py`, `custom_components/spectra_ls/sensor.py`, `docs/testing/raw/meta_stack_step3_full_validation.jinja`, `docs/CHANGELOG.md`): remove active TV/movie/non-music filtering semantics from now-playing display-policy contracts and parity diagnostics. Runtime now-playing media-class output is normalized to active-vs-none (`music`/`none`) without TV/movie/non_music classification branches, display-allowed no longer gates on media class, and preview-key no longer encodes media-class/display-policy bits. Component metadata-prep diagnostics no longer enforce music-only/non-music contract consistency and no longer expose TV/movie policy artifacts (`expected_display_allowed`, `music_guard_active`, `display_contract_consistent`) as authoritative checks. Validation template expectations are aligned to contract-surface presence/readiness rather than non_music policy outcomes. Runtime track disposition: implemented (TV/movie filtering behavior and artifact retirement in active runtime contract surfaces). Component track disposition: implemented (policy-artifact retirement and parity-diagnostic alignment in component owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; policy-surface simplification and false-gate risk reduction only. README parity: no material repo-state change.

- Runtime+Component/Compatibility Baseline Audit — HA Core 2026.6.x + HAOS 18 + ESPHome 2026.6.x + Device Builder (`docs/CHANGELOG.md`, `README.md`): complete an evidence-first compatibility review by cross-checking upstream changelogs/releases (Home Assistant Core/OS, ESPHome, ESPHome add-on, Device Builder backend/frontend) against current Spectra runtime and component contract surfaces. Baseline outcome: no blocking incompatibilities identified from static contract scan for the active codebase shape; compatibility posture is published as **verified up to** (reviewed against current code surfaces) and **expected** (requires live build/runtime soak confirmation). Key checked vectors: ESPHome `custom_components` loader retirement (active path uses `external_components`), HA config-flow `show_advanced_options` removal (no usage in `custom_components/spectra_ls`), ESP8266 WPA2 default change (not applicable to current ESP32-focused configs), and HA/ESPHome BLE scanning-mode default changes (noted as operational watch item for `bluetooth_proxy` nodes). Runtime track disposition: implemented (compatibility audit + published baseline/caveats). Component track disposition: implemented (component contract-surface compatibility audit + published baseline/caveats). P1/P2/P3 impact: no source-of-truth ownership reassignment; version-compatibility transparency hardening only. README parity: updated in same change set with verified-vs-expected matrix.

- Runtime/HA Recorder Supported SQLite Backend Restore (`configuration.yaml`, `docs/CHANGELOG.md`): replace unsupported in-memory recorder URL (`sqlite:///:memory:`) with a supported on-disk SQLite URL (`sqlite:////config/home-assistant_v2.db`) per current Home Assistant recorder requirements. Native HA config validation previously reported warning `In-memory SQLite database is not supported` and runtime probes showed repeated `recorder/info` timeout during frontend bootstrap. Runtime track disposition: implemented (supported recorder backend contract restored). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed as non-owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; recorder backend compatibility and frontend bootstrap reliability hardening only. README parity: no material repo-state change.

- Runtime/HA WiiM Reachability-Gated Protocol Hint Autoheal (`custom_components/wiim/__init__.py`, `docs/CHANGELOG.md`): fix startup-unavailable behavior for mixed protocol fleets where cached capability preference can drift from real device posture (for example Spectra LS reachable on HTTPS:443 while cached capability preference remains HTTP-first). When no endpoint cache is present, setup now performs a bounded preferred-first status-endpoint reachability probe (`/httpapi.asp?command=getStatusEx`) and passes the reachable protocol hint to pywiim for this setup cycle, including HTTPS self-signed tolerance for probe checks. Runtime track disposition: implemented (setup protocol reachability correctness hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed as non-owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; startup protocol selection robustness only. README parity: no material repo-state change.

- Runtime/HA WiiM Protocol Autoheal Heuristic (Legacy `Axx` Hardware Fallback) (`custom_components/wiim/__init__.py`, `docs/CHANGELOG.md`): harden cached-endpoint recovery logic by keeping `protocol_priority` as the primary source-of-truth and adding a bounded hardware-generation fallback when priority is absent. Legacy LinkPlay hardware revisions (for example `A31` and older) are treated as HTTP-preferred; newer revisions (`A32+`) are treated as HTTPS-preferred for endpoint re-probe decisions. This prevents stale protocol pinning when cached endpoints conflict with discovered capability posture while preserving discovery-first behavior and no hardcoded host/IP assumptions. Runtime track disposition: implemented (protocol autoheal decision hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed as non-owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; transport protocol selection robustness only. README parity: no material repo-state change.

- Runtime/HA Recovery-Mode YAML Parse Fix (`configuration.yaml`, `docs/CHANGELOG.md`): resolve Home Assistant startup recovery activation caused by unquoted recorder in-memory SQLite URL (`sqlite:///:memory:`) being parsed as invalid YAML mapping syntax. Quoted `recorder.db_url` value to restore valid configuration parsing while preserving temporary Sev-1 recorder containment behavior. Runtime track disposition: implemented (startup parse-blocker correction). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed as non-owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; configuration syntax correctness restoration only. README parity: no material repo-state change.

- Runtime/HA Sev-1 Recorder Crash Containment (temporary in-memory recorder backend) (`configuration.yaml`, `docs/CHANGELOG.md`): apply emergency stabilization for repeated Home Assistant `Fatal Python error: Bus error` events whose dominant crash locus is Recorder/SQLAlchemy execution against the local SQLite recorder file. Recorder is temporarily redirected to an in-memory SQLite backend (`sqlite:///:memory:`) to isolate runtime stability from on-disk recorder corruption/IO contention while preserving immediate control-path operation. This is a containment slice pending controlled DB recovery and persistence-path restore. Runtime track disposition: implemented (temporary runtime containment applied). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed as non-owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; emergency stability containment only. README parity: no material repo-state change.

## 2026-06-18

- Runtime/HA Recorder High-Churn Entity Filter Hardening (`configuration.yaml`, `docs/CHANGELOG.md`): apply targeted `recorder.exclude.entities` filters for top confirmed high-write diagnostic/helper entities (for example `sensor.ma_meta_age_s`, `automation.spectra_ls_hw_status_refresh_now_playing`, and component/shadow status surfaces) to reduce database regrowth pressure without changing control-path behavior. Runtime track disposition: implemented (recorder storage policy tuning in active runtime config). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; recorder exclusion only). P1/P2/P3 impact: no source-of-truth ownership reassignment; storage-write pressure reduction only. README parity: no material repo-state change.

## 2026-06-17

- Runtime/HA Recorder Retention + DB-Pressure Hardening (`configuration.yaml`, `docs/CHANGELOG.md`): set explicit recorder lifecycle controls for low-disk resilience and right-size retention for current Spectra control-stack needs. Updated recorder policy to short-window retention (`purge_keep_days: 3`) with explicit auto-maintenance (`auto_purge: true`, `auto_repack: true`) and high-churn event exclusions to reduce database growth without impacting active control-path behavior. Dependency/logic review confirms current runtime/component stack uses live state/relative freshness clocks (`last_updated`/`last_changed` and bounded age thresholds) rather than multi-day recorder-history queries, so 3-day retention preserves practical troubleshooting context while reducing storage pressure. Runtime track disposition: implemented (HA recorder policy hardening in active runtime config). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; no long-window recorder dependency found in component logic). P1/P2/P3 impact: no source-of-truth ownership reassignment; storage lifecycle hardening only. README parity: no material repo-state change.

## 2026-06-16

- Runtime/OLED Status Sensor Parity Hardening (Passthrough No-Progress Suppression) (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/CHANGELOG.md`): align `ESP OLED Status` synthesis with the OLED fail-closed policy by suppressing source/friendly passthrough fallback labels when transport remains source-only (`Optical In`/passthrough) with empty metadata and no progress (`pos=0/0`). This removes stale source-text reporting from status telemetry in the same idle no-payload windows targeted by OLED render suppression, preventing false “still playing source label” status surfaces. Runtime track disposition: implemented (status telemetry logic hardening only; control/transport contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent status-synthesis failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; telemetry/render parity hardening only. README parity: no material repo-state change.

- Runtime/OLED Source-Only Passthrough Fallback Clamp (Immediate No-Payload Blank) (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): close remaining render loophole where passthrough source-only sessions (`Optical In`, no title/artist, `pos=0/0`) could still paint top-line source/friendly labels via fallback title/source paths while sticky upstream `playing` stayed true. Added explicit no-payload passthrough suppression in compute, display-state, and render branches so source-only fallback text is clamped to blank outside popup window, with no-progress sessions fail-closed immediately instead of relying on stale timers alone. Runtime track disposition: implemented (OLED rendering/state hardening only; transport/control contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; fallback suppression hardening only. README parity: no material repo-state change.

- Runtime/OLED Deterministic Passthrough Idle Cutoff (Sticky `playing` Override) (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix persistent top-line passthrough/source rendering (`Optical In` / friendly source labels) by adding a hard stale-idle cutoff when source context remains passthrough-only with no text payload and no progress (`0/0`) beyond an extended idle window (fail-closed even if upstream transport/HA continue asserting `playing`). Applied across compute, display-state suppression, and render blanking paths so stale source-only sessions no longer hold OLED text indefinitely. Runtime track disposition: implemented (OLED suppression logic hardening only; transport/control contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; deterministic stale-idle suppression hardening only. README parity: no material repo-state change.

- Runtime/OLED Source-Only `playing` Heartbeat De-Qualification (No-Progress Passthrough) (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): root-cause evidence showed `ha_audio1_last_playing_ms` and effective playing gates were continuously refreshed by source/friendly heartbeat updates while transport stayed in source-only posture (`state=playing`, `src=Optical In`, `title/artist=''`, `pos=0/0`), preventing stale suppression from ever arming. Tightened OLED gates to require concrete payload/progress evidence (text metadata or advancing position/duration) for HA `playing` effectiveness and playing-tail refresh; source-only no-progress passthrough is now fail-closed after popup window instead of holding top-line source text indefinitely. Runtime track disposition: implemented (OLED state-gate hardening only; control/transport contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent stale-playing suppression posture reviewed against component metadata freshness model). P1/P2/P3 impact: no source-of-truth ownership reassignment; stale source-only render suppression hardening only. README parity: no material repo-state change.

- Runtime/OLED Stale Passthrough False-Playing Suppression (HA+ESP Evidence-Driven) (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): harden OLED fail-closed behavior for long idle passthrough sessions where upstream contracts keep reporting `playing` + source (`Optical In`) with empty metadata and no progress movement (`pos=0/0`). Added suppression gating for stale no-payload passthrough posture so source labels are not rendered indefinitely when no concrete playback payload/progress evidence exists. Runtime track disposition: implemented (OLED suppression logic hardening only; control/transport contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; false-playing stale-window suppression hardening only. README parity: no material repo-state change.

- Runtime/OLED Paused-Passthrough Keepalive Tightening (Require Recent Playing Evidence) (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): harden long-idle fail-closed behavior after persistent `Optical In` top-line reports by requiring both recent playback activity and recent `playing` evidence for paused passthrough keepalive windows. This prevents stale paused/source context from holding OLED source labels indefinitely after transport has been idle. Runtime track disposition: implemented (OLED keepalive gate hardening only; control/transport contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; stale paused-window suppression hardening only. README parity: no material repo-state change.

- Runtime/OLED Idle Passthrough Fail-Closed Blanking Guard (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix long-idle OLED source-label stickiness (for example top-line `Optical In` hours after playback stopped) by tightening paused passthrough keepalive gates to require recent playback activity evidence instead of allowing stale freshness-only posture to hold source-context rendering indefinitely. Runtime track disposition: implemented (OLED state-gate hardening only; transport/control contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; stale-display fail-closed correctness hardening only. README parity: no material repo-state change.

## 2026-06-14

- Runtime/Remote Usability-Bake Sleep Cadence Tuning (10min -> 15min daytime auto wake) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): reduce daytime periodic wake frequency for better battery life by increasing timed deep-sleep wake duration from `10min` to `15min` while retaining HA time-based quiet-hours behavior and existing wake inputs. Runtime track disposition: implemented (runtime remote power cadence tuning). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded sleep cadence adjustment only. README parity: no material repo-state change.

- Runtime/Remote HA Time-Based Quiet-Hours Sleep Policy (23:00–07:00 no timer auto-wake) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): implement Home Assistant time-sourced sleep-priority logic so quiet hours (`23:00–07:00`) enter deep sleep in wake-on-input mode (no periodic timer wake), while daytime retains the usability profile timer wake cadence (`10min`). Existing wake inputs (center button + encoder A/B EXT1 pins) remain unchanged. Runtime track disposition: implemented (runtime remote power schedule hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded HA-time schedule policy enhancement only. README parity: no material repo-state change.

- Runtime/Remote Usability-Bake Sleep Profile (1min idle / 10min sleep, existing wake inputs retained) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): tune deep-sleep cadence for real usability testing by setting idle sleep entry to `60s` and sleep duration to `10min` while preserving the current wake-input set (encoder center button + encoder A/B EXT1 wake pins). Runtime track disposition: implemented (runtime remote power/usability tuning). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded runtime sleep-profile tuning only. README parity: no material repo-state change.

- Runtime/Remote Deep-Sleep Wake Check Tuning (Center Button Wake + 30s Idle Timeout) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): tune remote deep-sleep behavior for wake validation by reducing idle entry threshold to `30s` and extending EXT1 wake pin set to include the encoder center button (`GPIO1`) in addition to encoder channels, so mute-button press can wake the node from deep sleep for the requested wake check pass. Runtime track disposition: implemented (runtime remote power/wake behavior tuning). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded runtime wake-path validation tuning only. README parity: no material repo-state change.

- Runtime/Remote Battery Divider Micro-Calibration (100k/100k Fine Tune) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): apply measured post-divider fine tuning after live verification (`Remote Battery Voltage` ~`4.04–4.08V` vs meter `4.172V`) by adjusting divider ratio from `2.0` to `2.05` for closer on-device voltage agreement while keeping the same physical 100k/100k sensing topology on `GPIO6`. Runtime track disposition: implemented (runtime remote telemetry precision hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded calibration precision adjustment only. README parity: no material repo-state change.

- Runtime/Remote HA ESPHome Include Portability Fix (`esphome/spectra_ls_remote.yaml`, `bin/esphome_spectra_remote_build_local.sh`, `docs/CHANGELOG.md`): fix Home Assistant ESPHome compile failure in `/config/esphome` caused by machine-specific absolute include path (`/mnt/homeassistant/esphome/arylic_tcp_remote.h`) by switching to project-local include resolution (`arylic_tcp_remote.h`). Also update local staged remote build helper to copy the header into stage so relative include resolution works in both local staged builds and HA container/runtime compilation contexts. Runtime track disposition: implemented (runtime remote build portability hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded include-path portability correction only. README parity: no material repo-state change.

- Runtime/Remote Battery Sense Hardware Divider Alignment (100k/100k on GPIO6) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): after field confirmation that battery telemetry was unstable/invalid due to non-deterministic ADC sense path, align runtime scaling to explicit operator-installed divider hardware (`BAT+ -> 100k -> GPIO6 -> 100k -> GND`, ratio `2.0`). This restores deterministic battery voltage estimation anchored to known resistor topology and removes prior temporary ratio overfitting from unstable pre-divider readings. Runtime track disposition: implemented (runtime remote telemetry correctness hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded remote battery telemetry alignment to new physical sensing path only. README parity: no material repo-state change.

- Runtime/Remote Battery Calibration Correction from Live Meter Evidence + NaN Guard Hardening (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): correct battery telemetry under-reporting using measured field evidence (`Remote Battery Voltage` reading `3.55V` while true battery measured `4.174V` unplugged), updating divider scaling to align published voltage with real pack voltage. Also harden battery voltage/SoC lambdas to suppress transient startup NaN propagation when ADC/raw state is not yet valid. Runtime track disposition: implemented (runtime remote telemetry correctness hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded remote measurement-calibration and telemetry-quality correction only. README parity: no material repo-state change.

- Runtime/Remote Stale-Feedback Step-Base Fix + Rotary Overflow Pressure Reduction (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix repeated identical direct `VOL` writes during rapid detents by stepping from the remote’s local volume shadow state (with bounded fresh-sensor re-anchor only on large divergence), instead of re-basing each detent on potentially lagging HA volume telemetry. Also reduce `rotary_encoder` overflow pressure by switching decoder capture to polling (`use_interrupt: false`) and widening detent emit gate timing. Runtime track disposition: implemented (runtime remote detent/write-path correctness hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded remote input/write stabilization only. README parity: no material repo-state change.

- Runtime/Remote Direct Arylic TCP Transport Include Staging Fix (`esphome/spectra_ls_remote.yaml`, `esphome/arylic_tcp_remote.h`, `docs/CHANGELOG.md`): continue the direct control-path migration by switching remote volume/mute actions to `esphome::arylic_tcp` helper calls and aligning remote build-time include sourcing to a repo-local helper file intended for staged compile resolution. Runtime track disposition: implemented (runtime remote control transport alignment). Component track disposition: deferred with rationale (no `custom_components/spectra_ls` transport-path mutation in this runtime-only remote firmware slice). P1/P2/P3 impact: no source-of-truth reassignment; bounded transport-path parity correction for remote runtime only. README parity: no material repo-state change.

- Runtime/Remote Encoder-Button WiiM Mute Command (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): add explicit encoder push-button input wiring (`GPIO1`) and route press actions to a guarded resolved-target mute command lane using `media_player.volume_mute`, enabling direct WiiM mute API control from the remote encoder center switch. Runtime track disposition: implemented (runtime remote input/action enhancement only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded remote button control-path enhancement only. README parity: no material repo-state change.

- Runtime/Remote Volume API Route Diagnostics Instrumentation (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): add explicit per-detent diagnostics for remote volume actions to print target resolution inputs and resolved host-route context (`route_host`, `route_hosts`, `route_port`) before dispatching `media_player.volume_set`. This enables direct verification of what the remote is attempting to control when live volume does not change. Runtime track disposition: implemented (runtime diagnostics only; behavior contract unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; observability hardening only. README parity: no material repo-state change.

- Runtime/Remote WiiM Volume API Path Restore (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): resolve real-world no-op after number-lane write attempt by restoring direct resolved-target `media_player.volume_set` control with bounded percent-step writes derived from active volume telemetry and a deterministic fallback target when helper/fallback feeds are unresolved. This aligns remote detents with live WiiM target volume behavior instead of local Arylic-only number lane writes. Runtime track disposition: implemented (runtime remote volume-path correction only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded remote action-lane correction only. README parity: no material repo-state change.

- Runtime/Remote Volume Write-Lane Restoration to Arylic Number Entity (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix real-world no-op volume behavior after encoder detents by switching remote volume actions from `media_player.volume_up/down` back to the proven runtime write lane `number.set_value` on `number.control_board_v2_arylic_volume_set` with bounded UI-step progression. This removes dependency on transient target resolution for volume detents and restores deterministic control-path writes to the active WiiM/Arylic runtime lane. Runtime track disposition: implemented (runtime remote write-lane correction only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded runtime lane-alignment correction only. README parity: no material repo-state change.

- Runtime/Remote Encoder Direction Inversion + Detent Burst Gate Tuning (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): after post-rewire field verification confirmed opposite direction semantics (`6 CCW` test producing CW-leading logs) and twitchy burst emission, add explicit direction multiplier inversion and a bounded encoder event gate to suppress micro-burst repeats while preserving clean detent-driven volume actions. Runtime track disposition: implemented (runtime remote encoder mapping + interaction tuning only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded runtime input-mapping and noise-shaping correction only. README parity: no material repo-state change.

- Runtime/Remote Encoder Cleanup + WiiM Volume Path Restore (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): after hardware root-cause confirmation (encoder power/pin wiring correction), remove temporary software conditioning layers introduced during noisy-input diagnosis (settle-window accumulator/direction confirmation/reversal suppression stack) and restore a clean ESPHome-native detent handler that directly triggers guarded volume actions. Re-enable explicit `remote_volume_up_guarded` / `remote_volume_down_guarded` service path using resolved active target + fallback entity chain, restoring practical detent→WiiM volume control behavior. Runtime track disposition: implemented (runtime remote input/action cleanup and control-path restore). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded runtime remote control-path restoration only. README parity: no material repo-state change.

- Runtime/Remote Encoder Settle-Window Accumulator (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): replace immediate per-update detent emission with an accumulator + settle-window flush model that aggregates bursty quadrature deltas and emits a single net-direction detent after edge activity quiets. This targets severe mechanical bounce where edge overflow warnings persist and prior confirmation-lock heuristics either contaminated direction or under-counted detents. Runtime track disposition: implemented (runtime encoder event-shaping only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded runtime signal-conditioning redesign only. README parity: no material repo-state change.

- Runtime/Remote Encoder Direction Confirmation Filter (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): replace prior reversal-lock heuristic with a bounded two-sample same-direction confirmation gate so direction flips are accepted without long lockouts while transient single-step bounce reversals are rejected. Runtime track disposition: implemented (runtime encoder stabilization logic only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded runtime detent-direction normalization only. README parity: no material repo-state change.

- Runtime/Remote Encoder Direction Polarity Correction (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): based on CCW-only physical test logs still yielding majority CW detents, apply explicit runtime direction multiplier inversion and tighten bounded anti-chatter timing windows to reduce false-sign emissions while preserving one-detent event semantics. Runtime track disposition: implemented (runtime polarity/stability tuning only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded runtime encoder normalization pass only. README parity: no material repo-state change.

- Runtime/Remote Encoder Quadrature Burst Hardening (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): after validating continued mixed-direction detent logs and `rotary_encoder` overflow warnings during CW-only physical turns, move encoder capture to polling (`use_interrupt: false`) and add bounded direction-reversal suppression windows to reject immediate opposite-sign bounce clusters around each detent. Runtime track disposition: implemented (runtime encoder stability hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth reassignment; bounded runtime signal-conditioning pass only. README parity: no material repo-state change.

- Runtime/Remote Battery Divider Calibration Adjustment (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): align reported battery voltage with measured pack voltage by increasing `battery_divider_ratio` based on live evidence (`3.88 V` reported vs `4.05 V` measured), reducing systematic low-bias in `Remote Battery Voltage` and derived SoC telemetry. Runtime track disposition: implemented (calibration tuning only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; bounded runtime telemetry calibration hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Burst-Bounce Suppression Gate (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): after confirming edge storms on physical rotation, add a bounded time-based detent emit gate to suppress rapid bounce bursts and mixed-direction chatter while preserving ESPHome-native rotary counting. This provides a deterministic stabilization baseline for functional validation before deeper hardware/wiring tuning. Runtime track disposition: implemented (bounded runtime noise suppression only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime debounce hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Detent-Level Baseline Refinement (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): after validating callback visibility, reduce edge-bounce log storms by moving from raw edge-direction callbacks to detent-oriented position-delta logging using ESPHome-native `rotary_encoder` counting (`resolution: 4` + `on_value` delta classification). This keeps the implementation canonical to ESPHome guidance while making real rotation behavior observable at practical detent granularity. Runtime track disposition: implemented (bounded baseline refinement only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime observability granularity hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Baseline Callback Visibility Hotfix (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix missing visible detent callback logs after clean reset by setting `on_clockwise`/`on_anticlockwise` logger actions to explicit `INFO` level so encoder turns are observable with the current logger configuration. Runtime track disposition: implemented (baseline observability correction only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; bounded runtime logging-visibility hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Full Reset to ESPHome Canonical Baseline (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): remove all accumulated custom/guarded encoder behavior and reset the remote to a clean ESPHome-native `rotary_encoder` baseline using the existing pin assignments only, with direct `on_clockwise`/`on_anticlockwise` logging from the official guidance as the new starting point for subsequent iteration. Runtime track disposition: implemented (clean-slate encoder baseline reset). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; bounded runtime reset for deterministic re-baselining only. README parity: no material repo-state change.

- Runtime/Remote Encoder Emergency Recovery Rollback (Control Restored) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): after field report confirmed hard regression (no encoder logs and no volume changes), rollback from the new polled quadrature decode path to the last known-working rotary input path to immediately restore functional control. This is an emergency continuity rollback; advanced stability hardening remains pending follow-up after control restoration is verified. Runtime track disposition: implemented (control continuity recovery). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; emergency runtime recovery only. README parity: no material repo-state change.

- Runtime/Remote Encoder Definitive Overflow-Queue Elimination (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): live evidence showed persistent `rotary_encoder` overflow warnings and mixed CW/CCW output during single-direction turns, confirming ISR event-queue overload under noisy burst transitions. Replace the ISR-based `rotary_encoder` path with deterministic polled quadrature state-machine decoding (Gray-transition validation + detent accumulation), removing the overflow source entirely and restoring direction truthfulness under burst/noise conditions. Runtime track disposition: implemented (decoder-path replacement for root-cause elimination). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; encoder correctness hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Overflow-Burst Opposite-Blip Suppression (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): live logs showed decoder overflow warnings (`Captured more rotation events than expected`) and occasional opposite-direction actions during single-direction turns. Root cause is bursty/noisy quadrature transition capture under rotary ISR decode windows. Mitigation: keep settled commit flow but add explicit short-window opposite-direction suppression so transient reverse ticks during the same motion burst are ignored instead of converted into reverse volume actions. Runtime track disposition: implemented (direction-truthfulness hardening under burst noise). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; encoder burst-noise behavior hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Circular Direction Root-Cause Fix (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): correct the core direction error where volume could go up then down during same-direction turns. Root cause: immediate per-tick dispatch from rotary position deltas allowed quadrature contact bounce (`+/-` jitter) to emit opposite-direction actions. Fix: accumulate signed movement and commit only settled net direction after a short idle window, so bounce cancels instead of generating false reverse actions. Runtime track disposition: implemented (settled net-motion decoder commit path hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; encoder direction correctness hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Emergency Functional Recovery (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): restore a known-working rotary input path after live diagnostics confirmed regression-level behavior (“no volume change while rotating”) under the experimental phase-poll decoder. This slice prioritizes operator control continuity by rolling encoder handling back to the stable rotary component path, preserving existing guard/cooldown semantics and deep-sleep test posture. Runtime track disposition: implemented (functional recovery first, follow-on stability tuning deferred). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; emergency runtime control continuity recovery only. README parity: no material repo-state change.

- Runtime/Remote Encoder No-Event Live Diagnostics Pass (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): after OTA runtime showed stable battery scaling but user-observed “no encoder result while rotating”, add explicit phase-level transition diagnostics in the remote quadrature path so A/B edge visibility is provable in live logs (distinguishing wrong-pin/no-signal conditions from decode-threshold issues). This slice is diagnostics-first and keeps behavior contracts unchanged while enabling deterministic root-cause isolation during active rotation tests. Runtime track disposition: implemented (live signal observability hardening for encoder path). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; encoder signal-path diagnostics hardening only. README parity: no material repo-state change.

- Runtime/Remote Battery+Encoder Stabilization + Fast Deep-Sleep Test Window (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix two active runtime issues from live evidence: (1) incorrect battery telemetry scaling and (2) overflow-driven rotary direction noise (`Captured more rotation events than expected` with phantom CCW events). Battery model is recalibrated to the observed device voltage baseline by updating divider scaling, deep-sleep test duration is shortened to `30s` for rapid wake-cycle verification, and rotary input path is migrated from `sensor.rotary_encoder` ISR decoding to a bounded quadrature state-machine poll in ESPHome intervals to eliminate overflow warning storms and reduce false reverse detents under bounce/chatter. Runtime track disposition: implemented (measurement calibration + input-path stabilization + test-cycle sleep tuning). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime reliability/tuning hardening only. README parity: no material repo-state change.

- Runtime/Remote Deep-Sleep Wake Correctness + Cleanup Pass (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): perform targeted cleanup and correct ESP32-C6 deep-sleep wake configuration for encoder-based wake intent. Replace timer-only deep-sleep posture with explicit EXT1 multi-pin wake on encoder channels (`GPIO2`/`GPIO3`, `ANY_LOW`) so rotational edge activity can wake the node regardless of direction while preserving existing idle-sleep behavior and control contracts. Runtime track disposition: implemented (power/wake path correctness + config hygiene). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime power/wake reliability hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Responsiveness + Burst-Coalescing Tuning (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): improve “normal knob feel” by reducing over-conservative detent/direction guard windows and coalescing rotary `on_value` delta bursts into single guarded volume writes with multi-step UI deltas (instead of issuing repeated per-detent script executions that were self-throttled and dropped under fast turns). This restores responsive multi-detent behavior while preserving existing press-crosstalk protections and route/write contracts. Runtime track disposition: implemented (input responsiveness tuning and burst-step handling). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime interaction-feel correction only. README parity: no material repo-state change.

## 2026-06-13

- Runtime/Remote Emergency Input Recovery (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): rollback custom quadrature migration after field regression where encoder rotation produced no volume change and no rotation logs. Restore known-working `rotary_encoder` path immediately to recover operator control continuity while preserving prior center-press guard hardening and route/write contract behavior. Runtime track disposition: implemented (functional rollback for control continuity). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; emergency runtime rollback only. README parity: no material repo-state change.

- Runtime/Remote Encoder Overflow Elimination Pass (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): replace `rotary_encoder` ISR-driven decode with a guarded custom quadrature state-machine path to avoid `Captured more rotation events than expected` overflow bursts and reduce bounce-induced direction flips under real rotation. Added transition timing guard and retained center-press crosstalk protections. Runtime track disposition: implemented (decoder-path migration; control/write contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime input-path stabilization only. README parity: no material repo-state change.

- Runtime/Remote Encoder ISR Stability Guard (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): decouple deep-sleep wake configuration from encoder signal pins to avoid shared-pin interrupt contention during active rotary use. Keep deep-sleep timer behavior but remove wake-pin coupling while rotation reliability is being stabilized. Runtime track disposition: implemented (input-path isolation; control/write contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime interrupt-path hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Direction Stability Hardening (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): follow-up to recovery path by tuning rotary decode for noisy quadrature conditions (full-detent `resolution: 4`) and widening opposite-direction guard timing to suppress stray bounce-driven reversals while preserving intended user reversals. Removed stale custom quadrature globals no longer used after rollback to the `rotary_encoder` path. Runtime track disposition: implemented (decode hardening; write contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime input stability tuning only. README parity: no material repo-state change.

- Runtime/Remote Encoder Logging Regression Recovery (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix non-responsive encoder behavior introduced by the raw polled quadrature path (rotation produced no detent logs/volume actions). Restore the proven `rotary_encoder` sensor-driven decode path with net-delta handling and keep strengthened center-press crosstalk guards, returning reliable rotation event logging while preserving false-press suppression improvements. Runtime track disposition: implemented (input decode recovery + guard retention; write contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; regression recovery only. README parity: no material repo-state change.

- Runtime/Remote Encoder-Press Crosstalk Regression Fix (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): correct a post-decoder-migration regression where center-press actions could fire while rotating despite no intentional button press. Add raw quadrature activity timestamp tracking on any A/B transition and reuse that activity guard in press filtering/qualification so rotation-induced switch crosstalk cannot satisfy press acceptance windows. Also strengthen press debounce timing for noisy transitions. Runtime track disposition: implemented (input qualification hardening; volume write contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; false-press suppression hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Overflow Hardening (Polled Quadrature Decoder) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): replace ESPHome rotary edge-decoder path with an internal polled quadrature state-machine decoder to eliminate `rotary_encoder` overflow bursts (`Captured more rotation events than expected`) that can invert effective direction under heavy edge chatter. New logic decodes net signed movement from Gray-state transitions and emits guarded volume detents only on full-step completion, preserving existing volume write contracts while reducing opposite-direction false events. Runtime track disposition: implemented (input decode path hardening only; no control-hub contract mutation). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; encoder direction/truthfulness hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Direction Stability Follow-up (Overflow-Tolerant Decode) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): live post-OTA telemetry still showed intermittent opposite-direction events alongside repeated `rotary_encoder` overflow warnings (`Captured more rotation events than expected`). Replace direct edge callbacks (`on_clockwise`/`on_anticlockwise`) with net-position delta decoding via `on_value`, executing guarded volume scripts from signed position deltas so direction is derived from net movement rather than fragile single-edge ordering during bursty transitions. Runtime track disposition: implemented (input decode hardening only; volume write contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; encoder direction truthfulness hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder False Opposite-Direction Suppression (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix mixed `cw/ccw` detent logs during single-direction physical rotation by hardening rotary decode and direction-change acceptance. Change rotary decoder sensitivity from `resolution: 1` to `resolution: 4` (full quadrature cycle) to reduce partial-transition bounce artifacts, and add single-event opposite-direction confirmation gating so a lone reverse blip cannot immediately flip effective direction. Runtime track disposition: implemented (input-path stability hardening; volume write/service contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; encoder direction-truthfulness hardening only. README parity: no material repo-state change.

- Runtime/Remote Battery Telemetry Disablement (No Verified VBAT Sense Path) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): disable battery voltage/SoC publishing by setting `remote_has_battery=false` after operator-confirmed TP4054-only board power path review (charger IC present but no verified onboard ADC-coupled VBAT sense line for accurate telemetry). This prevents physically impossible battery readings from being surfaced and avoids false low-battery posture decisions. Runtime track disposition: implemented (telemetry gating only; control/write path unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; measurement-safety hardening only. README parity: no material repo-state change.

- Runtime/Remote Battery ADC Range + Stability Fix and Detent Log Quieting (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): correct implausible battery telemetry (`~0.8–2.4V` with persistent `0%` SoC) by forcing explicit ESP32-C6 ADC attenuation (`12db`) on battery input and adding median smoothing on raw ADC samples to reduce clipping/jitter artifacts before voltage/SoC derivation. Also reduce route detent visibility noise by downgrading resolved route write logs from `INFO` to `DEBUG` while preserving behavior. Runtime track disposition: implemented (measurement and observability tuning only; control/write contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; battery telemetry correctness and operator log-noise hardening only. README parity: no material repo-state change.

- Runtime/Remote Detent Log Noise Cleanup (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): remove temporary per-lane raw diagnostic spam (`route_feeds up/dn ...`) now that route hydration is verified stable, while retaining concise resolved-route detent logs (`route_host/route_hosts/route_port`) for operator visibility. Runtime track disposition: implemented (observability cleanup only; control behavior unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; logging-noise reduction only. README parity: no material repo-state change.

- Runtime/HA ESPHome Entry Static-IP Alignment (`.storage/core.config_entries`, `docs/CHANGELOG.md`): after remote static-IP deployment, align HA ESPHome entry host for `spectra-ls-remote` to `192.168.10.41` so HA API subscriptions persist against the pinned network identity and do not regress to stale `.209`. Runtime track disposition: implemented (control-plane host alignment). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; transport identity alignment only. README parity: no material repo-state change.

- Runtime/Remote Static IP Hardening (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): pin `spectra-ls-remote` to static network identity to prevent host drift between HA ESPHome entry and device runtime IP. Applied `manual_ip` with `static_ip: 192.168.10.41`, `subnet: 255.255.255.0` (`/24`), `gateway: 192.168.10.1`, and `dns1: 192.168.10.7`. Runtime track disposition: implemented (network transport stability). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; deployment-network stability hardening only. README parity: no material repo-state change.

- Runtime/HA ESPHome Entry Host Realignment (`.storage/core.config_entries`, `docs/CHANGELOG.md`): resolve remote `homeassistant` feed starvation (`route_feeds` all lanes `has_state=0`) by correcting Home Assistant ESPHome config-entry host for `spectra-ls-remote` from stale `192.168.10.207` to active `192.168.10.209` so HA re-establishes API subscription to the remote device and upstream route sensor states can hydrate. Runtime track disposition: implemented (control-plane connectivity correction; firmware write-path unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; HA transport attachment correction only. README parity: no material repo-state change.

- Runtime/Remote HA State Subscription Enablement (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): root-cause unresolved route telemetry after OTA evidence (`route_feeds` all lanes `has_state=0`) as missing/disabled Home Assistant state ingestion on remote API; explicitly enable HA state subscriptions in remote `api` configuration (plus services continuity) so route host/hosts/port text sensors can hydrate from HA. Runtime track disposition: implemented (API ingestion contract hardening; write-path unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; transport-observability parity hardening only. README parity: no material repo-state change.

- Runtime/Remote Route Feed Evidence Logging (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): add per-detent raw upstream feed diagnostics for route host/hosts/port across all read lanes (generic ESP exports, prefixed ESP exports, MA fallback helpers) including `has_state` flags and raw values, so unresolved route telemetry is root-caused from runtime evidence instead of assumptions. Runtime track disposition: implemented (diagnostic observability only; write-path unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; evidence instrumentation only. README parity: no material repo-state change.

- Runtime/Remote Prefixed ESP Route-Entity Compatibility Shim (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): resolve remaining `<unresolved>` route telemetry caused by HA entity-id prefixing (`sensor.living_room_spectra_ls_system_esp_control_*`) by adding a dual-feed subscription chain: generic ESP route entity IDs plus prefixed ESP route entity IDs discovered in HA registry, then using first-resolved value for route logs before runtime fallback/cache lanes. Runtime track disposition: implemented (diagnostic read-lane compatibility shim only; volume write-path unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; HA entity-id compatibility hardening for observability only. README parity: no material repo-state change.

- Runtime/Remote HA Entity-Domain Reuse Correction for ESP Route Telemetry (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): correct remote route telemetry subscriptions to reuse the same HA entity-domain semantics as `spectra_ls_system`: ESPHome `text_sensor` exports are represented in Home Assistant under `sensor.*` entity IDs. Revert remote subscriptions from `text_sensor.spectra_ls_system_esp_control_*` to `sensor.spectra_ls_system_esp_control_*` so host/hosts/port states can hydrate correctly. Runtime track disposition: implemented (diagnostic subscription-domain correctness only; volume write-path unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; observability correctness hardening only. README parity: no material repo-state change.

- Runtime/Remote ESP Route-Telemetry Entity-Domain Fix (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix persistent `<unresolved>` route logs after ESP telemetry export by correcting remote subscription entity IDs from `sensor.spectra_ls_system_esp_control_*` to the correct ESPHome text-sensor entities `text_sensor.spectra_ls_system_esp_control_*`. The exported system telemetry was valid, but remote subscriptions targeted the wrong HA domain, preventing state hydration. Runtime track disposition: implemented (diagnostic subscription correctness only; write-path behavior unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; route observability correctness hardening only. README parity: no material repo-state change.

- Runtime/System+Remote Explicit Route Telemetry Export (ESP Cache Surfaces) (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): add explicit HA-exported system route telemetry entities sourced from live/cached control lane (`ESP Control Hosts`, `ESP Control Host`, `ESP Control Port`) and switch remote route-context logging subscriptions to these system-exported surfaces so detent logs reflect real runtime routing state even when component/runtime helper entities are transiently unresolved from the remote’s HA read lane. Runtime track disposition: implemented (observability path hardening only; volume write semantics unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; route-telemetry visibility correctness hardening only. README parity: no material repo-state change.

- Runtime/Remote System-Aligned Route Feed Resilience (Cache + Forced Refresh) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): mirror `spectra_ls_system` host-ingest resilience on the remote by caching last-valid control route values (`host/hosts/port`) from both primary (`sensor.component_control_*`) and runtime-fallback (`sensor.ma_control_*`) surfaces, then using cached values when live feeds are transiently unresolved. Add periodic `homeassistant.update_entity` refresh calls for route feeds and emit explicit `<unresolved>` placeholders when no valid route context has ever been observed. Runtime track disposition: implemented (diagnostic telemetry resilience hardening only; volume write-path unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; observability robustness and operator-trace clarity hardening only. README parity: no material repo-state change.

- Runtime/Remote Route-Context Fallback Surface Resolution (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix empty remote route-context logs (`route_host= route_hosts= route_port=`) by adding fallback telemetry reads from stable runtime host surfaces (`sensor.ma_control_host`, `sensor.ma_control_hosts`, `sensor.ma_control_port`) whenever component-native route surfaces are unresolved. Remote detent logs now resolve route context from component-first then runtime-fallback contracts for operator visibility while preserving unchanged volume write semantics (`number.set_value` lane). Runtime track disposition: implemented (diagnostic visibility correction only; routing ownership unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; observability robustness hardening only. README parity: no material repo-state change.

- Runtime/Remote Control-Host Context Logging (Visibility Only) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): add remote-side read-only logging of resolved control host/hosts/port contracts during volume detents so operator logs show where lane writes are expected to route (`route_host`, `route_hosts`, `route_port`) while preserving existing reliable HA number-write lane behavior. This is observability-only and does not alter routing ownership or send-path semantics. Runtime track disposition: implemented (remote diagnostics visibility only; write behavior unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; operator traceability hardening only. README parity: no material repo-state change.

- Runtime/Remote Volume Detent Reliability Fix (UI-Step Deterministic Writes) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix perceived no-op remote turns caused by percent-to-UI quantization (`1..11`) where multiple `5%` detents mapped to the same UI value and therefore emitted no new HA `number` write. Remote volume scripts now step the runtime write lane in deterministic UI increments (`±1` per detent, clamped to UI min/max) with lane-local shadow state, while retaining percent estimate logging for operator visibility. Runtime track disposition: implemented (remote write-shaping correction only; host routing lane ownership unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; detent-to-write reliability hardening only. README parity: no material repo-state change.

- Runtime/Remote Deep-Sleep Wake Polarity Correction for Encoder Turn (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix post-sleep encoder-turn wake failures on ESP32-C6 by switching deep-sleep wake pin mode from `KEEP_AWAKE` to `INVERT_WAKEUP` for encoder channel wake source so low-pulse quadrature transitions can trigger wake reliably without requiring stable active-level hold at sleep boundary. Runtime track disposition: implemented (remote wake behavior correction only; volume lane/OLED/component contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; power/wake reliability hardening only. README parity: no material repo-state change.

- Runtime/Remote Deep-Sleep Wake-on-Encoder Restore (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix remote failing to wake on encoder interaction after idle deep-sleep entry by wiring ESP deep-sleep wake source to encoder channel A (`wakeup_pin: ${encoder_pin_a}`) with keep-awake guard mode, preserving battery sleep posture while allowing physical dial interaction to wake the node. Runtime track disposition: implemented (remote power/wake behavior correction only; volume/OLED/component contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; wake-path reliability hardening only. README parity: no material repo-state change.

- Runtime/Remote Volume Lane Target-Decoupling Simplification (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): remove remote-side target resolution/sync from volume up/down scripts and treat `number.control_board_v2_arylic_volume_set` as the sole master write lane for volume detents. Remote volume stepping now uses lane-local shadow state + sensed volume fallback only, without helper/fallback/ESP target authority lookups or `spectra_ls.set_active_target` side effects. This eliminates persistent `volume_lane_default` regressions and aligns architecture with contract ownership (runtime control-hub owns active-target routing; remote owns detent intent only). Runtime track disposition: implemented (remote volume script simplification only; transport button target semantics unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; write-lane contract alignment and degraded-path reliability hardening only. README parity: no material repo-state change.

- Runtime/Remote Unresolved Authority Recovery Fallback to Configured Target (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix persistent `volume_lane_default` targeting when helper + fallback + runtime authority sensors are all unresolved on the remote by adding a bounded final fallback to configured target (`remote_forced_target`) in non-forced mode, before shadow-lane defaulting. Also suppress helper-sync calls for the synthetic default lane and only sync when resolved target is a real media-player entity. This restores effective volume routing in degraded authority windows while preserving discovery-first behavior whenever live authority surfaces are available. Runtime track disposition: implemented (remote resolution/sync guard correction only; OLED/component contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; degraded-path control targeting reliability hardening only. README parity: no material repo-state change.

- Runtime/Remote Target Resolution Authority Fallback Expansion + Conditional Active-Target Sync (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix remote volume detents writing against unresolved shadow lane (`volume_lane_default`) by expanding target resolution chain to include runtime authority sensors (`sensor.ma_active_target`, `sensor.spectra_ls_system_esp_control_target`) before fallback shadowing, and by conditionally invoking `spectra_ls.set_active_target` only when helper target is unresolved and a valid authoritative target is recovered. This restores correct host targeting for number-write volume lane while avoiding constant per-detent target churn when helper is already healthy. Runtime track disposition: implemented (remote target-resolution and sync gating correction only; OLED/component contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; control-lane target-authority correctness hardening only. README parity: no material repo-state change.

- Runtime/OLED Fallback Lockdown — Remove Friendly-Target Reentry Paths (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix recurring regression where target-friendly labels (for example `Kitchen Speakers`) could return as now-playing title during wake/paused-passthrough fallback windows. Removed `ha_now_playing_friendly` / `ma_active_friendly` promotion from title/source fallback lanes in compute + render keepalive paths so title fallback is constrained to app/source context only. Runtime track disposition: implemented (display fallback hardening only; control contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; display fallback determinism hardening only. README parity: no material repo-state change.

- Runtime/OLED Policy Simplification — Remove `non_music` Display Distinction (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): removed media-class-based `non_music` blanking and suppression branches from now-playing compute/state gates so OLED decisions are driven only by live playback/meta freshness/source evidence (not class labels). This eliminates class-mislabel false blanks and messy branch divergence in passthrough scenarios. Runtime track disposition: implemented (display policy simplification only; control contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` mutation; equivalent display semantics reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; policy cleanup/hardening only. README parity: no material repo-state change.

- Runtime/OLED Passthrough Live Playback Non-Music Blank-Policy Relaxation (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix OLED blanking during active AppleTV/YouTube optical passthrough when payloads are classified as `non_music` or when progress freshness is sparse by allowing now-playing/source-context rendering whenever passthrough live-play evidence is present. This preserves fail-closed behavior for true idle/no-evidence windows while avoiding false blank screens during real playback. Runtime track disposition: implemented (display-state gate policy correction only; control contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; passthrough display continuity hardening only. README parity: no material repo-state change.

- Runtime/OLED Passthrough Title Fallback De-Pinning from Target Friendly (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix OLED showing target-friendly labels (for example `Kitchen Speakers`) instead of now-playing/app context under optical passthrough by tightening fallback order: prefer HA metadata/app/title lanes when available, avoid active-target-friendly promotion as primary title fallback in passthrough no-text windows, and only use normalized source context as last-resort label. Runtime track disposition: implemented (OLED fallback/selection policy correction only; control contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; display-context correctness hardening only. README parity: no material repo-state change.

- Runtime/Remote Passthrough Volume Lane Unresolved-Target Decoupling (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix remote encoder volume no-op during AppleTV/YouTube optical passthrough when `input_select.ma_active_target` and fallback target sensor are transiently blank. Volume up/down scripts no longer hard-return on unresolved target for the fixed number-write lane (`number.control_board_v2_arylic_volume_set`); instead they continue with a deterministic shadow key fallback and emit bounded diagnostics. Runtime track disposition: implemented (volume write-path gating correction only; play/pause/next target semantics unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote consumes same helper/sensor contracts with fail-open volume lane). P1/P2/P3 impact: no source-of-truth ownership reassignment; passthrough volume reliability hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Single-Detent Ghost-Reversal Suppression Tuning (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix residual one-detent CCW tests leaking delayed opposite-direction callbacks (~100–200ms later) by widening detent lockout windows (`encoder_step_min_interval_ms`, `encoder_direction_guard_ms`) so post-detent mechanical rebound cannot pass throttle/direction gates during unresolved-target sessions. Runtime track disposition: implemented (input debounce tuning only; service/helper contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; encoder detent determinism hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Guard Latch Ordering Fix (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix one-detent callback storms when helper/fallback target is unresolved by latching per-step throttle and direction guard immediately after accepting encoder edge (before target-resolution branching). Previously guard state updates occurred only after resolved-target path, allowing unresolved sessions to bypass suppression and emit CW/CCW floods. Runtime track disposition: implemented (remote guard ordering correction only; helper/entity contract unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote still consumes same contract surfaces). P1/P2/P3 impact: no ownership migration; input robustness correction only. README parity: no material repo-state change.

- Runtime/Remote Encoder Directional Bounce Hardening (ESPHome Native Decoder Alignment) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): after full-stack review and ESPHome rotary guidance alignment, switch remote rotary decode back to native directional callbacks with `resolution: 1` (most accurate pulse-per-cycle mode in ESPHome), remove custom settled-net dispatcher, and add a short opposite-direction rejection window to suppress immediate CW<->CCW bounce flips from a single physical detent. Runtime track disposition: implemented (remote input decode hardening only; target/service contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing HA contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; encoder direction correctness hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Detent Stabilization + Rotation-Adjacent Press Suppression (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix one-direction single-detent motion producing multiple mixed CW/CCW callbacks by replacing raw edge-triggered rotary handling with a settled net-step dispatcher (commit only after short inactivity window) and by filtering press-high events that occur immediately after encoder activity. This keeps intended rotation logs visible while suppressing bounce/crosstalk-induced event fanout and random rotation-adjacent press artifacts. Runtime track disposition: implemented (remote input processing hardening only; target/service contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing HA contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; encoder interaction correctness hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Press Qualification + Rotation Log Visibility Fix (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): refine the prior press-crosstalk mitigation after field feedback by restoring explicit CW/CCW detent logs in guarded volume scripts and switching play/pause trigger semantics to a qualified press-release path (minimum hold + zero-rotation-delta + recent-rotation guard), so rotation remains observable while rotation-induced switch chatter cannot execute button actions. Runtime track disposition: implemented (input event-qualification + observability hardening only; target/service contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing HA contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; input robustness + operator diagnostics hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Press Crosstalk Guard (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): mitigate center-button press bursts that were generating phantom rotary CW/CCW callbacks by adding an explicit post-press encoder ignore window and press-state guard in volume scripts, so button interactions do not leak into volume detents during switch bounce/crosstalk intervals. Runtime track disposition: implemented (remote input guard hardening only; target/service contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing HA contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; input-path robustness hardening only. README parity: no material repo-state change.

- Runtime/Remote Battery + Deep-Sleep Enablement (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): enable battery-aware runtime posture and deep-sleep path for the ESP32-C6 remote target by switching the remote feature flags from bring-up defaults to active battery mode (`remote_has_battery=true`, `remote_deep_sleep_enabled=true`). Runtime track disposition: implemented (remote power-mode behavior toggle only; control contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing HA contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; power-management behavior alignment hardening only. README parity: no material repo-state change.

- Runtime/Remote ESP32-C6 Super Mini Board Profile + Control Pinout Baseline (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): switch the remote node baseline from `esp32-s3-devkitc-1` to ESP32-C6 (`esp32-c6-devkitm-1`, `variant: esp32c6`) and adapt default encoder/button pin mapping for C6 bring-up (`encoder A/B/press` + `next` button) to avoid known strapping-pin defaults in the shipped pinout while preserving existing remote control contracts, interaction scripts, and safety gates. Runtime track disposition: implemented (board/variant + pin-baseline update only; service/entity contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing HA contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; hardware profile alignment hardening only. README parity: no material repo-state change.

- Runtime/OLED Stop-State Stale Metadata Eviction Guard (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix stale track text persisting on OLED after playback stops by adding a deterministic eviction guard that clears title/artist/album caches when transport is no longer actively playing, HA state is not paused keepalive posture, and now-playing freshness is stale beyond the contract window. This preserves paused passthrough keepalive behavior while preventing old metadata carryover during true stop/idle conditions. Runtime track disposition: implemented (OLED metadata cache/state policy correction only; control/remote contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; component contracts remain authoritative inputs). P1/P2/P3 impact: no source-of-truth ownership reassignment; stale-metadata fail-closed correctness hardening only. README parity: no material repo-state change.

## 2026-06-12

- Runtime/OLED Passthrough Paused Keepalive Regression Fix (`OPT -> blank`) (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/CHANGELOG.md`): fix OLED collapsing from short passthrough source code (`OPT`) to blank during paused/no-track windows by (1) normalizing source shorthand to stable human labels (`Optical In`, `Line In`, `Bluetooth`, `HDMI/ARC`), (2) preserving paused passthrough keepalive when HA display contract remains allowed and freshness evidence is recent, and (3) exempting valid paused keepalive windows from passthrough suppression in the display-state gate. Runtime track disposition: implemented (OLED compute/render/state policy correction only; remote volume behavior unchanged per operator freeze). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; component metadata contracts remain authoritative input surfaces). P1/P2/P3 impact: no source-of-truth ownership reassignment; passthrough display continuity and operator-context readability hardening only. README parity: no material repo-state change.

- Runtime/OLED Passthrough Paused Keepalive (No-Track Source Context) (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/CHANGELOG.md`): resolve operator-visible blank OLED (`audio|oled:-`) during healthy passthrough paused windows (for example `Optical In` with no title/artist payload) by allowing bounded source-context keepalive rendering/status when display contract is allowed and playback context remains recent, instead of forcing strict blank posture outside the short source-popup timer. Runtime track disposition: implemented (OLED render/status policy correction only; control host/transport contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; component contract surfaces remain authoritative inputs). P1/P2/P3 impact: no source-of-truth ownership reassignment; display-policy correctness and operator-context continuity hardening only. README parity: no material repo-state change.

- Runtime/Remote Volume Scale Contract Fix + OLED Target-Churn Guard (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix remote “tiny to max” jumps by honoring the active runtime volume-helper contract (`number.control_board_v2_arylic_volume_set` uses UI range `1..11`, not direct `0..100`). Remote detents now remain `5%` in shadow percent space and are mapped to UI scale before `number.set_value` writes, eliminating per-detent max-clamp jumps. Also stop unconditional target reassertion during volume detents (no automatic `spectra_ls.set_active_target` on each turn) so OLED/active-target context no longer gets pinned/churned to fallback labels (for example persistent `Kitchen Speakers`) during normal volume-only use. Runtime track disposition: implemented (remote mapping/target-write behavior correction only; active `spectra_ls_system` transport contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing component/runtime contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; control-lane contract correctness and operator-facing target context stability hardening only. README parity: no material repo-state change.

- Runtime/Remote Volume No-Op Root-Cause Fix (Target Lock + Oscillation) (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix remote encoder no-op behavior by removing non-forced fallback promotion to `remote_forced_target` (which could keep locking target to `media_player.kitchen_wiim` even when forced mode was disabled), and by hardening step-base selection to prefer stable per-target shadow progression instead of repeatedly re-syncing to noisy sensed-volume deltas that caused `48↔53` / `33↔38` oscillation loops. Runtime track disposition: implemented (remote volume-action logic correction only; active `spectra_ls_system` transport contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing service/sensor contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; remote routing/step determinism hardening only. README parity: no material repo-state change.

- Runtime/Remote Unresolved-Target Recovery Fallback Hardening (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): prevent encoder no-op during brief post-restart target-surface gaps by adding bounded target recovery fallback order in remote volume actions: helper target -> now-playing fallback target -> last resolved remote target -> configured forced target. This keeps default non-forced routing behavior intact while avoiding dropped volume detents when helper/sensor surfaces are transiently unresolved. Runtime track disposition: implemented (remote action resilience hardening only; active `spectra_ls_system` transport contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing service/sensor contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; transient recovery robustness hardening only. README parity: no material repo-state change.

- Runtime/Remote Detent-Size + Forced-Target Default Safety Correction (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): set remote encoder detent volume step to `5%` (`remote_volume_step_pct: "5"`) and disable forced-target pinning by default (`remote_force_target_enabled: "false"`) so active-target routing follows live helper/now-playing contracts instead of repeatedly reasserting Kitchen target during passthrough sessions (for example AppleTV/YouTube over Optical). Runtime track disposition: implemented (remote interaction/routing behavior correction only; active `spectra_ls_system` transport contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing service/sensor contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; remote UX/routing correctness hardening only. README parity: no material repo-state change.

- Runtime/Remote Forced-Target Switch Back to Kitchen WiiM (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): correct remote target-lane expectation mismatch by switching forced target from `media_player.spectra_ls_2` to `media_player.kitchen_wiim` while keeping forced mode enabled for deterministic control routing. Runtime track disposition: implemented (remote target-selection correction only; active `spectra_ls_system` transport code unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming existing service contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; forced-target routing alignment with operator-selected speaker endpoint only. README parity: no material repo-state change.

- Runtime/Remote Volume Entity-ID Correction + Shadow-Step Stabilization (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix no-audible-change regression after write-lane migration by correcting remote volume write target from non-existent `number.spectra_ls_system_arylic_volume_set` to active runtime entity `number.control_board_v2_arylic_volume_set`, and by adding bounded per-target shadow stepping so rapid encoder turns do not collapse to repeated `0/2` writes when component volume telemetry lags between detents. Runtime track disposition: implemented (remote write-target + step-state correction only; active `spectra_ls_system` transport path unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; existing service/sensor contracts consumed). P1/P2/P3 impact: no source-of-truth ownership reassignment; remote command determinism and control-lane correctness hardening only. README parity: no material repo-state change.

- Runtime/Remote Volume Write-Lane Contract Fix (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): correct remote volume control path to use the Spectra runtime write lane (`number.set_value` on Arylic volume-set entity) instead of generic `media_player.volume_up/down` calls, and gate service execution explicitly so rate-limit/unresolved-target branches cannot fall through to stale service invocations. Remote volume actions now (1) resolve/validate target intent, (2) compute bounded next volume from component now-playing volume telemetry, (3) pin active target through `spectra_ls.set_active_target`, and (4) write volume through the same ESP `arylic_tcp` contract path that emits `VOL enqueue host=<...> port=8899`. Runtime track disposition: implemented (remote write-path correction only; active `spectra_ls_system` transport contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; existing service contracts consumed as designed). P1/P2/P3 impact: no source-of-truth ownership reassignment; control-lane contract alignment and stale-call guard hardening only. README parity: no material repo-state change.

- Runtime/Remote Encoder Direction Correction (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): fix reversed encoder semantics by swapping default encoder A/B pin mapping so clockwise rotation aligns with expected volume increase behavior for current hardware wiring. Runtime track disposition: implemented (remote input direction fix only; active `spectra_ls_system` unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; remote control ergonomics correction only. README parity: no material repo-state change.

- Runtime/Remote Forced-Target Correction to Real Spectra Host Lane (`esphome/spectra_ls_remote.yaml`, `docs/program/SPECTRA-LS-REMOTE-SCOPE-PROGRESS.md`, `docs/CHANGELOG.md`): correct temporary forced-target routing from `media_player.kitchen_speakers` to the real Spectra control target `media_player.spectra_ls_2`, matching the validated runtime control host lane (`192.168.10.51:8899`) used by the main Spectra system. Runtime track disposition: implemented (remote target correction only; active `spectra_ls_system` unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote continues consuming HA service contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; forced-target routing correction for operator-intended host path only. README parity: no material repo-state change.

- Runtime/Remote Encoder Chatter Mitigation + Volume Action Observability (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): harden remote input behavior for rotation-only interaction by tightening press-button debounce/interrupt posture to reduce false press chatter and adding explicit rotation/service-intent logs (`volume_up` / `volume_down`) so runtime path verification is deterministic during live knob testing. Runtime track disposition: implemented (remote input/readability hardening only; active `spectra_ls_system` unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote remains HA service consumer). P1/P2/P3 impact: no source-of-truth ownership reassignment; remote diagnostics and input stability hardening only. README parity: no material repo-state change.

- Runtime/Remote Forced Target Override (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): add a temporary explicit forced-target mode for remote control actions so encoder/buttons always route to `media_player.kitchen_speakers`, bypassing HA control-hub active-target drift during TV/video/Plex context changes. Runtime track disposition: implemented (remote lane override only; active `spectra_ls_system` routing contracts unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote remains a consumer of HA services). P1/P2/P3 impact: no source-of-truth ownership reassignment; bounded remote targeting override for operator stability only. README parity: no material repo-state change.

- Runtime/Remote Batteryless Log-Noise + Target-Resolution Hardening (`esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): suppress batteryless devboard telemetry noise by stopping battery sensor updates when no battery is installed (instead of repeatedly publishing `NaN`), and reduce encoder/button unresolved-target warning spam by adding bounded warning-rate throttling plus fallback target resolution from now-playing entity context when helper target is unresolved. Runtime track disposition: implemented (remote interaction/logging hardening; active `spectra_ls_system` unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote lane consumes existing HA contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; remote bring-up robustness and operator signal-quality hardening only. README parity: no material repo-state change.

- Runtime/Remote No-Battery Devboard Safety Mode (`esphome/spectra_ls_remote.yaml`, `docs/program/SPECTRA-LS-REMOTE-SCOPE-PROGRESS.md`, `docs/CHANGELOG.md`): add an explicit no-battery development posture for placeholder ESP32-S3 bring-up by introducing `remote_has_battery` gating, suppressing battery telemetry outputs when no battery is installed, and preventing deep-sleep entry when battery hardware is absent. Runtime track disposition: implemented (batteryless bring-up safety for remote lane, no impact to active `spectra_ls_system`). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote node consumes existing HA contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; remote hardware-profile safety hardening only. README parity: no material repo-state change.

- Tooling/Runtime Flow — Separate ESPHome Operational Helpers for Remote Node (`bin/esphome_spectra_remote_build_local.sh`, `bin/esphome_spectra_remote_upload_local.sh`, `docs/program/SPECTRA-LS-REMOTE-SCOPE-PROGRESS.md`, `README.md`, `docs/CHANGELOG.md`): add a dedicated local staged build/upload helper pair for `esphome/spectra_ls_remote.yaml` so the remote lane follows the same operational guardrails as the main Spectra system (staged compile path, stale-upload prevention via rebuild-by-default, artifact summary, optional clean-stage, explicit device-target OTA). Runtime track disposition: implemented (separate remote operational flow now available without mutating active `spectra_ls_system` build path). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; remote tooling consumes existing HA contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; tooling/ops parity hardening only. README parity: updated with remote helper discoverability.

- Program+Runtime Planning — `Spectra LS Remote` Architecture Pivot + Initial ESPHome Scaffold (`docs/program/SPECTRA-LS-REMOTE-SCOPE-PROGRESS.md`, `README.md`, `esphome/spectra_ls_remote.yaml`, `docs/CHANGELOG.md`): pivot remote concept from analog pot/click-mute to encoder-first tabletop control box design (rotary encoder volume steps + center-press play/pause + separate momentary next-track button), remove mute-by-switch assumptions from MVP, and formalize comms posture as battery-aware BLE-bridge-primary strategy with Wi-Fi/API control path scaffolded first for immediate bring-up and validation. Added first-pass standalone ESPHome node scaffold (`spectra-ls-remote`) with battery telemetry, deep-sleep control knobs, and HA service action lanes for transport/volume operations. Runtime track disposition: implemented (new additive runtime node scaffold; existing active runtime lanes unchanged). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation in this slice; remote consumes existing HA control contracts). P1/P2/P3 impact: no source-of-truth ownership reassignment; additive remote lane introduction and architecture-direction codification only. README parity: updated to reflect remote product posture and control-path direction.

- Program/Hardware Planning — `Spectra LS Remote` Momentary Next-Track Switch Scope Update (`docs/program/SPECTRA-LS-REMOTE-SCOPE-PROGRESS.md`, `docs/CHANGELOG.md`): extend the remote project scope to include one additional momentary button lane for `next track` actions, with explicit control-hub compatibility guardrails for mixed-source environments (execute only when control handoff is ready and active source/target are transport-capable; passthrough sources remain no-op guarded). Updated behavior contract, milestones, risk register, and decisions log to reflect this input expansion while preserving single-dial-first product intent. Runtime track disposition: deferred with rationale (planning/docs-only slice; no runtime package behavior mutation). Component track disposition: deferred with rationale (planning/docs-only slice; no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; scope/governance readiness hardening only. README parity: no material repo-state behavior change.

- Program/Hardware Planning — New `Spectra LS Remote` Scope + Progress Baseline (`docs/program/SPECTRA-LS-REMOTE-SCOPE-PROGRESS.md`, `docs/README.md`, `docs/CHANGELOG.md`): add a dedicated project charter/progress tracker for a new standalone ESPHome node (`Spectra LS Remote`) focused on a single volume dial with integrated click-off switch semantics, with explicit battery-first target hardware direction (`ESP32-S3-DEVKIT-LIPO` final) and development target (`ESP32-S3` test module). Captured architecture scope, behavior contract (analog volume + discrete click switch), milestone gates, validation checklist, risk register, and decision log template so implementation can proceed without ambiguity. Runtime track disposition: deferred with rationale (planning/docs-only slice; no runtime package behavior mutation). Component track disposition: deferred with rationale (planning/docs-only slice; no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; scope/governance readiness hardening only. README parity: no material repo-state behavior change.

- Diagnostics/DT Template — Intentional Control-vs-Now-Playing Divergence Classification (`docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): fix false FAIL classification when control target is intentionally transport-anchored (for control path correctness) while now-playing metadata entity intentionally diverges under component metadata authority (for example WiiM transport target with Oracle/AppleTV metadata carrier). Validator now classifies this as an expected divergence posture when route/helper/ESP target alignment remains coherent and meta-target aligns to effective now-playing entity, preventing incorrect “selection authority drift” guidance in healthy split-role operation. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; diagnostics-only artifact). Component track disposition: implemented (component-owner diagnostics classification correctness hardening). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics signal-quality and operator guidance correctness hardening only. README parity: no material repo-state change.

- Component/Metadata Passthrough Promotion Transport-State Continuity Fix (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix passthrough metadata promotion where component now-playing entity could switch to an upstream metadata carrier in `paused` posture (for title/app recovery) while transport playback remained actively `playing`, causing downstream display consumers to blank despite valid promoted metadata. Component metadata prep now preserves transport `playing` state when passthrough promotion selects a non-playing metadata carrier, keeping title/app continuity without regressing idle fail-closed behavior. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged; equivalent failure mode reviewed). Component track disposition: implemented (metadata promotion state continuity hardening in owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; passthrough metadata/state contract correctness hardening only. README parity: no material repo-state change.

- Runtime/Metadata Selector Passthrough Carrier De-Pinning Fix (`packages/ma_control_hub/template.inc`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix now-playing metadata selection drift where passthrough transport entities (for example `Kitchen Speakers` on `Optical In`) could win `ma_meta_detected_entity` despite lacking title/artist/app payload, causing active meta entity and OLED contracts to pin to source-only context instead of upstream app metadata carriers (for example Apple TV / YouTube providers). Candidate scoring now demotes passthrough-without-metadata entities and promotes metadata-bearing app-context entities, with passthrough no-meta candidates blocked from winning unless they carry real metadata. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component metadata authority/contracts unchanged; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime metadata-candidate selection correctness hardening only. README parity: no material repo-state change.

- Runtime/OLED Passthrough Fallback Label Priority Hardening (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix active passthrough no-metadata rendering regressing to source-only top-line text (`Optical In`) even when richer non-source context is available. Runtime fallback now keeps source context as source/pill payload but promotes title from non-source context labels first (app/friendly), falling back to source label only when no richer context exists. This preserves active-playback unblank behavior and fail-closed idle suppression while reducing repeated source-only title stickiness. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component contract authority unchanged; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime passthrough title-selection correctness hardening only. README parity: no material repo-state change.

- Runtime/OLED Active Passthrough Source-Context Fallback Guard Fix (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix blank OLED during active passthrough playback (AppleTV/YouTube over Optical In) caused by an unreachable condition in no-metadata passthrough fallback (`!source_hint_window_active` outer guard combined with inner `source_hint_window_active` title assignment check). Runtime now promotes live passthrough source label into title fallback when transport playback is actively live, preserving fail-closed blanking for true idle/no-play evidence windows. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component metadata authority/contracts unchanged; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; active passthrough display correctness hardening only. README parity: no material repo-state change.

## 2026-06-11

- Runtime+HA/WLED Remove Palette Hardcoding (Dynamic Sampling Restoration) (`bin/spectra_wled_palette_e2e.py`, `docs/CHANGELOG.md`): rollback the Traffic Light canonical-color normalization guard and restore fully dynamic WLED-driven sampling so room-target assignments are sourced only from live WLED payloads (no per-palette hardcoded color maps). This aligns behavior with discovery-first and non-hardcoded palette contract requirements. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; hardcode rollback and dynamic sampling fidelity restoration only. README parity: no material repo-state change.

- Runtime+HA/WLED Traffic-Light Palette Fidelity Guard (`bin/spectra_wled_palette_e2e.py`, `docs/CHANGELOG.md`): enforce non-magenta sampling when active WLED palette is `Traffic Light` by normalizing the sampled color set to canonical traffic-light anchors (red/yellow/green) before room-target assignment. This corrects operator-visible drift where effect-expanded palette rows could include pink/purple tones even with `Traffic Light` selected. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; palette-fidelity correction only. README parity: no material repo-state change.

- Runtime+HA/WLED Strict Palette-Sampling Contract Restoration (`bin/spectra_wled_palette_e2e.py`, `docs/CHANGELOG.md`): restore direct palette-following semantics so room-target assignments come from sampled WLED colors in palette order without synthetic remap heuristics (distance scoring or extra per-target diversification logic). This aligns sync output with intended “connected to WLED palette” behavior and removes pseudo-random-looking assignments. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; palette-contract fidelity correction only. README parity: no material repo-state change.

- Runtime+HA/WLED Duplicate Trigger Suppression (Package vs Top-Level Bind) (`packages/spectra_ls_lighting_hub.yaml`, `docs/CHANGELOG.md`): prevent dual execution of room palette sync on one button press when both package automation and top-level bind automation are present. Package trigger automation now self-suppresses whenever `automation.control_board_sync_to_vulva_button_bind` is enabled, ensuring a single deterministic shell-command run per press and reducing override/race-like color outcomes. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; trigger-lane determinism hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Contrast-Aware Target Assignment for Visible Re-Press Changes (`bin/spectra_wled_palette_e2e.py`, `docs/CHANGELOG.md`): improve practical visibility when re-triggering room sync by assigning colors per target using max-distance-from-current scoring (with uniqueness preference), so lights are less likely to appear unchanged when the sampled palette contains near-neighbor hues. This keeps discovery/scope behavior intact while making repeated button presses produce obvious remaps for non-WLED room lights. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; operator-visible sync feedback hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Per-Run Color Assignment Rotation (`bin/spectra_wled_palette_e2e.py`, `docs/CHANGELOG.md`): reduce perceived no-op behavior on repeated sync presses by rotating the palette start index each run before distributing colors to room targets, preventing fixed target-slot pinning (for example the same lamp always receiving the first color entry) while still using the sampled WLED palette set. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; operator-visible color remap responsiveness hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Effect-Mode Palette Fidelity Upgrade via `/json/palx` (`bin/spectra_wled_palette_e2e.py`, `docs/CHANGELOG.md`): fix persistent two-light pink bias in effect/palette mode by sampling expanded per-segment palette colors from WLED `/json/palx` (segment palette table) instead of relying only on `state.seg[*].col` base color slots. Sync now prefers richer `/json/palx` colors when available during palette/effect runs, yielding better distribution across room target lights. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; effect-mode color fidelity hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Dedicated Button Bind in `automations.yaml` (`automations.yaml`, `docs/CHANGELOG.md`): add a top-level, explicit automation binding `input_button.control_board_sync_wled_palette_room` directly to `shell_command.control_board_sync_wled_palette_room` (state trigger + call_service fallback guard) to eliminate package-reload drift and ensure deterministic dashboard button behavior in active runtime. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; trigger-path determinism hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Input-Button Trigger Hardening (`packages/spectra_ls_lighting_hub.yaml`, `docs/CHANGELOG.md`): add `call_service` event trigger fallback for `input_button.press` alongside state-trigger path, with explicit entity guard, so shell-command sync action executes reliably even when input-button state-trigger behavior is inconsistent. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; trigger reliability hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Button Trigger Routing Fix (`packages/spectra_ls_lighting_hub.yaml`, `docs/CHANGELOG.md`): fix button no-op caused by wrapper script execution lane remaining stuck (`script.control_board_sync_wled_palette_room` stuck `on/current:1`) while direct shell-command service calls succeeded. Input-button automation now invokes `shell_command.control_board_sync_wled_palette_room` directly, bypassing wrapper-script stall and restoring deterministic button execution. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; trigger-lane reliability hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Button Wrapper Deterministic Execution Fix (`packages/spectra_ls_lighting_hub.yaml`, `docs/CHANGELOG.md`): fix button-path no-op where wrapper script invocations (`script.control_board_sync_wled_palette_room`) could complete without executing the downstream shell command while direct `shell_command.control_board_sync_wled_palette_room` calls succeeded. Wrapper sequence is now shell-command-first/only to preserve deterministic button execution. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; HA wrapper-path reliability hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Live JSON Auto-Resolution from Entity Source (`bin/spectra_wled_palette_e2e.py`, `packages/spectra_ls_lighting_hub.yaml`, `docs/CHANGELOG.md`): fix color mismatch where room sync in HA-entity mode could sample only a single HA-reported color from the WLED light entity (often stale/solid-like) instead of the live WLED segment palette. Sync utility now attempts to auto-resolve WLED API URL from the selected/discovered source entity (entity attrs + derived `sensor.<wled>_ip` heuristics) and, when resolvable, samples live `/json/state` + `/json/palettes` before falling back to HA-attribute sampling. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; palette fidelity and room-sync correctness hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Button-Path Runtime Instrumentation (`packages/spectra_ls_lighting_hub.yaml`, `docs/CHANGELOG.md`): add explicit shell-command stdout/stderr capture to `/config/docs/testing/raw/_tmp_wled_palette_sync_exec.log` for button-triggered sync execution so failures can be diagnosed from HA runtime evidence instead of guesswork. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime diagnosability hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Button Path Interpreter Hardening (`packages/spectra_ls_lighting_hub.yaml`, `docs/CHANGELOG.md`): harden shell-command execution for HA button-triggered sync by switching to an explicit interpreter path (`/usr/local/bin/python3`) instead of PATH-dependent `python3`, reducing silent no-op risk when HA shell environment lacks expected PATH entries. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; HA command-lane execution reliability hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Room-Matched Source + Non-WLED Target Scope Enforcement (`bin/spectra_wled_palette_e2e.py`, `docs/CHANGELOG.md`): enforce intended sync contract: auto-discover WLED sources globally, resolve the source for the selected room when available, and apply sampled colors only to other lights in that same room (excluding discovered WLED entities from target writes). Room target discovery now also honors `no-spectra`/`no_spectra` label exclusions to keep non-participating lights out of sync target lists. This prevents source/self writes and keeps room sync behavior scoped to “one WLED source + other room lights” as designed. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; room-scoped discovery/targeting correctness hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Global Source Auto-Discovery (`packages/spectra_ls_lighting_hub.yaml`, `docs/CHANGELOG.md`): expand WLED source helper discovery from room-scoped area entities to global light inventory with room-first ordering, and broaden source matching from strict effect-token signature checks to WLED-capable effect-catalog detection (`effect_list` present/large or WLED naming hints) so helper options no longer omit valid WLED-style entities. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; discovery-surface completeness hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Room Sync Stale-Entity Guard (`bin/spectra_wled_palette_e2e.py`, `docs/CHANGELOG.md`): fix button-path no-op when room discovery includes stale/missing light entities (for example `light.your_vulva_main`) and `light.turn_on` is invoked with unavailable targets. Sync utility now filters room/fallback target entities against live HA state before apply, skips unavailable/unsupported targets, and records skipped reasons while continuing valid per-entity apply execution instead of failing the whole run on the first bad target. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; HA runtime write-path resilience hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Sync Button No-Op Root-Cause Fix (`packages/spectra_ls_lighting_hub.yaml`, `docs/CHANGELOG.md`): fix button-triggered room sync failing silently when helper placeholders were passed as literal CLI values (for example `--wled-url "unknown"`) and when shell command paths targeted host-only locations from HA runtime context. Shell-command argument templating now sanitizes room and WLED URL helper values to empty for placeholder states (`unknown`, `unavailable`, `none`, empty, `No Rooms`) and command paths now use HA runtime mounts (`/config/bin/...`, `/config/secrets.yaml`, `/config/docs/...`) to restore execution from HA button presses. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; HA trigger-path correctness hardening only. README parity: no material repo-state change.

- Runtime+HA/WLED Room Sync Discovery-First Source Resolution + Operator Feedback Hardening (`bin/spectra_wled_palette_e2e.py`, `packages/spectra_ls_lighting_hub.yaml`, `docs/CHANGELOG.md`): fix `Sync Room from WLED Palette` no-op behavior where helper/room source resolution could fail with no visible outcome (`No WLED source resolved...`). Sync utility now applies discovery-first WLED candidate resolution (room helper, inferred active-target room, room candidates, then global candidates) rather than requiring manual URL/entity preselection, and sync trigger flow now surfaces explicit operator feedback on invocation to avoid silent presses. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no ownership reassignment; discovery correctness and operator actionability hardening only. README parity: no material repo-state change.

- Runtime/OLED Passthrough Active-Playback Source-Context Unblank Fix (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix residual blank OLED windows during active passthrough playback (for example AppleTV YouTube over Optical In) where no track metadata is present but transport/playback evidence remains active. Passthrough no-text suppression now stays fail-closed for true idle/no-evidence conditions while allowing active-playback source-context rendering instead of forcing `audio|oled:-` blank posture. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component metadata authority/contracts unchanged; equivalent failure mode reviewed). P1/P2/P3 impact: no ownership reassignment; passthrough active-window display correctness hardening only. README parity: no material repo-state change.

- Runtime+ESP/HA-First WLED Room Palette Sync Path (`packages/spectra_ls_lighting_hub.yaml`, `bin/spectra_wled_palette_e2e.py`, `esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-lighting.yaml`, `docs/CHANGELOG.md`): add a Home Assistant-native room sync workflow that can be triggered from HA and from ESP, with a future physical momentary button placeholder wired in substitutions and RP input handling. The HA package now provides WLED source selection helpers, a room sync trigger button/script path, and a shell-command wrapper that executes the WLED sync utility. The sync utility now supports HA-first source selection (room/helper-selected WLED entity) when direct WLED URL input is not provided, while preserving direct URL mode for existing workflows. Runtime track disposition: implemented (HA package + ESP trigger plumbing + script source-selection hardening). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; control-surface integration and operator workflow hardening only. README parity: no material repo-state change.

## 2026-06-10

- Docs/Ops Auth Reference Hardening (`docs/notes/NOTES-control-board-2.md`, `docs/control-py/NOTES-control-board-2.md`, `docs/CHANGELOG.md`): add explicit operator guidance that Home Assistant API auth for diagnostics/tooling is sourced from `/config/secrets.yaml` key `ha_ll_token` (long-lived token; `Bearer` prefix optional in tooling because clients can normalize either form). This prevents repeated auth confusion during runtime triage. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; documentation hardening only). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation). P1/P2/P3 impact: no source-of-truth ownership reassignment; operator runbook clarity hardening only. README parity: no material repo-state change.

- Runtime/HA WiiM Startup Reload-Loop Guard (`custom_components/wiim/__init__.py`, `docs/CHANGELOG.md`): fix repeated WiiM platform setup/reload churn by hardening the config-entry update listener to reload only when options actually change. Setup-time entry data/title/cache updates (endpoint/capabilities/title normalization) now no longer self-trigger reload storms. Runtime track disposition: compatibility-shimmed (runtime host contracts unchanged; consumer integration stability hardening only). Component track disposition: implemented (custom integration setup lifecycle correctness fix). P1/P2/P3 impact: no source-of-truth ownership reassignment; startup/reload loop suppression only. README parity: no material repo-state change.

- Runtime/HA Workstation Access Hardening During Active Diagnostics (`configuration.yaml`, `docs/CHANGELOG.md`): prevent repeated operator/workstation lockouts during iterative recovery triage by disabling automatic IP bans and increasing login-attempt tolerance in the active HTTP config (`ip_ban_enabled: false`, `login_attempts_threshold: 50`). This is a temporary diagnostics posture to keep admin access stable while startup/crash triage continues. Runtime track disposition: implemented (access-control lockout mitigation). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation; non-owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; operational access continuity hardening only. README parity: no material repo-state change.

- Component+Diagnostics/Passthrough Metadata Stale-Album Guard (`custom_components/spectra_ls/metadata_stack.py`, `docs/testing/raw/oled_now_playing_validation.jinja`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix optical/line-in passthrough metadata contamination where selected transport entities could carry stale `media_album_name` from prior sessions (for example old MA queue album) while current passthrough title/artist were fresh. Component metadata prep now suppresses passthrough album propagation when source is non-meta, passthrough promotion did not occur, title is present, and app context is absent—preventing mixed-era metadata payloads from reaching OLED contracts. Added explicit telemetry (`passthrough_album_suppressed`) for template-side diagnosis. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Component track disposition: implemented (metadata-prep hygiene guard in component owner path). P1/P2/P3 impact: no source-of-truth ownership reassignment; passthrough metadata correctness hardening only.

- Runtime+Validation/WLED Smart Adaptive Strategy (palette/effect aware) (`bin/spectra_wled_palette_e2e.py`, `docs/testing/raw/wled_palette_room_e2e_runbook.md`, `docs/CHANGELOG.md`): restore complementary-color capability as an automatic strategy gate instead of operator-forced behavior. The E2E script now detects live WLED operating context and uses direct sampled-color mapping when palette/effect/playlist activity is present, while only switching to complementary generation for true solid/static single-color states. This prevents false complementary overrides during active WLED palette/effect playback and preserves adaptive behavior for real solid-color scenes. Runtime track disposition: implemented (tooling behavior correction only; no runtime write-authority ownership cutover). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; operator-intent alignment and write-path correctness hardening only.

- Runtime+Validation/WLED Palette-Following Enforcement (remove solid-mode branch) (`bin/spectra_wled_palette_e2e.py`, `docs/testing/raw/wled_palette_room_e2e_runbook.md`, `docs/CHANGELOG.md`): remove the optional solid/complementary strategy controls and restore strict palette-following behavior so sampled WLED segment colors are always mapped directly to target lights. Removed `--solid-mode`, `--max-complement-colors`, and `--force-solid` to avoid accidental non-palette writes during effect/palette operation. Runtime track disposition: implemented (tooling behavior correction; no runtime write-authority ownership cutover). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` contract mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; operator behavior correctness hardening only.

- Runtime+Validation/WLED Solid-Color Complementary Palette Strategy (`bin/spectra_wled_palette_e2e.py`, `docs/testing/raw/wled_palette_room_e2e_runbook.md`, `docs/CHANGELOG.md`): enhance the E2E WLED sampler so solid-color conditions auto-switch to a generated complementary palette strategy (`--solid-mode complementary`) instead of repeating a single color across all room targets. Add programmable controls for strategy behavior (`--solid-mode`, `--max-complement-colors`, `--force-solid`) and emit explicit output telemetry (`solid_detected`, `color_strategy`, `source_sample_rgb`) for deterministic downstream automation/integration logic. Runtime track disposition: implemented (tooling behavior hardening only; no runtime write-authority ownership cutover). Component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` contract mutation in this slice; integration consumers can use emitted strategy metadata). P1/P2/P3 impact: no source-of-truth ownership reassignment; validation/actionability hardening only.

- Runtime+Validation/WLED Palette Live-Sample End-to-End Test Harness (`bin/spectra_wled_palette_e2e.py`, `docs/testing/raw/wled_palette_room_e2e_runbook.md`, `docs/README.md`, `README.md`, `docs/CHANGELOG.md`): add a deterministic operator-grade E2E utility that (1) samples live WLED palette/segment colors from `/json/state` + `/json/palettes`, (2) gathers room light entities from Home Assistant area contracts (or explicit entity fallback), (3) maps sampled colors across discovered targets with optional dry-run safety, and (4) emits integration-ready summary output for Spectra LS workflow stitching. Runtime track disposition: implemented (additive tooling/validation lane, no runtime write-authority cutover). Component track disposition: compatibility-shimmed (custom-component behavior/contracts unchanged; integration-readiness notes added for service wiring follow-on). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics/operator workflow hardening only. README/wiki parity: README + docs index updated in same change set.

## 2026-06-09

- Docs/Control-Contract Alignment Sweep + Drift Corrections (`docs/circuitpy/RP-LEGEND.md`, `docs/hardware/WIRING-LAYOUT-PROTOCOL.md`, `docs/hardware/SUBSTITUTIONS-TUNING-LEGEND.md`, `docs/CHANGELOG.md`): perform a deep docs-vs-runtime control sweep and correct drift in documented RP analog baseline constants and active substitutions defaults, while recording current control-contract watch-items for analog expansion safety (mode-nav mirrored button overlap risk and pending `meta_select_button_id` wiring validation for event id `40`). Runtime track disposition: compatibility-shimmed (no runtime behavior or contract mutation in this docs-only slice). Component track disposition: compatibility-shimmed (no component behavior or contract mutation in this docs-only slice). P1/P2/P3 impact: no source-of-truth ownership reassignment; documentation accuracy + pre-expansion risk visibility hardening only. README parity: no material repo-state change.

- Runtime/Lighting Brightness-Only Apply Path Color-Coupling Fix (`esphome/spectra_ls_system/packages/spectra-ls-lighting.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix analog brightness interaction regression where slider/brightness-mode writes could also re-apply cached HS color payload, causing visible hue/saturation shifts during dim/brighten adjustments. Runtime brightness apply path now sends brightness-only payloads to `script.control_board_set_light_dynamic` and avoids injecting HS during brightness-only operations. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component metadata/control contracts unchanged; runtime lighting write consumer behavior corrected). P1/P2/P3 impact: no ownership reassignment; lighting write-path correctness hardening only. README parity: no material repo-state change.

## 2026-06-08

- Runtime/OLED Passthrough Source-Title Promotion Leak Closure + Status Telemetry Fail-Close Alignment (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix residual idle passthrough leak where source-only context (for example `Optical In`) could still be promoted into cached title text by guarded passthrough fallback logic, causing no-text suppression to be bypassed and OLED to remain source-labeled outside popup/wake intent. Runtime now blocks passthrough source-title promotion outside active source-hint windows, prevents passthrough placeholder re-injection in the late compute pass, and aligns `esp_oled_status` fallback policy to fail closed for passthrough no-track windows instead of exporting source-label text as pseudo-title. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component metadata authority/selection contracts unchanged; runtime display + telemetry consumer policy tightened). P1/P2/P3 impact: no ownership reassignment; source-only idle fail-close correctness hardening only. README parity: no material repo-state change.

- Runtime/OLED Source-Only Passthrough Strict Fail-Close Policy (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): tighten idle/readability policy so passthrough source-only windows (no title/artist/album payload) now fail closed to blank outside bounded source-popup or wake-overlay windows, regardless of upstream `playing` heartbeat noise. This prevents persistent overnight `Optical In` chrome when no track metadata exists and restores deterministic no-text passthrough blank posture. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component metadata/authority contracts unchanged; runtime display consumer policy tightened). P1/P2/P3 impact: no ownership reassignment; display fail-closed policy hardening only. README parity: no material repo-state change.

- Runtime/OLED Passthrough Idle Source-Heartbeat Suppression Fix (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix residual idle false-active OLED posture where passthrough source-only windows (`Optical In`/line-in style) could remain render-eligible from stale/heartbeat HA playing recency even with no track text and no concrete transport/position activity. Runtime passthrough no-text suppression now keys no-play evidence to transport-playing flags + advancing position evidence (not HA source-heartbeat recency), so idle passthrough fails closed to blank while preserving active playback paths with real activity signals. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component metadata/authority contracts unchanged; runtime display consumer gate corrected). P1/P2/P3 impact: no ownership reassignment; idle fail-closed correctness hardening only. README parity: no material repo-state change.

- Runtime/OLED Idle Passthrough False-Playing Gate Fix (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): fix idle no-track false-positive now-playing rendering where raw HA `playing` state could keep OLED passthrough/source+volume posture active without fresh playback evidence. Display-state gating now treats HA `playing` as effective only when paired with recent playback activity (meta or position recency), and passthrough silent-window suppression uses this effective signal instead of raw state. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component metadata/authority contracts unchanged; runtime consumer gate corrected). P1/P2/P3 impact: no ownership reassignment; idle fail-closed correctness hardening only. README parity: no material repo-state change.

- Runtime/Control Target Auto-Convergence Playback Gate + Trigger De-Dupe (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): tighten split-brain auto-heal semantics so runtime convergence requests execute only during active playback context (`playing`/`paused`) in passthrough windows, preventing idle passthrough source posture from driving unnecessary target handoff attempts. Also remove redundant nested convergence trigger in the forced-refresh branch because interval-level autonomous reevaluation already covers that path. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component service schema/authority guards unchanged). P1/P2/P3 impact: no ownership reassignment; runtime convergence precision/noise reduction only. README parity: no material repo-state change.

- Runtime/Control Target Deterministic Auto-Convergence Trigger Expansion (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/CHANGELOG.md`): eliminate remaining manual-poke dependence for passthrough split-brain recovery by extending bounded `ensure_control_target_sync` execution beyond control-write paths to autonomous HA state transitions and the periodic HA refresh cadence. Runtime now reevaluates convergence when now-playing entity/source/play-state changes and on refresh-loop ticks, while preserving existing fail-closed guards (ambiguity gate, passthrough-only scope, resolved targets, cooldown window). Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component authority + service guards unchanged; runtime trigger coverage now reaches the existing service contract deterministically). P1/P2/P3 impact: no source-of-truth ownership reassignment; convergence timing/coverage hardening only. README parity: no material repo-state change.

## 2026-06-07

- Runtime/Control Target Split-Brain Auto-Heal Service-Payload Fix (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/CHANGELOG.md`): fix root-cause no-op in runtime passthrough split-brain auto-heal where `spectra_ls.set_active_target` was called with payload key `entity_id` instead of required `target`. This caused accepted service calls with empty requested target in component service handling, leaving `ma_active_target`/control host pinned to the wrong device during optical passthrough mismatch. Runtime now sends `target=<ha_now_playing_entity>` so active-target convergence can execute deterministically under the existing guarded auto-heal gate. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component contract unchanged; runtime now matches required service schema). P1/P2/P3 impact: no ownership reassignment; control-path convergence correctness hardening only. README parity: no material repo-state change.

- Runtime/Control Target Split-Brain Auto-Heal for Passthrough (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/CHANGELOG.md`): fix control-loss scenario where control target/host remained bound to an idle target (`ma_active_target`) while effective now-playing passthrough playback was on a different entity (`component_now_playing_entity` / `ha_now_playing_entity`), causing pot/encoder volume+EQ writes to hit the wrong host. Runtime now triggers bounded auto-heal handoff (`spectra_ls.set_active_target`) to the effective now-playing entity during passthrough split-brain windows (only when non-ambiguous and cooldown allows), then refreshes target options/pending window so host feeds converge before writes. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component contracts unchanged; runtime consumes existing `set_active_target` service). P1/P2/P3 impact: no ownership reassignment; control-path convergence hardening for passthrough split-brain only. README parity: no material repo-state change.

- Runtime/Control Host Cache Rehydration + Passthrough Secondary-Line Rollback (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix control-loss regression where pot/encoder volume+EQ writes could be blocked (`no_hosts`) after active-target churn when cached control-host globals were cleared but live HA control-host entities did not emit a new state-change event. Runtime send paths now rehydrate host targets from live HA control-host/control-hosts sensors on each write attempt before fail-closing, restoring deterministic control continuity. Also roll back the added passthrough secondary target line (for example scrolling `Kitchen Speakers`) so source-only no-metadata passthrough display remains concise (`Optical In` context) without unrelated room-name clutter. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no component contract mutation). P1/P2/P3 impact: no ownership reassignment; control-path resilience + passthrough UX refinement only. README parity: no material repo-state change.

- Runtime/OLED Passthrough Source-Context Parity + Detail-Line Fix (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/CHANGELOG.md`): fix residual operator-visible gap where OLED could show only top-line source text + volume bar in active passthrough no-metadata windows while ESP status contract still reported blank payload (`audio|oled:-`). Runtime now enriches passthrough source-fallback presentation with a secondary context line (active target label when available) in no-metadata active playback windows, and ESP OLED status export now uses source-fallback context when title metadata is absent but display is allowed and live playback evidence is present. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no component contract mutation). P1/P2/P3 impact: no ownership reassignment; runtime renderer/status parity and operator UX hardening only. README parity: no material repo-state change.

- Runtime/OLED Passthrough Source-Title Self-Clear Fix (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix regression where active passthrough fallback text (for example `Optical In`) could be assigned and then immediately cleared by generic source-like title filtering, leaving OLED blank/volume-only despite active playback context. Runtime now preserves passthrough source-label title fallback during active passthrough no-metadata windows and only applies source-like title clearing outside that guarded posture. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no component contract mutation). P1/P2/P3 impact: no ownership reassignment; renderer-state correctness hardening only. README parity: no material repo-state change.

- Runtime/OLED Passthrough Suppression Guard Fix (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix residual regression where OLED could remain blank (`audio|oled:-`) in active passthrough windows with no track metadata (`Optical In` source present, playback state active). Display-state suppression and renderer blanking now require missing playback evidence before fail-closing passthrough no-text windows, so active passthrough keeps source-label visibility while still failing closed when passthrough is truly idle/no-track. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no component contract mutation in this runtime gating correction). P1/P2/P3 impact: no ownership reassignment; passthrough state-machine correctness hardening only. README parity: no material repo-state change.

- Runtime/OLED Passthrough Live-Label Stability Fallback (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix intermittent blank OLED windows during active passthrough playback (AppleTV/YouTube over Optical) when metadata title/artist fields are temporarily unavailable at track boundary. Runtime now preserves a live passthrough label fallback (`source/source_name` context such as `Optical In`) in no-metadata passthrough windows instead of blanking the entire frame, while still blocking stale cached track-title resurrection from previous MA playlists. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no component contract mutation in this renderer-state stabilization slice). P1/P2/P3 impact: no ownership reassignment; passthrough no-metadata UX stability hardening only. README parity: no material repo-state change.

- Runtime/OLED Passthrough Stale-Title Cache Rescue Block (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix regression where old Music Assistant track titles could reappear while playing passthrough sources (e.g., YouTube over Optical) with no current metadata payload. Runtime now fail-closes cache-rescue for passthrough no-metadata windows and prevents stale title/artist/album reuse outside explicit source-hint interaction windows. This restores expected passthrough behavior (blank/source-context only) and prevents stale playlist carryover text. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (component contracts unchanged for this runtime renderer-state fix). P1/P2/P3 impact: no ownership reassignment; stale cache-rescue policy hardening only. README parity: no material repo-state change.

- Component Metadata Winner Scoring / MA Carrier Preference Hardening (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): fix persistent first-track OLED pinning when stale route-carrier entities remained transport-active and competed with live Music Assistant carrier entities. Component now-playing winner selection now uses a scored ordering over normalized candidates (fresh-play signal, real metadata payload presence, recent play-progress, Music Assistant app/source hints, and stale track-end penalties), so MA metadata carriers win over stale DLNA route carriers under mixed-signal conditions. Runtime track disposition: compatibility-shimmed (runtime consumer unchanged; receives corrected component winner). Component track disposition: implemented. P1/P2/P3 impact: no ownership reassignment; selection-order determinism hardening only. README parity: no material repo-state change.

- Component Metadata Selection Hardening / First-Track Pinning Fix (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): fix issue where album playback could remain stuck on the first song when route-active entities emitted fresh transport signals but lacked per-track metadata updates. Component now-playing entity selection now prefers candidates that are both freshness-valid and metadata-bearing (`title/artist/album`) before accepting freshness-only candidates, with explicit metadata fallback ordering. This prevents stale cached-title carryover from dominating when a non-metadata carrier remains active. Runtime track disposition: compatibility-shimmed (runtime consumes corrected component contract surfaces unchanged). Component track disposition: implemented. P1/P2/P3 impact: no ownership reassignment; metadata candidate ordering and guard hardening only. README parity: no material repo-state change.

- Runtime/OLED Idle Wake-Overlay Gate Tightening (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix regression where volume-bar-only now-playing frame could remain visible while nothing was playing. Wake overlay path is now strictly gated to recent user input window (`idle <= 2.5s`) rather than generic wake timer alone, in both display-state selection and now-playing renderer. This preserves interaction feedback while restoring fail-closed idle blank behavior. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no component changes). P1/P2/P3 impact: no ownership reassignment; display-state/wake-gate correction. README parity: no material repo-state change.

- Component+Runtime/Contract Re-Alignment for Target + Metadata Continuity (`custom_components/spectra_ls/metadata_stack.py`, `custom_components/spectra_ls/sensor.py`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/CHANGELOG.md`): fix post-ingest contract drift where rich metadata briefly appeared then degraded to title-only, and where ESP-reported control target could diverge from canonical helper/route target due stale friendly-name surface. Component path now preserves passthrough metadata cache continuity without overwriting non-empty artist/album fields during degraded snapshots, and component now-playing artist/album/app/source sensors read component metadata values first (owner surface) before raw entity attrs. Runtime path now reports canonical `ma_active_target` as ESP control target to prevent stale friendly-label mismatch false negatives/positives in validation packets. Runtime track disposition: implemented. Component track disposition: implemented. P1/P2/P3 impact: no ownership reassignment; contract-owner consistency and metadata continuity hardening. README parity: no material repo-state change.

## 2026-06-06

- Runtime/Passthrough Wake-Overlay Visibility Fix (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): preserve fail-closed idle blanking for passthrough-no-metadata states, but stop suppressing the entire audio frame during explicit user interaction windows. Display-state gating now allows audio wake overlays in passthrough silent windows, and now-playing render now shows a short source wake header plus volume bar during wake activity (encoder/pot/button), fixing missing on-device feedback while keeping idle blank policy intact. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no component contract changes). P1/P2/P3 impact: no ownership reassignment; renderer/display-state interaction-path hardening. README parity: no material repo-state change.

- Runtime/Popup Stability + Reboot Metadata Bootstrap Tightening (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/CHANGELOG.md`): remove compute-loop source-popup retrigger ownership to stop popup timer thrash, replace alternating/wrapped popup layout with fixed 3-line single-page render (header + source + detail) to eliminate overlap and flashing, and add early-uptime guarded bootstrap refresh/playing timestamp seeding so rebooted ESP can materialize now-playing without waiting for a track switch. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (selection contracts unchanged in this slice). P1/P2/P3 impact: no ownership reassignment; display stability and startup hydration hardening. README parity: no material repo-state change.

- Runtime/Popup Two-Page Render + Restart Metadata Bootstrap Refresh (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/CHANGELOG.md`): fix source popup line overlap by rendering two alternating pages during popup window (`SOURCE/<source>` then `TARGET/<target> + MODE/<mode>`), removing crowded multi-field single-frame draw. Also add guarded HA contract refresh loop for restart windows where playback is active but title/artist remain unresolved, reducing blank OLED until next track change. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (selection contracts unchanged in this slice). P1/P2/P3 impact: no ownership reassignment; popup readability + restart hydration robustness hardening. README parity: no material repo-state change.

- Corrective Review Pass — Selector-Path Metadata Lock + Popup Layout De-overlap (`custom_components/spectra_ls/metadata_stack.py`, `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): root-cause correction after regression review. Moved passthrough metadata retention emphasis to component entity-selection path (contract owner) by preferring passthrough candidates with real metadata payload under active/paused playback even when progress clock is sparse, instead of depending on renderer-only behavior. Simplified source popup rendering to a compact 3-line layout (`SOURCE`, source, `T:<target> M:<mode>`) to prevent line overlap on small OLED height. Runtime track disposition: implemented (popup layout stability). Component track disposition: implemented (metadata selection lock hardening). P1/P2/P3 impact: no ownership reassignment; contract-owner placement and UI readability correction.

- Runtime/OLED Source Popup UX Format Upgrade (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/CHANGELOG.md`): align source-context prompt to operator-preferred format by rendering `SOURCE / <source>` with explicit `Target: <active target>` and `Mode: <optical|wifi|bluetooth|line-in|...>` lines. Popup label producer was simplified to source/app text only, with target/mode derived in renderer for stable presentation. Runtime track disposition: implemented. Component track disposition: compatibility-shimmed (no component contract changes). P1/P2/P3 impact: no ownership reassignment; source-context UX clarity hardening only.

- Runtime/OLED Passthrough Prompt Oscillation Rollback + Stability Hardening (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): revert the added passthrough no-metadata transition popup trigger that caused repeated `INPUT` / `Optical In` oscillation under source-only windows, and return to stable source-popup behavior driven by source-label change only. Runtime track disposition: implemented (oscillation path removed). Component track disposition: compatibility-shimmed (component metadata continuity behavior retained; no contract rollback). P1/P2/P3 impact: no ownership reassignment; UI oscillation regression removed.

- Component+Runtime/Passthrough Metadata Persistence + Input Prompt Guard (`custom_components/spectra_ls/metadata_stack.py`, `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): fix post-restart passthrough regression where valid now-playing text could appear briefly (`INPUT YOUTUBE` / track title) and then collapse to source-only (`Optical In`) causing OLED blank posture despite active playback. Component metadata-prep now reuses a bounded last-known metadata tuple (title/artist/album/app) during passthrough source-only windows when playback signal remains active, preventing premature no-title collapse while preserving fail-closed blanking when no recent metadata is available. Runtime now emits a one-shot source-context popup when transitioning into passthrough-without-metadata state (Optical/Line-In/AUX/Coax/HDMI/ARC), providing explicit operator override signal prompt without persistent on-screen stickiness. Validator template now surfaces component selector source + passthrough cache reuse telemetry to distinguish true no-meta input from selector/dropout regressions. Runtime track disposition: implemented. Component track disposition: implemented. P1/P2/P3 impact: no ownership reassignment; passthrough metadata continuity + explicit source-only transition signaling hardening.

- Priority A/B Metadata + OLED Blanking Hardening (`custom_components/spectra_ls/metadata_stack.py`, `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`):
  - **A (component freshness gate):** stop treating `playing` without progress clock as fresh-play signal. `fresh_play_signal` now requires actual recent progress evidence (or paused-with-recent-progress), and `playing_without_fresh_signal` explicitly includes no-progress-clock cases.
  - **B (runtime blank/no-bar posture):** for passthrough contexts with no text payload outside active source-hint window, force no-audio posture and suppress now-playing render payload (including right-side volume bar), preventing bar-only “playing” screens when metadata is absent.
  Runtime track disposition: implemented. Component track disposition: implemented. P1/P2/P3 impact: no ownership reassignment; stricter freshness integrity + fail-closed OLED rendering for no-meta passthrough.

- Runtime/OLED Source-Hint Cache Carryover Guard (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix persistent `Optical In` (or equivalent source-like title) on OLED after source-popup timeout by preventing source-hint/cache-rescue titles from surviving outside the active popup window. Source-context labels now remain transient by policy and are cleared fail-closed when hint window is not active, even if transport state remains `playing` for passthrough contexts.

- Runtime/OLED Source Hint Timeout + Button Shortcut (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/CHANGELOG.md`): change source-only passthrough presentation from persistent fallback to time-bounded hint behavior. Source labels (e.g., Optical In) are now shown only during bounded source-popup windows (default `${source_popup_ms}`), and source-context popup can be invoked directly by button press with host attribution (`source @ host:port`) for operator clarity. Runtime track disposition: implemented (UI behavior hardening + discoverability shortcut). Component track disposition: compatibility-shimmed (contracts unchanged; renderer/input UX behavior only).

- Diagnostics/Template Source Attribution Upgrade (`docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): extend OLED now-playing validation packet to include effective control-host and control-port attribution (component/runtime/route fallbacks) so passthrough source reports answer “from what host/target” directly in the output. This reduces ambiguity for source-only contexts like Optical/Line-In where track metadata is absent.

- Runtime/OLED Passthrough No-Track Idle Blanking Gate (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): harden deep display-state detection so passthrough source contexts (Optical/Line-In/AUX/Coax/HDMI/ARC) with no title/artist/album payload, no transport-playing flags, and no recent position advancement are treated as no-audio-activity for display-state purposes. This prevents persistent audio-mode `oled:-` posture from lingering under false-positive `playing` states and forces blank OLED until real track/playback evidence resumes. Runtime track disposition: implemented (display-state policy hardening). Component track disposition: compatibility-shimmed (contract surfaces unchanged; consumer policy only).

- Runtime/OLED Deep Source-Placeholder Gate Tightening (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-ui.yaml`, `docs/CHANGELOG.md`): fix remaining idle `SRC` render path after OTA by removing unconditional source-context placeholder promotion in the OLED metadata compute pipeline and requiring live transport playback evidence before emitting `SRC` placeholder labels. Also retire legacy template helper default `Now Playing Source => SRC` fallback to fail-closed blank when source metadata is absent. Runtime track disposition: implemented (deep renderer-path gate hardening). Component track disposition: compatibility-shimmed (component contracts unchanged; renderer-only policy correction). P1/P2/P3 impact: no ownership change; stricter idle blanking behavior under no-playback/no-freshness posture.

- Runtime/ESP OLED Status Fail-Closed Playback Evidence Gate (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/CHANGELOG.md`): fix sticky `audio|oled:SRC` posture when nothing is actively playing by requiring live playback evidence (HA/transport playing signal or fresh advancing now-playing clock) before emitting title/fallback OLED text in `ESP OLED Status`. When evidence is absent, audio OLED status now fail-closes to blank payload (`-`) instead of source placeholder carryover. Runtime track disposition: implemented (active ESP OLED status contract hardening). Component track disposition: compatibility-shimmed (component metadata contracts unchanged; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; stale-render suppression and idle-state correctness hardening only. README parity: no material repo-state change.

## 2026-06-05

- Runtime/ESP OLED Stale-Metadata Clear Guard (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/CHANGELOG.md`): fix stale OLED now-playing text carryover when playback has stopped (or passthrough source remains selected with no live track metadata) by tightening cache-rescue semantics in metadata compute: cached title/artist/album are no longer rehydrated unless live transport playback is currently active. This preserves source-first passthrough behavior while fail-closing stale track text persistence on idle/no-transport states. Runtime track disposition: implemented (ESP OLED compute/render hardening in active runtime path). Component track disposition: compatibility-shimmed (component metadata contracts unchanged; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership reassignment; stale-render suppression and operator UX correctness hardening only. README parity: no material repo-state change.

- Component/Control+Metadata Error-Hardening Sweep (`custom_components/spectra_ls/metadata_stack.py`, `custom_components/spectra_ls/scaffold_fabric.py`, `custom_components/spectra_ls/router.py`, `docs/CHANGELOG.md`): harden high-churn control/meta paths against malformed helper/entity payloads by replacing unsafe numeric casts with bounded parse fallbacks (metadata stale/paused timers), adding safe numeric coercion for media position/duration attributes, guarding resolver best-score parsing in scaffold assembly, and hardening route-trace assembly against non-mapping registry payload shapes. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged in this slice; equivalent failure modes reviewed). Component track disposition: implemented (fail-closed parsing/shape guards added in component control/metadata hot paths). P1/P2/P3 impact: no source-of-truth ownership reassignment; error-path resilience and snapshot stability hardening only. README parity: no material repo-state change.

- Component/Metadata Selection — Passthrough Upstream Metadata Promotion (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): fix source-only passthrough winner lock (`Optical In` with empty title/artist) by adding a bounded passthrough promotion pass in metadata prep selection: when passthrough is detected and selected entity lacks media title/artist, component now promotes the first resolved resolver candidate (`selected_meta_entity`, `best_candidate`, `detected_candidate`) that carries upstream metadata fields. This preserves control-target authority while improving now-playing artist/title visibility for AppleTV/YouTube-over-optical style paths. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged in this slice; equivalent failure mode reviewed). Component track disposition: implemented (metadata winner fallback correctness hardening for passthrough no-meta surfaces). P1/P2/P3 impact: no source-of-truth ownership reassignment; metadata visibility and diagnostics correctness hardening only. README parity: no material repo-state change.

- Diagnostics/DT Template — Upstream App-Context Detection for Passthrough Paths (`docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): fix false `AppleTV/YouTube-like context detected: False` outputs when active control source is passthrough (`Optical In`) but upstream metadata provider/app context exists on meta-target/control-target attributes (for example `app_id=com.google.ios.youtube`, `app_name=YouTube`). Template now computes app/source context hints from component/runtime effective fields plus packet values and entity-attribute fallbacks on effective meta/route/control targets. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; diagnostics-only artifact). Component track disposition: implemented (context classification correctness hardening in diagnostics template). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics signal-quality hardening only. README parity: no material repo-state change.

- Diagnostics/DT Template — Passthrough Detection Scope Fix (`docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): fix false `passthrough_source_detected=false` results for valid passthrough sources (for example `Optical In`) by replacing loop-local boolean assignment with namespace-backed mutation in Jinja, preserving deterministic source token matching (`optical`, `optical in`, `line in`, `line-in`, `aux`, `coax`, `hdmi`, `arc`). Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; diagnostics-only artifact). Component track disposition: implemented (template classification correctness hardening for passthrough context hints). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics signal-quality hardening only. README parity: no material repo-state change.

- Component/Metadata Selection — Non-Meta Input Detection + Fallback Provider Preference (`custom_components/spectra_ls/metadata_stack.py`, `docs/CHANGELOG.md`): add source-aware metadata selection behavior for non-meta-capable passthrough inputs (optical/line-in/aux/coax/hdmi/arc). When active control target source is passthrough and no local track metadata is expected, metadata-prep selection now prefers resolved resolver-selected/detected metadata entities (when available and fresh) before falling back to target-bound entity semantics. This keeps control-target authority unchanged while improving now-playing metadata quality for passthrough paths. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged in this slice; equivalent failure mode reviewed). Component track disposition: implemented (component metadata entity selection now source-aware with bounded fallback order and diagnostics transparency). P1/P2/P3 impact: no source-of-truth ownership reassignment; metadata selection correctness and operator experience hardening only. README parity: no material repo-state change.

- Diagnostics/DT Template — OLED + Control Target + Meta Target Validation (`docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): expand the focused Home Assistant Developer Tools validator to cover end-to-end render chain integrity (control target alignment across route/helper/ESP, metadata target alignment across packet/runtime/now-playing winner, and OLED `SRC` fallback classification). Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; diagnostics-only artifact). Component track disposition: implemented (component-first now-playing + metadata contract surfaces consumed with bounded runtime fallback for comparison). P1/P2/P3 impact: no source-of-truth ownership reassignment; operator diagnostics depth and triage speed hardening only. README parity: no material repo-state change.

- Diagnostics/DT Template — Control-Target Normalization + Source-Only OLED Blank Classification (`docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): fix false FAILs when `sensor.spectra_ls_system_esp_control_target` reports a friendly label (for example `Kitchen Speakers`) while route/helper surfaces use entity IDs (`media_player.kitchen_wiim`) by adding normalized friendly/entity comparison logic. Also add explicit `audio_payload_blank_source_only_no_title` cause classification (WARN) for valid source-without-title postures (common on Optical/line-in paths) so blank OLED payload (`audio|oled:-`) is not misreported as target drift. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; diagnostics-only artifact). Component track disposition: implemented (component-first metadata owner token handling now includes `component_contract_surfaces` in this validator). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics correctness and false-positive reduction only. README parity: no material repo-state change.

- Diagnostics/DT Template — Passthrough-Source Smart Classification (`docs/testing/raw/oled_now_playing_validation.jinja`, `docs/CHANGELOG.md`): add passthrough-input awareness (optical/line-in/aux/coax/hdmi/arc token detection) so source-present/title-missing OLED blank payloads are classified as `audio_payload_blank_expected_passthrough_no_track_metadata` when the active source is a passthrough input, reducing noise and steering operator next actions toward optional upstream metadata bridging only when desired. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; diagnostics-only artifact). Component track disposition: implemented (component-first now-playing surfaces remain primary; classification semantics improved). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics context accuracy and triage signal quality hardening only. README parity: no material repo-state change.

- Docs+Component/Version Metadata Alignment (File-Level Audit) (`custom_components/spectra_ls/manifest.json`, `docs/CHANGELOG.md`): perform file-level version audit across all component Python modules and release metadata surfaces, then align the Home Assistant integration release version in `manifest.json` from `2026.4.29.5` to `2026.05.17.1` to match the latest validated component implementation header cadence. No behavior/contract logic changed; this slice corrects release/version observability drift only. Runtime track disposition: compatibility-shimmed (runtime track reviewed; no runtime behavior or contract mutation required). Component track disposition: implemented (release metadata now aligned to validated component file-level version state). P1/P2/P3 impact: no source-of-truth ownership reassignment; metadata governance/readability hardening only. README parity: no material repo-state change.

## 2026-05-17

- Runtime+Component/Slice-CW Control-Host Active-Target Guard (Stale Candidate Slip Fix) (`packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/sensor.py`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): fix a stale host-candidate slip where component-authority host candidate surfaces could be consumed without validating candidate-target alignment to the current active target, allowing occasional host drift (`control host != active target host`) under feed churn. Runtime `sensor.ma_control_hosts` and component `sensor.component_control_hosts` now accept host-cutover candidates only when gate readiness is `ready` and candidate target matches active target (or active target is unresolved), otherwise falling back to selected-target host and parity-safe surfaces. Runtime track disposition: implemented (active-target host guard in runtime contract selector). Component track disposition: implemented (matching guard in component control-host surface selector). P1/P2/P3 impact: no source-of-truth ownership reassignment; host/target coherence correctness hardening only. README parity: no material repo-state change.

- Runtime+Component/Slice-CV Arylic HTTP Probe Suppression Stability Hardening (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/CHANGELOG.md`): harden the runtime Arylic HTTP probe failure path for hosts with no known-good HTTP transport by applying a longer suppression pause when repeated connect-reset failures persist, reducing repetitive probe noise while preserving active TCP control/handoff behavior. Runtime track disposition: implemented (ESP HTTP probe suppression/backoff behavior tightened; control-target acquisition semantics unchanged). Component track disposition: compatibility-shimmed (component contracts unchanged; equivalent failure mode reviewed/not-applicable). P1/P2/P3 impact: no source-of-truth ownership reassignment; transport-noise suppression and operator-triage clarity hardening only. README parity: no material repo-state change.

## 2026-05-05

- Runtime+Component/Slice-CU LC6-L05 Template/Read-Lane Consumer Bridge + Wiki/Deploy Parity (`packages/ma_control_hub/template.inc`, `docs/testing/raw/meta_component_full_stack_tester.jinja`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/setup/SPECTRA-HA-CONFIG-PLACEHOLDERS.md`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): continue LC6-L05 by reducing remaining helper-stack dependence in runtime/read consumers: `sensor.ma_server_profile_effective` now resolves component backend-profile contracts first (bounded helper fallback retained), full-stack validation template now emits component-first backend/API endpoint readiness, and operator-facing wiki/deploy docs now document phase-3 runtime consumer posture (component-first endpoint/profile surfaces with rollback-safe helper fallback). Runtime track disposition: compatibility-shimmed (helper-backed profile/url contracts retained as bounded fallback and rollback lane). Component track disposition: implemented (component backend/API bridge entities remain primary runtime/read consumer surfaces). P1/P2/P3 impact: no source-of-truth ownership reassignment; LC6-L05 read-lane dependency reduction + operator deploy guidance parity hardening only. README/wiki parity: wiki + deploy docs updated in-slice; README parity no material end-user contract change.

- Runtime+Component/Slice-CT LC6-L05 Runtime REST Consumer Bridge Tightening (`packages/ma_control_hub/rest.inc`, `packages/ma_control_hub/rest_command.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): continue legacy cleanup by moving runtime MA REST URL consumers to component-first endpoint surfaces (`sensor.component_ma_api_url`) with bounded fallback to legacy runtime helper URL (`sensor.ma_api_url`) when component bridge data is unresolved. This reduces direct LC6-L05 helper-stack dependency on active runtime API callers while preserving rollback-safe behavior and existing endpoint continuity. Runtime track disposition: compatibility-shimmed (legacy helper/API surface retained as fail-safe fallback). Component track disposition: implemented (component endpoint bridge entity now primary runtime REST consumer source). P1/P2/P3 impact: no source-of-truth ownership reassignment; LC6-L05 consumer-lane dependency reduction and retirement-readiness hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CS Meta-Stack Validation Optimization + Contract Hardening (`packages/ma_control_hub/script.inc`, `packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/validation_fabric.py`, `docs/testing/raw/p3_candidate_payload_parity_validation.jinja`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): apply full-pass validation/optimization fixes for this round by removing runtime target-option clobber behavior in detected-candidate merge flow, promoting runtime control-port sensor to component-first dynamic contract resolution with bounded helper/default fallback, tightening route-safety validation verdict semantics when control-host surfaces are unresolved, and reducing false parity warnings for component candidate payload checks in raw validator packets. Runtime track disposition: compatibility-shimmed (bounded fallback retained while active-path correctness/perf hardening lands). Component track disposition: implemented (component contracts remain primary and validator semantics now better aligned with component packet shape). P1/P2/P3 impact: no source-of-truth ownership reassignment; determinism/diagnostics correctness/performance hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component+ESP/Slice-CR Legacy-Active Participation Dormant-by-Default Gate (`packages/ma_control_hub/automation.inc`, `packages/ma_control_hub/script.inc`, `esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): enforce strict component-primary operation by disabling remaining runtime legacy-active automation/script participation when authority is component and by adding explicit ESP fallback-listener apply gating via `input_boolean.ma_control_fallback_enabled` (default off), so legacy fallback code remains available but dormant unless explicitly enabled. Runtime track disposition: compatibility-shimmed (fallback surfaces retained but default-dormant; no active legacy writer participation in component mode). Component track disposition: implemented (component contracts remain sole active control/metadata participant by default). P1/P2/P3 impact: no source-of-truth ownership reassignment; active-lane participation hardening and rollback-safe fallback gating only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CQ LC6-L06 Non-Override Candidate Consumer Bridge (`docs/testing/raw/p3_s03_metadata_prep_validation.jinja`, `docs/testing/raw/p3_candidate_payload_parity_validation.jinja`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): start the deferred non-override LC6-L06 tail campaign by migrating P3 candidate-focused validator consumers to component-first metadata-candidate surfaces (`sensor.spectra_ls_component_meta_candidates`, `binary_sensor.spectra_ls_component_meta_low_confidence`) with bounded `sensor.ma_meta_candidates` compatibility fallback retained for rollback-safe continuity. Runtime track disposition: compatibility-shimmed (legacy `sensor.ma_meta_candidates` remains bounded fallback source for unresolved environments). Component track disposition: implemented (component metadata-candidate contract becomes primary validator consumer lane). P1/P2/P3 impact: no source-of-truth ownership reassignment; non-override diagnostics consumer-lane determinism hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CP LC6-L06 Override-Consumer Campaign Closeout + Deferred Remainder Seal (`docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): close the active override-focused LC6-L06 consumer migration campaign by sealing completed scope (runtime template, step-3 validator, devtools package-loader/health checks, CA-S06 wave-2 validator, and end-to-end checklist now component-first with bounded helper fallback) and recording explicit defer rationale for remaining non-override `sensor.ma_meta_*` runtime resolver surfaces pending broader component packet-family parity expansion. Runtime track disposition: compatibility-shimmed (legacy helper storage + non-override resolver tails remain bounded compatibility/deferred lanes). Component track disposition: implemented (component metadata override diagnostics contracts remain primary consumer surfaces). P1/P2/P3 impact: no source-of-truth ownership reassignment; closeout ledger clarity and deferred-tail governance hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CO LC6-L06 CA-S06 Validator Override-Surface Gate Hardening (`docs/testing/raw/ca_s06_runtime_write_helper_retirement_wave2_validation.jinja`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden CA-S06 wave-2 validator semantics by requiring explicit component metadata-override diagnostics surface presence as first-class gate signals, while keeping legacy helper presence as compatibility telemetry only. Runtime track disposition: compatibility-shimmed (legacy helper storage remains bounded compatibility evidence lane). Component track disposition: implemented (component diagnostics entities remain primary metadata override consumer contract). P1/P2/P3 impact: no source-of-truth ownership reassignment; validator gate determinism hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CN LC6-L06 End-to-End Checklist Consumer Bridge (`docs/testing/raw/meta_stack_end_to_end_validation_checklist.md`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): migrate end-to-end checklist guidance to component-first metadata override diagnostics contract language and preserve legacy helper continuity as hidden compatibility fallback wording. Runtime track disposition: compatibility-shimmed (legacy helper storage retained as bounded compatibility fallback). Component track disposition: implemented (component diagnostics entities remain primary metadata override read contract). P1/P2/P3 impact: no source-of-truth ownership reassignment; operator checklist consumer-lane determinism hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CM LC6-L06 CA-S06 Wave-2 Validator Consumer Bridge (`docs/testing/raw/ca_s06_runtime_write_helper_retirement_wave2_validation.jinja`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): migrate CA-S06 wave-2 one-screen validator consumer emphasis to component metadata override diagnostics surfaces first while retaining explicit legacy helper presence as compatibility telemetry only. Runtime track disposition: compatibility-shimmed (legacy helper storage remains bounded compatibility lane). Component track disposition: implemented (component diagnostics entities remain primary metadata override read contract). P1/P2/P3 impact: no source-of-truth ownership reassignment; retirement-validator consumer read-lane determinism hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CL LC6-L06 DevTools Package-Loader Consumer Bridge (`esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): migrate package-loader regression diagnostics to include component metadata override diagnostics entities as required consumer surfaces while retaining legacy helper checks as bounded compatibility inventory. Runtime track disposition: compatibility-shimmed (legacy helper storage retained as compatibility inventory lane). Component track disposition: implemented (component diagnostics entities primary for override-state consumer visibility). P1/P2/P3 impact: no source-of-truth ownership reassignment; devtools consumer read-lane migration hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CK LC6-L06 DevTools Health Consumer Bridge (`esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): migrate active DevTools health-check consumer expectations for metadata override visibility to component-first surfaces (`binary_sensor.component_metadata_override_active`, `sensor.component_metadata_override_entity`) while preserving bounded legacy helper compatibility elsewhere. Runtime track disposition: compatibility-shimmed (legacy helper storage remains bounded compatibility source). Component track disposition: implemented (component diagnostics entities remain primary metadata override read contract). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics consumer read-lane migration hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CJ LC6-L06 Step-3 Validation Gate Bridge (`docs/testing/raw/meta_stack_step3_full_validation.jinja`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): migrate Step-3 ESP auto-only gate checks to component-first metadata override contracts with bounded helper fallback so validation packets reflect active component authority surfaces before compatibility storage. Runtime track disposition: compatibility-shimmed (legacy helper storage retained as bounded fallback lane). Component track disposition: implemented (component metadata override diagnostics remain primary). P1/P2/P3 impact: no source-of-truth ownership reassignment; validation gate read-lane determinism hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CI LC6-L06 Runtime Metadata Override Consumer Bridge (`packages/ma_control_hub/template.inc`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): migrate eligible runtime metadata-override read lanes to component diagnostics contracts first (`binary_sensor.component_metadata_override_active`, `sensor.component_metadata_override_entity`) with bounded helper fallback retained for rollback-safe compatibility. Runtime track disposition: compatibility-shimmed (legacy helper storage remains bounded compatibility source). Component track disposition: implemented (component packet/entity contracts remain primary metadata override diagnostics lane). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime consumer read-lane migration and contract determinism hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CH LC6-L06 Metadata Override Diagnostics Consumer Cleanup (`custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): tighten LC-08 consumer behavior by removing direct legacy-helper state reads from component metadata-override diagnostics and enforcing packet-first read semantics from `write_controls.metadata_override` surfaces, including deterministic fallback values when packet data is transiently missing. Runtime track disposition: compatibility-shimmed (legacy helper storage remains bounded compatibility lane; no write-owner reassignment). Component track disposition: implemented (override diagnostics consumers now use component packet contracts only). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics-read contract determinism and migration-readiness hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/Slice-CG LC-08 Metadata Override Packet-First Surface Hardening (`custom_components/spectra_ls/validation_fabric.py`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): implement component packet-first metadata override status/entity surfaces in write-controls and rewire component diagnostics entities to consume those packet contracts first (with bounded legacy-helper fallback when packet state is missing). This reduces direct helper coupling for LC-08 diagnostics consumers while preserving rollback-safe compatibility storage semantics. Runtime track disposition: compatibility-shimmed (runtime helper storage remains bounded compatibility lane; no ownership reassignment). Component track disposition: implemented (metadata override diagnostics consumers now read component packet contracts first). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics contract-read determinism and migration-readiness hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/CA-S08 Closeout Validation + Board-State Seal (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`): close the CA-S08 implementation loop by promoting roadmap board state from `Active` to `Validated`, sealing immediate-pointer wording to no-further-activation posture unless plan delta reopens scope, and aligning tracker final-seal language so closure governance is explicit without implying premature legacy lane retirement. Runtime track disposition: compatibility-shimmed (runtime rollback-safe compatibility baseline remains intentionally bounded for rollback and migration safety). Component track disposition: implemented (component-first closeout packet ownership and closure evidence contract remain canonical). P1/P2/P3 impact: no source-of-truth ownership reassignment; closeout state consistency and governance-ledger integrity hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/CA-S08 Domain Closeout Packet + Rollback-Safe Seal + Retirement Ledger Closure (`docs/program/CA-S08-DOMAIN-CLOSEOUT-ROLLBACK-SEAL-CHECKLIST.md`, `docs/testing/raw/ca_s08_domain_closeout_rollback_seal_validation.jinja`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/README.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): activate CA-S08 by publishing deterministic final-seal governance for LC-06/LC-07/LC-08 closeout, including explicit lane-disposition closure packet fields, rollback-readiness assertions, and fail-closed ledger consistency checks so final retirement claims are auditable and reversible. Runtime track disposition: compatibility-shimmed (runtime rollback-safe compatibility lane remains available and explicitly bounded in final seal posture). Component track disposition: implemented (component-first ownership and closure-evidence packet contract are now explicit for final domain closeout validation). P1/P2/P3 impact: no source-of-truth ownership reassignment; final closure governance, rollback-safety proofing, and retirement-ledger integrity hardening only. README/wiki parity: docs index updated for CA-S08 artifact discoverability; no material end-user workflow contract change.

- Runtime+Component/CA-S07 Legacy Diagnostics/Template Fallback Strip + Cleanup Verification (`docs/program/CA-S07-LEGACY-DIAGNOSTICS-TEMPLATE-FALLBACK-CLEANUP-CHECKLIST.md`, `docs/testing/raw/ca_s07_legacy_diagnostics_template_fallback_cleanup_validation.jinja`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/README.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): activate CA-S07 by publishing deterministic cleanup governance for LC-08 legacy diagnostics/template fallback retirement, including explicit component-first consumer requirements, legacy-reference inventory checks, fallback-hit classification, and fail-closed blocker taxonomy so retirement claims remain evidence-driven. Runtime track disposition: compatibility-shimmed (runtime compatibility diagnostics surfaces remain bounded until template/devtools fallback references are fully dispositioned). Component track disposition: implemented (component-first diagnostics/template contracts are now explicitly required and measurable in CA-S07 validation output). P1/P2/P3 impact: no source-of-truth ownership reassignment; LC-08 closure-governance and cleanup verification hardening only. README/wiki parity: docs index updated for CA-S07 artifact discoverability; no material end-user workflow contract change.

- Runtime+Component/CA-S06 Runtime Write/Helper Retirement Wave-2 Tails (`docs/program/CA-S06-RUNTIME-WRITE-HELPER-RETIREMENT-WAVE2-CHECKLIST.md`, `docs/testing/raw/ca_s06_runtime_write_helper_retirement_wave2_validation.jinja`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/README.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): activate CA-S06 by publishing deterministic wave-2 retirement governance for remaining LC-06 runtime tails (writer remnants, helper-storage compatibility surfaces, and metadata-resolver read surfaces), including lane-level disposition rules (`implemented`, `compatibility-shimmed`, `deferred with rationale`), fail-closed evidence expectations, and a one-screen validation template for closure packet capture. Runtime track disposition: compatibility-shimmed (runtime tails remain bounded until lane-level retirement/defer decisions are explicitly evidenced). Component track disposition: implemented (component-first replacement contracts and read-lane ownership signals are now explicitly wired into CA-S06 governance artifacts). P1/P2/P3 impact: no source-of-truth ownership reassignment; LC-06 wave-2 retirement-readiness governance hardening only. README/wiki parity: docs index updated for CA-S06 artifact discoverability; no material end-user workflow contract change.

- Runtime+Component/CA-S05 ESP Fallback-Listener Soak Evidence + Fail-Closed Retirement Gates (`docs/program/CA-S05-ESP-FALLBACK-LISTENER-SOAK-CHECKLIST.md`, `docs/testing/raw/ca_s05_esp_fallback_listener_soak_validation.jinja`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/README.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): activate CA-S05 by publishing deterministic soak and fail-closed retirement gate artifacts for LC-07 fallback-listener retirement readiness, including explicit fallback-hit threshold policy, soak-window evidence schema, blocker taxonomy, and next-action guidance so retirement claims cannot pass on ad-hoc interpretation. Runtime track disposition: compatibility-shimmed (fallback listeners remain bounded compatibility lane until soak gates are satisfied with clean evidence windows). Component track disposition: implemented (component-first route feed and fallback telemetry interpretation contract are now explicit and consumable in one-screen validation output). P1/P2/P3 impact: no source-of-truth ownership reassignment; LC-07 retirement-readiness governance hardening only. README/wiki parity: docs index updated for CA-S05 artifact discoverability; no material end-user workflow contract change.

- Runtime+Component/CA-S04 Runtime Write-Lane Retirement Wave-1 Baseline (`docs/program/CA-S04-RUNTIME-WRITE-LANE-RETIREMENT-CHECKLIST.md`, `docs/testing/raw/ca_s04_runtime_write_lane_retirement_validation.jinja`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/README.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): activate CA-S04 with a deterministic wave-1 retirement checklist and a one-screen runtime write-lane validation template that emits authority posture, helper-write guard posture, lane-level retirement readiness, and explicit blocker taxonomy for LC6 wave-1 execution. Runtime track disposition: compatibility-shimmed (runtime helper/script surfaces remain bounded while lane retirement gates are evaluated with explicit readiness evidence). Component track disposition: implemented (component-side write-contract authority expectation and runtime retirement readiness evidence are now first-class published artifacts). P1/P2/P3 impact: no source-of-truth ownership reassignment; runtime write-lane retirement governance/readiness hardening only. README/wiki parity: docs index updated for CA-S04 artifact discoverability; no material end-user workflow contract change.

- Runtime+Component/CA-S03 Component-First Consumer Projection Cutover Baseline (`docs/program/CA-S03-CONSUMER-PROJECTION-CUTOVER-CHECKLIST.md`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/README.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): start CA-S03 by publishing a deterministic consumer-projection cutover checklist and wiring explicit projection-readiness telemetry into the active scheduler validation consumer so component-first surfaces are measured and auditable in one template output (component-vs-runtime source lane selection, fallback hit tracking, and fail-closed cutover readiness hints). Runtime track disposition: compatibility-shimmed (runtime fallback surfaces retained as bounded compatibility lane only). Component track disposition: implemented (consumer projection observability contract and cutover-readiness signals now explicit in active diagnostics output). P1/P2/P3 impact: no source-of-truth ownership reassignment; projection-consumer migration hardening only. README/wiki parity: docs index updated for CA-S03 artifact discoverability; no material end-user workflow contract change.

- Runtime+Component/CA-S02B Resolver Replay Validation Checklist + Deterministic Sample Packet Publication (`docs/program/CA-S02B-RESOLVER-REPLAY-CHECKLIST.md`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/README.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): publish an explicit replay-validation execution checklist and canonical sample acceptance packet so resolver-determinism verification can be run and audited with a single repeatable contract artifact (input fingerprint capture, replay run matrix, stable winner/rank/hash assertions, and fail-closed blocker taxonomy). Runtime track disposition: compatibility-shimmed (runtime compatibility/rollback lanes unchanged; replay proof remains governance/evidence lane). Component track disposition: implemented (deterministic replay validation contract + sample packet now first-class published artifacts for CA-S02 closure progression). P1/P2/P3 impact: no source-of-truth ownership reassignment; resolver-governance replay determinism hardening only. README/wiki parity: docs index updated for artifact discoverability; no material end-user workflow contract change.

- Runtime+Component/CA-S02A Resolver Determinism Scoring Matrix + Acceptance Packet Fields (`docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): formalize deterministic resolver scoring policy with explicit weighted-metric matrix, tie-break order, and bounded acceptance packet schema so CA-S02 progression uses reproducible scoring/closure evidence instead of ad-hoc interpretation. Runtime track disposition: compatibility-shimmed (runtime compatibility read/rollback lanes unchanged). Component track disposition: implemented (canonical resolver scoring + acceptance packet field contract now explicitly documented). P1/P2/P3 impact: no source-of-truth ownership reassignment; resolver-governance determinism hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/CA-S01D Proof-Packet Checklist + Closure Rubric Publication (`docs/program/CA-S01D-PROOF-PACKET-CHECKLIST.md`, `docs/README.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): publish a deterministic CA-S01 closure artifact defining required evidence packet sections, pass/fail closure rubric, scoring gates, and rollback-proof requirements so slice closure decisions are repeatable and auditable (not operator-interpretation dependent). Runtime track disposition: compatibility-shimmed (runtime compatibility lanes unchanged; used as evidence/rollback lane only). Component track disposition: implemented (CA-S01 closure decisioning now governed by explicit checklist + rubric artifact). P1/P2/P3 impact: no source-of-truth ownership reassignment; governance/closure determinism hardening only. README/wiki parity: docs index updated for operator/developer discoverability; no material end-user workflow contract change.

- Runtime+Component/CA-S01B Authority-Token + Resolver-Invariant Contract Table Publication (`docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): publish explicit canonical contract tables for authority tokens and resolver invariants so CA-S01 freeze criteria are concrete and auditable in one architecture surface. This formalizes required authority-mode semantics, degraded-reason token expectations, and resolver-stage invariants for bridge/no-mix readiness posture. Runtime track disposition: compatibility-shimmed (runtime compatibility lanes unchanged; no ownership expansion). Component track disposition: implemented (canonical contract tables published as active source-of-truth for CA-S01 governance). P1/P2/P3 impact: no source-of-truth ownership reassignment; contract-audit determinism and migration governance hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/CA-S01C Metadata-Prep Template Key-Safe Access Sweep (`docs/testing/raw/p3_s03_metadata_prep_validation.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): remove remaining ambiguous mapping dot-access on metadata-prep payload surfaces (`metadata.values`, `metadata.checks`, and sibling map fields) by converting to explicit key-safe indexing where applicable. This aligns Phase-3 diagnostics templates with deterministic payload access semantics already applied in scheduler validation, reducing method-name collision risk and template-engine shape drift. Runtime track disposition: compatibility-shimmed (runtime fallback surfaces unchanged). Component track disposition: implemented (component metadata payload consumers now consistently key-safe in active diagnostics templates). P1/P2/P3 impact: no source-of-truth ownership reassignment; diagnostics contract-read reliability hardening only. README/wiki parity: no material operator workflow contract change.

- Runtime+Component/CA-S01A Scheduler Metadata-Values Contract Hardening (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden deterministic scheduler diagnostics by switching `metadata_prep_validation.values` extraction to explicit key-safe map access (`metadata_prep_validation['values']`) so template evaluation cannot drift when dict-method name collision behavior appears under runtime/template engine differences. This preserves component-first now-playing/source consumer preference while removing a latent ambiguity in metadata values payload reads. Runtime track disposition: compatibility-shimmed (runtime fallback surfaces retained for bounded compatibility windows). Component track disposition: implemented (component metadata packet read lane remains primary and now key-safe). P1/P2/P3 impact: no source-of-truth ownership reassignment; CA-S01 contract determinism and diagnostics robustness hardening only. README/wiki parity: no material operator workflow contract change.

- Program/Canonical Architecture (CA) Roadmap Codification + Activation (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `README.md`): codify the accepted execution program as `CA-S01..CA-S08` with explicit functional tracks (`CORE`, `PROJ`, `COMPAT`, `OPS`), anti-fluff usability gates, and required two-track disposition per slice. Add explicit LC debt mapping (`LC-06/LC-07/LC-08`) into the CA lane structure and activate `CA-S01` (canonical contract freeze + authority boundary lock + consumer proof requirements) as the working roadmap baseline for next implementation slices. Runtime track disposition: compatibility-shimmed (legacy runtime remains rollback-safe baseline; no immediate ownership expansion in this codification slice). Component track disposition: implemented (canonical program-of-record and first active slice published). P1/P2/P3 impact: no source-of-truth ownership reassignment in this slice; execution-governance hardening and migration sequencing clarity only. README/wiki parity: README roadmap wording updated; no additional operator workflow contract change.

## 2026-05-04

- Runtime+Component/Slice-CE Resolver-Authority Component Activation (No-Mix Bridge Satisfaction + Handoff Status Rewire) (`custom_components/spectra_ls/metadata_stack.py`, `custom_components/spectra_ls/scaffold_fabric.py`, `custom_components/spectra_ls/validation_fabric.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): promote resolver-authority lane from attempt-driven scaffold semantics to component-activation semantics by (1) allowing metadata-bridge readiness to PASS under component no-mix startup posture when metadata prep is ready and component-selected now-playing authority is resolved, (2) using component now-playing entity fallback as resolver candidate when resolver-plan selection is absent, and (3) marking handoff inventory metadata resolver lane `implemented` when component authority/cutover/bridge satisfaction conditions are met even without explicit trial write attempts. Runtime track disposition: compatibility-shimmed (legacy resolver surfaces remain rollback-safe fallback only). Custom-component track disposition: implemented (resolver authority readiness and inventory status now derive from active component authority state, not legacy-attempt gating). P1/P2/P3 impact: no source-of-truth ownership change; resolver-authority activation reliability and migration-status truthfulness hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Slice-CD Scheduler Diagnostics Rewire to Component Sensor Truth Surfaces (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): rewire scheduler deterministic validation now-playing/source read-lane to consume component sensor contracts first (`sensor.component_now_playing_entity/state/title/artist/album/source`) with metadata-prep values and runtime surfaces only as fallback. This removes brittle dependence on nested shadow attribute payload shape when `metadata_prep_validation.values` is absent/transient, and aligns OLED/context diagnostics with active component entities. Runtime track disposition: compatibility-shimmed (runtime sensors retained strictly as fallback diagnostics lane). Custom-component track disposition: implemented (component sensor truth surfaces become primary diagnostics consumer path). P1/P2/P3 impact: no source-of-truth ownership change; consumer read-lane reliability and anti-drift hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Slice-CC Scheduler Template Component-First Now-Playing Read-Lane Cleanup (`custom_components/spectra_ls/metadata_stack.py`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): remove remaining legacy-first now-playing read assumptions in scheduler deterministic validation by preferring component metadata prep values (`metadata_prep_validation.values`) for now-playing entity/state/title/artist/album/source and using runtime `sensor.now_playing_*`/`sensor.ma_active_source` only as compatibility fallback; enrich component metadata prep values with selected now-playing entity artist/album/source fields to reduce fallback hits; additionally clarify all-green next-action wording when bridge posture is intentionally `skipped_component_startup_no_mix` so PASS guidance does not imply bridge verdict PASS. Runtime track disposition: compatibility-shimmed (runtime now-playing entities retained as fallback read lane only in diagnostics template). Custom-component track disposition: implemented (component metadata packet + template now consume component-selected now-playing winner/value lane first). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics consumer-lane migration and operator guidance precision hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Slice-CB Now-Playing Active-Player Selector Truthfulness Fix (`custom_components/spectra_ls/metadata_stack.py`, `docs/testing/raw/meta_stack_step3_full_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): fix component diagnostics winner drift by introducing component-native now-playing winner selection in metadata prep validation, preferring route-active target and active-meta candidates with freshness checks before falling back to legacy now-playing entity, then sourcing component now-playing state/title/position/duration from that selected entity; align Step-3 packet to read component-provided now-playing state/title values first. Runtime track disposition: compatibility-shimmed (runtime selector logic unchanged in this slice). Custom-component track disposition: implemented (component now-playing winner and values are selected in-component rather than blindly mirroring legacy winner state). P1/P2/P3 impact: no source-of-truth ownership change; component diagnostics winner truthfulness hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Slice-CA LC6-L05 Server-Profile/API Helper-Stack Consumer Cutover (Phase-2) (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/testing/raw/meta_stack_step3_full_validation.jinja`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): advance LC6-L05 by moving diagnostics/operator consumer guidance to component-native backend/API contract surfaces (`sensor.component_backend_profile`, `sensor.component_ma_api_url`) with explicit runtime helper fallback references retained for compatibility windows. Runtime track disposition: compatibility-shimmed (runtime helper stack still serves as fallback source while phase-2 consumer migration evidence is collected). Custom-component track disposition: implemented (component bridge entities are now the primary read path in active diagnostics/operator guidance). P1/P2/P3 impact: no source-of-truth ownership flip; consumer cutover and retirement-readiness hardening only. README/wiki parity: wiki/operator setup guidance updated in same slice; README parity no material repo-state change.

- Runtime+Component/Slice-BY LC6-L05 Server-Profile/API Helper-Stack Migration Bridge (Phase-1) (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/scaffold_fabric.py`, `custom_components/spectra_ls/sensor.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): start LC6-L05 execution by introducing component-facing backend profile/API diagnostics contract surfaces sourced from coordinator snapshot (`ma_backend_profile`) so active validation/triage can consume component entities instead of runtime helper/template entities directly. Runtime track disposition: compatibility-shimmed (runtime helper stack remains active source while migration bridge is validated). Custom-component track disposition: implemented (component diagnostics surfaces for backend profile and API URL now published). P1/P2/P3 impact: no source-of-truth ownership flip; LC6-L05 bridge execution and retirement-readiness hardening only. README/wiki parity: no material operator workflow change.

- Diagnostics/Slice-BZ Scheduler Deterministic Template Guidance Consistency (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): fix conflicting next-action guidance by treating component no-mix bridge-skip posture as non-actionable bridge failure context, preventing `RUN_BRIDGE` hints and bridge penalty scoring when bridge status is intentionally `skipped_component_startup_no_mix`, and preferring metadata-prep blocker guidance in that posture. Runtime track disposition: compatibility-shimmed (no runtime behavior mutation). Custom-component track disposition: implemented (operator guidance/triage correctness hardening). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics actionability hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Slice-BX LC6-L04 Runtime Provider Sink Retirement (Phase-2) (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/metadata_stack.py`, `custom_components/spectra_ls/snapshot_fabric.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/services.yaml`, `packages/ma_control_hub/script.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): retire runtime helper-sink authority for metadata provider telemetry by introducing component-owned telemetry record service + in-memory packet storage and switching runtime provider dispatch script to publish provider telemetry through component service instead of helper text sink writes. Runtime track disposition: implemented (runtime helper sink writes removed from active provider dispatch path; compatibility fallback no longer required in normal component-primary operation). Custom-component track disposition: implemented (component service/state packet now authoritative telemetry source for provider-refresh diagnostics). P1/P2/P3 impact: no source-of-truth ownership flip; LC6-L04 retirement execution and provider telemetry contract hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Slice-BW LC6-L04 Provider Telemetry Helper-Sink Migration Bridge (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/snapshot_fabric.py`, `custom_components/spectra_ls/sensor.py`, `docs/testing/raw/meta_stack_step3_full_validation.jinja`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): introduce component-owned provider telemetry packet exposure in `write_controls` (status/providers/response/item URI/reason/updated-at + age) and publish a dedicated component diagnostics sensor so validation/triage consumers no longer read provider telemetry directly from runtime helper sink entities. Runtime track disposition: compatibility-shimmed (runtime helper writes retained temporarily as bounded compatibility source while consumers migrate). Custom-component track disposition: implemented (component provider telemetry packet + diagnostics surface now authoritative for consumer reads). P1/P2/P3 impact: no source-of-truth ownership flip; LC6-L04 consumer cutover bridge and retirement readiness hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-BV Arylic HTTP Connect-Reset Churn Stabilization (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`): harden Arylic HTTP poll failure handling under repeated connect-reset errors by tightening failure-tier backoff progression, escalating unknown-transport hosts to max backoff at sustained failure (`fail>=3`), and suppressing scheme-flip probe churn when the active host has no known-good HTTP transport history yet. This keeps discovery-first behavior while reducing repeated error bursts/transport oscillation when endpoints expose TCP control but reject HTTP status polling. Runtime track disposition: implemented (poll/backoff guard correction only; ownership unchanged). Custom-component track disposition: compatibility-shimmed (component contracts unchanged; equivalent failure mode checked/not-applicable). P1/P2/P3 impact: no source-of-truth ownership change; runtime transport resilience and noise-reduction hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-BU Fallback Telemetry Sensor Schema Fix (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`): repair ESPHome config validation failure in fallback telemetry by correcting `esp_control_fallback_*` metric surfaces to use numeric `sensor.template` contracts (with proper units/state class) while keeping `esp_control_fallback_last_type` as `text_sensor.template`. This removes invalid `text_sensor` numeric fields (including `unit_of_measurement`) and preserves LC-07 fallback instrumentation semantics for build/OTA verification continuity. Runtime track disposition: implemented (schema/type correction only; no ownership/path cutover change). Custom-component track disposition: compatibility-shimmed (component contracts unchanged; equivalent failure mode checked/not-applicable). P1/P2/P3 impact: no source-of-truth ownership change; validation/build integrity hardening only. README/wiki parity: no material operator workflow change.

- Component+ESP/Slice-BT LC-07/LC-08 Execution Sweep (`custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): implement component-owned metadata override status packet surfaces (`binary_sensor.component_metadata_override_active`, `sensor.component_metadata_override_entity`) and migrate active ESP metadata override substitutions to those component entities, while adding explicit fallback-hit telemetry counters/age/type surfaces (`esp_control_fallback_*`) to instrument LC-07 fallback listener usage before retirement. Runtime track disposition: implemented (ESP substitution/telemetry hardening only; fallback listeners intentionally retained pending soak evidence). Custom-component track disposition: implemented (metadata override status packet published and consumed by active ESP lane). P1/P2/P3 impact: no source-of-truth ownership change; migration evidence depth and read-lane cutover hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-BS LC-06 Runtime Retirement Decomposition (`docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): convert LC-06 from a high-level placeholder into explicit runtime retirement lanes for `packages/ma_control_hub/*` (authority + target-selection writers, metadata override helper storage, provider telemetry helpers, and server-profile REST helper stack), each with concrete replacement contracts and exit criteria. Runtime track disposition: compatibility-shimmed (docs-only planning slice; no runtime behavior mutation). Custom-component track disposition: compatibility-shimmed (docs-only planning slice; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership change; retirement sequencing clarity and traceability hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-BR ESP Legacy Rip-Out Inventory Documentation (`docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): complete the explicit ESP legacy retirement documentation pass by adding a concrete `esphome/spectra_ls_system/**` compatibility inventory (fallback host/port feeds, fallback listeners, metadata helper passthrough bindings, and dual-path devtools references), and map each candidate to retirement gates (`LC-06`/`LC-07`/`LC-08`) with measurable exit criteria. Runtime track disposition: compatibility-shimmed (no runtime behavior mutation in this docs slice). Custom-component track disposition: compatibility-shimmed (no component behavior mutation in this docs slice). P1/P2/P3 impact: no source-of-truth ownership change; retirement traceability/completeness hardening only. README/wiki parity: no material operator workflow change.

- Component/Slice-BQ LC-05 Legacy Scaffold Constant Governance Split (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/diagnostics.py`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): implement the next cleanup slice by splitting internal `LEGACY_*` constant surfaces into explicit governance buckets (`compat_required` vs `retire_candidate`) with deterministic retirement gate IDs (`LC-05`/`LC-06`) and exporting the classification in component diagnostics for operator/audit visibility. Verified no dead/unreachable `LEGACY_*` constants remain in active component paths in this slice. Runtime track disposition: compatibility-shimmed (no runtime behavior/contract mutation; version bump not applicable). Custom-component track disposition: implemented (constant-surface governance split + diagnostics exposure). P1/P2/P3 impact: no source-of-truth ownership change; migration-governance and retirement-traceability hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-BP LC-01 Component Entity Namespace Binding Remap (`esphome/spectra_ls_system/substitutions.yaml`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): align active ESP metadata/control read-lane substitutions to the live Home Assistant component entity namespace (`sensor.component_*`, `binary_sensor.component_*`) after runtime verification showed `sensor.spectra_ls_component_*` surfaces are absent in this environment. This removes read-lane 404 bindings while preserving existing component contracts/services and legacy fallback semantics. Runtime track disposition: implemented (ESP substitution consumer remap only). Custom-component track disposition: compatibility-shimmed (component contract surfaces unchanged; naming/failure-mode posture verified). P1/P2/P3 impact: no source-of-truth ownership change; environment-binding correctness hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-BO Control-Host Feed Fallback + Lighting OLED Label Hardening (`esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): resolve split-brain telemetry where component shadow route/host was healthy but ESP could still emit `no_target`/`no_hosts` by adding runtime compatibility feed fallbacks for control host/hosts/port ingestion (used only when component-native feeds are transiently unresolved) and by classifying handoff readiness from resolved cached host state. Also replace lighting-mode blank OLED placeholder from `lighting|oled:-` to explicit `lighting|oled:LIGHTING` for clearer operator-facing status. Runtime track disposition: implemented (ESP ingest/status/render hardening only; no ownership/authority cutover). Custom-component track disposition: compatibility-shimmed (component contracts unchanged; equivalent failure-mode posture reviewed). P1/P2/P3 impact: no source-of-truth ownership change; runtime handoff/host-gate correctness and telemetry clarity hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Validation/Slice-BN ESP Handoff False-`no_target` Guard + Lighting-OLED Tester Semantics (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/testing/raw/meta_component_full_stack_tester.jinja`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden ESP handoff status classification to avoid transient false `no_target` when active-target helper state is momentarily unresolved but resolved active-friendly context is present, and refine one-screen tester interpretation so `lighting|oled:-` is treated as expected lighting-mode posture instead of implied display degradation. Runtime track disposition: implemented (telemetry classification hardening, no authority-path ownership change). Custom-component track disposition: compatibility-shimmed (component contracts unchanged; validation interpretation aligned to runtime UI-mode semantics). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics signal correctness and operator-triage clarity hardening only. README/wiki parity: no material operator workflow change.

- Component+ESP/Slice-BM LC-01 Metadata Override Write-Lane Cutover (Phase 2) (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/metadata_stack.py`, `esphome/spectra_ls_system/packages/spectra-ls-ui.yaml`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): complete LC-01 phase-2 by introducing explicit component service `spectra_ls.set_metadata_override` (guarded apply/clear semantics) and migrating ESP metadata override menu apply flow away from direct helper writes to the component service contract. Runtime track disposition: implemented (active ESP metadata override write lane is now component-mediated; helper entities remain compatibility storage only). Custom-component track disposition: implemented (new metadata override service contract + guarded metadata-stack apply/clear workflow). P1/P2/P3 impact: source-of-truth ownership unchanged; bounded metadata override write-lane retirement and contract hardening only. README/wiki parity: no material operator workflow change.

- Component+ESP/Slice-BL LC-01 Metadata Candidate Read-Lane Cutover (Phase 1) (`custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `esphome/spectra_ls_system/substitutions.yaml`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): add component-native metadata candidate and low-confidence contract entities (`sensor.spectra_ls_component_meta_candidates`, `binary_sensor.spectra_ls_component_meta_low_confidence`) and migrate ESP read-lane substitutions (`ha_ma_meta_candidates`, `ha_ma_meta_low_confidence`) to component surfaces. Runtime track disposition: compatibility-shimmed (override write lane remains helper-backed for now). Custom-component track disposition: implemented (component metadata candidate/low-confidence read contracts exposed and consumed). P1/P2/P3 impact: bounded read-path retirement only; explicit metadata override write-lane retirement remains a follow-on phase. README/wiki parity: no material operator workflow change.

- Component+ESP/Slice-BK LC-02 Active-Target Write-Lane Cutover (Phase 2) (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/selection_fabric.py`, `custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/__init__.py`, `esphome/spectra_ls_system/packages/spectra-ls-ui.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): complete LC-02 write-lane retirement by introducing explicit component service `spectra_ls.set_active_target` (guarded helper apply semantics with authority/debounce/reentrancy protections) and migrating ESP UI prompt-commit flow away from direct `input_select.select_option` helper writes to the component service contract. Runtime track disposition: implemented (ESP prompt write lane now routes through component service surface; direct helper write removed from active UI path). Custom-component track disposition: implemented (new explicit target-selection service contract + guarded selection workflow). P1/P2/P3 impact: no source-of-truth ownership rollback; bounded write-lane retirement and contract hardening only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Slice-AZ Now-Playing Entity Stale-Winner Unlock (Missing Progress Clock Guard) (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): fix the remaining stale-winner path where `sensor.now_playing_entity` and related sticky metadata guards could treat missing `media_position_updated_at` as implicitly fresh (`pos_ts == 0` shortcut), causing resolver/preferred winners to remain pinned while playback context moved. Runtime freshness gates now derive recency from bounded timestamp fallback semantics (position timestamp when present, otherwise entity `last_changed`) and require fresh playback evidence for playing/paused winner acceptance in resolver and now-playing selection branches. Runtime track disposition: implemented (legacy runtime stale-lock root cause corrected). Custom-component track disposition: compatibility-shimmed (component now-playing freshness path already uses bounded `last_changed` fallback; equivalent failure mode reviewed, no additional code delta required). P1/P2/P3 impact: no source-of-truth ownership change; metadata winner truthfulness and stale-lock recovery hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-AY.7 Idle-Transition Poll Guard + Extended Input Quiet Window (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `esphome/spectra_ls_system/substitutions.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): reduce remaining hitch spikes by suppressing Arylic HTTP poll attempts during brief HA idle/off transition windows when Arylic is already known-playing, and extending post-input poll suppression so polling does not re-enter immediately after high-frequency knob bursts. This specifically targets long interval blocking windows observed around transient `ha_audio_state='idle'` transitions and heavy input bursts while preserving discovery-first route ownership and component authority contracts. Runtime track disposition: implemented (ESP poll-loop guard hardening). Custom-component track disposition: compatibility-shimmed (component path does not own ESP HTTP poll loop; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership change; runtime hitch-risk reduction only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-AY.6 Arylic HTTP Timeout + Protocol Bootstrap Correction (False-Collapse Reduction) (`esphome/spectra_ls_system/substitutions.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): root-cause correction for intermittent `http_request` collapse windows under healthy CPU/heap conditions by (1) widening the Arylic request timeout budget and tuning poll/input-guard cadence for endpoint jitter tolerance, and (2) correcting default probe bootstrap from HTTPS:443 to HTTP:80 so HTTP-only LinkPlay targets do not enter repeated TLS handshake failure/backoff loops after reboot before fallback can engage. This removes avoidable connect-failure churn without changing authority ownership or discovery-first routing contracts. Runtime track disposition: implemented (active ESP transport resilience hardening). Custom-component track disposition: compatibility-shimmed (component path does not own ESP HTTP transport poll loop; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership change; runtime transport stability correction only. README/wiki parity: no material operator workflow change.

- Runtime+Component/Slice-AY.5 Hard Component Pin (Legacy Authority Disabled) (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/control_execution_fabric.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/selection_fabric.py`, `packages/ma_control_hub/template.inc`): enforce component-only write authority by pinning authority normalization/defaults to component mode, ignoring legacy authority mode requests, and defaulting runtime authority contract fallback to component. Additionally harden component auto-select scaffold to keep current helper target when valid unless explicit force is requested, reducing unsolicited target/host flips during noisy metadata/state churn. Runtime track disposition: implemented (legacy authority lane disabled for normal operation; runtime authority fallback now component). Custom-component track disposition: implemented (component coordinator/control execution/selection lanes now component-pinned and sticky by default). P1/P2/P3 impact: active source-of-truth for target/host write ownership is now component-only (legacy retained only as dormant rollback artifact, not selectable authority mode). README/wiki parity: no additional operator workflow delta beyond existing component-primary note.

- Runtime+HA/Slice-AY.4 Non-Hybrid Authority Cutover (Component-Primary Target/Host Ownership) (`packages/ma_control_hub/template.inc`, `packages/ma_control_hub/script.inc`, `packages/ma_control_hub/automation.inc`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): move active target/host ownership out of hybrid behavior for normal operation by introducing a runtime authority-mode contract surface (`sensor.ma_authority_mode`), gating legacy target-option/auto-select/restore/lock automations and scripts when component authority is active, delegating cycle-target action to component service (`spectra_ls.cycle_active_target`) in component mode, and preferring component host-cutover candidate host for `sensor.ma_control_hosts` when authority mode is component. This prevents dual-writer churn and keeps ESP host feeds aligned to component authority in component mode while preserving fail-closed legacy fallback when component authority is not active. Runtime track disposition: implemented (legacy writers now authority-gated; host feed follows component candidate in component mode). Custom-component track disposition: implemented (existing component authority + cycle service + host-cutover gate consumed as canonical authority contract; no new component schema change required). P1/P2/P3 impact: source-of-truth ownership remains migration-bounded but normal operation is now component-primary and non-hybrid for target/host selection lanes. README/wiki parity: README updated (program-status authority note); no material wiki workflow change.

- Runtime+HA/Slice-AY.3 Active-Target Stickiness Guard (No-Playback Host Churn Stop) (`packages/ma_control_hub/script.inc`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): stop unnecessary `input_select.ma_active_target` flips between control-capable hosts when no stronger playback candidate exists by adding a selector guard that retains the current target when it is still control-ready and no detected-playing override is present. This removes idle/noise-driven host churn (`ma_control_host`/`ma_control_hosts` thrash) while preserving explicit operator target changes and valid detected-playing handoff behavior. Runtime track disposition: implemented (active runtime selector stability hardening). Custom-component track disposition: compatibility-shimmed (component path does not own legacy `ma_control_hub` selector lane; equivalent failure-mode posture reviewed). P1/P2/P3 impact: no source-of-truth ownership change; runtime target-selection stability hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-AY.2 Arylic HTTP Per-Host Scheme Memory (Target-Churn Stability) (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): fix protocol churn under active-target host switching by promoting Arylic HTTP preferred transport memory from single-host to bounded two-host memory slots, so host A/B route flips retain each host’s last-known good scheme/port instead of repeatedly restarting on default protocol. This directly addresses repeated HTTPS handshake failures on hosts that are HTTP-only while preserving discovery-first behavior and fallback safety. Runtime track disposition: implemented (active ESP runtime transport-selection stability hardening). Custom-component track disposition: compatibility-shimmed (component path does not own ESP HTTP polling lane; equivalent failure-mode posture reviewed). P1/P2/P3 impact: no source-of-truth ownership change; runtime transport-selection correctness hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-AY.1 Arylic HTTP Interaction-Priority Guard + Timeout Tightening (`esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): further reduce residual UI hitch under transport-fault windows by tightening Arylic HTTP request timeout budget (`arylic_http_timeout_ms`) and adding a short input-priority poll guard that skips non-critical HTTP status polls for a bounded window immediately after user interaction (`last_input_ms`), preserving discovery-first behavior while ensuring tactile control paths are prioritized during rapid knob activity. Runtime track disposition: implemented (active ESP runtime poll-loop responsiveness hardening). Custom-component track disposition: compatibility-shimmed (component path does not own ESP HTTP poll loop; equivalent failure-mode posture reviewed). P1/P2/P3 impact: no source-of-truth ownership change; runtime interaction smoothness hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-AY Arylic HTTP Hitch Stabilization Under Transport Errors (`esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): reduce perceived ESP UI hitch/freeze windows during repeated Arylic `getPlayerStatus` transport failures by tightening request timeout budget, slowing early retry cadence through stronger fail-backoff defaults, and delaying protocol fallback scheme switching until sustained failure counts (instead of early two-fail flips). This keeps discovery-first behavior while reducing expensive fallback churn and main-loop stall pressure during unreachable/error windows. Runtime track disposition: implemented (active ESP runtime poll-loop stability hardening). Custom-component track disposition: compatibility-shimmed (component path does not own ESP HTTP poll loop; equivalent failure-mode posture reviewed). P1/P2/P3 impact: no source-of-truth ownership change; runtime transport resilience and interaction smoothness hardening only. README/wiki parity: no material operator workflow change.

## 2026-05-03

- Runtime/Slice-BI Now-Playing Freshness Age Fallback Clock Fix (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): fix stale freshness-age reporting where `sensor.now_playing_freshness_age_s` returned `9999` whenever `media_position_updated_at` was missing, even during active/paused metadata transitions. Runtime freshness sensor now uses bounded fallback clock semantics (`media_position_updated_at` → entity `last_updated` → entity `last_changed`) to avoid false degraded-fallback posture from missing progress timestamp attributes. Runtime track disposition: implemented (runtime freshness contract correctness hardening). Custom-component track disposition: compatibility-shimmed (component metadata stack already applies bounded fallback semantics; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership change; stale-freshness false-negative reduction only. README/wiki parity: no material operator workflow change.

- Component+ESP/Slice-BJ LC-02 Active-Target Read-Lane Cutover (Phase 1) (`esphome/spectra_ls_system/substitutions.yaml`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): migrate ESP active-target read consumption from runtime helper `input_select.ma_active_target` to component surface `sensor.spectra_ls_component_active_target` via substitution binding, while intentionally retaining helper write-path usage in UI prompt selection as a temporary compatibility shim until a component service supports direct explicit target selection. Runtime track disposition: compatibility-shimmed (helper write lane retained intentionally for prompt-commit compatibility). Custom-component track disposition: implemented (component active-target contract now drives ESP read lane). P1/P2/P3 impact: bounded read-path retirement only; no write-authority expansion in this step. README/wiki parity: no material operator workflow change.

- Component+ESP/Slice-BH Legacy Control-Port Feed Cleanup (LC-03) (`custom_components/spectra_ls/sensor.py`, `esphome/spectra_ls_system/substitutions.yaml`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add component-native control-port contract sensor (`sensor.spectra_ls_component_control_port`) derived from route/host-control packet semantics (linkplay default port when component route is control-capable), and switch ESP control-port substitution from runtime `sensor.ma_control_port` to this component surface. Runtime track disposition: compatibility-shimmed (runtime port surface retained as rollback artifact). Custom-component track disposition: implemented (component now provides control-port feed for active ESP lane). P1/P2/P3 impact: bounded read-path migration only; no immediate runtime contract deletion in this slice. README/wiki parity: no material operator workflow change.

- Component+ESP/Slice-BG Legacy Active-Friendly Feed Cleanup (LC-04) (`esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): retire direct ESP dependency on runtime `sensor.ma_active_friendly` by switching active-friendly ingest to component-native friendly surface (`sensor.spectra_ls_component_now_playing_friendly`) through substitutions. This closes LC-04 in the cleanup tracker while preserving existing runtime contracts as compatibility artifacts. Runtime track disposition: compatibility-shimmed (runtime friendly surface remains available but no longer consumed by this ESP lane). Custom-component track disposition: implemented (component friendly surface now powers active-friendly ingest). P1/P2/P3 impact: no immediate runtime removal; one legacy read-path dependency retired with build/OTA validation gate. README/wiki parity: no material operator workflow change.

- Governance/Slice-BF Legacy Codepath Cleanup Tracker Activation (`docs/roadmap/LEGACY-CODEPATH-CLEANUP-TRACKER.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add an explicit legacy-retirement task ledger that inventories remaining runtime/ESP/component legacy dependencies, defines phase-aligned cleanup tasks with blockers, and records concrete exit criteria before removal so stale compatibility scaffolding does not linger unowned. Runtime track disposition: compatibility-shimmed (legacy path retained intentionally until task exit gates are met). Custom-component track disposition: implemented (component program now carries explicit cleanup backlog ownership and completion gates). P1/P2/P3 impact: no immediate contract removal; this slice formalizes cleanup accountability and de-risked retirement sequencing. README/wiki parity: no material operator workflow change.

- Component+ESP/Slice-BE Component-Native Now-Playing Volume Consumer Swap (`custom_components/spectra_ls/sensor.py`, `esphome/spectra_ls_system/substitutions.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add component-derived now-playing volume contract sensor (`sensor.spectra_ls_component_now_playing_volume`) from active now-playing entity attributes and switch ESP volume ingest substitution from legacy runtime `sensor.ma_active_volume` to this component surface. Runtime track disposition: compatibility-shimmed (legacy volume contract retained as fallback artifact). Custom-component track disposition: implemented (component now provides now-playing volume signal for active ESP consumer lane). P1/P2/P3 impact: no immediate runtime retirement; component-owned now-playing consumer coverage now includes volume in addition to context/state/duration/position/media-policy surfaces. README/wiki parity: no material operator workflow change.

- Component+ESP/Slice-BD Component-Native Control-Route Feed Swap (Targets/Hosts/Host) (`esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): shift ESP control-route consumer feeds from runtime `sensor.ma_control_targets`/`sensor.ma_control_hosts`/`sensor.ma_control_host` to component-native route surfaces (`sensor.spectra_ls_component_control_targets`, `sensor.spectra_ls_component_control_hosts`, `sensor.spectra_ls_component_control_host`) via substitutions, including forced-refresh update-entity calls and target-options attribute reads. Runtime track disposition: compatibility-shimmed (runtime route surfaces retained as rollback-safe compatibility and `sensor.ma_control_port` remains runtime source). Custom-component track disposition: implemented (component route packet surfaces now drive active ESP target/host ingest lanes). P1/P2/P3 impact: no immediate legacy retirement; component-owned control-route read-path coverage expanded while preserving bounded fallback semantics for port and helper-driven active-target flows. README/wiki parity: no material operator workflow change.

- Component+ESP/Slice-BC Component-Native Now-Playing Context Completion (Friendly/Artist/Album/App/Source) (`custom_components/spectra_ls/sensor.py`, `esphome/spectra_ls_system/substitutions.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add component-derived now-playing context sensors for friendly label and core metadata context fields (artist/album/app/source) and switch ESP substitutions to these component surfaces for active metadata ingest lanes. This removes remaining routine ESP dependence on legacy runtime now-playing context fields while preserving runtime contracts as migration-safe compatibility fallback. Runtime track disposition: compatibility-shimmed (legacy contracts retained, no write-path ownership change). Custom-component track disposition: implemented (component now publishes and serves complete now-playing context packet family for ESP consumers). P1/P2/P3 impact: no immediate legacy retirement; component-owned consumer read-path coverage is expanded to full now-playing context tuple. README/wiki parity: no material operator workflow change.

- Component+ESP/Slice-BB Component-Native Now-Playing Consumer Contract Swap (`esphome/spectra_ls_system/substitutions.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): move ESP now-playing consumer bindings from legacy runtime `sensor.now_playing_*`/`binary_sensor.now_playing_display_allowed` surfaces to component-native contract entities published by `custom_components/spectra_ls` (entity/state/title/position/duration/media_class/preview_key/freshness-age + display-allowed). This keeps HA/ESP metadata ingest aligned with component-owned authority packets while preserving runtime legacy templates as compatibility fallback artifacts during migration. Runtime track disposition: compatibility-shimmed (legacy runtime now-playing contracts retained; no ownership expansion). Custom-component track disposition: implemented (component contract surfaces now consumed directly by ESP metadata path). P1/P2/P3 impact: no immediate source-of-truth retirement; consumer lane now prefers component-native contract ownership for now-playing read-paths. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-BA Component-Native Contract Sensor Surfaces (Target/Host/Now-Playing) (`custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`): add first-class component-owned product contract entities so downstream consumers can migrate off legacy runtime names without waiting on template mirrors. New component surfaces expose active target, control host(s), control-target inventory, and now-playing contract packets (entity/state/title/position/duration/media_class/preview_key/freshness-age + display-allowed binary) directly from coordinator snapshot validation data and route/registry context. Runtime track disposition: deferred with rationale (runtime legacy contract surfaces retained as compatibility shim during active cutover). Custom-component track disposition: implemented (component now publishes migration-ready contract entities in parallel with existing diagnostics). P1/P2/P3 impact: no immediate source-of-truth flip; this is contract-surface expansion to enable deterministic consumer cutover sequencing. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-AX3+AX4 Freshness-Age Consumer + Deterministic Preview-Key Contract (`packages/ma_control_hub/template.inc`, `esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/testing/raw/meta_component_full_stack_tester.jinja`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): add explicit now-playing freshness-age and deterministic preview-key contract surfaces on the HA runtime side, then consume those surfaces on ESP for bounded display recompute and stale-cache suppression during handoff churn. `sensor.now_playing_preview_key` now emits a stable, content-derived key (entity/state/title/artist/album/media class/display policy + coarse freshness bucket) instead of a blank placeholder, and `sensor.now_playing_freshness_age_s` exposes bounded source-freshness age for consumer-side guardrails. ESP ingest now subscribes to freshness-age updates and OLED fallback cache rescue is freshness-aware to reduce stale carryover windows without changing HA display-policy authority. Runtime track disposition: implemented (active HA+ESP consumer hardening). Custom-component track disposition: compatibility-shimmed (component behavior-path unchanged; equivalent failure-mode posture reviewed). P1/P2/P3 impact: no source-of-truth ownership change; consumer-side determinism/freshness hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-AX1+AX2 HA Now-Playing Consumer Reactivity + Source-Fallback Polish (`esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): harden ESP consumer behavior after HA metadata rewrite by wiring explicit now-playing entity/friendly/preview-key contract surfaces into runtime ingest and using entity-change reactivity to force bounded metadata refresh/display-state recompute on source-entity switches. OLED source fallback logic now prefers HA now-playing friendly context before generic placeholders, reducing low-information `SRC` fallback windows during sparse metadata transitions without changing HA display policy authority. Runtime track disposition: implemented (active ESP consumer ingest/render hardening). Custom-component track disposition: compatibility-shimmed (component behavior-path unchanged; equivalent failure-mode posture reviewed). P1/P2/P3 impact: no source-of-truth ownership change; consumer-side coherence/reactivity hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-AW3 OLED Metadata Tuple Coherency Fix (`esphome/spectra_ls_system/substitutions.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): fix mixed-source OLED metadata tuples where title could resolve from `sensor.now_playing_title` while artist/album resolved from `sensor.ma_active_*`, causing stale album carryover from older contexts. ESP Home Assistant metadata substitutions now use a single now-playing contract family for title/artist/album to enforce same-source tuple coherence on OLED render paths. Runtime track disposition: implemented (active ESP runtime metadata contract alignment). Custom-component track disposition: compatibility-shimmed (component behavior unchanged; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership flip; metadata display coherence hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AW2 Restart Cutover Persistence + Active-Target Capability Recovery Hardening (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/control_execution_fabric.py`, `custom_components/spectra_ls/registry.py`, `custom_components/spectra_ls/__init__.py`): fix restart regression where authority mode reverted to legacy by default and failed to retain operator-selected component cutover posture, and harden registry fallback for active-target host/path inference so valid active targets are less likely to degrade into `defer_not_capable` during transient host-feed churn. Adds persisted default write-authority option handling, startup authority restore from entry options, and a bounded active-target host fallback path from current control-host surfaces when target-level discovery is temporarily incomplete. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged; equivalent failure mode reviewed). Custom-component track disposition: implemented (component startup authority persistence + route capability fallback hardening). P1/P2/P3 impact: no source-of-truth ownership flip; restart stability and route-readiness reliability hardening only. README/wiki parity: no material operator workflow change.

- Hardening/Slice-AW Meta-Stack Guard Reuse + Tester Lightweight Contract Polish (`custom_components/spectra_ls/metadata_stack.py`, `docs/testing/raw/meta_component_full_stack_tester.jinja`): reduce one-off guard/attempt bookkeeping drift by reusing shared `WritePathFabric` guard and stamp helpers in metadata resolver scaffold write path, and streamline tester read-path contract consumption by introducing one-pass shadow attribute normalization (single attribute object fan-out) for clearer, lower-overhead template evaluation. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; no write-ownership expansion). Custom-component track disposition: implemented (guard-chain reuse and diagnostics-template polish). P1/P2/P3 impact: no source-of-truth ownership change; maintainability + execution-safety hardening only. README/wiki parity: no material operator workflow change.

- Validation/Slice-AV Full-Stack Tester ESP Runtime Gate (`docs/testing/raw/meta_component_full_stack_tester.jinja`): add explicit ESP-side sanity visibility to the metadata full-stack tester by checking runtime telemetry surfaces (`sensor.spectra_ls_system_esp_control_handoff_status`, `sensor.spectra_ls_system_esp_oled_status`) and scoring an additional full-stack gate (`esp_runtime_not_ready` blocker when unavailable/invalid). Suggested-next-action guidance now includes a direct ESP recovery lane when component metadata/authority gates are green but ESP runtime telemetry is degraded. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; diagnostics coverage expanded). Custom-component track disposition: compatibility-shimmed (template-level observability hardening only). P1/P2/P3 impact: no source-of-truth ownership change; full-stack diagnostics completeness hardening only. README/wiki parity: no material operator workflow change.

- Validation/Slice-AU Full-Stack Tester Metadata-Only Scope Trim (`docs/testing/raw/meta_component_full_stack_tester.jinja`): remove Phase-4 optional diagnostic blocks (`capability_profile_validation`, `action_catalog_validation`, `crossfade_balance_validation`) from the one-screen metadata tester because those payloads are intentionally not exposed on `sensor.shadow_active_target` in the current recorder-safe contract. Template scoring/gates now stay metadata/control/authority focused without emitting misleading `n/a_not_exposed` lines. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged). Custom-component track disposition: compatibility-shimmed (diagnostic template scope correction only). P1/P2/P3 impact: no source-of-truth ownership change; operator diagnostics clarity hardening only. README/wiki parity: no material operator workflow change.

- Validation/Slice-AT Full-Stack Tester Authority-Proof Guidance Refinement (`docs/testing/raw/meta_component_full_stack_tester.jinja`): improve Suggested next action logic for the common post-restart false-negative lane where metadata prep is PASS but authority remains not-ready due missing/stale bridge proof telemetry; template now explicitly points operators to the proof-capture sequence (`set_write_authority(component)` → resolver scaffold → trial bridge scaffold) before re-evaluating closeout gates. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged). Custom-component track disposition: compatibility-shimmed (diagnostic guidance refinement only). P1/P2/P3 impact: no source-of-truth ownership change; operator triage guidance hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AS Metadata Prep Freshness Tolerance for Resolved Active Playback (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden metadata-prep authority gating to avoid false-negative degraded fallback when active playback contract surfaces are resolved (`state/title/position/duration`) but progress-clock freshness is stale due upstream timing jitter. Metadata handoff and authority freshness gates now accept a bounded resolved-playback fallback posture instead of fail-closing strictly on `no_fresh_play_signal`/`playing_without_recent_progress` in this specific contract-complete condition. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged; equivalent mode reviewed). Custom-component track disposition: implemented (metadata authority gate false-negative path corrected). P1/P2/P3 impact: no source-of-truth ownership change; authority closeout reliability hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-AR MA Control Port Parse Guard + Reconnect No-Clear Noise Hardening (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden ESPHome HA-feed handling so transient `sensor.ma_control_port` non-numeric states (`unknown`/`unavailable`/empty) no longer emit conversion warnings and transient reconnect-window host/hosts invalid updates no longer produce forced fail-closed clear warnings when reconnect grace is active. Runtime track disposition: implemented (active ESP runtime parser/reconnect guard hardening). Custom-component track disposition: compatibility-shimmed (component behavior unchanged; equivalent failure mode reviewed). P1/P2/P3 impact: no source-of-truth ownership change; runtime ingest/observability hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AQ Metadata Trial Audit Completeness Contract Fix (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): fix a metadata trial contract gap where `expected_meta_entity`-matched runs could complete without emitting a canonical trial `status`, causing partial audit payloads (`missing_audit_fields=['status']`), `eligible_for_closeout=false`, and downstream false blocker `cutover_prep_incomplete` despite successful bridge/trial execution. Trial preflight now fail-closes only on actual expected-meta mismatch while preserving normal status assignment on matched paths, restoring deterministic audit completeness and authority closeout eligibility semantics. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; equivalent audit-surface failure mode reviewed). Custom-component track disposition: implemented (metadata trial status emission contract corrected for expected-meta matched paths). P1/P2/P3 impact: no source-of-truth ownership change; metadata trial audit-contract correctness hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AP Metadata Bridge Expected-Target Stability Guard (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden metadata bridge scaffold sequencing to avoid unintended active-target flips during bounded cutover windows when a validated `expected_target` is provided and already matches the current routed target. This prevents bridge preflight auto-select from reselecting a non-control-capable metadata source target mid-window (for example Apple TV metadata entity), which otherwise forces `defer_not_capable` route regression and deterministic `blocked_metadata_not_ready` trial failures unrelated to metadata-owner authority logic. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; equivalent route-stability failure mode reviewed). Custom-component track disposition: implemented (bridge sequence now preserves expected-target route stability and only runs recovery auto-select when target/routing preconditions are not already satisfied). P1/P2/P3 impact: no source-of-truth ownership change; bounded execution-window stability and authority-lane correctness hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AO Metadata Bridge In-Window Authority Proof Alignment (`custom_components/spectra_ls/metadata_stack.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): fix metadata bridge-trial sequencing so in-window cutover proof remains captured under active component metadata authority when metadata prep/cutover is already ready, instead of force-switching to legacy during the trial stage and producing deterministic false blockers (`proof_in_window_cutover_inactive`, `proof_in_window_owner_not_component`) despite live component ownership being active. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; metadata authority bridge semantics checked for equivalent failure mode). Custom-component track disposition: implemented (bridge authority-stage selection now preserves component-mode in-window proof when cutover is active and only uses legacy trial mode when cutover is not active). P1/P2/P3 impact: no source-of-truth ownership change; authority contract closeout correctness hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AN Registry Capability-Path Alignment for WiiM Host Targets (`custom_components/spectra_ls/registry.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): align component registry control-path/capability classification with effective host-resolved transport reality so eligible WiiM-discovered targets that resolve to Linkplay/TCP-capable control hosts are not force-classified as `control_path=unknown` and `control_capable=false`. This removes false `defer_not_capable` route decisions and unblocks scheduler candidate formation for host-cutover readiness where transport support is present. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; component registry classification corrected). Custom-component track disposition: implemented (registry route-capability inference hardening for current packet/gate consumers). P1/P2/P3 impact: no source-of-truth ownership change; routing capability classification correctness hardening only. README/wiki parity: no material operator workflow change.

- Validation/Slice-AM Meta Full-Stack Tester Schema+Scoring Correction (`docs/testing/raw/meta_component_full_stack_tester.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): fix false-fail conditions in the one-screen tester by aligning to current shadow packet schema (`unresolved_sources`/`mismatches` from top-level attributes), counting list-valued contract/parity fields correctly, deriving `contract_ready` fallback when `ready` is not emitted, and treating capability/action/crossfade checks as optional when those lanes are not exposed in current shadow attributes. Runtime track disposition: compatibility-shimmed (runtime behavior unchanged; diagnostics contract consumption corrected). Custom-component track disposition: implemented (validation tooling logic corrected for current component packet shape). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics correctness and operator triage reliability hardening only. README/wiki parity: no material operator workflow change.

- Runtime+ESP/Slice-AL Control-Host Reconnect Fail-Closed Guard Hardening (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden Control Board v2 host-feed parsing during HA/API reconnect churn so transient `unknown`/empty updates no longer immediately clear `ma_control_hosts`/`ma_control_host` while reconnect grace or short bounded stale cache windows are active. Also downgrade noisy audio-state/duration unknown transitions to reconnect-aware informational diagnostics where appropriate, preserving fail-closed behavior for sustained invalid feed states while reducing false degradation loops. Runtime track disposition: implemented (active ESP runtime reconnect guard hardening). Custom-component track disposition: compatibility-shimmed (component path reviewed for equivalent failure-mode posture; no direct behavior mutation required). P1/P2/P3 impact: no source-of-truth ownership change; reconnect resilience + observability noise hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AK Coordinator Async-Import Hardening (`custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): replace dynamic runtime module imports in coordinator initialization (`importlib.import_module`) with static module-class imports to eliminate Home Assistant event-loop blocking import warnings and reduce startup churn risk under reload/restart. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged; equivalent startup warning posture checked). Custom-component track disposition: implemented (component coordinator startup import lane hardened). P1/P2/P3 impact: no source-of-truth ownership change; startup reliability/compliance hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AJ Snapshot Contract Regression Fix (`custom_components/spectra_ls/validation_fabric.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): fix runtime snapshot-refresh crashes caused by stale references to removed coordinator-private attributes (`_legacy_surfaces`, `_legacy_rooms_raw`) in contract validation assembly. Validation fabric now reads canonical legacy entity surfaces from shared constants instead of non-existent coordinator internals, restoring deterministic snapshot/build/update flow for state-change and deferred-refresh paths. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged; failure mode reviewed for equivalent risk). Custom-component track disposition: implemented (component crash-path fixed at validation source). P1/P2/P3 impact: no source-of-truth ownership change; regression correction and stability hardening only. README/wiki parity: no material operator workflow change.

- Validation/Slice-AI Meta Component Full-Stack Jinja Tester (`docs/testing/raw/meta_component_full_stack_tester.jinja`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a single copy/paste raw Home Assistant template that performs broad component-stack validation in one render (authority/host gate, route/contract, scheduler, metadata prep, action/capability/crossfade, control-center/readiness, write-attempt surfaces, parity/freshness rollup, and deterministic next-action guidance). Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (validation tooling expanded for full-stack operator evidence capture). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics/evidence workflow hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AH Pack D Reuse + Cache + Hardening Implementation (`custom_components/spectra_ls/payload_surface_fabric.py`, `custom_components/spectra_ls/snapshot_fabric.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/refresh_validation_fabric.py`, `custom_components/spectra_ls/authority_contract.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): complete Pack D by introducing shared payload-surface extraction helpers for repeated dict/list shape-safe reads, rewiring snapshot/service summary paths to consume shared helpers, adding bounded micro-caching in refresh-validation snapshot publish flow (short TTL, cache lifecycle safeguards), and adding bounded authority-contract packet caching keyed by snapshot capture timestamp for repeated diagnostics/sensor read reuse. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (Pack D reuse/caching/hardening landed in component internals). P1/P2/P3 impact: no source-of-truth ownership change; internal reuse/caching and brittleness-hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-AH Pack D Reuse + Cache + Hardening Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open a docs-first Pack D macro cycle to complete the next reuse/caching/hardening layer in `custom_components/spectra_ls` by introducing shared payload-surface extraction helpers for repeated dict/list shape guards, adding safe bounded micro-caching for high-frequency snapshot publish paths, and tightening defensive guardrails around cache lifecycle/state churn behavior without behavior-contract expansion. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; internal reuse/caching/hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AG Pack C Optimization + Reuse + Hardening Implementation (`custom_components/spectra_ls/utility_fabric.py`, `custom_components/spectra_ls/selection_fabric.py`, `custom_components/spectra_ls/event_recovery_fabric.py`, `custom_components/spectra_ls/snapshot_fabric.py`, `custom_components/spectra_ls/control_execution_fabric.py`, `custom_components/spectra_ls/startup_recovery_fabric.py`, `custom_components/spectra_ls/validation_fabric.py`, `custom_components/spectra_ls/refresh_validation_fabric.py`, `custom_components/spectra_ls/write_path_fabric.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): complete Pack C by adding reusable payload extraction helpers (`extract_payload_list`) for list-or-mapping JSONish contracts, converting selection candidate assembly to linear-time de-duplication with set-backed uniqueness guards, reusing shared helper-state/options normalization in control/startup/validation/event paths, and hardening callback churn with bounded global-state coalescing cache pruning and in-flight dedupe safeguards; also reduce snapshot/refresh brittleness by replacing repeated dict-shape extraction and repeated publish flows with reusable helpers. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (Pack C optimization/reuse/hardening landed across selection/event/snapshot/control/startup/validation/refresh paths). P1/P2/P3 impact: no source-of-truth ownership change; internal performance/reuse and defensive-hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-AG Pack C Optimization + Reuse + Hardening Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open a docs-first macro cycle to execute Pack C performance optimization in `custom_components/spectra_ls` (linear-time candidate de-duplication in selection planning and reduced high-frequency callback churn), expand reusable helper abstractions for frequently repeated extraction/normalization patterns, and apply a brittleness hardening pass for defensive guardrails and cache lifecycle safety without behavior-contract expansion. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; internal performance/reuse/hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AF Pack A+B Hardening Implementation (`custom_components/spectra_ls/write_path_fabric.py`, `custom_components/spectra_ls/selection_fabric.py`, `custom_components/spectra_ls/control_execution_fabric.py`, `custom_components/spectra_ls/event_recovery_fabric.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): implement shared write-path helper/guard utilities and rewire high-churn write lanes to consume them while preserving behavior contracts, including deduped helper-options parsing, common authority/reentrancy/debounce guard-chain evaluation, centralized write-attempt bookkeeping helpers, and bounded global state-change event coalescing/in-flight dedupe for event-recovery auto-select callbacks. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (Pack A+B hardening landed in component write and event-recovery lanes). P1/P2/P3 impact: no source-of-truth ownership change; internal reliability/performance and dedupe hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-AF Pack A+B Hardening Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open a docs-first macro cycle to implement Pack A+B component hardening in `custom_components/spectra_ls` by introducing shared write-path guard/dedupe helpers (helper-state packet parsing, guard-chain evaluation, and write-attempt recording utilities), then rewiring high-churn write lanes to consume the shared helpers while preserving behavior contracts; in the same slice, add bounded global state-change event coalescing in event-recovery orchestration to reduce task-storm risk during rapid HA state churn. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; internal reliability/performance hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AE Coordinator Endgame Internals Extraction (`custom_components/spectra_ls/utility_fabric.py`, `custom_components/spectra_ls/refresh_validation_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract the remaining coordinator internals into workflow boundaries by moving `_snapshot_for_entity` parsing semantics and write-authority mode normalization policy to `UtilityFabricWorkflow`, and by moving `_async_update_data` refresh dispatch to `RefreshValidationFabricWorkflow`; rewire coordinator methods to thin delegation wrappers while preserving internal helper contracts. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (endgame internals moved from inline coordinator monolith to workflow boundaries). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-AE Coordinator Endgame Internals Extraction Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open the final endgame decomposition cycle to extract the remaining coordinator internals (`_snapshot_for_entity` field parsing semantics, write-authority mode normalization policy, and `_async_update_data` refresh path dispatch) from `custom_components/spectra_ls/coordinator.py` into workflow boundaries and rewire coordinator methods to thin delegation wrappers. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this slice step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; architecture hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AD Coordinator Utility-Helper Final One-Shot Extraction (`custom_components/spectra_ls/utility_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract the remaining coordinator utility/helper lane into dedicated `UtilityFabricWorkflow` boundary methods (state normalization, JSON payload parsing, float-helper parsing, availability scoring, empirical bonus scoring, resolved-state checks, and timestamp age parsing) and rewire coordinator helper methods to thin delegation wrappers while preserving helper contracts consumed across workflow boundaries. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (remaining utility/helper lane moved from inline coordinator monolith to dedicated workflow module). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-AD Coordinator Utility-Helper Final One-Shot Extraction Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open the next super-macro decomposition cycle to extract the remaining coordinator utility/helper lane (state normalization, JSON payload parsing, float-helper parsing, availability scoring, empirical bonus scoring, resolved-state checks, and timestamp age parsing) from `custom_components/spectra_ls/coordinator.py` into a dedicated workflow boundary module and rewire coordinator helper methods to thin delegation wrappers after extraction. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this slice step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; architecture hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AutoCoordinator Coordinator Refresh+Validation Super-Chunk Extraction (`custom_components/spectra_ls/refresh_validation_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract coordinator refresh/validation service orchestration into dedicated `RefreshValidationFabricWorkflow` boundary methods (`_refresh_snapshot`, deferred refresh callback handling, `async_rebuild_registry`, `async_validate_contracts`, `async_dump_route_trace`, `async_validate_selection_handoff`, `async_validate_capability_profile`, `async_validate_action_catalog`, `async_validate_crossfade_balance`, and `async_validate_scheduler`) and rewire coordinator methods to thin delegation wrappers while preserving service and callback contracts. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (refresh/validation super-chunk lane moved from inline coordinator monolith to dedicated workflow module). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-AutoCoordinator Coordinator Refresh+Validation Super-Chunk Extraction Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open the next super-macro decomposition cycle to extract coordinator refresh/validation service orchestration (`_refresh_snapshot`, deferred refresh callback handling, `async_rebuild_registry`, `async_validate_contracts`, `async_dump_route_trace`, `async_validate_selection_handoff`, `async_validate_capability_profile`, `async_validate_action_catalog`, `async_validate_crossfade_balance`, and `async_validate_scheduler`) from `custom_components/spectra_ls/coordinator.py` into a dedicated workflow boundary module and rewire coordinator to thin delegation wrappers after extraction. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this slice step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; architecture hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AB Coordinator Lifecycle Listener Lane Extraction (`custom_components/spectra_ls/lifecycle_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract coordinator lifecycle listener orchestration into dedicated `LifecycleFabricWorkflow` boundary methods (`async_setup` and `async_shutdown`) and rewire coordinator lifecycle methods to thin delegation wrappers while preserving listener/watch-set contracts and unload cleanup semantics. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (lifecycle listener lane moved from inline coordinator monolith to dedicated workflow module). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-AB Coordinator Lifecycle Listener Lane Extraction Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open the next super-macro decomposition cycle to extract coordinator lifecycle listener orchestration (`async_setup` and `async_shutdown`) from `custom_components/spectra_ls/coordinator.py` into a dedicated workflow boundary module and rewire coordinator to thin delegation wrappers after extraction. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this slice step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; architecture hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-AA Coordinator Control-Execution Lane Extraction (`custom_components/spectra_ls/control_execution_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract coordinator control-execution service orchestration into dedicated `ControlExecutionFabricWorkflow` boundary methods (`async_apply_control_center_settings`, `async_execute_control_center_input`, `async_set_write_authority`, and `async_route_write_trial`) and rewire coordinator service methods to thin delegation wrappers while preserving service payload/status contracts and guarded write semantics. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (control-execution lane moved from inline coordinator monolith to dedicated workflow module). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-AA Coordinator Control-Execution Lane Extraction Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open the next super-macro decomposition cycle to extract coordinator control-execution service orchestration (`async_apply_control_center_settings`, `async_execute_control_center_input`, `async_set_write_authority`, and `async_route_write_trial`) from `custom_components/spectra_ls/coordinator.py` into a dedicated workflow boundary module and rewire coordinator to thin delegation wrappers after extraction. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this slice step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; architecture hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-Z Coordinator Snapshot-Lane Extraction (`custom_components/spectra_ls/snapshot_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract coordinator snapshot-assembly orchestration into dedicated `SnapshotFabricWorkflow` boundary methods (`build_snapshot` for snapshot packet assembly and `build_write_controls` for write-controls packet packaging), and rewire coordinator `_build_snapshot` / `_build_write_controls` methods to thin delegation wrappers while preserving coordinator-facing payload contracts and service behavior. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (snapshot lane moved from inline coordinator monolith to dedicated workflow module). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-Z Coordinator Snapshot-Lane Extraction Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open the next super-macro decomposition cycle to extract coordinator snapshot-assembly orchestration (`_build_snapshot` and write-controls snapshot packaging seams) from `custom_components/spectra_ls/coordinator.py` into a dedicated workflow boundary module and rewire coordinator to thin delegation wrappers after extraction. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this slice step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; architecture hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-Y Startup-Recovery Lane Extraction (`custom_components/spectra_ls/startup_recovery_fabric.py`, `custom_components/spectra_ls/meta_fabric.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract startup-recovery orchestration out of `meta_fabric.py` into dedicated `StartupRecoveryFabricWorkflow` boundary methods (`async_schedule_startup_recovery`, timer callback dispatch, startup recovery attempt engine, boot readiness evaluation, and startup wait-reason formatting) and rewire `MetaFabricWorkflow` to thin delegation wrappers while preserving coordinator callback/service contracts. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (startup-recovery lane now boundary-owned by dedicated workflow module). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-Y Startup-Recovery Lane Extraction Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open the next super-macro decomposition cycle to extract the startup-recovery orchestration lane from `custom_components/spectra_ls/meta_fabric.py` into a dedicated workflow boundary module and rewire meta-fabric to thin delegation wrappers after extraction. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this slice step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; architecture hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-X Event-Recovery Lane Extraction (`custom_components/spectra_ls/event_recovery_fabric.py`, `custom_components/spectra_ls/meta_fabric.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract event/recovery orchestration out of `meta_fabric.py` into dedicated `EventRecoveryFabricWorkflow` boundary methods (auto-select loop preflight/execution, players-change refresh, ambiguity lock, stale unlock/timer handling, no-control feedback hold/self-heal/post-heal notification lifecycle, and global/state change callback orchestration), then rewire `MetaFabricWorkflow` to thin delegation wrappers while preserving callback/service contracts. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (event/recovery lane now boundary-owned by dedicated workflow module). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-X Event-Recovery Lane Extraction Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open the next super-macro decomposition cycle to extract the event/recovery orchestration lane from `custom_components/spectra_ls/meta_fabric.py` into a dedicated workflow boundary module and rewire meta-fabric to thin delegation wrappers after extraction. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this slice step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; architecture hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-W Scaffold-Backend Lane Extraction (`custom_components/spectra_ls/scaffold_fabric.py`, `custom_components/spectra_ls/meta_fabric.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract scaffold/backend assembly out of `meta_fabric.py` into dedicated `ScaffoldFabricWorkflow` boundary methods (`build_component_scaffolds`, `build_handoff_inventory`, `build_ma_backend_profile`) and rewire `MetaFabricWorkflow` to thin delegation wrappers while preserving packet contracts. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (scaffold/backend lane now boundary-owned by dedicated workflow module). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-W Scaffold-Backend Lane Extraction Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open the next super-macro decomposition cycle to extract the scaffold/backend assembly lane from `custom_components/spectra_ls/meta_fabric.py` into a dedicated workflow boundary module and rewire meta-fabric to thin delegation wrappers after extraction. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this slice step; implementation follows in the same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; architecture hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-V Validation-Control Lane Extraction (`custom_components/spectra_ls/validation_fabric.py`, `custom_components/spectra_ls/meta_fabric.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract the remaining validation/control assembly lane from `meta_fabric.py` into a dedicated `ValidationFabricWorkflow` module (metadata validation bundle/policy surfaces, handoff status/dependency map, scheduler/contract/selection/route safety builders, host cutover gate, control-center validation, F4 capability/action/crossfade validation, and snapshot validation packet assembly), then rewire `MetaFabricWorkflow` methods to thin delegations through the new boundary while preserving existing contracts and outputs. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (validation/control lane removed from monolithic inline meta-fabric assembly and moved behind dedicated workflow boundary). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-V Validation-Control Lane Extraction Activation (`docs/CHANGELOG.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): open the next super-macro decomposition cycle with docs-first execution for extracting the remaining validation/control assembly lane from `custom_components/spectra_ls/meta_fabric.py` into a dedicated workflow boundary module and rewiring meta-fabric to thin delegation wrappers after extraction. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: compatibility-shimmed (docs-first activation only in this slice step; implementation follows in same macro cycle). P1/P2/P3 impact: no source-of-truth ownership change; architecture hardening workflow activation only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-U Selection-Fabric Lane Extraction (`custom_components/spectra_ls/selection_fabric.py`, `custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): reduce meta-fabric monolith size by extracting the large scheduler/selection/write-orchestration lane (target-options planning, scheduler ranking/choice/apply, target-options scaffold, auto-select scaffold, track/restore/cycle active target) into a dedicated selection-fabric workflow module and rewiring meta-fabric methods to thin delegation wrappers while preserving service/callback contracts. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (major orchestration lane moved out of meta-fabric into dedicated workflow boundary). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-T Meta-Fabric Event-Recovery Orchestration Extraction (`custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): complete the remaining coordinator event/recovery orchestration lane extraction by moving auto-select loop preflight/execution, players-change refresh sequencing, ambiguity lock, stale unlock/timer handling, no-control feedback self-heal/notification lifecycle, and state/global event callback orchestration into meta-fabric workflow methods; rewire coordinator callback/service surfaces to thin delegation wrappers in the same slice. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (remaining event/recovery orchestration lane now boundary-owned by meta-fabric). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-S Meta-Fabric Write-Orchestration Super-Macro (`custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): perform a super-macro extraction by moving the core component write-orchestration service lane (scheduler choice/apply, target-options scaffold, auto-select scaffold, track-last-valid, restore-last-valid, cycle-target) out of coordinator into meta-fabric workflow methods, then rewiring coordinator service methods to thin delegation wrappers in the same change set. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (major async write-orchestration service lane now boundary-owned by meta-fabric). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-R Meta-Fabric Compute-Engine Macro Extraction (`custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): execute a larger consolidation pass by moving coordinator target-options planning and scheduler-decision ranking engines into meta-fabric workflow helpers, then rewiring coordinator compute methods and downstream callers to delegate through the same boundary in one slice. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (major coordinator compute seams moved behind meta-fabric boundary). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-Q Meta-Fabric Scaffolds+Inventory+Backend Macro Rewire (`custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): execute a multi-seam macro extraction by moving component scaffold planning assembly, handoff inventory packet assembly, and MA backend-profile assembly from coordinator into meta-fabric workflow helpers, then rewiring coordinator methods to delegate through those boundaries in the same slice. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (coordinator orchestration surface narrowed across scaffolds/inventory/backend seams). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-P Meta-Fabric Snapshot Validation Packetization (`custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a bundled snapshot-validation packet helper in meta-fabric that assembles host-cutover, contract/selection/route safety, metadata validation bundle normalization, F4 capability/action/crossfade validations, scheduler validation, and control-center validation in one boundary call; rewire coordinator snapshot assembly to consume this packet and unwrap deterministic surfaces. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (snapshot validation orchestration coupling reduced through single packetized boundary). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-O Meta-Fabric Validation + Gate Macro Rewire Bundle (`custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): continue macro de-monolithing by moving host-cutover gate validation, control-center validation assembly, and F4 capability/action/crossfade validation builders from coordinator into meta-fabric workflow helpers, with same-slice coordinator delegation rewires. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (coordinator validation/gate assembly surface narrowed through boundary delegation). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-N Meta-Fabric Macro Rewire Bundle (`custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): perform a macro extraction/rewire pass by moving coordinator contract/selection/route-safety/scheduler-validation assembly logic into meta-fabric workflow helpers and rewiring coordinator methods to delegation boundaries in the same change set. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (coordinator orchestration surface narrowed with simultaneous boundary rewiring). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-M Meta-Fabric Follow-on Seam Bundle (`custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): continue coordinator de-monolithing by extracting additional metadata-adjacent assembly seams into meta-fabric helpers in a bounded multi-seam pass (write-controls metadata surface assembly, handoff metadata-status evaluation, and snapshot metadata-bundle normalization/unpack). Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (coordinator metadata assembly coupling reduced via behavior-preserving delegation). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-L Meta-Fabric Metadata-Validation Bundle Delegation (`custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract the remaining metadata-prep/bridge-facing snapshot assembly seam from coordinator into the meta-fabric boundary via a dedicated metadata validation bundle helper that centralizes metadata prep, bridge validation, and cutover-prep validation orchestration. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (metadata validation assembly coupling reduced in coordinator with behavior-preserving delegation). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-K Meta-Fabric Startup-Orchestration Extraction (`custom_components/spectra_ls/meta_fabric.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extract metadata startup-recovery boot-gate/orchestration logic from coordinator monolith into a dedicated meta-fabric workflow module while preserving public behavior and existing metadata-stack contracts. Coordinator now delegates startup bridge orchestration and MA boot-wait reason formatting through the new meta-fabric boundary, reducing coordinator coupling and making further metadata fabric decomposition safer. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (meta startup orchestration moved behind explicit component workflow boundary). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-I+J Host-Cutover Service Contract + Diagnostics Highlights (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/diagnostics.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `README.md`): add response-capable `spectra_ls.get_host_cutover_gate` service to return deterministic host-cutover gate packets (with optional fail-closed readiness/activation enforcement and optional snapshot summary envelope), and add compact host-cutover highlights to diagnostics export for one-screen operator triage. Runtime track disposition: compatibility-shimmed (legacy host helper/entity contracts remain rollback baseline authority). Custom-component track disposition: implemented (component now exposes explicit host-cutover service packet contract + diagnostics highlights for automation/triage consumption). P1/P2/P3 impact: no source-of-truth ownership change; service-level evidence/actionability and observability hardening only. Version-parity review: runtime version bump not applicable for this component-focused slice; rationale captured here. README/wiki parity: README updated for service-surface parity; no material wiki workflow change.

- Custom Component/Slice-H Host-Cutover Gate Enforcement + Binary Readiness Surface (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/binary_sensor.py`, `docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a dedicated binary diagnostic surface for host-cutover readiness and enforce host-control cutover-gate checks in scheduler-apply write attempts (fail-closed when authority is component but gate is not ready). Runtime track disposition: compatibility-shimmed (legacy host helper/entity contracts remain rollback baseline authority). Custom-component track disposition: implemented (component now exposes one-glance gate readiness and blocks unsafe component-authority scheduler writes when host cutover parity is not satisfied). P1/P2/P3 impact: no source-of-truth ownership change; write-path safety and observability hardening only. Version-parity review: runtime version bump not applicable for this component-focused slice; rationale captured here. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-G Host-Control Cutover Gate + Authoritative Host Surfaces (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/sensor.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a deterministic component-side host-control cutover gate packet (ready/blocked + fail-closed blocker reasons, route/host/path checks, legacy parity consistency checks, and activation-vs-readiness distinction), and expose component-authoritative host candidate diagnostics through dedicated sensor/state attributes for operator-visible migration readiness. Runtime track disposition: compatibility-shimmed (legacy host helpers/entities remain rollback baseline authority during migration windows). Custom-component track disposition: implemented (component now computes and publishes explicit host-control authority readiness surfaces without forcing immediate ownership flip). P1/P2/P3 impact: no source-of-truth ownership change; cutover-readiness observability and gate determinism hardening only. Version-parity review: runtime version bump not applicable for this component-focused slice; rationale captured here. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-F Service Input Coercion + Service Lifecycle Dedupe (`custom_components/spectra_ls/__init__.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden service boolean input parsing by replacing raw `bool(...)` coercion with explicit truthy/falsy normalization, and collapse duplicated service register/unregister chains into shared service-definition lifecycle plumbing so setup/unload behavior stays aligned as services evolve. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (internal safety/maintainability hardening only). P1/P2/P3 impact: no source-of-truth ownership change; input-safety and lifecycle-drift hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-E Follow-up Hardening — Coordinator↔Metadata Workflow Coupling + Sequence Runner Cleanup (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/metadata_stack.py`, `custom_components/spectra_ls/__init__.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): apply two incremental cleanup slices: (1) expose a coordinator snapshot refresh hook and route metadata workflow refreshes through that public surface (instead of direct private snapshot building calls), and (2) consolidate repeated service stage-run loops in integration setup into a shared sequence runner with unchanged fail-closed stage reporting semantics. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (internal maintainability and boundary-hardening only). P1/P2/P3 impact: no source-of-truth ownership change; internal robustness/readability hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-E Metadata Stack Final Extraction + Service Routing Cleanup (`custom_components/spectra_ls/metadata_stack.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/__init__.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): complete metadata-stack ownership transfer by moving metadata trial/resolver/bridge attempt-state ownership into `MetadataStackWorkflow`, removing remaining coordinator-owned metadata attempt state and wrapper methods, and routing metadata services through `coordinator.metadata_stack` directly. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (metadata stack extracted to dedicated workflow ownership with coordinator reduced to orchestration). P1/P2/P3 impact: no source-of-truth ownership change; component architecture and contract-boundary hardening only. Version-parity review: runtime version bump not applicable for this component-only refactor; rationale captured here. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-E Metadata Bridge Workflow Module Extraction (`custom_components/spectra_ls/workflows/metadata_bridge.py`, `custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): begin structural de-monolithing by extracting metadata authority-state, bridge validation, and cutover-prep gate computation into a dedicated workflow module while preserving existing behavior and outputs. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (component internals restructured toward plugin-core workflow boundaries with no behavior-path expansion). P1/P2/P3 impact: no source-of-truth ownership change; internal architecture hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-D Authority Validator JSON Capture + Three-Slice Hardening Bundle (`docs/testing/DEVTOOLS-TEMPLATES.local.md`, `custom_components/spectra_ls/authority_contract.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/diagnostics.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extend operator workflow with copy/paste JSON capture guidance and implement three additive authority slices: (1) packet-level `authority_contract_ready` + deterministic blocker summary fields, (2) stricter service gating option for required proof checkpoints, and (3) diagnostics/service contract exposure for the new readiness surfaces. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (authority packet/service/diagnostics usability and fail-closed readiness signaling hardened). P1/P2/P3 impact: no source-of-truth ownership change; validation/actionability hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-D Authority Validator Dual-Input Envelope Support (`docs/testing/raw/slice_d_authority_contract_validation.jinja`, `docs/testing/DEVTOOLS-TEMPLATES.local.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extend the raw authority validation template to accept both direct packet attribute payloads and service-style response envelopes (`authority_contract` + optional `snapshot_summary`) so operators can validate either entity-sourced packet data or copied service response JSON with one template flow. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (validation tooling now envelope-aware for lower-friction operator checks). P1/P2/P3 impact: no source-of-truth ownership change; operator validation ergonomics hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-D Authority Contract Three-Slice Bundle (`custom_components/spectra_ls/authority_contract.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/diagnostics.py`, `docs/testing/raw/slice_d_authority_contract_validation.jinja`, `docs/testing/DEVTOOLS-TEMPLATES.local.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): implement three additive meta-stack slices in one pass: (1) expand `get_authority_contract` packet with deterministic cutover-prep/proof summary fields and packet metadata (`schema_version`, `packet_generated_at`), (2) harden service response handling with optional strict fail-closed gating plus compact snapshot summary envelope for automation consumers, and (3) expose packet observability in diagnostics plus a copy/paste raw validation template for deterministic operator checks (and register it in the template index). Runtime track disposition: compatibility-shimmed (runtime contracts unchanged). Custom-component track disposition: implemented (authority-contract response/consumption path hardened end-to-end). P1/P2/P3 impact: no source-of-truth ownership change; meta-stack evidence/actionability hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-D Strict Endpoint Closeout Packet Hardening (`docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`, `docs/testing/raw/meta_stack_end_to_end_validation_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a strict endpoint-complete evidence packet example and extend rolling meta-stack component evidence schema with explicit endpoint blocker count + cutover checkpoint presence/assertion fields (`pre_window|in_window|post_window`, in-window `metadata_cutover_active`, in-window owner component assertion) so closeout classification is fail-closed and deterministic from a single packet. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged; docs/evidence schema hardening only). Custom-component track disposition: implemented (existing endpoint/proof contract now captured in stricter operator packet fields). P1/P2/P3 impact: no source-of-truth ownership change; closeout evidence precision hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-D Closeout Endpoint Evidence Wiring (`docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`, `docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`, `docs/testing/raw/meta_stack_end_to_end_validation_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): wire active monitor/checklist artifacts to explicit endpoint-gate evidence (`cutover_prep_validation.cutover_prep_complete`, endpoint blockers, and cutover proof checkpoint presence) so final cutover closeout packets can be classified deterministically from one run-window record. Runtime track disposition: compatibility-shimmed (runtime contracts unchanged; evidence wiring only). Custom-component track disposition: implemented (component endpoint/proof contract now consumed by closeout artifacts). P1/P2/P3 impact: no source-of-truth ownership change; closeout evidence determinism hardening only.

- Custom Component/Slice-D Metadata Bridge Cutover Proof Packet + Endpoint Gate (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/authority_contract.py`, `custom_components/spectra_ls/services.yaml`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/notes/NOTES-engineering-rigor.md`): add explicit bridge-result cutover proof fields that capture authority/cutover state in pre-window, in-window, and post-window checkpoints, and compute a deterministic endpoint gate (`cutover_prep_validation.cutover_prep_complete`) with checklist/blockers. Authority contract response now includes `cutover_prep_complete` for one-glance cutover-prep readiness. Runtime track disposition: compatibility-shimmed (legacy metadata contracts remain rollback baseline). Custom-component track disposition: implemented (bridge payload now emits deterministic pre/in/post proof + endpoint readiness gate). P1/P2/P3 impact: source-of-truth remains migration-bounded; evidence quality and cutover-audit determinism improved.

- Custom Component/Slice-D Metadata Trial Authority-Satisfied Contract (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/authority_contract.py`, `custom_components/spectra_ls/services.yaml`, `docs/notes/NOTES-engineering-rigor.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extend metadata trial/bridge contracts so authority checks are satisfied by either legacy authority baseline or active component metadata cutover (`metadata_cutover_active=true`) rather than legacy-only gating. Authority packet/service surfaces now expose metadata owner + cutover state for deterministic operator validation and plan-note parity. Runtime track disposition: compatibility-shimmed (legacy metadata write surfaces remain rollback baseline). Custom-component track disposition: implemented (authority-satisfied trial gating + packet visibility + plan parity updates). P1/P2/P3 impact: source-of-truth remains migration-bounded; component metadata cutover can now progress through explicit contract state without false legacy-only blockers. README/wiki parity: no material operator workflow change.

## 2026-05-02

- Custom Component/Slice-D Metadata Authority Rewrite Kickoff (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/const.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): begin actual component-owned metadata authority migration by replacing hardcoded legacy-only metadata ownership diagnostics with component-aware authority resolution derived from resolver scaffold readiness and write-authority mode, and by downgrading legacy-bridge-only blockers when component metadata cutover is active. Runtime track disposition: compatibility-shimmed (legacy metadata helper/entity contracts preserved as rollback baseline; no runtime write-path ownership expansion). Custom-component track disposition: implemented (metadata authority owner/cutover contract now computed in component diagnostics path). P1/P2/P3 impact: source-of-truth ownership remains migration-bounded; metadata authority diagnostics/decision path now supports component-first cutover posture instead of permanent legacy lock. README/wiki parity: no material operator workflow change.

- Docs/Slice-D Sub-slice 36 — Lean v2.7 Test-Scope Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit test-scope line (`full|targeted|minimal`) so lean reviewers can rapidly calibrate verification breadth before deep evidence reading. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; verification-breadth signaling hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 37 — Lean v2.7 Contract-Delta Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit contract-delta line (`none|minor|major`) to keep contract-impact expectations visible in compact review packets. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; contract-impact visibility hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 38 — Lean v2.7 Communication-Note Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit communication-note reference line (`N/A` if none) so operator/reviewer communication follow-through is traceable without expanding mandatory prose sections. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; communication traceability hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 33 — Lean v2.6 Validation-Confidence Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit validation-confidence line (`high|medium|low`) so reviewers can quickly calibrate confidence vs residual uncertainty in lean packets. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; validation-confidence signaling hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 34 — Lean v2.6 Rollback-Posture Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit rollback-posture line (`safe|needs-attention`) to preserve fast reversibility checks in compact lean packets. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; rollback-readiness visibility hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 35 — Lean v2.6 Handoff-Note Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit handoff-note reference line (`N/A` if none) so reviewer/operator continuity details are captured without expanding mandatory narrative sections. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; operational handoff traceability hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 30 — Lean v2.5 Change-Risk Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit change-risk line (`low|medium|high`) in the lean core packet so reviewers can triage review depth quickly without expanding checklist size. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; review-depth triage clarity hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 31 — Lean v2.5 Follow-Up Need Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit follow-up needed line (`yes|no`) to separate merge-readiness from post-merge action planning in lean packets. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; post-merge planning visibility hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 32 — Lean v2.5 Follow-Up Reference Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add follow-up tracking reference line (`N/A` if none) so declared follow-up work is durable and auditable without adding long narrative sections. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; follow-up traceability hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 27 — Lean v2.4 Core Coherence Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit core packet coherence line (`coherent|needs-fix`) so reviewers can quickly identify declaration/mode packet consistency issues before deep review. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; packet consistency signaling hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 28 — Lean v2.4 Evidence Reference Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit evidence artifact reference line (URL/path or `N/A`) to reduce ambiguity when validating supporting proof location in lean reviews. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; evidence lookup clarity hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 29 — Lean v2.4 Blocker Summary Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add one-line blocker reason summary (`N/A` when ready) to make blocked PR disposition explicit without expanding checklist overhead. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; blocker triage clarity hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 24 — Lean v2.3 Blocker Verdict Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit blocker verdict line (`ready|blocked`) to accelerate merge-readiness interpretation while preserving existing evidence checks. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; reviewer decision clarity hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 25 — Lean v2.3 Optional-Section Completion Summary Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add one-line optional-section completion summary (`A done`, `B done`, `A+B done`, `none`) to reduce reviewer scan depth for scope closure checks. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; optional-section closure visibility hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 26 — Lean v2.3 Reviewer Timestamp Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add reviewer fast-path timestamp capture (`UTC`) for compact attestation recency proof with low overhead. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; attestation recency confidence hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 21 — Lean v2.2 Core Owner Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit core packet owner field to reduce reviewer ambiguity about who is accountable for packet completeness and evidence updates. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; accountability clarity hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 22 — Lean v2.2 Scope Trigger Summary Line Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add one-line scope trigger summary (`A/B active` or `none`) so reviewers can verify optional-section expectations at a glance before reading detail blocks. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; reviewer scan-speed hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 23 — Lean v2.2 Evidence Freshness Hint Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add lightweight evidence freshness hint (`fresh|stale`) beside UTC timestamp to improve merge-time confidence while keeping lean packet overhead low. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; recency confidence signaling hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 18 — Lean v2.1 Core Packet Status Line Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a single explicit core-packet status field (`complete|incomplete`) to accelerate reviewer triage and reduce checklist scanning overhead while preserving blocker-critical requirements. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; review-speed clarity hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 19 — Lean v2.1 Compact Scope Toggles Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add compact yes/no scope-toggle capture lines for scheduler/metadata-bridge, shared-contract, and runtime-touch checks so optional sections can be dispositioned faster without ambiguity. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; scope-decision speed and consistency hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 20 — Lean v2.1 Evidence Timestamp Signal Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit evidence-capture timestamp field (`UTC`) and fast-path reviewer check for recency signaling to strengthen merge-readiness confidence with minimal overhead. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; evidence-recency clarity hardening only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 15 — Lean v2 One-Screen Core Packet Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): compress the PR template into a one-screen “must-fill first” packet with optional deep sections moved below the fold for speed while preserving declaration mismatch and exception blockers. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; lean throughput hardening). P1/P2/P3 impact: no source-of-truth ownership change; contributor cycle-time reduction only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 16 — Lean v2 Optional Blocks Default-`N/A` Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): default optional sections to explicit `N/A (not in scope)` and simplify trigger language so contributors can close irrelevant sections in seconds without governance ambiguity. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; optional-section friction reduction only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 17 — Lean v2 Reviewer Fast-Path Attestation Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): collapse reviewer sign-off language into a minimal fast-path attestation line that still enforces merge-blocking declaration mismatch and manual/CI evidence requirements. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; review-loop speedup with blocker preservation only. README/wiki parity: wiki updated; README parity: no material repo-state change.

- Docs/Slice-D Sub-slice 10 — Lean Template Trim Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): reduce PR template overhead by collapsing always-on requirements to a compact core gate and moving extended checks to explicitly optional, scope-triggered sections. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; contributor friction reduction). P1/P2/P3 impact: no source-of-truth ownership change; form-completion time reduction only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-D Sub-slice 11 — Scope Trigger Matrix Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a compact scope-trigger matrix so contributors can skip irrelevant PR sections confidently without second-guessing. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; scope clarity hardening only. README/wiki parity: wiki updated.

- Docs/Slice-D Sub-slice 12 — Quick-Fill Evidence Snippets Pack (`.github/pull_request_template.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add short copy/paste evidence snippet examples for `ci_enforced` and `manual_enforced_now` packets to cut fill-time and formatting drift. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; contributor speedup only. README/wiki parity: wiki updated.

- Docs/Slice-D Sub-slice 13 — Lean Parity Stamp Format Pack (`CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): adopt concise parity-stamp run notes for lean mode to keep roadmap updates informative but lightweight. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; ledger overhead reduction only.

- Docs/Slice-D Sub-slice 14 — Two-Person Fast-Start Pack (`CONTRIBUTING.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a 60-second two-person fast-start checklist for opening a compliant PR with the lean core packet. Runtime track disposition: compatibility-shimmed (governance/docs-only). Custom-component track disposition: compatibility-shimmed (governance/docs-only). P1/P2/P3 impact: no source-of-truth ownership change; onboarding/time-to-first-PR reduction only. README/wiki parity: wiki updated.

- Docs/Slice-D Sub-slice 6 — Lean-Team Mode Activation Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): activate an explicit lean-team operating mode for the current two-person workflow so merge gating prioritizes only high-signal fields and reduces repetitive checklist overhead. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; workflow streamlining). P1/P2/P3 impact: no source-of-truth ownership change; process throughput hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-D Sub-slice 7 — Core-Gate Minimum Packet Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): define a minimum required merge packet for lean-team mode (issue link, declaration mode, merge-blocker decision, and mode-specific evidence) while treating extended governance sections as conditional rather than always-on. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; minimum-packet efficiency hardening). P1/P2/P3 impact: no source-of-truth ownership change; review cycle-time reduction only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-D Sub-slice 8 — Docs-Parity Batching Policy Pack (`CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): allow bounded documentation batching in lean-team mode (up to four micro-slices per parity sync) while keeping changelog-first and blocker-critical artifacts immediate. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; documentation-effort smoothing). P1/P2/P3 impact: no source-of-truth ownership change; operator/dev throughput balance hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-D Sub-slice 9 — Masterwork Baseline as Non-Blocking Target Pack (`CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): reclassify masterwork baseline controls as strategic target-state guidance (non-blocking during lean-team execution) while preserving merge-blocking safety checks for declaration mismatches and exception evidence. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; strategic-vs-operational clarity hardening). P1/P2/P3 impact: no source-of-truth ownership change; execution tempo optimization only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-D Sub-slice 5 — Manual Waiver Tracking Reference Contract Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): require explicit tracking reference for `manual_enforced_now` gate exceptions so waiver ownership/expiry records are linked to a durable issue/decision artifact. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; exception traceability hardening). P1/P2/P3 impact: no source-of-truth ownership change; waiver lifecycle auditability hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-D Sub-slice 4 — Manual-to-CI Cutover Target Contract Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): require explicit `ci_enforced` target date for `manual_enforced_now` exception posture so manual governance state has a bounded cutover horizon and cannot drift indefinitely. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; cutover-horizon accountability hardening). P1/P2/P3 impact: no source-of-truth ownership change; enforcement transition planning hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-D Sub-slice 3 — CI Verdict Contract Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): require explicit CI gate verdict capture for `ci_enforced` mode (`pass|fail|not_run`) plus mandatory failure-reason evidence when verdict is `fail`, so merge-readiness does not rely on implied CI interpretation. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; CI verdict clarity hardening). P1/P2/P3 impact: no source-of-truth ownership change; CI decision traceability hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-D Sub-slice 2 — Manual Gate Waiver Expiry Contract Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add bounded waiver-expiry requirements for `manual_enforced_now` declaration-gate exceptions so merge-time manual allowances always carry owner + expiry accountability and cannot remain indefinite. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; waiver accountability hardening). P1/P2/P3 impact: no source-of-truth ownership change; temporal enforcement accountability hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-D Sub-slice 1 — CI Evidence Contract Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): operationalize declaration-gate modes by requiring explicit CI workflow/job and run evidence fields when PRs use `ci_enforced`, while preserving `N/A` fallback for manual gate mode. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; CI gate evidence traceability hardening). P1/P2/P3 impact: no source-of-truth ownership change; enforcement evidence quality hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-D Kickoff — Declaration Gate Mode Contract Pack (`.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): start Slice-D by adding explicit declaration-gate mode capture (`manual_enforced_now` vs `ci_enforced`) to PR workflow so declaration-vs-diff governance can transition cleanly from reviewer-enforced checks to future CI enforcement without ambiguity. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; enforcement-mode clarity hardening). P1/P2/P3 impact: no source-of-truth ownership change; enforcement operationalization readiness only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-C Declaration-vs-Diff Merge-Blocker Hardening Pack (`.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/pull_request_template.md`, `.github/ISSUE_TEMPLATE/config.yml`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): tighten component-only governance by explicitly marking runtime file changes as merge-blocking whenever intake declaration is `component_only_declared` and no bounded legacy exception rationale/tracking evidence is supplied. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; declaration-vs-diff gate hardening). P1/P2/P3 impact: no source-of-truth ownership change; reviewer decision speed and auditability hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-C Component-Only Declaration Enforcement Pack (`.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit runtime-touch declaration fields so issues must declare component-only intent vs bounded legacy exception expectation, and require PR-side reconciliation between declared runtime-touch posture and actual changed-file scope. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; component-only declaration gate hardening). P1/P2/P3 impact: no source-of-truth ownership change; merge-readiness scoping clarity and reviewer throughput hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-C Component-Target File Evidence Pack (`.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): require explicit component-target file planning evidence at intake and PR time (expected `custom_components/spectra_ls/*` files and runtime-touch declaration) so component-only execution intent is auditable before implementation and reviewer merge checks. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; component-target evidence hardening). P1/P2/P3 impact: no source-of-truth ownership change; execution-scoping clarity and reviewer throughput hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-C Component-First Merge-Blocker Enforcement Pack (`.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): convert component-first policy from guidance into explicit merge-blocker language by requiring bounded legacy-exception evidence fields (or `N/A`) in intake/PR workflow and by stating that missing reconciliation/exception evidence blocks merge readiness. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; component-first enforcement hardening). P1/P2/P3 impact: no source-of-truth ownership change; reviewer gate clarity and auditability hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-C Component-First Execution Gate Pack (`.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/pull_request_template.md`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): enforce component-first implementation flow at intake and PR review so new behavior/contract work defaults to `custom_components/spectra_ls`, while runtime/legacy path edits require explicit bounded-exception rationale and reviewer acknowledgment. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; policy-routing hardening for component-first execution). P1/P2/P3 impact: no source-of-truth ownership change; execution routing and legacy-touch reduction hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-C Reconciliation Evidence + Reviewer Attestation Pack (`.github/pull_request_template.md`, `.github/ISSUE_TEMPLATE/config.yml`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden issue↔PR lane governance by adding explicit reconciliation evidence fields and reviewer attestation language requiring maintainers to verify linked issue baseline lane/owner mapping before merge-readiness claims. Also add issue-template config routing note so contributors see reconciliation expectations before opening write-path issues. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; no component behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; reconciliation auditability and reviewer accountability hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-C Issue↔PR Reconciliation Governance Closure Pack (`CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): close the next governance sub-slice by propagating mandatory issue↔PR lane/owner reconciliation wording (linked issue capture, intake-vs-PR mapping confirmation, and required mismatch rationale with explicit `N/A` only when no mismatch) into contributor workflow, governance standards blueprint, and wiki contribution flow so enforcement is unambiguous outside templates. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; no component behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; governance enforcement clarity and consistency hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-C Lane Reclassification Rationale Enforcement Pack (`.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/pull_request_template.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): tighten issue intake wording and PR workflow linkage so scheduler/metadata-bridge lane/owner selections are explicitly treated as intake baseline contracts and any issue-vs-PR lane/owner divergence requires documented reclassification rationale in the PR checklist flow before merge-readiness claims. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; no component behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; issue↔PR governance consistency and enforcement hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-C Wiki Intake + Workflow Parity Pack (`docs/wiki/Welcome-README-and-Bug-Workflow.md`, `docs/wiki/Contributing-Workflow.md`, `docs/wiki/Home.md`, `docs/wiki/_Sidebar.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): propagate scheduler/metadata-bridge Slice-C integrity expectations into operator-facing wiki flow so bug intake and contributor routing pages explicitly capture lane classification, canonical owner-file routing, and PR integrity checklist linkage before implementation starts. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; no component behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; wiki/operator workflow parity hardening only. README/wiki parity: wiki updated in same change set.

- Docs/Slice-C Issue Intake Integrity Wiring Pack (`.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/ISSUE_TEMPLATE/config.yml`, `CONTRIBUTING.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extend Slice-C enforcement upstream into issue intake so scheduler/metadata-bridge lane classification and canonical-owner routing are captured before implementation planning, then mirror the same governance language in contributor and blueprint quality standards. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; no component behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; intake-stage enforcement coverage hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-C Governance Propagation Pack (`CONTRIBUTING.md`, `.github/copilot-instructions.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): bundle the next Slice C step by propagating scheduler/metadata-bridge write-path integrity checks from reference notes into contributor intake guidance, workspace execution instructions, and governance blueprint quality standards so enforcement is consistently visible before code, during coding, and at PR review. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; no component behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; governance-consistency and enforcement coverage hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-C PR Workflow Wiring (`.github/pull_request_template.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): continue Slice C by wiring scheduler/metadata-bridge ownership enforcement into the PR checklist so lane classification, owner routing, parity-anchor disposition, and no-go verification are captured during review (not only in reference docs). Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; no component behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; workflow enforcement hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-C Enforcement Micro-Checklist Activation (`docs/notes/NOTES-engineering-rigor.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extend Slice C from owner-matrix publication into execution enforcement by adding a compact scheduler/metadata-bridge preflight + PR checklist block that requires owner-file routing confirmation, paired-review confirmation, and explicit two-track disposition before merge. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; no component behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; enforcement clarity and anti-mispatch execution discipline hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-C Owner Quick-Reference Matrix Kickoff (`docs/notes/NOTES-engineering-rigor.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): start Slice C by publishing a compact scheduler/metadata-bridge owner quick-reference matrix (`edit here` / `do not fork here`) so operators and implementers can route write-path changes to canonical files quickly during active slices. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; no component behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; execution-speed and anti-mispatch clarity hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-B Service Write-Path Ownership Integrity Map (`docs/notes/NOTES-engineering-rigor.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extend the Slice-A fabric ownership discipline with a compact lane-specific service/write-path integrity map for scheduler and metadata bridge execution paths, including canonical owner files, allowed mutation boundaries, anti-mispatch preflight checks, and no-go divergence rules for behavior-visible scheduler/bridge edits. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; no component behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; write-path boundary integrity and execution-discipline hardening only. README/wiki parity: no material operator workflow change.

- Docs/Slice-A Fabric File Ownership Integrity Capture (`docs/notes/NOTES-engineering-rigor.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): formalize an explicit data-fabric ownership map and anti-mispatch guardrails so authority-contract changes are routed to canonical owner files first (builder/authority/coordinator/diagnostics/projection/service/docs) with deterministic pre-edit validation checks and divergence handling rules. Runtime track disposition: compatibility-shimmed (governance/docs-only; no runtime behavior-path mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only; no component behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; execution-discipline and patch-boundary integrity hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Slice-A Authority Contract Hardening (`custom_components/spectra_ls/authority_contract.py`, `custom_components/spectra_ls/diagnostics.py`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/services.yaml`): remove authority-packet helper duplication by centralizing normalization in a shared module and wiring all diagnostics/shadow consumers to the same builder, then add an explicit service-layer authority echo contract (`spectra_ls.get_authority_contract`) with response-capable payload docs so authority mode/gates/reasons can be retrieved directly from service calls in addition to entity diagnostics. Runtime track disposition: compatibility-shimmed (no runtime behavior-path mutation). Custom-component track disposition: implemented (authority-contract dedup + service echo). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics/service contract consistency hardening only. README/wiki parity: no material operator workflow change.

- Planning/Scope Discipline — File-Level Authority-Contract Ledger (`docs/notes/NOTES-engineering-rigor.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add an explicit file-scoped execution ledger for MA authority-contract propagation so implementation naming/file touchpoints are tracked deterministically as `completed` vs `remaining` with owner files and risk notes, reducing miss risk during rapid slice execution. Runtime track disposition: compatibility-shimmed (planning/docs-only; no runtime behavior mutation). Custom-component track disposition: compatibility-shimmed (planning/docs-only; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership change; execution-discipline and scoping clarity hardening only. README/wiki parity: no material operator workflow change.

- Custom Component/Authority Contract Packet Propagation to Diagnostics + Shadow Surfaces (`custom_components/spectra_ls/diagnostics.py`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`): propagate the tokenized MA authority contract payload end-to-end by exposing a normalized `authority_contract` packet (mode, gate results, and tokenized degraded reasons) on config-entry diagnostics export and shadow/control-center sensor attributes. This keeps Control Center and diagnostics surfaces aligned to MA Authority Contract v1 terminology (`ma_primary`, `ma_degraded_fallback`, `ma_degraded_fallback_active`, `ma_payload_stale`, `ma_payload_shape_invalid`, `ma_api_unreachable`) without changing control ownership/write behavior. Runtime track disposition: compatibility-shimmed (no runtime behavior-path mutation). Custom-component track disposition: implemented (diagnostics/observability contract propagation). P1/P2/P3 impact: no source-of-truth ownership change; authority diagnostics visibility parity hardening only. README/wiki parity: no material operator workflow change.

- Docs/MA Authority Token-Lock Wording Parity (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `README.md`): normalize governance wording to match architecture MA Authority Contract v1 tokens exactly by stating `health.authority_mode = ma_primary | ma_degraded_fallback` and degraded reason tokens `ma_degraded_fallback_active`, `ma_payload_stale`, `ma_payload_shape_invalid`, `ma_api_unreachable` consistently across roadmap + README surfaces. Runtime track disposition: compatibility-shimmed (documentation-only; no runtime behavior mutation). Custom-component track disposition: compatibility-shimmed (documentation-only; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership change; terminology contract consistency hardening only. README/wiki parity: README updated in same change set; wiki parity no material operator workflow change.

- Component/Fabric Authority Token Baseline + Metadata-Prep Authority Gate Surface (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/coordinator.py`): add canonical MA authority contract tokens in component constants (`ma_primary`, `ma_degraded_fallback`, and required degraded reason tokens), and expose normalized authority-mode + authority-gate diagnostics in metadata-prep validation payloads so component diagnostics align to the canonical data-fabric governance vocabulary. Runtime track disposition: compatibility-shimmed (no runtime behavior-path ownership mutation). Custom-component track disposition: implemented (diagnostics contract alignment to MA Authority Contract v1). P1/P2/P3 impact: no source-of-truth ownership change; component diagnostics vocabulary and gate visibility hardening only. README/wiki parity: no material operator workflow change.

- Architecture/MA-First Governance Contract Publication (`docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`, `README.md`): insert a normative architecture-governance section directly after core design goals and before canonical model, formalizing plugin-core boundaries and MA-first authority rules (authority tiers, MA health gates, per-field fallback/recovery semantics, do-not-reimplement constraints, and required diagnostics surfaces). Runtime track disposition: compatibility-shimmed (no runtime ownership/path mutation in this slice). Custom-component track disposition: implemented (architecture/system-design governance contract hardened for upcoming CDF implementation slices). P1/P2/P3 impact: no source-of-truth ownership change; architecture-governance clarity and anti-drift hardening only. README/wiki parity: README updated with MA-first governance note; wiki parity no material operator workflow change.

- Plan Delta/Component-First Canonical Playback Data Fabric Architecture (`docs/architecture/COMPONENT-DATA-FABRIC-ARCHITECTURE.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`, `README.md`): formalize the next-step system direction to replace patch-style inline legacy progress resolution with a component-owned canonical playback contract architecture (field-level multi-source ingestion, confidence-based resolver, explicit provenance, deterministic healing, compatibility projection bridge, and cutover gates). This records the design contract for robust “never collapse on single-source gaps” playback/progress handling before implementation slices begin. Runtime track disposition: compatibility-shimmed (runtime ownership unchanged in this architecture/spec slice). Custom-component track disposition: implemented (architecture/system-design target published; execution slices queued). P1/P2/P3 impact: no source-of-truth ownership change in this slice; structured cutover path and anti-collapse design hardening only. README/wiki parity: README updated; wiki parity no material operator workflow change.

- Runtime+Component/Progress Contract Root-Cause Hardening (`packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/const.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): fix the persistent “playing with position but duration=0” progression fault by moving duration/position resolution to MA-authoritative multi-source augmentation semantics rather than single brittle entity-id matching. Runtime now evaluates an expanded candidate chain (active target/meta/now-playing/player-id surfaces + MA players payload + MA-managed peer entity evidence) for canonical duration/position extraction before fail-closed `0`. Component metadata-prep diagnostics now explicitly surface progress-contract readiness and blockers for active playback (`playing_with_missing_duration_contract`) so cutover validation can fail for root-cause contract drift instead of silently passing stale suppression-only checks. Runtime track disposition: implemented (contract-surface progress provenance hardening). Custom-component track disposition: implemented (metadata-prep diagnostics parity for progress-contract failure modes). P1/P2/P3 impact: no source-of-truth ownership change; systemic progress-contract correctness hardening only. README/wiki parity: no material repo-state change.

- Runtime/MA Jinja Duration Fallback Hardening for Progress Continuity (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): fix root cause where both `sensor.now_playing_duration` and `sensor.ma_active_duration` could remain `0` during valid playback/title/position updates when HA entity attributes did not expose `media_duration` but MA player payloads still carried duration metadata. Template duration resolution now includes MA player-fields fallback (`current_media.duration` / metadata duration) for both `ma_active_*` and `now_playing_*` duration contracts before fail-closed `0`, restoring downstream progress availability without ESP-side heuristic hacks. Runtime track disposition: implemented (contract-surface duration fallback hardening). Custom-component track disposition: compatibility-shimmed (component parity path checked; ownership remains legacy contract surfaces for these duration entities). P1/P2/P3 impact: no source-of-truth ownership change; runtime contract resilience hardening only. README/wiki parity: no material repo-state change.

- ESP Runtime/Now-Playing Progress Handoff Stability + Dynamic HTTP Scheme-Memory Hardening (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): fix track-progress bar dropouts during metadata source handoff by extending progress cache continuity under active playback when HA metadata becomes available before stable duration/position clocks, so title/meta transitions do not prematurely blank progress rendering. In the same slice, harden Arylic HTTP scheme chooser behavior with dynamic host capability memory (learn from successful host+scheme sessions and avoid unnecessary opposite-scheme flips for hosts with a known-good scheme), reducing `https↔http` churn without hardcoded host/IP pinning. Runtime track disposition: implemented (ESP progress continuity + adaptive scheme chooser hardening). Custom-component track disposition: compatibility-shimmed (component remains metadata/control contract authority; no component behavior-path mutation required for ESP transport/render internals). P1/P2/P3 impact: no source-of-truth ownership change; runtime resilience and observability continuity hardening only. README/wiki parity: no material repo-state change.

- ESP Runtime/OLED Hitch Diagnostics Logging Expansion (`esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`): add targeted Arylic HTTP diagnostics instrumentation to expose poll skip reasons (`inflight`, `paused/backoff`), request elapsed timing, response status/elapsed snapshots, and backoff-next timing so OLED hitch/freeze windows can be correlated to poll-loop stalls without broad log flooding. Diagnostics are gateable via substitutions (`arylic_http_diag_log_enabled`, `arylic_http_diag_log_interval_ms`) and do not change routing or display policy behavior. Runtime track disposition: implemented (observability-only diagnostics expansion). Custom-component track disposition: checked/not-applicable (component path does not own ESP runtime poll loop logging). P1/P2/P3 impact: no source-of-truth ownership change; stall-correlation observability hardening only. README/wiki parity: no material repo-state change.

- ESP Runtime/OLED Metadata Lag Fallback Hardening (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): fix transient `audio|oled:-` windows during active music playback where HA media policy is already `class=music display=on` but `ha_audio_title` has not yet populated while transport metadata is available. `ESP OLED Status` now applies a bounded fallback to local Arylic title cache (`arylic_title_state`) only when display policy allows title rendering and HA title is empty, preventing temporary no-meta blanks without restoring legacy non-music preview-timer behavior. Runtime track disposition: implemented (ESP render fallback hardening). Custom-component track disposition: compatibility-shimmed (component authority model unchanged; OLED renderer remains ESP runtime concern, so cutover is not required for this issue). P1/P2/P3 impact: no source-of-truth ownership change; render continuity hardening only. README/wiki parity: no material repo-state change.

- Runtime/Sticky Control-Host Divergence Guard (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): fix post-classification churn where `sensor.ma_control_host` could remain latched to a stale prior host while `sensor.ma_control_hosts` resolved a newer host, producing transient host split (`control_host` ≠ first host in `control_hosts`) and extra ambiguity churn. `ma_control_host` sticky hold now only applies when aggregate host feed is unresolved; when `ma_control_hosts` is resolved, `ma_control_host` follows the current aggregate-first host deterministically. Runtime track disposition: implemented (sticky-host convergence hardening). Custom-component track disposition: compatibility-shimmed (component consumes stabilized runtime host contract surfaces; no component behavior-path mutation in this slice). P1/P2/P3 impact: no source-of-truth ownership change; host-contract convergence and churn reduction only. README/wiki parity: no material repo-state change.

- Runtime/OLED Blank Fix After Timeout Rollback — YouTube Music-vs-Video Classification Refinement (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): fix false `non_music` classification for YouTube app playback carrying music-like metadata context (for example mix/remix/DJ/radio/set-style titles) after the non-music preview-timeout rollback. Runtime now weights YouTube music cues without restoring timer-based non-music display behavior, so `binary_sensor.now_playing_display_allowed` can correctly open when content is actually music while retaining the rollback policy for genuine non-music/video contexts. Runtime track disposition: implemented (classification refinement only). Custom-component track disposition: compatibility-shimmed (component consumes runtime contract surfaces; no component behavior-path mutation required in this slice). P1/P2/P3 impact: no source-of-truth ownership change; media-class correctness hardening only. README/wiki parity: no material repo-state change.

- Runtime+Component/OLED Non-Music Preview Timeout Rollback (`packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/meta_oled_control_host_flapping_diagnostic.jinja`, `docs/testing/raw/meta_stack_step3_full_validation.jinja`, `docs/testing/raw/meta_stack_scenario_matrix_report.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): remove the bounded TV/movie/non-music preview-timer policy from OLED display gating. `binary_sensor.now_playing_display_allowed` now remains strictly music-only (`music=true`; `non_music/none=false`) and component metadata-policy validation now mirrors the same contract (no preview-window timing dependency). Diagnostics templates are updated to match the reverted contract so policy/render mismatch checks stay deterministic. Runtime track disposition: implemented (preview timeout behavior removed from active runtime template path). Custom-component track disposition: implemented (metadata-prep contract validation aligned to new music-only display gate). P1/P2/P3 impact: no source-of-truth ownership change; policy simplification/rollback for stability. README/wiki parity: no material repo-state change.

- Runtime Sticky Capture Policy for Meta Source + Control Host (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): add hold-and-release semantics so captured `sensor.ma_active_meta_entity` and `sensor.ma_control_host` remain stable during active playback windows and are only released/re-resolved when freshness gates fail (`input_number.ma_meta_stale_s`) or paused hold exceeds timeout (`input_number.ma_meta_paused_hide_s`). This reduces needless continuous churn during steady playback while preserving deterministic stale/paused timeout recovery behavior. Runtime track disposition: implemented (sticky capture + release gates in active template path). Custom-component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior-path mutation in this slice; component remains contract-consumer of runtime surfaces). P1/P2/P3 impact: no source-of-truth ownership change; runtime resolver/host stability hardening only. README/wiki parity: no material repo-state change.

- RP2040/Unified POT Damping + Deep Flapping Diagnostics (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`, `docs/testing/raw/meta_oled_control_host_flapping_diagnostic.jinja`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`): apply a single shared RP damping profile across all named pot channels in the unified pot pipeline (bass/mid/treble/volume) by harmonizing slow-EMA response, min-change gates, and send cadence to reduce twitchy micro-movements without splitting volume-only behavior. In the same slice, add a diagnostics-first deep template for control-host/meta/OLED flapping triage (policy-vs-ESP coherence, route/host mismatch, recency hotspots, and deterministic next actions) prior to meta/OLED/control-host behavior changes. Runtime track disposition: implemented (RP damping profile unification + diagnostics tooling). Custom-component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation in this slice; diagnostics consumes existing shared contract surfaces). P1/P2/P3 impact: no source-of-truth ownership change; tactile input stability and triage observability hardening only. README/wiki parity: no material repo-state change.

## 2026-05-01

- Runtime+WiiM/Warning-Churn Hardening (`packages/ma_control_hub/automation.inc`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `custom_components/wiim/coordinator.py`): reduce recurring operational warning noise across startup orchestration, ESP Arylic HTTP polling, and WiiM polling error paths without changing core control ownership contracts. Runtime startup target-options automation now uses overlap-safe restart behavior to prevent repeated `Already running` warnings during startup/event bursts. ESP Arylic HTTP polling now keeps failure counters across scheme fallback attempts so backoff can grow instead of resetting to 1s retry loops, reducing repeated `getPlayerStatus` error churn and long-interval pressure during unreachable-host windows. WiiM coordinator now classifies common transport-unreachable failures (`request failed after ...`, protocol-attempt exhaustion, timeout/connectivity style errors) as expected-unreachable and logs them at debug level while continuing cached-state fallback behavior. Runtime track disposition: implemented (startup overlap + HTTP backoff/fallback warning hardening). Custom-component track disposition: compatibility-shimmed (no `custom_components/spectra_ls` behavior mutation in this slice); external WiiM integration track: implemented (expected-unreachable warning demotion). P1/P2/P3 impact: no source-of-truth ownership change; resilience/log-signal hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic Scheduler Template Legacy-Fallback Wording Alignment (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): align control-host blocker and deterministic next-action wording with new dual-track selection semantics so legacy-authority helper-vs-scheduler divergence is treated as expected/healthy when helper current target is not control-host eligible (non-control-capable or host-unresolved), rather than always classified as generic legacy target mismatch. Runtime track disposition: compatibility-shimmed (diagnostics/template-only wording alignment). Custom-component track disposition: compatibility-shimmed (diagnostics/template-only wording alignment to implemented scheduler fallback semantics). P1/P2/P3 impact: no source-of-truth ownership change; validation guidance correctness hardening only. README/wiki parity: no material repo-state change.

- Runtime+Component/Control-Host-Safe Targeting Parity Completion (`packages/ma_control_hub/script.inc`, `custom_components/spectra_ls/coordinator.py`): complete dual-track behavior alignment so both legacy runtime auto-selection and component scheduler-selection avoid locking onto non-control-capable/unresolved-host targets. Legacy `ma_auto_select` now keeps the current target only when it is control-host eligible, and falls back to detected/active/options candidates that satisfy control-host eligibility before fail-closed stop behavior; component legacy-authority pin now only pins helper current when control-capable + host-resolved, otherwise falls back to highest-ranked control-host-safe candidate (or explicit no-candidate). Runtime track disposition: implemented (legacy auto-select control-host eligibility + fallback hardening). Custom-component track disposition: implemented (legacy-authority pin safety + fallback hardening). P1/P2/P3 impact: no source-of-truth ownership change; selection/handoff correctness and no-host avoidance hardening only. Version-parity review: both tracks updated in same slice; no additional shim/defer required. README/wiki parity: no material repo-state change.

- Custom Component/Legacy Authority Control-Host Pin Safety Fix (`custom_components/spectra_ls/coordinator.py`): harden legacy-authority scheduler pin behavior so helper-current target is only pinned when it is registry control-capable with a resolved host; otherwise the scheduler now falls back to the highest-ranked control-capable host candidate (or explicit no-candidate) instead of forcing a non-control-capable/no-host pin that can propagate `no_host` handoff posture. Runtime track disposition: compatibility-shimmed (legacy runtime helper/entity contracts remain authoritative and unchanged). Custom-component track disposition: implemented (legacy-authority pin guard + control-host-safe fallback selection in component scheduler decision path). P1/P2/P3 impact: no source-of-truth ownership change; selection/handoff correctness hardening only. Version-parity review: runtime version bump not applicable for this component-only behavior fix; rationale recorded here per two-track policy. README/wiki parity: no material repo-state change.

- Docs/Reader-Journey Hardening Pass (`README.md`, `docs/wiki/Home.md`, `docs/wiki/Getting-Started.md`, `docs/wiki/Welcome-README-and-Bug-Workflow.md`, `docs/wiki/Install-on-Your-Own-HA.md`, `docs/wiki/Operations-Runbooks.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): improve end-user and contributor navigation by adding explicit “choose your path” journey routing, first-check triage guidance before bug filing, clearer entry/exit checkpoints for install and troubleshooting flows, and symptom-to-runbook mapping so operators can reach the right page in one hop. Runtime track disposition: compatibility-shimmed (documentation-only; no runtime behavior mutation). Custom-component track disposition: compatibility-shimmed (documentation-only; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership change; documentation usability and workflow clarity hardening only. README/wiki parity: completed in same change set.

## 2026-04-29

- Custom Component/Version-Metadata Housekeeping + Deploy-Readiness Validation (`custom_components/spectra_ls/manifest.json`, `custom_components/spectra_ls/const.py`): resolve integration version drift by synchronizing Home Assistant manifest-visible component version metadata with the current component constants version so operator-visible update state is coherent in Devices & Services/HACS-facing metadata surfaces. Runtime track disposition: compatibility-shimmed (runtime path unchanged). Custom-component track disposition: implemented (metadata/version synchronization only; no behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; release metadata correctness/readiness hardening only. README/wiki parity: no material repo-state change.

- ESPHome/HA-Meta Single-Contract Coherency Switch (`esphome/spectra_ls_system/substitutions.yaml`, `packages/ma_control_hub/template.inc`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`, `custom_components/spectra_ls/const.py`): switch ESP `ha_meta`-consumed state/source/app/title feeds from `sensor.ma_active_*` to coherent now-playing contracts (`sensor.now_playing_state`, `sensor.now_playing_source`, `sensor.now_playing_app`, `sensor.now_playing_title`) so the exported HA metadata snapshot is sourced from one now-playing contract family and no longer mixes MA-active fallback context with now-playing fields. This enforces consistent cross-field provenance in both HA and ESP observability surfaces. Runtime track disposition: implemented (ESP input-contract coherency hardening + now-playing app contract surface). Custom-component track disposition: compatibility-shimmed with explicit version-metadata review completed (`custom_components/spectra_ls/const.py` metadata bump only; no behavior-path mutation). P1/P2/P3 impact: no source-of-truth ownership change; contract-provenance coherence hardening only. README/wiki parity: no material repo-state change.

- Runtime/HA Metastack Title-Source Coherency Fix (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): remove `sensor.now_playing_entity` `resolved_title` fallback to `sensor.ma_active_title` so now-playing title resolution remains entity-local (resolved player fields, stream metadata, then current now-playing entity `media_title`) and does not pull cross-source stale MA-active titles into non-display/non-active contexts. This addresses the upstream mixed-source coherence gap (for example `class=none display=off` with unrelated carried-over title) at the HA contract layer. Runtime track disposition: implemented (HA template source-coherency hardening). Custom-component track disposition: compatibility-shimmed (component consumes runtime now-playing contract; no component write-path mutation needed in this slice). P1/P2/P3 impact: no source-of-truth ownership change; metadata source-coherency hardening only. README/wiki parity: no material repo-state change.

- ESPHome/HA-Meta Policy-Coherent Title Suppression (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): harden the 30s `ha_meta` diagnostics line to fail closed for `title` when HA policy says display is not allowed (`display_allowed=false`) or when media class is non-display (`none/unknown/unavailable`). This removes misleading stale MA carryover titles in non-music/hidden-display contexts (for example `class=none display=off` with unrelated prior MA title) while preserving source/app/state observability. Runtime track disposition: implemented (policy-coherent diagnostics hardening). Custom-component track disposition: checked/not-applicable (component path does not emit ESP runtime log lines). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics semantic correctness hardening only. README/wiki parity: no material repo-state change.

- ESPHome/HA-Meta Contract Periodic Logging (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): add a bounded 30s ESP-side HA-meta summary log line sourced from HA-authoritative metadata/display contracts (`ha_audio_media_class`, `ha_audio_display_allowed`, `ha_audio_state`, `ha_audio_source`, `ha_audio_app`, `ha_audio_title`) so operator logs can distinguish policy-authoritative HA metadata from transport-native Arylic status telemetry. Runtime track disposition: implemented (observability clarity hardening). Custom-component track disposition: checked/not-applicable (component path does not own ESP log emission cadence). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics provenance clarity hardening only. README/wiki parity: no material repo-state change.

- ESPHome/Telemetry Log-Cadence Noise Reduction + 30s CPU Periodic Stats (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-diagnostics.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): reduce high-frequency log churn by throttling high-chatter status text sensors (`ESP Control Handoff Status`, `ESP Control Target`, `ESP OLED Status`) from 1s publish cadence to bounded periodic cadence aligned with operator observability, while preserving existing 30s Arylic transport/status summary logs as primary long-window heartbeat evidence. Also standardize CPU periodic log cadence at 30s for consistent low-noise runtime health sampling. Runtime track disposition: implemented (ESP runtime observability cadence hardening). Custom-component track disposition: checked/not-applicable (component path does not control ESP log emit intervals). P1/P2/P3 impact: no source-of-truth ownership change; runtime logging/diagnostics signal-quality hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Now-Playing Signal Recency Fallback Parity Hardening (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/sensor.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): align component-side metadata freshness diagnostics with runtime stale-guard semantics by falling back to entity `last_changed` recency when `media_position_updated_at` is missing (instead of treating missing progress clocks as implicitly fresh). This prevents stale/low-telemetry `playing` or `paused` entities from being misclassified as fresh in component validation payloads and keeps suppression-reason signaling deterministic across legacy/runtime and component diagnostics tracks. Also expose recency age source (`media_position_updated_at` vs `last_changed`) in shadow diagnostics attributes for transparent edge-case triage. Runtime track disposition: compatibility-shimmed (runtime behavior already hardened in prior slice; no additional runtime mutation). Custom-component track disposition: implemented (coordinator now-playing freshness recency fallback hardening + diagnostics exposure). P1/P2/P3 impact: no source-of-truth ownership change; parity diagnostics correctness hardening only. README/wiki parity: no material repo-state change.

- Runtime/TV Preview Fallback Classification + Preview-Key Fallback (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): when `sensor.now_playing_entity` is unavailable but active playback context still indicates app-local video (`youtube`/OTT/AppleTV/HDMI hints), `sensor.now_playing_media_class` now classifies as `non_music` (instead of hard `none`) and `sensor.now_playing_preview_key` emits a fallback key from source/app context. This enables bounded 30s non-music preview behavior for TV-local playback surfaces that lack full MA entity metadata while preserving fail-closed `none` classification for idle/non-active contexts. Runtime track disposition: implemented (active runtime media-policy fallback hardening). Custom-component track disposition: compatibility-shimmed (component consumes runtime surfaces; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership change; display-policy correctness and operator UX hardening only. README/wiki parity: no material repo-state change.

- Runtime/Music-Guard Stale-Activity Fix + Scenario Matrix Primary/Secondary Output (`packages/ma_control_hub/template.inc`, `docs/testing/raw/meta_stack_scenario_matrix_report.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): fix false `music_guard_active=true` hold conditions that could block non-music TV preview windows by replacing “`media_position_updated_at` missing means active forever” behavior with bounded recency fallback (`last_changed`) in music-guard candidate checks. This preserves guard semantics for genuinely active music while avoiding indefinite preview suppression from stale/low-telemetry paused entities. Also add primary/secondary active-scenario summary output in the focused matrix template for cleaner operator interpretation when multiple corroborating scenarios are simultaneously true. Runtime track disposition: implemented (guard recency correctness hardening). Custom-component track disposition: compatibility-shimmed (no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership change; display-gate correctness + diagnostics clarity hardening only. README/wiki parity: no material repo-state change.

- Validation/Meta Stack Scenario Matrix YouTube/OTT Context Classification (`docs/testing/raw/meta_stack_scenario_matrix_report.jinja`): expand video-context hint detection to include common app-local OTT tokens (`youtube`, `netflix`, `hulu`, `prime video`, `disney+`, `max`, `paramount+`, `peacock`) so active TV-speaker playback without MA-tracked audio entities is classified as `hdmi_local_untracked_playback` instead of idle baseline when source context reflects app-local video playback. Runtime track disposition: compatibility-shimmed (diagnostics/template-only). Custom-component track disposition: compatibility-shimmed (diagnostics/template-only). P1/P2/P3 impact: no source-of-truth ownership change; validation classification correctness hardening only. README/wiki parity: no material repo-state change.

- Validation/Meta Stack Scenario Matrix HDMI-Local Classification (`docs/testing/raw/meta_stack_scenario_matrix_report.jinja`): add explicit focused scenario detection for active AppleTV/HDMI/video context where playback is occurring on TV-local speakers outside MA/control-host tracked paths (`media_class=none`, `display_allowed=false`, no active now-playing entity/title). This prevents ambiguous “where is it?” outputs and marks the condition as a classified expected-untracked path rather than an untyped inactive matrix state. Runtime track disposition: compatibility-shimmed (diagnostics/template-only). Custom-component track disposition: compatibility-shimmed (diagnostics/template-only). P1/P2/P3 impact: no source-of-truth ownership change; validation classification clarity hardening only. README/wiki parity: no material repo-state change.

- Validation/Meta Stack Scenario Matrix Idle-Baseline Classification Fix (`docs/testing/raw/meta_stack_scenario_matrix_report.jinja`): treat `media_class=none` + `display_allowed=false` as an explicit focused scenario (`idle_none_baseline`) instead of falling through to “0 active scenarios/WARN”. This removes false caution outputs for valid no-content idle states and keeps focused matrix verdicts aligned with runtime contract semantics. Runtime track disposition: compatibility-shimmed (diagnostics/template-only). Custom-component track disposition: compatibility-shimmed (diagnostics/template-only). P1/P2/P3 impact: no source-of-truth ownership change; validation signal-quality hardening only. README/wiki parity: no material repo-state change.

- Validation/Meta Stack Focused Scenario Matrix Report Template (`docs/testing/raw/meta_stack_scenario_matrix_report.jinja`, `docs/testing/DEVTOOLS-TEMPLATES.local.md`): add a one-screen PASS/WARN diagnostics template for focused Step-3 parity scenarios across four high-risk states: music-only gate, non-music preview (fresh vs stale), music-guard-active suppression, and no-audio/video expected-unknown context. Template emits active/inactive scenario rows with expected-vs-observed gate semantics and a compact overall verdict for rapid post-reboot/operator evidence capture. Runtime track disposition: compatibility-shimmed (diagnostics/template-only, no runtime contract mutation). Custom-component track disposition: compatibility-shimmed (diagnostics/template-only, no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership change; validation observability hardening only. README/wiki parity: no material repo-state change.

- ESPHome/ESP OLED Status Protobuf Decode Hardening (`esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/architecture/CODEBASE-RUNTIME-ARCHITECTURE.md`): fix reboot-time `TextSensorStateResponse` decode failures caused by byte-length truncation of UTF-8 now-playing titles in `sensor.spectra_ls_system_esp_oled_status` (`line.substr(0, 32)` could cut through multibyte glyphs, producing invalid protobuf string payloads). `ESP OLED Status` now sanitizes to transport-safe printable ASCII before bounded truncation so payloads are always valid UTF-8-safe telemetry strings. Runtime track disposition: implemented (active ESP text-sensor payload safety hardening). Custom-component track disposition: checked/not-applicable (component path does not publish ESPHome protobuf text payloads). P1/P2/P3 impact: no source-of-truth ownership change; runtime telemetry transport correctness hardening only. README/wiki parity: no material repo-state change.

## 2026-04-28

- Runtime+Component/Meta Stack Full-Parity Validation Hardening (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/sensor.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): complete the next synchronization pass so component diagnostics explicitly validate and surface the same HA media display contract now enforced in runtime templates (`sensor.now_playing_media_class`, `sensor.now_playing_preview_key`, `binary_sensor.now_playing_display_allowed`, `music_guard_active`, preview-window timing). Metadata-prep diagnostics now include contract-surface presence/readiness, computed expected display-allow semantics, and drift flags when runtime contract output mismatches expected policy state. Runtime track disposition: implemented (contract surfaces already authoritative in `packages/ma_control_hub/template.inc`; no behavior-path change this slice). Custom-component track disposition: implemented (parity diagnostics and policy telemetry expanded to match runtime contract semantics). P1/P2/P3 impact: no source-of-truth ownership change; cross-track validation and observability hardening only. README/wiki parity: no material repo-state change.

- Runtime/Now-Playing Music-Lite TV Preview Policy Hardening (`packages/ma_control_hub/template.inc`, `docs/testing/raw/meta_stack_step3_full_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): implement explicit “Music Lite” behavior for non-music (TV/AppleTV/YouTube-class) metadata surfaces: non-music content is previewed for a bounded 30-second window and re-opens per fresh preview key/title/source changes, while active music context (playing or paused) remains first-class and always display-eligible. Add an explicit music-guard gate to `binary_sensor.now_playing_display_allowed` so TV preview is allowed only when no other active music context is present. Runtime track disposition: implemented (display-policy contract update in active template path). Custom-component track disposition: compatibility-shimmed (component diagnostics lane unchanged; consumes runtime display-contract outputs). P1/P2/P3 impact: no source-of-truth ownership change; display-policy semantics hardening only. README/wiki parity: no material repo-state change.

- Runtime+Component/End-of-Track Stale-Playing Guard Hardening (`packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/meta_stack_step3_full_validation.jinja`): fix the false-green metadata condition where upstream playback could report `playing` while pinned at track end after reboot/reconnect, causing stale OLED metadata to appear fresh. Add explicit end-of-track stuck detection to freshness gating (playing at/near duration end with stale position clock) and surface the condition in metadata diagnostics so Step-3 no longer reports clean PASS semantics for this stale-playing edge case. Runtime track disposition: implemented (meta candidate/selection freshness gate tightened for end-of-track stuck playback). Custom-component track disposition: implemented (metadata prep signal now marks end-of-track stuck playing as non-fresh with explicit check visibility). P1/P2/P3 impact: no source-of-truth ownership change; stale-state classification correctness hardening only. README/wiki parity: no material repo-state change.

- Quality/Hardening Pass — Registry Host-Type/Control-Path Capability Guard (`custom_components/spectra_ls/registry.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): prevent unsupported host families from being implicitly promoted to `linkplay_tcp` control-capable targets by deriving `control_path`/`control_capable` from inferred host type rather than host presence alone. `linkplay_tcp` remains the only control-capable path in current scaffold; `wiim`/`generic` targets remain observable in profile/resolution diagnostics but are fail-closed for routing capability. Runtime track disposition: compatibility-shimmed (legacy runtime behavior unchanged). Custom-component track disposition: implemented (registry fail-closed capability classification hardening). P1/P2/P3 impact: no source-of-truth ownership change; capability classification correctness hardening only. README/wiki parity: no material repo-state change.

- Quality/Hardening Pass — Provider Refresh Boolean Normalization (`custom_components/spectra_ls/__init__.py`, `packages/ma_control_hub/script.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): normalize optional `trigger_provider_refresh`/`force_refresh` inputs with explicit truthy/falsy parsing instead of raw Python `bool(...)` coercion to avoid accidental refresh execution when string-like false payloads are supplied from service calls or templated automations. Runtime track disposition: implemented (dispatch argument normalization in runtime script path). Custom-component track disposition: implemented (service-trigger normalization prior to runtime dispatch). P1/P2/P3 impact: no source-of-truth ownership change; input-coercion safety hardening only. README/wiki parity: no material repo-state change.

- Quality/Hardening Pass — Metadata Provider Telemetry + Deterministic Template Resilience (`packages/ma_control_hub/script.inc`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden runtime provider-summary extraction to accept both mapping and list-shaped MA response payloads, add compact bounded provider-summary output to prevent helper-length overflow risk, and expose provider telemetry directly in deterministic scheduler validation output for one-screen triage parity with Step-3. Runtime track disposition: implemented (telemetry parse-shape and bounded-output hardening). Custom-component track disposition: implemented (existing component trigger path benefits from richer shared runtime telemetry without ownership expansion). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics reliability/visibility hardening only. README/wiki parity: no material repo-state change.

- MA Metadata-Provider Visibility Hardening (`packages/ma_control_hub/input_text.inc`, `packages/ma_control_hub/script.inc`, `docs/testing/raw/meta_stack_step3_full_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit provider-summary telemetry persistence so operator templates can render provider-level refresh evidence directly (not only compact response text). Runtime dispatch now stores `input_text.ma_metadata_provider_last_providers` with compact provider status summaries (for example `provider:ok/error`) from MA response payloads when available, with deterministic fallback values for skipped-invalid-URI and unstructured responses. Step-3 template now renders provider summary output and explicit no-dispatch/no-provider-parse diagnostics context, improving actionability when `candidate_uri_valid=false` or response payloads are non-provider-shaped. Runtime track disposition: implemented (provider observability hardening in active dispatch path). Custom-component track disposition: implemented (existing `validate_metadata_policy` trigger path now exposes provider summaries through shared runtime helper telemetry). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics/action-proof hardening only. README/wiki parity: no material repo-state change.

- MA Metadata-Provider URI Fallback Hardening (`packages/ma_control_hub/script.inc`, `docs/testing/raw/meta_stack_step3_full_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): resolve false `candidate_uri_valid=false` windows during valid playback by extending provider-refresh item URI selection beyond now-playing fields (`sensor.now_playing_entity.player_fields_json.ma_uri`) to include MA active-player payload fallback (`sensor.ma_active_player_id_resolved.player_json.current_media.uri`) before final `media_content_id` fallback. Step-3 template URI-readiness block now mirrors this fallback chain so rendered diagnostics match runtime dispatch eligibility. Runtime track disposition: implemented (URI fallback chain hardening for provider-refresh dispatch). Custom-component track disposition: implemented (existing component trigger path benefits from runtime fallback without ownership expansion). P1/P2/P3 impact: no source-of-truth ownership change; provider-refresh dispatch correctness hardening only. README/wiki parity: no material repo-state change.

- MA Metadata-Provider API Response Telemetry Wiring (`packages/ma_control_hub/input_text.inc`, `packages/ma_control_hub/script.inc`, `docs/testing/raw/meta_stack_step3_full_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): complete response observability for provider-refresh dispatch by persisting runtime API result telemetry to helper-backed fields (`status`, `response`, `item_uri`, `reason`, `updated_at`) inside `script.ma_send_metadata_to_providers`, including skipped-invalid-URI and HTTP error outcomes. Step-3 validation template now renders these persisted response fields directly so operator reboot-validation packets can include concrete API result evidence (not just URI readiness). Runtime track disposition: implemented (response persistence + diagnostics surfacing). Custom-component track disposition: implemented (existing `validate_metadata_policy` trigger path now yields readable response evidence through shared runtime helper telemetry). P1/P2/P3 impact: no source-of-truth ownership change; observability/action-proof hardening only. README/wiki parity: no material repo-state change.

- Validation/Meta Stack Step-3 DT Template Reboot-Validation Usability Update (`docs/testing/raw/meta_stack_step3_full_validation.jinja`): clarify pre-render refresh flow so operators can run a single component-side refresh action (`spectra_ls.validate_metadata_policy`) with provider-refresh intent (instead of manually sequencing multiple service calls) before reboot validation captures. Add explicit output guidance for the same call payload to reduce run-window ambiguity during post-reboot evidence checks. Runtime track disposition: compatibility-shimmed (docs/template-only guidance update; no runtime behavior mutation). Custom-component track disposition: implemented (guidance now reflects existing component service wiring for optional provider refresh trigger). P1/P2/P3 impact: no source-of-truth ownership change; validation execution clarity hardening only. README/wiki parity: no material repo-state change.

- MA Metadata-Provider Refresh Handoff Dispatch (`packages/ma_control_hub/script.inc`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/services.yaml`, `docs/testing/raw/meta_stack_step3_full_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add an explicit runtime write-path script (`script.ma_send_metadata_to_providers`) that dispatches Music Assistant command `metadata/update_metadata` through existing `rest_command.ma_api_command` using resolved now-playing MA item URI (`ma_uri`) with guardrails for missing/invalid URI. Component-side `spectra_ls.validate_metadata_policy` now optionally triggers the same runtime handoff script after metadata policy refresh so operator diagnostics and provider-refresh action stay aligned in one call path. Step-3 validator usage notes now include this refresh action for deterministic evidence captures. Runtime track disposition: implemented (explicit MA provider-refresh dispatch path in active runtime contracts). Custom-component track disposition: implemented (service-level parity trigger to invoke runtime refresh path) with compatibility-shim behavior when runtime script is unavailable. P1/P2/P3 impact: no source-of-truth ownership change; metadata-provider refresh actionability hardening only. README/wiki parity: no material repo-state change.

## 2026-04-27

- Validation/Meta Stack Step-3 Context-Aware No-Audio Video Handling (`docs/testing/raw/meta_stack_step3_full_validation.jinja`): harden Step-3 classification so video-only/no-audio-output sessions (for example Plex on Apple TV over HDMI with no active audio route) do not hard-fail as metadata regressions when `metadata_prep` is blocked by `entity_missing` and `sensor.now_playing_media_class` is transiently `unknown`. Adds explicit contextual no-audio/video detection, media-contract expected-unknown handling, and FAIL/WARN gating alignment so true contract/path failures still block while expected no-audio metadata-empty contexts downgrade to cautionary diagnostics. Runtime track disposition: implemented (diagnostics/template-only hardening). Custom-component track disposition: compatibility-shimmed (no write-authority/behavior-path expansion). P1/P2/P3 impact: no source-of-truth ownership change; validation signal-quality hardening only. README/wiki parity: no material repo-state change.

- Validation/Meta Stack Step-3 Full Runtime Packet Expansion (`docs/testing/raw/meta_stack_step3_full_validation.jinja`): expand the one-shot Step-3 runtime validator to cover the full current metadata stack contract set, including HA-authoritative media-class/display-allow surfaces (`sensor.now_playing_media_class`, `sensor.now_playing_preview_key`, `binary_sensor.now_playing_display_allowed`), preview-window timing consistency checks, and explicit contract-surface quality classification alongside existing scheduler/metadata-prep/stale-root-cause/ESP gate outputs. Runtime track disposition: implemented (diagnostics/template-only hardening). Custom-component track disposition: compatibility-shimmed (no write-authority or behavior-path expansion). P1/P2/P3 impact: no source-of-truth ownership change; validation completeness and evidence quality hardening only. README/wiki parity: no material repo-state change.

- HA-Authoritative OLED Media-Class Contract + ESP Consumer-Only Policy (`packages/ma_control_hub/template.inc`, `esphome/spectra_ls_system/substitutions.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): move media classification and non-music preview/blank decision authority to HA template contracts so ESP consumes explicit class/visibility surfaces rather than running local app/source heuristics. Add bounded HA-side non-music preview-window semantics (default 10s) with less-twitchy class persistence and explicit display-allow signal; ESP OLED render path now honors HA-provided media class/display-allow contracts as consumer-only gates. Runtime track disposition: implemented (HA-owned classification/preview contract + ESP consumer wiring). Custom-component track disposition: compatibility-shimmed (no write-authority expansion in this slice; component diagnostics remain compatible with legacy metadata ownership). P1/P2/P3 impact: source-of-truth remains legacy/runtime helper contracts for metadata authority; classification/policy ownership moved from ESP heuristics to HA contract surfaces only. README/wiki parity: no material repo-state change.

- ESP/OLED Media Priority Policy Hardening — historical note: superseded in this same date slice by HA-authoritative media-class/display-allow contracts above. Preview timer authority is now HA-side (`template.inc`) and ESP runtime is consumer-only for blanking/display gating. Runtime track disposition: compatibility-shimmed (legacy local ESP preview policy retired). Custom-component track disposition: compatibility-shimmed (no write-authority expansion). P1/P2/P3 impact: no source-of-truth ownership change beyond HA template policy authority migration for display gating. README/wiki parity: no material repo-state change.

- Validation/Meta Stack Step-3 Full Runtime DT Packet (`docs/testing/raw/meta_stack_step3_full_validation.jinja`, `docs/testing/DEVTOOLS-TEMPLATES.local.md`, `docs/testing/raw/meta_stack_end_to_end_validation_checklist.md`): add a single one-paste Developer Tools template that aggregates scheduler status, metadata-prep policy/suppression signals, stale root-cause classification, and ESP handoff/UI gate checks into one Step-3 runtime assessment surface. This is validation-surface hardening only (no runtime/component behavior mutation) and closes the prior gap where Step-3 evidence required manually stitching multiple templates. Runtime track disposition: compatibility-shimmed (diagnostics/template-only). Custom-component track disposition: compatibility-shimmed (diagnostics/template-only). P1/P2/P3 impact: no source-of-truth ownership change; validation observability hardening only. README/wiki parity: no material repo-state change.

- Runtime+Component/Meta Freshness Dual-Threshold Policy Hardening (`packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/sensor.py`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/testing/raw/stale_meta_root_cause_diagnostic.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/notes/NOTES-engineering-rigor.md`): split metadata freshness gating into two explicit windows instead of one blended window: `ma_meta_stale_s` for `playing` progress freshness and `ma_meta_paused_hide_s` for paused hold/hide behavior. This removes residual ghost-broadcaster lag (playing-with-stale-progress lingering too long) while preserving longer paused visibility policy, and keeps runtime/component/diagnostic templates in parity with explicit suppression semantics. Runtime track disposition: implemented (resolver/now-playing freshness logic updated to dual-threshold policy). Custom-component track disposition: implemented (now-playing signal + metadata policy diagnostics updated to matching dual-threshold policy). P1/P2/P3 impact: no source-of-truth ownership change; parity/decision-correctness hardening only. README/wiki parity: no material repo-state change.

- Validation Governance/Meta Stack Closeout Checklist Activation (`docs/testing/raw/meta_stack_end_to_end_validation_checklist.md`, `docs/testing/DEVTOOLS-TEMPLATES.local.md`, `docs/notes/NOTES-engineering-rigor.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add an explicit rolling validation/test recording contract for the active metadata-stack work so each remaining slice captures deterministic pre/in/post evidence and final closeout proof in one reusable packet (runtime + component + ESP/OLED + stale-healing checks). This formalizes “record while implementing” discipline and prevents end-of-work evidence gaps. Runtime track disposition: compatibility-shimmed (governance/docs-only activation; no runtime contract mutation). Custom-component track disposition: compatibility-shimmed (governance/docs-only activation; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership change; validation-evidence discipline hardening only. README/wiki parity: no material repo-state change.

- ESP UI/Meta Menu Auto-Only Gating (`esphome/spectra_ls_system/packages/spectra-ls-ui.yaml`, `esphome/spectra_ls_system/substitutions.yaml`): hide manual metadata source picks from normal hardware menu flow while preserving existing hidden emergency fallback contracts (`input_boolean.ma_meta_override_active`, `input_text.ma_meta_override_entity`) and existing Auto behavior (clear override + clear entity). Added explicit compile-time UI gate (`meta_manual_picker_enabled`) defaulting to disabled so production UX remains auto-first and stale-safe, with maintenance-only opt-in if needed. Runtime track disposition: implemented (ESP menu behavior hardening). Custom-component track disposition: checked/not-applicable (component write-path/services remain unchanged; emergency fallback contract retained). P1/P2/P3 impact: no source-of-truth ownership change; operator UX gating hardening only. README/wiki parity: no material repo-state change.

- Runtime+Component/Ghost-Broadcaster Healing + Meta-Policy Diagnostics Surfaces (`packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/__init__.py`): harden now-playing/meta winner logic so entities reporting `playing` but carrying stale progress clocks are treated as stale (fail-closed) instead of remaining indefinitely active, align preferred/resolver paused/playing freshness windows to canonical `input_number.ma_meta_paused_hide_s`, and expose policy/suppression telemetry as first-class component diagnostics for operator triage. Runtime track disposition: implemented (freshness gate hardening for ghost broadcasters). Custom-component track disposition: implemented (matching freshness/suppression reasoning + policy diagnostics exposure). P1/P2/P3 impact: no source-of-truth ownership change; parity/decision-correctness hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic Scheduler Template Stale-Paused Entity-Truth Gate Hardening (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): fix residual virtual-screen stale metadata exposure by deriving OLED freshness from the resolved now-playing entity’s own state/attributes (`state`, `play_state`, `is_playing`, `is_paused`, `media_position_updated_at`) instead of trusting `sensor.now_playing_state` alone. Add an explicit paused stale-hide window (`600s`) so long-paused metadata cannot remain visible in Song/Album/Artist rows. Runtime track disposition: compatibility-shimmed (diagnostics template behavior only; no runtime write-path mutation). Custom-component track disposition: compatibility-shimmed (template-level stale-visibility correction only). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics truthfulness hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic Scheduler Template OLED Stale-Metadata Fail-Closed Guard (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): harden the compact OLED text-frame diagnostics path so title/album/artist fields no longer render long-stale now-playing metadata indefinitely. The template now computes a freshness signal from active now-playing entity playback-state + `media_position_updated_at` recency and only displays now-playing text when metadata is fresh/active; otherwise the frame degrades to `-` placeholders instead of preserving stale values. Runtime track disposition: compatibility-shimmed (diagnostics template only; no runtime write-path mutation). Custom-component track disposition: compatibility-shimmed (template-level stale-visibility correction only). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics truthfulness hardening only. README/wiki parity: no material repo-state change.

- Runtime+Component/Startup Handoff Deadspot Reduction (`packages/ma_control_hub/automation.inc`, `custom_components/spectra_ls/coordinator.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): reduce observed 3–4 minute post-boot control handoff latency by tightening startup and retry cadence on both tracks. Runtime startup target-options refresh now starts sooner (`15s` startup delay), periodic cadence is tightened (`/1` minute instead of `/5`), and last-valid target restore now re-attempts on early MA/control-target feed changes (instead of startup-only one-shot). Component startup auto-recovery cadence is also tightened (shorter initial/retry delays and reduced max wait window) so bridge/selection recovery reacts faster to MA readiness transitions. Runtime track disposition: implemented (startup/refresh deadspot reduction). Custom-component track disposition: implemented (startup auto-recovery cadence hardening). P1/P2/P3 impact: no source-of-truth ownership change; bounded startup-latency and recovery responsiveness hardening only. README/wiki parity: no material repo-state change.

- ESPHome/HA Reboot-Safe Control-Handoff Refresh Guard (`esphome/spectra_ls_system.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): replace ESP-uptime-only startup gating for active-target forced `homeassistant.update_entity` calls with a Home Assistant API reconnect grace window plus readiness checks on target/host template feeds before issuing forced refreshes for `sensor.ma_control_targets`, `sensor.ma_control_hosts`, and `sensor.ma_control_host`. This prevents HA-restart race windows (ESP remained up, HA rebooted) from triggering template assertion failures while preserving immediate forced-refresh behavior once HA reconnect settles and feeds are initialized. Runtime track disposition: implemented (reboot-safe forced-refresh guard hardening in active ESP runtime path). Custom-component track disposition: checked/not-applicable (component path does not originate ESP-side `homeassistant.update_entity` calls for these template entities). P1/P2/P3 impact: no source-of-truth ownership change; startup/reconnect race hardening only. README/wiki parity: no material repo-state change.

## 2026-04-26

- ESPHome/HA First-Boot Template Assertion Guard (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`): harden active-target host-refresh push sequencing so ESP no longer force-calls `homeassistant.update_entity` for `sensor.ma_control_targets`, `sensor.ma_control_hosts`, and `sensor.ma_control_host` during the earliest startup window when HA template internals may not yet be fully initialized (`_template_result_info` assertion path). Add a startup grace gate before issuing those forced updates; active-target cache refresh behavior remains unchanged after grace elapses. Runtime track disposition: implemented (startup race hardening in active ESP runtime path). Custom-component track disposition: checked/not-applicable (component path does not issue these ESP-triggered HA template force-updates). P1/P2/P3 impact: no source-of-truth ownership change; startup ordering/race hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Recorder Oversize Warning Suppression for Shadow Diagnostics (`custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`): harden high-volume shadow diagnostics entities so Recorder no longer attempts to persist large nested attributes (registry/validation/write-controls payload clusters) that exceed HA’s 16KB attribute storage threshold. Add explicit recorder-unrecorded attribute filtering on `sensor.shadow_active_target` and `binary_sensor.shadow_active_control_capable` heavy fields to preserve live diagnostics/state-machine visibility while preventing repeated `db_schema` size warnings and DB churn. Runtime track disposition: checked/not-applicable (runtime behavior path unchanged; no `packages/`/`esphome/` contract mutation required). Custom-component track disposition: implemented (Recorder persistence-safety hardening). P1/P2/P3 impact: no source-of-truth ownership change; observability persistence hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic ESP Diagnostics Layout Reorganization (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): reorganize ESP diagnostics rendering into status-first grouped sections (state/route-host/media context) near the top of template output for faster operator triage. Field content and contracts remain unchanged; only display ordering/grouping is refactored for readability and workflow speed during live checks. Runtime track disposition: implemented (diagnostics presentation only). Custom-component track disposition: implemented (template display organization only). P1/P2/P3 impact: no source-of-truth ownership change; observability UX hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic ESP Source-Options Anchor Hardening (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): harden `AVAILABLE SOURCES` derivation so diagnostics anchor to the active playback/control player source list first (`PLAYBACK PLAYER` / helper target surface) and only fall back to multi-entity union when player-local source options are unavailable. This avoids misleading source-option inflation from unrelated candidate entities and keeps operator output aligned to the active control host surface. Runtime track disposition: implemented (diagnostics-only expansion using existing runtime attributes; no ownership/authority change). Custom-component track disposition: implemented (template-level observability correction only). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics correctness hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Component-Boot No-Mix Startup Guard + ESP Diagnostics Source/Modality Expansion (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): harden component-authority startup so automatic boot recovery stays component-only (target/options recovery path) and does not run legacy-trial bridge authority transitions during startup windows, preventing mixed-authority boot semantics. Expand deterministic validation ESP diagnostics to include metadata source provenance and active playback modality context (current source/app/entity plus available source options from control-host candidate surfaces) so operator triage can identify what is playing and from where in one block. Runtime track disposition: implemented (diagnostics surface expansion consumed from existing runtime contracts; no runtime authority ownership expansion). Custom-component track disposition: implemented (component startup no-mix guardrail + richer diagnostics parity context). P1/P2/P3 impact: no source-of-truth ownership change; startup safety and observability hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Legacy Boot No-Mix Guard + Scheduler Legacy-Authority Pin (`custom_components/spectra_ls/coordinator.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): harden legacy-authority startup behavior so component startup recovery no longer executes bridge-driven component target mutation while authority remains `legacy` (prevents mixed-writer semantics during early boot), and pin scheduler decision output to helper-current target in legacy authority mode so diagnostics and control-host blocker classification do not report helper/scheduler ownership drift during boot churn. Runtime track disposition: implemented (legacy runtime remains single-writer source-of-truth via helper/scripts; no runtime contract mutation required in this slice). Custom-component track disposition: implemented (legacy-safe startup recovery guard + legacy-authority scheduler selection alignment). P1/P2/P3 impact: no source-of-truth ownership change; migration safety/diagnostics correctness hardening only. README/wiki parity: no material repo-state change.

- Component+ESP Handoff Visibility Parity Slice (`custom_components/spectra_ls/coordinator.py`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): keep runtime/component behavior in sync for handoff acceleration by extending component state-change refresh sequencing to mirror legacy trigger intent on both detected-receiver and now-playing-entity changes (not MA-players-only), and add explicit ESP-side exported telemetry surfaces for handoff status, resolved control target, and OLED/UI status so deterministic validation can report end-to-end ESP handoff posture directly. Deterministic scheduler template now includes an ESP status section with resilient fallback reporting when ESP telemetry entities are unavailable. Runtime track disposition: implemented (ESP observability expansion for handoff/OLED status). Custom-component track disposition: implemented (trigger parity expansion for detected-receiver + now-playing changes). P1/P2/P3 impact: no source-of-truth ownership change; parity/observability hardening only. README/wiki parity: no material repo-state change.

- Runtime+ESP/Target-Handoff Latency Reduction (legacy authority lane) (`packages/ma_control_hub/automation.inc`, `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): reduce observed multi-minute lag between healthy scheduler diagnostics and effective ESP control-host handoff in legacy-authority operation by (1) adding event-driven runtime refresh triggers for target-options/auto-select on `sensor.ma_detected_receiver_entity` and `sensor.now_playing_entity` changes (instead of waiting for `/5` periodic cadence), and (2) adding an optimistic ESP-side host-refresh push on active-target changes (`homeassistant.update_entity` for `sensor.ma_control_targets`, `sensor.ma_control_hosts`, `sensor.ma_control_host`) so host/target handoff converges immediately after helper target transitions. Runtime track disposition: implemented (handoff cadence optimization). Custom-component track disposition: compatibility-shimmed (no component ownership/contract change in this slice). P1/P2/P3 impact: no source-of-truth ownership change; runtime handoff-latency hardening only. README/wiki parity: no material repo-state change.

- Custom Component/MA-Players Target-Options Refresh Parity (`custom_components/spectra_ls/coordinator.py`, `docs/notes/NOTES-parity-gap-backlog.md`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): close parity gap where legacy `ma_update_target_options_start` triggers target-options refresh before auto-select on `sensor.ma_players` changes, while component listener path only ran auto-select. Component now performs bounded target-options scaffold refresh first on MA-player change in component-authority windows, then runs existing guarded auto-select loop parity behavior. Candidate synthesis is extended additively (no rewrite) by incorporating capability-aware registry/profile signals (`control_capable`, resolved host, supported `control_path`, availability quality) from existing component data surfaces. Runtime track disposition: compatibility-shimmed (legacy automation retained as rollback baseline). Custom-component track disposition: implemented (players-change refresh parity + guarded sequencing + additive capability-aware candidate feed). P1/P2/P3 impact: no source-of-truth ownership change; bounded parity and selection/options consistency hardening only. README/wiki parity: no material repo-state change.

- Custom Component/No-Control-Capable Feedback Lifecycle Parity (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/const.py`, `docs/notes/NOTES-parity-gap-backlog.md`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): close the next parity gap by mirroring legacy `ma_feedback_no_control_capable_hosts` lifecycle in component migration windows: 30s degradation hold, one-shot self-heal attempt (component target-options + auto-select path), post-heal 10s persistence check, and persistent notification create/dismiss flow (`spectra_ls_no_control_capable_hosts`) with state-derived reason context. Component behavior is authority-gated (`component`) to avoid duplicate compatibility-path notifications while legacy automation remains rollback baseline. Runtime track disposition: compatibility-shimmed (legacy automation retained as baseline). Custom-component track disposition: implemented (feedback lifecycle parity + guarded adaptation). P1/P2/P3 impact: no source-of-truth ownership change; bounded parity + operator-feedback resilience hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Auto-Select Loop Watcher Parity + Cleanup (`custom_components/spectra_ls/coordinator.py`, `docs/notes/NOTES-parity-gap-backlog.md`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): close the captured parity gap where component global state-change watcher narrowed watched helper options to `media_player.*` only. Component watcher now follows legacy-style watched-options membership semantics and removes the unnecessary media-player prefix filter that could suppress valid parity triggers. Runtime track disposition: implemented (legacy baseline unchanged). Custom-component track disposition: implemented (gap closure + behavior cleanup). P1/P2/P3 impact: no source-of-truth ownership change; bounded parity-correctness + useless-filter removal only. README/wiki parity: no material repo-state change.

- Parity Gap Capture/Auto-Select Loop Watcher Filter (`docs/notes/NOTES-parity-gap-backlog.md`): log confirmed parity mismatch where legacy `ma_auto_select_loop` event-branch watches full `input_select.ma_active_target` options membership while component global watcher currently filters watched options to `media_player.*` entries. Added full pickup-ready gap record (`GAP-20260426-auto-select-loop-watched-options-filter`) with evidence lines, repro, expected parity outcome, and fix/validation plan for follow-up closure. Runtime track disposition: implemented (baseline unchanged). Custom-component track disposition: deferred with rationale (gap recorded for next patch). P1/P2/P3 impact: no ownership flip; parity-gap governance hardening only. README/wiki parity: no material repo-state change.

- Docs/Parity Gap Backlog Protocol (`docs/notes/NOTES-parity-gap-backlog.md`): add a dedicated structured parity-gap backlog artifact with required pickup-ready schema (legacy source, component mismatch, repro, expected outcome, fix direction, validation plan, and two-track disposition fields) so future runtime↔component parity misses are captured once and resumed without rediscovery drift. Runtime track disposition: compatibility-shimmed (process/docs-only update). Custom-component track disposition: compatibility-shimmed (process/docs-only update). P1/P2/P3 impact: no source-of-truth ownership change; execution-discipline hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Selection Ownership Migration Slice — Ambiguity Lock + Meta-Stale Unlock + Auto-Select Loop Parity Build-Out (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/services.yaml`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): extend the component-side selection lifecycle with parity behavior matching legacy control-hub automations by adding component-authority lock-on-ambiguous-select handling, bounded stale-meta unlock handling (`binary_sensor.ma_meta_stale` hold semantics), and guarded auto-select loop triggering on parity-relevant state transitions. This keeps fail-closed authority/debounce/reentrancy protections while reducing reliance on legacy-only automation ownership for selection lock lifecycle behavior. Runtime track disposition: compatibility-shimmed (legacy automations remain rollback baseline). Custom-component track disposition: implemented (selection lock lifecycle parity expanded from partial to behavior-complete guarded path). P1/P2/P3 impact: no source-of-truth ownership flip; bounded selection-domain migration progression only. README/wiki parity: no material repo-state change.

- Custom Component/Selection Ownership Migration Slice — Cycle/Restore/Track Last-Valid Target (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/services.yaml`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): migrate legacy selection pipeline core behaviors into component-owned executable services by adding deterministic component-side equivalents for target cycling (`ma_cycle_target` parity), startup/repair restore of last valid target (`ma_restore_last_valid_target` parity), and bounded last-valid tracking updates (`ma_track_last_valid_target` parity) with guarded authority/debounce/reentrancy semantics and explicit audit payloads. This reduces dependency on runtime-only selection scripts for active migration lanes while preserving fail-closed rollback posture and compatibility contracts. Runtime track disposition: compatibility-shimmed (legacy scripts remain available as rollback baseline). Custom-component track disposition: implemented (selection ownership execution parity expanded from scaffold-only to functional service path). P1/P2/P3 impact: no source-of-truth ownership flip; bounded selection-domain migration progression only. README/wiki parity: no material repo-state change.

- Custom Component/Next Parity Build-Out — Metadata Prep Gate Resilience + Handoff Inventory Maturity (`custom_components/spectra_ls/coordinator.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden metadata-prep readiness classification so transient title payload gaps no longer force false blocker states when fresh-play evidence and resolver-ready metadata context are present, and expand metadata-candidate readiness fallback to include resolver best-candidate surfaces when candidate payload shape is delayed. Also promote handoff inventory maturity classification from static scaffold heuristics to evidence-based status transitions (`implemented` once target-options/auto-select/metadata-resolver scaffolds demonstrate successful guarded execution), reducing repeated validation loops and making next-slice parity progress explicit. Runtime track disposition: compatibility-shimmed (legacy ownership/contracts remain authoritative). Custom-component track disposition: implemented (metadata gate + handoff progression diagnostics hardening). P1/P2/P3 impact: no source-of-truth ownership change; parity-readiness signal correctness hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Authority-Policy Validation Alignment (`custom_components/spectra_ls/coordinator.py`): align scheduler/metadata diagnostic validation gates with bounded dual-authority operation so temporary or operator-selected `component` authority no longer produces false readiness regressions (`authority_mode_not_legacy`, `trial_authority_not_legacy`) while guarded apply-path checks are healthy. Validation `no_authority_expansion` checks now treat supported authority modes as policy-valid diagnostics posture, and bridge authority check remains satisfied for completed bridge context even when post-bridge authority restores to `component`. Runtime track disposition: compatibility-shimmed (legacy authority/contracts remain baseline and rollback-safe). Custom-component track disposition: implemented (diagnostics-policy consistency hardening across scheduler + metadata bridge lanes). P1/P2/P3 impact: no source-of-truth ownership change; authority-policy consistency hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Bridge Validation False-WARN Inference Fix (`custom_components/spectra_ls/coordinator.py`): harden `metadata_bridge_validation` trial-stage status inference so `bridge_completed` outcomes are not misclassified as `trial_stage_not_ok` when trial stage payload status is transiently `unknown`/missing in bridge-stage telemetry. Validation now infers trial-stage success from completed bridge context (`trial_dry_run` flag) when canonical trial status is unresolved, preventing contradictory `bridge_completed` + WARN blocker outputs. Runtime track disposition: compatibility-shimmed (legacy authority/contracts unchanged). Custom-component track disposition: implemented (diagnostics correctness hardening). P1/P2/P3 impact: no source-of-truth ownership change; validation-state consistency fix only. README/wiki parity: no material repo-state change.

- Custom Component/Metadata Bridge Target-Recovery Debounce Sequencing Fix (`custom_components/spectra_ls/coordinator.py`): fix deterministic bridge execution where `run_metadata_trial_bridge_scaffold` could fail early with `blocked_target_recovery_stage` because the internal auto-select recovery stage was debounce-blocked by a prior in-session dry-run/write timestamp. Bridge-internal auto-select recovery is now executed with explicit sequence override so debounce does not break same-transaction recovery orchestration. Runtime track disposition: compatibility-shimmed (legacy authority/contracts unchanged). Custom-component track disposition: implemented (bridge sequencing reliability hardening). P1/P2/P3 impact: no source-of-truth ownership change; bounded orchestration correctness fix only. README/wiki parity: no material repo-state change.

- Governance/Parity Cadence Directive Addition (`.github/copilot-instructions.md`): add an explicit required execution cadence directly beside the top-of-file parity directive for active parity operations: (1) parity, (2) validate-test-debug-fix loop, (3) parity re-check, (4) repeat step 2 on post-parity state until clean closure evidence is captured. This codifies the user-directed working rhythm as a mandatory migration execution rule. Runtime track disposition: compatibility-shimmed (process/governance directive only). Custom-component track disposition: compatibility-shimmed (process/governance directive only). P1/P2/P3 impact: no source-of-truth ownership change; execution-discipline hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic Scheduler Template Shadow-Entity Source Parity Correction (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): align shadow-entity source-selection precedence with current component entity-id behavior (`has_entity_name` short ids) by preferring `sensor.shadow_active_target` as the primary component diagnostics surface and treating `sensor.spectra_ls_shadow_active_target` as compatibility fallback. Also update operator-facing next-action wording to reference both accepted entity-id styles explicitly. Runtime track disposition: compatibility-shimmed (template/docs-only guidance correction). Custom-component track disposition: compatibility-shimmed (observability-template parity alignment to implemented entity naming). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics correctness hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Scaffold Dry-Run Debounce Guard Fix (`custom_components/spectra_ls/coordinator.py`): ensure dry-run scaffold executions are not blocked by write debounce timing from prior attempts. `run_auto_select_scaffold`, `build_target_options_scaffold`, and `run_metadata_resolver_scaffold` now apply debounce checks only for non-dry-run writes, preserving fail-closed write safeguards while allowing deterministic back-to-back dry-run validation and bridge preflight sequencing. Runtime track disposition: compatibility-shimmed (legacy scripts remain authority baseline). Custom-component track disposition: implemented (guard semantics corrected for dry-run parity behavior). P1/P2/P3 impact: no ownership/source-of-truth flip; component scaffold validation determinism hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Target-Options + Auto-Select Scaffold Parity Expansion (`custom_components/spectra_ls/coordinator.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): implement component-side parity hardening for legacy `ma_update_target_options`/`ma_auto_select`-critical behavior by extending scaffold planning to synthesize helper options from multiple discovery surfaces (rooms contracts, MA players payload, live IP-backed media_player discovery with LinkPlay/WiiM hints, detected receiver candidate, helper current/last-valid context) and by adding default-option recovery semantics on component target-options apply path (select fallback when current helper target is invalid/not-in-options). Auto-select scaffold planning now prefers detected active receiver candidates and active playable helper options before fallback. Runtime track disposition: compatibility-shimmed (legacy scripts remain authoritative during transition). Custom-component track disposition: implemented (parity-critical selection/options behavior now available in component scaffolds). P1/P2/P3 impact: no source-of-truth ownership flip; behavior-parity expansion to de-risk legacy retirement path. README/wiki parity: no material repo-state change.

- Governance/Execution-Discipline Hardening — Changelog-Adjacent Dual-Track Parity Rule (`.github/copilot-instructions.md`): place an explicit required rule directly beside the existing “update `docs/CHANGELOG.md` before code edits” directive: every feature, bugfix, or behavior-contract slice must review and keep both runtime (`packages/` + `esphome/`) and custom-component (`custom_components/spectra_ls`) tracks updated and working in the same slice when feasible, with mandatory shim/defer rationale plus version-metadata rationale recorded when strict lock-step is not applicable. Also mirror this requirement in roadmap parity discipline so no slice is considered complete without a recorded two-track parity/version review disposition. Runtime track disposition: compatibility-shimmed (process/governance update only). Custom-component track disposition: compatibility-shimmed (process/governance update only). P1/P2/P3 impact: no source-of-truth ownership change; enforcement clarity hardening only. README/wiki parity: no material repo-state change.

- HA/MA Permanent Target-Options Recovery Fallback for Control-Capable IP Targets (`packages/ma_control_hub/script.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): harden `script.ma_update_target_options` so helper option synthesis no longer depends solely on `sensor.ma_players.result` availability during transient contract gaps. Add a second discovery-first pass over live `media_player.*` entities and include only IP-backed targets with explicit control hints (`control_capable=true`, `control_path=linkplay_tcp`) or deterministic manufacturer/model/purpose/entity hints for currently supported LinkPlay/WiiM-class control paths (`wiim`, `linkplay`, `arylic`, `up2stream`). Final selectable/default-target computation now includes this live fallback set, making recovery from stable-helper/no-candidate windows persistent without manual `input_select.set_options` injection. Runtime track disposition: implemented (target-option synthesis resilience hardening). Custom-component track disposition: compatibility-shimmed (consumes stabilized helper contract; no component write-path ownership expansion). P1/P2/P3 impact: no source-of-truth ownership change; fail-safe option availability hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic Scheduler Template No-Candidate Guidance Clarification (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): refine next-action classification for `no_scheduler_candidate` snapshots where helper state is already stable (`auto_select=noop_already_selected`) but the active target has no resolved control-capable host route (common with AppleTV-like/non-TCP playback targets). Template now emits an explicit “stable but non-control-capable target” guidance branch instead of repeatedly recommending generic scheduler reruns. Runtime track disposition: compatibility-shimmed (docs/template-only guidance). Custom-component track disposition: compatibility-shimmed (observability wording aligned to existing scheduler semantics). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics actionability hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic Scheduler Template Control-Expected + Helper-Fallback Eligibility Alignment (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): align template-side fallback semantics with runtime/component hardening so the template no longer force-promotes helper current target when scheduler produced `no_candidate` unless helper current is registry-backed control-capable with resolved host. Add decision-reason visibility and explicit runtime `binary_sensor.ma_no_control_capable_hosts` reason surfacing (including `target_not_control_expected`) to prevent false green interpretations during AppleTV-like/non-TCP target windows. Runtime track disposition: compatibility-shimmed (template observability update only). Custom-component track disposition: compatibility-shimmed (template reflects implemented scheduler guard semantics). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics correctness hardening only. README/wiki parity: no material repo-state change.

- HA/MA AppleTV-Like Target Control-Expectation Hardening + Scheduler Helper-Fallback Guard (`packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/coordinator.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): prevent false/noisy host-capability degradation loops when the active target is metadata-active but not expected to expose a direct TCP control host (for example AppleTV-like playback entities with no `ip_address`/room host contract) by gating `binary_sensor.ma_no_control_capable_hosts` on explicit control expectation signals (direct/member IP or room control-capable/tcp_host contract). Add explicit reason surface `target_not_control_expected` for transparent diagnostics. In component scheduler fallback, stop forcing helper-current fallback as implicitly control-capable; helper fallback now only promotes when the helper target has a resolved registry entry with control capability + host, otherwise scheduler remains `no_candidate` with explicit rationale. Runtime track disposition: implemented (control-expected warning gating + reason transparency). Custom-component track disposition: implemented (no-candidate fallback correctness hardening). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics/triage correctness hardening only. README/wiki parity: no material repo-state change.

- HA/MA No-Control-Capable Warning Self-Heal + Contract-Readiness Debounce (`packages/ma_control_hub/template.inc`, `packages/ma_control_hub/automation.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): reduce recurring false/noisy `no_hosts_and_not_control_capable` persistent notifications by adding contract-readiness gating (MA players + control-target surfaces ready) and ambiguity suppression to `binary_sensor.ma_no_control_capable_hosts`, and by adding an automatic one-shot recovery attempt (`script.ma_update_target_options` + `script.ma_auto_select`) before creating the operator notification. Notification now emits only when the degradation remains after self-heal and includes an updated suggested action aligned to refresh/reselect flow. Runtime track disposition: implemented (runtime warning-noise hardening + bounded self-recovery). Custom-component track disposition: compatibility-shimmed (no custom-component behavior mutation required for this runtime warning path). P1/P2/P3 impact: no source-of-truth ownership change; runtime diagnostics/actionability hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Bridge Trial-Status Inference + Registry Target-Room Lookup Optimization (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/registry.py`): reduce false bridge WARN outputs where metadata trial stage completed inside bridge scaffolding but global `metadata_trial_last_attempt` remained `never_attempted` by inferring trial status from bridge-stage telemetry (`stages.metadata_trial.status`) as a bounded fallback in bridge validation. Also optimize registry snapshot build path by reusing first-hit room mapping per target (avoids per-target rescans through full rooms list), reducing avoidable O(n²)-style overhead during frequent snapshot refreshes. Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (diagnostics correctness + micro-performance hardening only). P1/P2/P3 impact: no source-of-truth ownership change; observability/performance hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Registry Seed Fallback + Route-Defer Classification Hardening (`custom_components/spectra_ls/registry.py`, `custom_components/spectra_ls/router.py`, `custom_components/spectra_ls/coordinator.py`): improve resilience when legacy control-target/rooms feeds are temporarily sparse by seeding registry target discovery from active-target helper options/current state and active-target surface fallback, plus broader rooms payload parsing fallbacks from `rooms_raw`/`rooms_json` state+attribute shapes. Route-trace defer ordering is tightened so unresolved/non-control-capable targets classify as `defer_not_capable` instead of noisy generic `defer_unsupported_path` when control capability is absent. Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (read-side registry/route diagnostics hardening only). P1/P2/P3 impact: no source-of-truth ownership change; post-boot registry/route observability reliability hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic Template Hotspot-Triage Upgrade (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): expand diagnostics output with explicit hotspot signals for decision-source provenance, shadow-entity source selection, route decision, registry entry coverage, contract hard/soft blocker surfaces, metadata-prep blockers, and top-ranked candidate score/context details. This makes PASS/WARN snapshots more actionable by identifying which subsystem is degrading (data-source staleness, contract quality, candidate scoring, or bridge/metadata readiness) without requiring separate ad-hoc templates. Runtime track disposition: compatibility-shimmed (no runtime behavior change). Custom-component track disposition: compatibility-shimmed (diagnostics-template observability hardening only). P1/P2/P3 impact: no source-of-truth ownership change; operator troubleshooting quality hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic Template Helper-Current Fallback Hardening (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): add a second fallback path for decision display fields when both `scheduler_last_decision` and `scheduler_validation.default_decision` are unavailable/stale on the active shadow entity. In PASS-ready snapshots, the template now reuses resolved helper current target (`input_select.ma_active_target`) when it is present in helper options, preventing false `candidate_count=0` / `selected_target=none` outputs and stale no-candidate next-action guidance. Runtime track disposition: compatibility-shimmed (no runtime behavior change). Custom-component track disposition: compatibility-shimmed (diagnostics-template resilience hardening only). P1/P2/P3 impact: no source-of-truth ownership change; operator diagnostics correctness hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic Template Decision-Source Precedence Hardening (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): when `write_controls.scheduler_last_decision` is still `never_attempted`, the template now falls back to `scheduler_validation.default_decision` so live scheduler readiness snapshots do not report false `selected_target=none` / `candidate_count=0` inconsistencies. This aligns displayed scheduler decision fields with current coordinator validation payloads and prevents stale next-action advice in healthy PASS snapshots. Runtime track disposition: compatibility-shimmed (no runtime behavior change). Custom-component track disposition: compatibility-shimmed (diagnostics-template logic hardening only). P1/P2/P3 impact: no source-of-truth ownership change; operator diagnostics correctness hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Scheduler No-Candidate Fallback Hardening (`custom_components/spectra_ls/coordinator.py`): when scheduler ranking returns no candidates due to transient profile/host freshness gaps, reuse a resolved `input_select.ma_active_target` helper current value as a guarded fallback candidate (only when present in helper options and not `none/unknown/unavailable`). This aligns scheduler-choice behavior with existing auto-select scaffold resilience, prevents false `no_scheduler_candidate` WARN loops during reboot churn, and preserves fail-closed apply guards (authority/debounce/option checks remain unchanged). Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (scheduler decision fallback hardening only). P1/P2/P3 impact: no source-of-truth ownership change; deterministic decision resilience hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Soft Contract-Surface Gating for Control Host Feeds (`custom_components/spectra_ls/coordinator.py`): reclassify `sensor.ma_control_hosts` / `sensor.ma_control_host` as soft-required contract surfaces in component validation payloads. Core contract validity now fails only on hard-required metadata/route/control-target entities, while host-feed instability is surfaced as warning telemetry (`soft_surface_warnings`) instead of forcing global `contract_invalid` fail state cascades. This reduces false FAIL outcomes during transient ESP fail-closed host feed clears while keeping operator visibility on host-surface quality. Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (validation-gating hardening only). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics reliability hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Bridge Resolver Debounce False-Block Fix + Contract-Invalid Guidance Polish (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): remove an internal startup-bridge sequencing edge where resolver scaffold dry-run could be blocked by immediate debounce after auto-select stage (`blocked_debounce`), causing premature `blocked_resolver_stage` despite valid selected helper target. Bridge resolver stage now bypasses same-transaction debounce guards and dry-run resolver attempts no longer consume debounce blocking. Deterministic template next-action guidance now explicitly calls out contract-invalid + no-candidate scenarios as control-host/contract feed issues (not MA boot). Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (startup sequencing and operator guidance hardening only). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics/bridge reliability hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Startup Wait-Reason Classification + Recorder Attribute-Size Hardening (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/sensor.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): refine startup wait semantics so “waiting for Music Assistant boot readiness” is reserved for true MA boot gates (players/control-targets), while helper/options post-boot contract delays are reported as control-contract readiness waits rather than MA-not-booted messaging. Also trim oversized shadow sensor attribute payloads to avoid Recorder 16KB attribute-drop warnings (`sensor.shadow_active_target`, `sensor.shadow_active_control_path`, `sensor.shadow_control_hosts`), preserving deterministic diagnostics availability and reducing DB churn. Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (observability/message-shape + payload-size hardening only). P1/P2/P3 impact: no source-of-truth ownership change; startup diagnostics correctness and recorder reliability hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Startup Recovery False-Wait + No-Candidate Hardening (`custom_components/spectra_ls/coordinator.py`): tighten startup-recovery gating and target-recovery behavior for post-boot churn cases where MA is up but host-list surfaces lag. Boot-readiness no longer hard-blocks solely on unresolved `sensor.ma_control_hosts`, and auto-select scaffold now allows a valid current helper target fallback (`input_select.ma_active_target`) instead of hard-blocking `blocked_no_candidate` when scaffold candidates are temporarily empty. This reduces spurious `waiting_for_ma_boot` loops and unnecessary startup bridge exhaustion while preserving fail-closed write guards. Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (startup robustness hardening only). P1/P2/P3 impact: no source-of-truth ownership change; startup reliability/diagnostics hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Startup Recovery Authority-Restore + Boot-Gate Hardening (`custom_components/spectra_ls/coordinator.py`): fix startup bridge early-failure path so authority mode is always restored to pre-run value (prevents stale `component` authority residue after blocked recovery attempts), and tighten boot-readiness gating to require resolved control-host surface plus non-`none` active-target helper options before consuming bridge attempts. This prevents premature “exhausted after 3 attempts” warnings during transient post-reboot surface churn and keeps diagnostics aligned with fail-closed startup posture. Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (startup safety/telemetry hardening only). P1/P2/P3 impact: no source-of-truth ownership change; startup robustness and diagnostics correctness hardening only. README/wiki parity: no material repo-state change.

- Validation/Deterministic Template Entity-Selection Hardening (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): fix shadow-sensor selection precedence so diagnostics prefer the component shadow entity surface (`sensor.spectra_ls_shadow_active_target`) and avoid fallback to legacy/plain shadow mirrors that may not carry `write_controls`/scheduler/bridge attributes. This removes false-negative template outputs caused by entity-name collisions after reboot. Runtime track disposition: compatibility-shimmed (no runtime behavior change). Custom-component track disposition: compatibility-shimmed (diagnostics-template source-selection hardening only). P1/P2/P3 impact: no source-of-truth ownership change; operator diagnostics correctness hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Reboot Hardening — MA Boot-Wait Classification for Scheduler+Bridge Diagnostics (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): polish existing reboot behavior so pre-boot Music Assistant gaps are surfaced as explicit wait-state diagnostics (`waiting_for_ma_boot`) with operator-friendly reasons, instead of being misclassified as hard scheduler/bridge failure states during early startup windows. Deterministic template guidance now prioritizes “waiting for Music Assistant …” next-actions when boot-readiness gates are not yet satisfied. Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (diagnostics/message-shape hardening only). P1/P2/P3 impact: no source-of-truth ownership change; startup observability correctness hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Startup Wait Messaging UX Polish (`custom_components/spectra_ls/coordinator.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): improve startup auto-recovery wait-state messaging by translating technical boot-gate tokens into operator-readable “waiting for Music Assistant …” reason strings in bridge telemetry/log surfaces, reducing noisy/opaque startup diagnostics during MA cold boot. Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (message-shape/observability polish only). P1/P2/P3 impact: no source-of-truth ownership change; diagnostics UX hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Startup Recovery MA-Readiness Gate (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/const.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): harden post-restart startup auto-recovery by waiting for MA boot-readiness signals (control-target and MA players surfaces resolved) before consuming bounded bridge-recovery attempts, preventing premature retry exhaustion during MA cold boot windows. Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (MA boot-gated startup orchestration). P1/P2/P3 impact: no source-of-truth ownership change; startup reliability hardening only. README/wiki parity: no material repo-state change.

- Custom Component/HA-Restart Startup Auto-Recovery for Target/Options Alignment (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/__init__.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): add an automatic post-setup startup recovery pass that runs the existing metadata-trial bridge scaffold sequence (including pre-bridge auto target/options recovery) after Home Assistant restart, with bounded retries and fail-closed diagnostics, so operators are not required to manually run scaffold actions to recover from cold-start `no control-capable hosts`/`selected_target=none` states. Runtime track disposition: compatibility-shimmed (legacy runtime authority/contracts unchanged). Custom-component track disposition: implemented (startup-triggered auto-recovery orchestration). P1/P2/P3 impact: no source-of-truth ownership change; startup recovery automation and operator-friction reduction only. README/wiki parity: no material repo-state change.

- Validation/Scheduler Deterministic Next-Action All-Green Branch (`docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): fix deterministic next-action precedence so a fully healthy snapshot (`scheduler_validation=PASS`, `metadata_bridge_validation=PASS`, selected target/options valid) reports “no action required” instead of emitting a stale authority warning. Runtime track disposition: compatibility-shimmed (runtime authority contract unchanged). Custom-component track disposition: compatibility-shimmed (diagnostics-template logic refinement only). P1/P2/P3 impact: no source-of-truth ownership change; operator guidance correctness hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Automatic Target Recovery for Scheduler+Bridge Flow (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): remove manual helper-rebuild dependency from the active migration lane by adding a pre-bridge automatic recovery stage that (under guarded component authority) syncs `input_select.ma_active_target` options and auto-selects a control-capable candidate when the current active target is non-control-capable or options are stale. This converts the previous operator-heavy “manually build helper options first” loop into an automatic fail-closed sequence before metadata bridge/trial checks run. Runtime track disposition: compatibility-shimmed (legacy runtime contracts remain authoritative; no runtime helper contract rename/removal). Custom-component track disposition: implemented (automatic pre-bridge options/selection recovery + stage telemetry). P1/P2/P3 impact: no source-of-truth ownership change; additive migration automation and operator friction reduction only. README/wiki parity: no material repo-state change.

- Custom Component/Resolver-Authority Gating + Metadata Trial Bridge Alignment (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/sensor.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): add an explicit metadata bridge scaffold lane that aligns component metadata resolver planning with legacy-authoritative metadata trial semantics. Introduce deterministic bridge diagnostics and a one-shot bridge service that enforces authority transitions/gates (`legacy` trial requirement), propagates resolver context into trial audit payloads, and surfaces bridge-readiness/blocking reasons for top-to-bottom handoff execution. Runtime track disposition: compatibility-shimmed (legacy metadata resolver/trial ownership unchanged). Custom-component track disposition: implemented (bridge orchestration + observability + guardrails). P1/P2/P3 impact: no source-of-truth ownership change; additive migration bridge controls/diagnostics only. README/wiki parity: no material repo-state change.

- Custom Component/Metadata Resolver Handoff Scaffold (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/services.yaml`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`): add a component-native metadata resolver scaffold service (`run_metadata_resolver_scaffold`) that deterministically selects metadata entity candidates from resolver/detected/target fallback signals and supports bounded dry-run-first + guarded helper apply semantics (`input_text.ma_meta_override_entity`, `input_boolean.ma_meta_override_active`) with authority/debounce/reentrancy/existence checks. Expose `metadata_resolver_last_attempt` diagnostics in write-controls and handoff inventory so metadata ownership migration readiness can be tracked alongside target-options/auto-select scaffolds. Runtime track disposition: compatibility-shimmed (legacy metadata resolver remains authoritative runtime path). Custom-component track disposition: implemented (metadata scaffold planning + bounded helper apply path). P1/P2/P3 impact: no source-of-truth ownership change; additive migration scaffold/observability only. README/wiki parity: no material repo-state change.

- Custom Component/Legacy Selection Handoff Scaffolds (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/sensor.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): add component-native scaffold services for legacy-only selection ownership (`build_target_options_scaffold`, `run_auto_select_scaffold`) with deterministic dry-run-first planning payloads and guarded helper-apply options (`authority`, `debounce`, `reentrancy`, helper/options checks, force override). Expose `target_options_last_attempt` and `auto_select_last_attempt` diagnostics in write-controls and handoff inventory so cutover readiness can be tracked slice-by-slice without authority expansion. Runtime track disposition: compatibility-shimmed (legacy scripts remain authoritative execution path). Custom-component track disposition: implemented (scaffold planning + bounded apply path). P1/P2/P3 impact: no source-of-truth ownership change; additive migration scaffold/observability only. README/wiki parity: no material repo-state change.

- Custom Component/Guarded Scheduler Apply Service + Handoff Inventory Scaffold (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/sensor.py`, `docs/testing/raw/scheduler_apply_deterministic_validation.jinja`, `docs/testing/DEVTOOLS-TEMPLATES.local.md`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): add a guarded scheduler-apply execution service that runs scheduler selection and (optionally dry-run) writes the selected target to `input_select.ma_active_target` with explicit authority/reentrancy/debounce/option-membership safeguards and full decision/apply audit payloads. Also add a component-side handoff inventory scaffold surface that classifies legacy-dependent features and current component scaffold status for top-to-bottom migration planning. Runtime track disposition: compatibility-shimmed (legacy runtime ownership retained; no authority expansion). Custom-component track disposition: implemented (scheduler apply guard path + handoff inventory diagnostics scaffold). P1/P2/P3 impact: no source-of-truth ownership change; additive guarded-execution + migration-readiness observability only. README/wiki parity: no material repo-state change.

- Custom Component/Scheduler Decision Engine Service (`custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/sensor.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): add an explicit scheduler decision layer that scores and ranks targets from pluggable host resolution + feature profiles + empirical overlay data, and exposes deterministic selection results (ranked candidates, per-candidate score breakdown, selected target/reason, policy echo) through services and diagnostics attributes. Runtime track disposition: compatibility-shimmed (legacy scheduler behavior unchanged; remains authoritative runtime execution path). Custom-component track disposition: implemented (scheduler decision engine + diagnostics/service contract). P1/P2/P3 impact: no source-of-truth ownership change; additive scheduler intelligence and observability only. README/wiki parity: no material repo-state change.

- Custom Component/Empirical Feature-Profile Feed for Scheduler Decisions (`custom_components/spectra_ls/registry.py`, `custom_components/spectra_ls/sensor.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): add per-target normalized feature/capability profiles (integration/domain/device identity, supported-feature masks, observed capability surface, availability-quality/freshness signals) and an empirical overlay ingestion hook (`sensor.spectra_ls_feature_profile_db.profiles_json`) so scheduling can be driven by measured profile data instead of static heuristics. Extend diagnostics surfaces with profile coverage counts and target-level profile payload visibility for deterministic route/scoring policy inputs. Runtime track disposition: compatibility-shimmed (legacy runtime behavior unchanged in this slice). Custom-component track disposition: implemented (profile + empirical scheduler feed layer). P1/P2/P3 impact: no source-of-truth ownership change; scheduler-input determinism/observability hardening only. README/wiki parity: no material repo-state change.

- Custom Component/Pluggable Host-Module Resolver Feed for Routing Scheduler (`custom_components/spectra_ls/registry.py`, `custom_components/spectra_ls/sensor.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): replace single-path host derivation assumptions in component registry snapshots with deterministic host-type module resolution (`wiim`, `linkplay_tcp`, `generic`) and explicit candidate tracing (`source`, `host`, `resolved`) so host selection can feed larger scheduler logic as a stable, explainable contract. Add a dedicated diagnostics sensor exposing host-module counts, resolved/unresolved summary, and per-target host-resolution details for operator triage and scheduler readiness checks. Runtime track disposition: compatibility-shimmed (legacy runtime host path unchanged in this slice). Custom-component track disposition: implemented (pluggable resolver modules + deterministic diagnostics surface). P1/P2/P3 impact: no source-of-truth ownership change; scheduler-feed determinism and observability hardening only. README/wiki parity: no material repo-state change.

- HA/MA Target-Options Script Recovery + Fail-Safe Candidate Injection (`packages/ma_control_hub/script.inc`): resolve `script.ma_update_target_options` hard-fail (`from_json got invalid input`) by removing the brittle JSON re-parse step from helper option assembly and restoring a non-throwing option write path. Add a fail-safe second-pass helper options injection that forces `['none', detected_candidate]` when a valid detected receiver still fails to appear in helper options after the primary update, preventing startup deadlock at `input_select.ma_active_target=none`. Runtime track disposition: implemented (script recovery + startup fail-safe). Custom-component track disposition: compatibility-shimmed (no component behavior mutation; consumes stabilized helper contract). P1/P2/P3 impact: no source-of-truth ownership change; startup target-option reliability hardening only. README/wiki parity: no material repo-state change.

- HA/MA Target-Options Native List Write Hardening (`packages/ma_control_hub/script.inc`): fix a helper option-write shape edge where computed option lists could be rendered as string payloads during `input_select.set_options`, leaving `input_select.ma_active_target` stuck at baseline `['none']` even when a valid detected receiver candidate existed. Normalize final options to JSON and write back as a native list (`from_json`) so detected fallback candidates reliably land in helper options during startup refresh. Runtime track disposition: implemented (write-shape hardening). Custom-component track disposition: compatibility-shimmed (no component behavior mutation; consumes stabilized helper contract). P1/P2/P3 impact: no source-of-truth ownership change; startup target-option application reliability hardening only. README/wiki parity: no material repo-state change.

- HA/MA Active-Target Selection Pipeline Unstick + Diagnostic Template (`packages/ma_control_hub/script.inc`, `docs/testing/raw/ma_target_selection_pipeline_diagnostic.jinja`): fix post-reboot `input_select.ma_active_target` stalls at `none` by including a valid detected receiver entity in selectable target options even when MA discovery payloads do not expose direct `ip_address`, allowing the existing host-derivation alias/member fallback path to resolve control hosts instead of failing early at option construction. Add a focused copy/paste diagnostic template that surfaces option-building gates (`rooms`, `detected receiver`, `ma_players`, active/pick readiness) for deterministic startup triage. Runtime track disposition: implemented (selection pipeline hardening + diagnostics). Custom-component track disposition: compatibility-shimmed (no component behavior mutation; consumes stabilized legacy surface). P1/P2/P3 impact: no source-of-truth ownership change; startup target-selection reliability hardening only. README/wiki parity: no material repo-state change.

- HA/Room-Map Ghost Entity Filter + Kitchen Triage Hardening (`packages/spectra_ls_setup.yaml`, `docs/testing/raw/kitchen_target_flow_triage_legacy_component.jinja`): prevent stale/non-existent media player IDs from entering `sensor.spectra_ls_rooms_raw` room contracts by requiring helper-mapped room entity existence before room rows are emitted, and harden Kitchen-focused triage to avoid selecting unavailable/unknown friendly-name matches as valid focus entities (reporting them as stale candidates instead). Runtime track disposition: implemented (contract filtering hardening). Custom-component track disposition: compatibility-shimmed (consumes cleaner upstream room contract; no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership change; startup routing correctness hardening only. README/wiki parity: no material repo-state change.

- Validation/Kitchen Speakers Focused Live-State Triage Template (`docs/testing/raw/kitchen_target_flow_triage_legacy_component.jinja`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`): add a host-agnostic copy/paste diagnostic template that performs one-screen PASS/WARN triage for `Kitchen Speakers` target flow (`target pick -> host derive -> route decision`) across both legacy runtime (`ma_control_hub`) and custom-component shadow route-trace surfaces (`spectra_ls`). Template verifies discovery-first host matching (direct entity/member and alias candidate paths) without hardcoded IP assumptions and highlights legacy/component parity drift signals for reboot-time validation. Runtime track disposition: compatibility-shimmed (diagnostics-only template, no runtime contract mutation). Custom-component track disposition: compatibility-shimmed (diagnostics-only template, no component behavior mutation). P1/P2/P3 impact: no source-of-truth ownership change; validation tooling hardening only. README/wiki parity: no material repo-state change.

- HA/MA Active-Target Host Resolution Lock-Step Fix — Direct Entity Alias Query for Wrapper Targets (`packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/registry.py`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden control-host derivation when selected helper targets are wrapper/group entities that do not expose `ip_address` directly (for example label/friendly wrapper entities). Runtime host resolution now performs a direct alias-entity lookup pass (detected receiver, active meta, now-playing, active-player entity, and friendly-name-matched media_player candidates with `ip_address`) before falling back to static room `tcp_host`. Component registry mirrors the same discovery posture by expanding host discovery to include entity members/group-members and friendly-name alias candidates so diagnostics and route trace stay parity-aligned with runtime behavior. Runtime track disposition: implemented (direct entity lookup + alias/member fallback hardening). Custom-component track disposition: implemented (registry host discovery parity alignment). P1/P2/P3 impact: no ownership/source-of-truth flip; routing correctness hardening only. README/wiki parity: no material repo-state change.

- HA/MA Fast Backend Profile Switch for Beta↔Stable Testing (`packages/ma_control_hub/input_select.inc`, `packages/ma_control_hub/input_text.inc`, `packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/coordinator.py`, `README.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a discovery-safe operator quick-switch contract for Music Assistant backend selection via `input_select.ma_server_profile` (`beta`, `stable`, `manual`), profile URL helpers (`input_text.ma_server_url_beta`/`input_text.ma_server_url_stable`), profile-aware API resolution (`sensor.ma_api_url`), and explicit runtime visibility surface (`sensor.ma_server_profile_effective`). Component diagnostics now track the active MA backend profile/effective URL surface for parity visibility in the custom-component path. Runtime track disposition: implemented (profile switch + effective URL visibility). Custom-component track disposition: compatibility-shimmed (diagnostics visibility + legacy-surface watch updates; no ownership flip). P1/P2/P3 impact: no source-of-truth ownership change; additive operator workflow hardening for bounded beta/stable testing only. README/wiki parity: completed in same change set.

- ESPHome/Volume Pot Step Granularity + Transport Log Cadence Tuning (`esphome/spectra_ls_system/substitutions.yaml`): increase volume pot jump-step threshold used by the universal pot apply/defer/send handler from single-step to `3` (`volume_pot_jump_steps: "3"`) so TCP volume dispatch occurs on larger movement deltas instead of every 1-step delta, and raise Arylic TCP log-throttle interval (`arylic_tcp_log_interval_ms: "800"`) to reduce enqueue/TX log chatter while preserving queue/coalescing behavior. Runtime track disposition: implemented (substitutions tuning only). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; runtime control-feel/log-shaping tuning only. README/wiki parity: no material repo-state change.

- ESPHome/Host Cache Refresh + Wrong-Subnet Guardrail Hardening (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`): harden dynamic host-cache behavior so resolved control hosts are refreshed on active-target changes (cache invalidation + pending-refresh window), and transient invalid host payloads during refresh windows keep bounded last-good cache instead of immediate hard clear. This reduces stale-route stickiness and startup churn while preserving fail-closed behavior when refresh grace expires. Runtime stack sweep confirmed no active `192.168.1.x` literals in `esphome/spectra_ls_system/**` or active `packages/**` routing path. Runtime track disposition: implemented (cache refresh semantics + resilience hardening). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; runtime transport-host cache correctness and resilience only. README/wiki parity: no material repo-state change.

- ESPHome/Audio Pot Pipeline Normalization (volume aligned to EQ universal handler) (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`): implement ESP stack normalization by replacing dedicated volume-pot dispatch lambda behavior with a shared universal pot apply script used by EQ channels and volume channel, so new potentiometer channels can be attached to the same apply/defer/send flow. Volume pot on-value handling now publishes display/cache signals and delegates apply/defer/send decisions through the universal handler (`channel=3`), and deferred volume sends now route via `arylic_set_volume` for parity with EQ’s script-driven path. As part of staged cleanup, explicit inline notes mark remaining volume-specific dead-end branches for later retirement after verification soak. Runtime track disposition: implemented (ESP pot pipeline refactor, staged cleanup). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; runtime control pipeline normalization only. README/wiki parity: no material repo-state change.

- HA/ESP Wrong-Subnet Control-Host Resolution Fix (`packages/spectra_ls_setup.yaml`, `packages/ma_control_hub/template.inc`): fix stale host override precedence that could route ESP volume/EQ TCP dispatch to outdated manual host values (for example `192.168.1.x`) even when live target/meta/member `ip_address` discovery resolves current-network hosts (`192.168.10.x`). Room contract host derivation now prefers live entity/meta/member IP discovery first and only falls back to configured helper host when discovery is unavailable; MA control-host resolution now prefers discovered meta/member host candidates before `rooms[].tcp_host` fallback for active target lookup. Runtime track disposition: implemented (host resolution correctness hardening). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; runtime host-route correctness + ESP volume transport reliability hardening only. README/wiki parity: no material repo-state change.

- RP2040/Known-Good Tuning Baseline Lock (`CIRCUITPY/RP-LEGEND.md`, `docs/circuitpy/RP-LEGEND.md`, `CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): record the validated tactile-response baseline from live operator verification (full `0→100` sweep reachability plus responsive small-movement behavior) and pin the key analog constants with explicit inline notes so future tuning can restore this known-good profile deterministically. Runtime track disposition: implemented (RP note/documentation lock-in only; no behavior change). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; documentation + inline guidance only. README/wiki parity: no material repo-state change.

- RP2040/Pot Slow-Movement Responsiveness Tuning (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): reduce low-velocity dead-zone behavior at beginning/end travel by tightening ingress gates and endpoint clamp policy for named pots (volume + EQ): lower raw reprocess gate, reduce per-pot output min-change thresholds, raise slow-EMA responsiveness for named pots, and narrow zero-assume clamp so tiny movements near floor/top are not swallowed. Runtime track disposition: implemented (RP filtering/smoothing responsiveness tuning). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; RP tactile-response tuning only. README/wiki parity: no material repo-state change.

- RP2040/Volume Calibration Hard Reset (`CIRCUITPY/calibration.json` live runtime): remove volume calibration entries (`external.pot_a2` and `external.volume_pot`) per operator request to return volume path to uncalibrated/default scaling baseline for fresh validation. This intentionally discards prior volume min/max mapping so follow-up behavior can be measured without calibration bias. Runtime track disposition: implemented (RP calibration reset). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; runtime calibration-data reset only. README/wiki parity: no material repo-state change.

- RP2040/Volume Calibration Realignment from Full-Sweep Evidence (`CIRCUITPY/calibration.json` live runtime): update external volume calibration range using operator-provided full top-to-bottom sweeps (observed raw floor near `16`, observed peak near `16384`) to remove early-saturation behavior (`~91%` near top) and improve endpoint truthfulness. Legacy key (`pot_a2`) and named key (`volume_pot`) are aligned to the same range for compatibility during transition. Runtime track disposition: implemented (RP calibration-data correction). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; calibration-data correction only. README/wiki parity: no material repo-state change.

- RP2040/Large-Jump Instant Catch-Up (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): reduce residual lag during fast knob throws by snapping filtered value directly to calibrated target for named pots (volume + EQ) when raw delta exceeds the jump-reset threshold. This keeps high-velocity motion from trailing through EMA while preserving existing stable-stop/tail controls and packet contracts. Runtime track disposition: implemented (RP response tuning). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; RP responsiveness tuning only. README/wiki parity: no material repo-state change.

- RP2040/Stable-Band Snap Tightening (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): tighten post-stop drift elimination by widening the named-pot raw-stability band and reducing stable-sample snap requirement to one confirmed stable sample, so filtered output snaps to calibrated target sooner after motion ceases. Applies to volume + EQ named pots only; packet contracts unchanged. Runtime track disposition: implemented (RP responsiveness hardening). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; RP tuning only. README/wiki parity: no material repo-state change.

- RP2040/Stable-Sample Snap Tail Elimination (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): add named-pot stable-sample snap logic (volume + EQ) so once raw input is confirmed stable for a short bounded sample count, filtered output snaps directly to calibrated value to end remaining post-stop EMA drift. This complements prior median/end-stop tightening and preserves RP packet contracts. Runtime track disposition: implemented (RP response hardening). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; RP responsiveness tuning only. README/wiki parity: no material repo-state change.

- RP2040/Median-Lag Tightening for Fast Stops (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): further reduce residual stop-tail artifacts where `raw` reaches endpoint but `median` briefly lags by adding (1) named-pot history reset on large raw jumps and (2) endpoint-aware median bypass for named pots (volume + EQ). This keeps RP response aligned to actual knob stop intent without changing protocol contracts or ESP behavior. Runtime track disposition: implemented (RP ingest tuning). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; RP responsiveness hardening only. README/wiki parity: no material repo-state change.

- RP2040/Stop-Hold Strengthening + Duplicate Log Noise Reduction (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): tighten post-stop behavior by bypassing median-history drift for named pots (volume + EQ) when raw input is stable, so stop-state output no longer continues stepping due to stale median window carryover. Also disable duplicate POT direction line printing by default while keeping analog stream logs enabled for single-line traceability. Runtime track disposition: implemented (RP stop-stability + diagnostics-noise tuning). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; RP responsiveness/observability tuning only. README/wiki parity: no material repo-state change.

- RP2040/Stable-Input Tail-Cut Tuning (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): reduce continued output movement after knob stop by adding a bounded stable-input tail-cut path for named pots (volume + EQ). When raw input is effectively stationary and filtered-vs-calibrated delta is within a small band, RP snaps filtered value to calibrated target to end EMA drift without removing normal smoothing during motion. Runtime track disposition: implemented (RP stop-stability tuning). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; RP responsiveness/stability tuning only. README/wiki parity: no material repo-state change.

- RP2040/Volume Micro-Response Tuning (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): reduce volume output min-change gate from `3` to `2` while keeping EQ path unchanged and preserving shared pipeline structure. This improves low-amplitude knob responsiveness on RP with minimal risk and no protocol contract change. Runtime track disposition: implemented (RP ingress tuning). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; RP responsiveness tuning only. README/wiki parity: no material repo-state change.

- RP2040/Reversal Tail-Trim Tuning (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): trim residual EMA tail after strong direction reversals by introducing a short post-reversal fast-filter window for named pots (volume + EQ). This preserves shared volume/EQ ingest parity while reducing post-turnaround lag feel without changing packet contracts. Runtime track disposition: implemented (RP response tuning). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; RP responsiveness tuning only. README/wiki parity: no material repo-state change.

- RP2040/Analog Reversal De-Lag Guard (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): reduce perceived rebound/rubber-band behavior on quick direction changes by resetting per-channel median history for named pots (volume + EQ) when a strong raw-direction reversal is detected. This prevents stale opposite-direction samples from dominating the next median step while preserving shared volume/EQ pipeline parity on RP. Runtime track disposition: implemented (RP ingest smoothing hardening). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; RP ingest responsiveness hardening only. README/wiki parity: no material repo-state change.

- RP2040/Volume+EQ Logging Instrumentation (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): enable terminal-visible analog stream and named-pot direction logging for live verification of perceived volume rubber-banding effects, with volume and EQ channels logged under the same RP pipeline. This slice is observability-only (no packet contract or control-path routing changes). Runtime track disposition: implemented (RP ingress observability on). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; diagnostics visibility only. README/wiki parity: no material repo-state change.

- RP2040/Volume Pipeline Parity (`CIRCUITPY/code.py`, `esphome/circuitpy/code.py`): remove remaining volume-only analog profile divergence in RP firmware so volume follows the same ingest/filter/gate profile class as EQ pots on each MCU path. This includes removing volume-only per-name median/EMA/threshold overrides and matching pot endpoint behavior under the shared named-pot rules. Runtime track disposition: implemented (RP ingress parity hardening). Custom-component track disposition: compatibility-shimmed (no component contract change). P1/P2/P3 impact: no source-of-truth migration change; RP ingress consistency hardening only. README/wiki parity: no material repo-state change.

- ESPHome/EQ Pipeline Parity Step 1 (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`): align EQ pot ingest behavior with volume pipeline guardrails by suppressing EQ pot-driven send-path execution while calibration is active (`!calibration_active` gate on RP2040 UART EQ pot handlers). This is a bounded behavior-parity cleanup on the ESP MCU only; no RP2040 protocol/payload contract changes in this step. Two-track disposition: runtime track implemented (ESP parity guard hardening); custom-component track compatibility-shimmed (no component contract mutation). P1/P2/P3 impact: no source-of-truth ownership change; runtime control-path consistency hardening only. README/wiki parity: no material repo-state change.

## 2026-04-22

- Custom Component P8-S03 Fast Remap Presets + Effective Mapping Visibility (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/config_flow.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/services.yaml`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add operator-focused input remap presets (`media_default`, `scene_focus`, `target_navigation`, `custom`) in HA integration settings and service contract, preserve backward-compatible manual overrides, and expose active preset/effective mapping summary in diagnostics readiness surfaces for quick verification. Two-track disposition: runtime track compatibility-shimmed (no ownership expansion); component track implemented (fast remap UX + observability). P1/P2/P3 impact: no source-of-truth ownership change; additive settings UX and visibility hardening only.

- Plan Delta — Component-First Cutover Semantics + Input Remap UX Priority (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`): clarify that component development is primary while legacy remains an explicit rollback-safe authority baseline for specific compatibility contracts, and activate a new Phase-8 follow-on slice for fast operator input-remapping UX in HA settings (quick remap workflow + clearer “where to configure” guidance). Two-track disposition: runtime track compatibility-shimmed (no ownership expansion); component track implemented for plan/UX priority definition. P1/P2/P3 impact: no source-of-truth ownership change; roadmap/operator-flow clarity and next-slice prioritization only.

- Custom Component UX + Diagnostics Tightening (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/config_flow.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/diagnostics.py`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): improve Control Center setup defaults and visibility by adding scene-aware options defaults, stricter scene normalization, richer readiness diagnostics, and a dedicated readiness sensor surface. Two-track disposition: runtime track compatibility-shimmed (no runtime ownership or contract expansion); component track implemented (UX/readiness diagnostics improvement). P1/P2/P3 impact: no source-of-truth ownership change; additive setup/observability hardening only.

- P8-S02 Writing Standard Link Micro-Pass Run-11 (`docs/wiki/DOCUMENTATION-WRITING-STANDARD.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): upgrade remaining example/path references in the writing standard to explicit link-aware references while preserving style-guide semantics. Two-track disposition: runtime track unchanged/sealed compatibility baseline; component track unchanged/primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; documentation quality standard hardening only. README parity: no material repo-state change.

- P8-S02 Residual Policy Link Cleanup Run-10 (`docs/wiki/Contributing-Workflow.md`, `docs/wiki/Wiki-Content-Scope-Policy.md`, `docs/wiki/Install-on-Your-Own-HA.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): convert remaining policy/process literal path mentions to explicit links where operator navigation benefits and preserve code-literal path examples only where they are contract notation. Two-track disposition: runtime track unchanged/sealed compatibility baseline; component track unchanged/primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; documentation navigation quality hardening only.

- P8-S02 Process Navigation Residual Run-9 (`docs/wiki/Contributing-Workflow.md`, `docs/wiki/Operations-Runbooks.md`, `docs/wiki/Release-and-Changelog-Process.md`, `docs/wiki/README.md`, `docs/wiki/Home.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): convert remaining process/navigation references to link-first paths for parity, release hygiene, and wiki publishing operations. Two-track disposition: runtime track unchanged/sealed compatibility baseline; component track unchanged/primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; documentation navigation quality hardening only.

- P8-S02 Link Consistency Residual Run-8 (`docs/wiki/Control-Contracts-and-Scope-Paths.md`, `docs/wiki/Control-Surface-Inputs-and-Expanders.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/wiki/Custom-Component-Setup-Roadmap-Stub.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): normalize remaining contract/setup/control-surface wiki references to explicit clickable links and tighten canonical source routing in residual high-use pages. Two-track disposition: runtime track unchanged/sealed compatibility baseline; component track unchanged/primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; documentation navigation quality hardening only.

- P8-S02 Link Consistency High-Traffic Run-7 (`docs/wiki/System-Architecture.md`, `docs/wiki/Welcome-README-and-Bug-Workflow.md`, `docs/wiki/Install-on-Your-Own-HA.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): convert remaining high-traffic wiki orientation/install/architecture pages to link-first references for faster navigation and fewer dead-path callouts. Two-track disposition: runtime track unchanged/sealed compatibility baseline; component track unchanged/primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; documentation navigation quality hardening only.

- P8-S02 Link Consistency Long-Tail Run-6 (`docs/wiki/Getting-Started.md`, `docs/wiki/Discussions-and-Projects-Workflow.md`, `docs/wiki/Release-and-Changelog-Process.md`, `docs/wiki/Wiki-Content-Scope-Policy.md`, `docs/wiki/Contributing-Workflow.md`, `docs/wiki/Operations-Runbooks.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): convert key workflow/reference callouts to explicit clickable links and normalize page metadata/version cadence across high-traffic process pages. Two-track disposition: runtime track unchanged/sealed compatibility baseline; component track unchanged/primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; documentation navigation quality hardening only.

- P8-S02 Navigation + Reference Consistency Run-5 (`README.md`, `docs/wiki/Home.md`, `docs/wiki/_Sidebar.md`, `docs/wiki/README.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): align top-level and wiki navigation with link-first reference paths, remove low-value process narration in page-intent notes, and strengthen canonical source pointers for operators/contributors. Two-track disposition: runtime track unchanged/sealed compatibility baseline; component track unchanged/primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; documentation discoverability and execution clarity hardening only.

- P8-S02 Documentation Polish — Wiki Core Pages Run-4 (`docs/wiki/Operations-Runbooks.md`, `docs/wiki/System-Architecture.md`, `docs/wiki/Contributing-Workflow.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): update core workflow/architecture/contributor wiki pages for concise task flow and stronger in-repo references. Two-track disposition: runtime track unchanged/sealed compatibility baseline; component track unchanged/primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; documentation usability and execution clarity hardening only.

- P8 Ledger Consistency Correction (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): update summary-table row for `P8-S01` from stale `Active / In Progress` wording to validated closeout wording that matches captured pre/in/post PASS evidence and promotion state. Two-track disposition unchanged: runtime sealed compatibility baseline, component primary growth lane.

- P8-S02 Documentation Polish — Top-Level Docs Run-2 (`README.md`, `docs/README.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): tighten top-level docs for clearer action flow and reference routing. Two-track disposition: runtime track unchanged/sealed compatibility baseline; component track unchanged/primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; documentation usability hardening only.

- P8-S02 Validation — Wiki Sync Dry-Run Pass (`docs/wiki/**`, `.github/workflows/wiki-sync.yml` process): completed local no-push wiki sync dry-run by cloning wiki repo and staging `docs/wiki/` content into a temp workspace; result `dry_run_ok` with 17 staged wiki-page deltas relative to current wiki branch state, confirming publish-ready source updates and no clone/sync blocker.

## 2026-04-21

- Documentation Polish — Top-Level Contributor Guide Run-1 (`CONTRIBUTING.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): tighten contributor guidance for clearer action flow and branch/migration boundaries while preserving policy constraints (sealed legacy runtime posture, component-primary net-new lane, no active `menu-only`/`control-py` development on `main`). Two-track disposition: runtime track unchanged/sealed; component track unchanged/primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; contributor UX clarity only.

- Dual-Track Execution — Sidebar Usability + Documentation Polish Run-1 (`custom_components/spectra_ls/sensor.py`, `docs/wiki/Getting-Started.md`, `docs/wiki/Install-on-Your-Own-HA.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`):
  - Sidebar/component track: add a dedicated Control Center last-attempt status sensor so operators can see mapped input execution outcomes (`dry_run_ok`, `blocked_*`, `applied_scene_turn_on`, etc.) directly without digging through large shadow attributes.
  - Documentation/wiki track: tighten high-traffic onboarding/install/setup pages into task-first structure with explicit expected outcomes and practical failure actions.
  Two-track disposition: runtime track unchanged/sealed compatibility baseline; component track receives additive operator visibility improvement while docs/wiki quality lane executes. P1/P2/P3 impact: no source-of-truth ownership change; UX and documentation clarity hardening only.

- Program/Phase-8 P8-S02 Documentation + Wiki Quality Overhaul Activation (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/wiki/README.md`, `docs/wiki/Home.md`, `docs/wiki/_Sidebar.md`, `docs/wiki/DOCUMENTATION-WRITING-STANDARD.md`): activate a bounded documentation-quality lane to improve operator/contributor docs with direct, native-English style, reduce AI-sounding phrasing, and keep wiki pages task-oriented with clear outcomes, decision paths, and practical troubleshooting steps. Adds a reusable writing standard + page pattern so edits stay consistent across the wiki and repo docs. Two-track disposition: runtime track unchanged and sealed for compatibility/rollback; component track unchanged as primary product-growth lane. P1/P2/P3 impact: no source-of-truth ownership change; documentation clarity/usability hardening only. README/wiki parity: wiki sources updated in same change set.

- Branch-Policy Clarification — `menu-only` Legacy Status (`CONTRIBUTING.md`, `docs/developer/DEVELOPER-INSTRUCTIONS.md`): explicitly document that `menu-only` is a legacy/stabilized historical branch context and not an active implementation/run target for current work on `main`. This aligns branch guidance with the existing `control-py` legacy posture and prevents contributors from treating `menu-only` as a current development lane. Two-track disposition: runtime track remains sealed compatibility baseline; component track remains primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; contributor workflow clarity only. README/wiki parity: no material operator workflow change.

- README Ecosystem + Roadmap Clarity Refresh (`README.md`): expand audio ecosystem wording to include Sendspin-class endpoints as a roadmap target and introduce a dedicated README roadmap section for the active Spectra Control Center build direction (instead of burying roadmap bullets under documentation links). Two-track disposition: runtime track remains sealed compatibility baseline; component track remains primary growth lane and roadmap execution focus. P1/P2/P3 impact: no source-of-truth ownership change; end-user direction clarity only. README/wiki parity: README updated in same change set.

- Validation/Phase-8 P8-S01 Run-2 In-Window PASS + Run-3 Post-Window PASS + Promotion (`docs/testing/raw/p8_s01_legacy_seal_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record completed in/post evidence packet with `Status=PASS`, `P8-S01 readiness=READY`, `gate_score=4/4` for in-window (`2026-04-21 21:22:08.276673-07:00`) and post-window (`2026-04-21 21:22:20.668596-07:00`) captures, both with rollback-safe clean baseline (`monitor_source_sensor=sensor.shadow_active_target`, `authority_mode=legacy`, `route_decision=route_linkplay_tcp`, `contract_valid=true`, `missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`, freshness within threshold). Promote `P8-S01` to **Validated** with pre/in/post packet complete and no stop-condition triggers. Two-track disposition: runtime track remains sealed rollback-safe compatibility baseline with no net-new ownership growth; component track completes the legacy-seal governance/readiness lane and remains primary for forward control-plane growth. P1/P2/P3 impact: no source-of-truth ownership flip in this closeout packet; evidence-backed promotion only. README/wiki parity: no material operator workflow change.

- README/Contributor Direction Refresh — Control Center Primary + Control-Py Legacy Retirement Messaging (`README.md`, `CONTRIBUTING.md`, `docs/developer/DEVELOPER-INSTRUCTIONS.md`): clarify that active product development is focused on the Home Assistant Spectra Control Center program in `custom_components/spectra_ls`, and that `control-py` is a legacy exploration path (archived/not a development or run target on `main`). Two-track disposition: runtime track remains sealed compatibility baseline; component track remains primary growth lane. P1/P2/P3 impact: no source-of-truth ownership change; contributor/operator direction clarity only. README/wiki parity: README updated in same change set.

- Governance/Docs Legacy-Seal Positioning Sweep (`README.md`, `docs/README.md`, `docs/developer/DEVELOPER-INSTRUCTIONS.md`, `CONTRIBUTING.md`, `.github/pull_request_template.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): publish explicit GitHub-facing and developer-facing language that the legacy runtime path is now a sealed rollback-safe baseline and the custom component is the primary path for net-new feature/control-plane growth. Adds plain-English contribution and review rules so PRs are evaluated against legacy-seal boundaries (no net-new ownership expansion in runtime path without explicitly approved bounded slice + evidence packet). Two-track disposition: runtime track remains compatibility-sealed; component track remains primary for forward feature work. P1/P2/P3 impact: no source-of-truth ownership flip in this docs sweep; governance clarity and execution discipline only. README/wiki parity: README updated; wiki parity remains no material operator workflow change.

- Validation/Phase-8 P8-S01 Run-1 Pre-Window PASS Evidence Capture (`docs/testing/raw/p8_s01_legacy_seal_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record first post-Phase-7 stabilization pre-window monitor artifact as `Status=PASS`, `P8-S01 readiness=READY`, `gate_score=4/4` (`2026-04-21 21:18:39.504385-07:00`) with rollback-safe baseline and clean contract/parity snapshot (`monitor_source_sensor=sensor.shadow_active_target`, `authority_mode=legacy`, `route_decision=route_linkplay_tcp`, `contract_valid=true`, `missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`, freshness age `157.6s`). Two-track disposition: runtime track remains sealed rollback-safe baseline with no net-new ownership growth; component track remains active in governance/readiness lane. P1/P2/P3 impact: no source-of-truth ownership change; bounded evidence progression only. README/wiki parity: no material repo-state change.

- Program/Phase-8 P8-S01 Legacy-Seal Readiness Activation (`docs/testing/raw/p8_s01_legacy_seal_readiness_monitor.jinja`, `docs/testing/raw/p8_s01_legacy_seal_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): activate the first post-Phase-7 stabilization/governance lane as a fail-closed readiness gate with explicit rollback-safe baseline requirements (`authority_mode=legacy`), contract/parity cleanliness, rollback + single-writer verification, and legacy growth-freeze posture. Two-track disposition: runtime track remains sealed rollback-safe baseline with no net-new ownership growth; component track is active for governance/readiness execution with pre-window evidence pending. P1/P2/P3 impact: no source-of-truth ownership change; activation/evidence tooling and status-lane progression only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S04 Run-1 Closeout PASS Evidence + Promotion (`docs/testing/raw/p7_s04_phase_exit_closeout_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record phase-exit monitor closeout as `Status=PASS`, `P7-S04 readiness=READY`, `gate_score=4/4` (`2026-04-21 21:13:00.468462-07:00`) with rollback-safe baseline and clean contract/parity snapshot (`monitor_source_sensor=sensor.shadow_active_target`, `authority_mode=legacy`, `route_decision=route_linkplay_tcp`, `contract_valid=true`, `missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`, freshness age `60.0s`), no stop-condition triggers, and complete closeout packet evidence. Promote `P7-S04` to validated and mark Phase-7 closeout packet accepted. Two-track disposition: runtime track remains rollback-safe compatibility baseline at closeout; component track completes and validates phase-exit closeout packet lane. P1/P2/P3 impact: no immediate source-of-truth ownership change; evidence-backed phase/slice promotion only. README/wiki parity: no material repo-state change.

- Program/Phase-7 P7-S04 Phase-Exit Closeout Activation Packet (`docs/testing/raw/p7_s04_phase_exit_functionality_monitor.jinja`, `docs/testing/raw/p7_s04_phase_exit_closeout_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): activate a dedicated Phase-7 closeout lane to consolidate validated `P7-S01/P7-S02/P7-S03` evidence, capture a final rollback-safe baseline packet, and classify promotion readiness via deterministic gate scoring (`baseline`, `entry`, `safety`, `governance`). Two-track disposition: runtime track remains rollback-safe compatibility baseline during closeout capture; component track is active for phase-exit closeout packet execution/governance. P1/P2/P3 impact: no immediate source-of-truth ownership change; activation/evidence tooling and status-lane progression only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S03 Run-2 Post-Window PASS Evidence + Promotion (`docs/testing/raw/p7_s03_metadata_domain_flip_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record bounded metadata-domain post-window monitor as `Status=PASS`, `P7-S03 readiness=READY`, `gate_score=4/4` (`2026-04-21 21:07:08.345292-07:00`) with rollback-safe baseline and trial diagnostics clean (`monitor_source_sensor=sensor.shadow_active_target`, `authority_mode=legacy`, `route_decision=route_linkplay_tcp`, `metadata_prep_validation.verdict=PASS`, `ready_for_metadata_handoff=true`, `metadata_prep_validation.missing_required=0`, `write_controls.metadata_trial_last_attempt.status=dry_run_ok`, `window_id=window_id: p7s03-2026-04-21-run2`, `trial_gate_verdict=PASS`, contract/parity counters clean, freshness within threshold) and no stop-condition triggers. Promote `P7-S03` to validated with pre/in/post packet complete. Two-track disposition: runtime track remains rollback-safe metadata authority baseline with post-window proof accepted; component track completes bounded metadata-domain authority-flip execution lane. P1/P2/P3 impact: no immediate global ownership flip in this closeout evidence capture; evidence-backed slice promotion only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S03 Run-1 In-Window PASS Evidence Capture (`docs/testing/raw/p7_s03_metadata_domain_flip_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record bounded metadata-domain in-window monitor as `Status=PASS`, `P7-S03 readiness=READY`, `gate_score=4/4` (`2026-04-21 21:05:29.978417-07:00`) with clean probe posture (`monitor_source_sensor=sensor.shadow_active_target`, `authority_mode=legacy`, `route_decision=route_linkplay_tcp`, `metadata_prep_validation.verdict=PASS`, `ready_for_metadata_handoff=true`, `metadata_prep_validation.missing_required=0`, `write_controls.metadata_trial_last_attempt.status=dry_run_ok`, `trial_gate_verdict=PASS`, contract/parity counters clean, freshness within threshold) and no stop-condition triggers. Slice remains active with post-window capture pending before closeout eligibility. Two-track disposition: runtime track remains rollback-safe metadata authority baseline during bounded lane execution; component track maintains active metadata-domain execution evidence progression. P1/P2/P3 impact: no immediate ownership flip in this in-window evidence capture; execution progression only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S03 Run-1 Pre-Window PASS Evidence Capture (`docs/testing/raw/p7_s03_metadata_domain_flip_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record first bounded metadata-domain pre-window monitor as `Status=PASS`, `P7-S03 readiness=READY`, `gate_score=4/4` (`2026-04-21 21:03:00.345491-07:00`) with clean baseline + metadata readiness (`monitor_source_sensor=sensor.shadow_active_target`, `authority_mode=legacy`, `route_decision=route_linkplay_tcp`, `metadata_prep_validation.verdict=PASS`, `ready_for_metadata_handoff=true`, `metadata_prep_validation.missing_required=0`, contract/parity counters clean, freshness within threshold). Slice remains active pending in-window and post-window captures before closeout eligibility. Two-track disposition: runtime track remains rollback-safe metadata authority baseline; component track is authorized for bounded in-window metadata trial execution. P1/P2/P3 impact: no immediate ownership flip in this pre-window evidence capture; execution progression only. README/wiki parity: no material repo-state change.

- Program/Phase-7 P7-S03 Metadata-Domain Activation Packet (`docs/testing/raw/p7_s03_metadata_domain_flip_readiness_monitor.jinja`, `docs/testing/raw/p7_s03_metadata_domain_flip_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): activate the next bounded Phase-7 execution lane for metadata-domain authority-flip validation with runnable pre/in/post monitor + checklist artifacts and synchronized roadmap ledgers. Slice scope is metadata trial path (`spectra_ls.metadata_write_trial`) under strict single-writer + rollback-safe controls with dry-run-first posture and explicit stop conditions. Two-track disposition: runtime track remains rollback-safe metadata authority baseline outside bounded windows; component track is active for bounded metadata-domain execution evidence progression. P1/P2/P3 impact: no immediate global ownership cutover in this activation packet; runbook/evidence activation only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S02 Run-1 Post-Window PASS Evidence + Promotion (`docs/testing/raw/p7_s02_domain_flip_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record post-window monitor as `Status=PASS`, `P7-S02 readiness=READY`, `gate_score=4/4` (`2026-04-21 20:57:00.162663-07:00`) with rollback-safe baseline verified (`monitor_source_sensor=sensor.shadow_active_target`, `authority_mode=legacy`, `route_decision=route_linkplay_tcp`, `missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`, freshness within threshold) and no stop-condition triggers; promote `P7-S02` to validated with Run-1 pre/in/post PASS packet complete. Two-track disposition: runtime track remains compatibility/rollback baseline with bounded-lane proof accepted; component track completes first bounded authority-flip execution lane and advances to next-slice readiness. P1/P2/P3 impact: no immediate global ownership cutover recorded in this entry; evidence-backed slice closeout/promotion only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S02 Run-1 In-Window PASS Evidence Capture (`docs/testing/raw/p7_s02_domain_flip_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record bounded routing/selection in-window monitor as `Status=PASS`, `P7-S02 readiness=READY`, `gate_score=4/4` (`2026-04-21 20:55:41.000516-07:00`) with clean probe posture (`monitor_source_sensor=sensor.shadow_active_target`, `authority_mode=legacy`, `route_decision=route_linkplay_tcp`, `missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`, freshness within threshold) and no stop-condition triggers. Slice remains active with post-window capture pending before closeout eligibility. Two-track disposition: runtime track remains rollback-safe compatibility baseline during bounded lane execution; component track maintains active first authority-flip execution lane evidence progression. P1/P2/P3 impact: no immediate ownership flip recorded in this in-window evidence capture; execution progression only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S02 In-Window Action Path Clarification (`docs/testing/raw/p7_s02_domain_flip_readiness_monitor.jinja`, `docs/testing/raw/p7_s02_domain_flip_readiness_checklist.md`): remove implied dependency on a non-existent dedicated UI “control surface” and define the concrete operator path via HA Developer Tools (`States` + `Actions`) for in-window probes: set `input_select.ma_active_target` once (A -> B), call `spectra_ls.execute_control_center_input` once, confirm `route_linkplay_tcp`, then capture monitor output as `window_phase: in`. Two-track disposition: runtime track `compatibility-shimmed` (docs/runbook only, no runtime mutation); component track `compatibility-shimmed` (same execution clarification). P1/P2/P3 impact: no ownership change; operator-runbook precision hardening only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S02 Phase-Interpretation Clarification (`docs/testing/raw/p7_s02_domain_flip_readiness_monitor.jinja`, `docs/testing/raw/p7_s02_domain_flip_readiness_checklist.md`): make pre/in/post execution semantics explicit and operator-runnable by adding a `window_phase` selector in the monitor (`pre|in|post`) plus phase-specific “what to run” instructions in both monitor output and checklist. Clarifies that pre/post are baseline verification captures and in-window is a bounded routing/selection probe set (single target change + one mapped control event + route eligibility confirmation) with immediate monitor capture. Two-track disposition: runtime track `compatibility-shimmed` (docs/tooling clarification only, no runtime contract mutation); component track `compatibility-shimmed` (same evidence-runbook clarification). P1/P2/P3 impact: no ownership change; interpretation/runbook hardening only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S02 Run-1 Pre-Window PASS Evidence Capture (`docs/testing/raw/p7_s02_domain_flip_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record first bounded domain-flip pre-window monitor as `Status=PASS`, `P7-S02 readiness=READY`, `gate_score=4/4` (`2026-04-21 20:38:00.223973-07:00`) with clean baseline (`authority_mode=legacy`, `route_decision=route_linkplay_tcp`, `missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`, freshness within threshold). Slice remains active pending required in-window and post-window captures plus stop-condition audit before closeout eligibility. Two-track disposition: runtime track remains rollback-safe compatibility baseline; component track is authorized for bounded in-window execution. P1/P2/P3 impact: no immediate ownership flip recorded in this pre-window evidence capture; execution progression only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S02 Monitor Artifact-Path + Snapshot Trace Clarification (`docs/testing/raw/p7_s02_domain_flip_readiness_monitor.jinja`, `docs/testing/raw/p7_s02_domain_flip_readiness_checklist.md`): fix the monitor usage note to point at the authoritative raw checklist path (`docs/testing/raw/p7_s02_domain_flip_readiness_checklist.md`), add explicit monitor-source sensor visibility in snapshot output so fallback binding (`sensor.shadow_active_target` vs `sensor.spectra_ls_shadow_active_target`) is auditable during capture, and align checklist evidence schema with the same `monitor_source_sensor` capture field. Two-track disposition: runtime track `compatibility-shimmed` (docs/template-only, no runtime contract mutation); component track `compatibility-shimmed` (same evidence-surface clarification, no component behavior change). P1/P2/P3 impact: no source-of-truth ownership change; documentation/evidence clarity hardening only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S01 Run-2 PASS Evidence + Promotion + P7-S02 Activation (`docs/testing/raw/p7_s01_cutover_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/p7_s02_domain_flip_readiness_monitor.jinja`, `docs/testing/raw/p7_s02_domain_flip_readiness_checklist.md`): record rerun cutover-readiness success (`Status=PASS`, `P7-S01 readiness=READY`, `gate_score=4/4` at `2026-04-21 20:34:32.639010-07:00`) with clean baseline (`authority_mode=legacy`, `route_decision=route_linkplay_tcp`, `missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`, freshness within threshold), promote `P7-S01` to validated, and activate `P7-S02` as the first bounded authority-flip execution slice. Two-track disposition: runtime track remains rollback-capable compatibility baseline with bounded domain flip controls; component track advances as primary control-plane owner candidate under explicit cutover gates. P1/P2/P3 impact: no immediate ownership flip recorded in this entry; promotion + next-slice activation and evidence tooling only. README/wiki parity: no material repo-state change.

- Validation/Phase-7 P7-S01 Run-1 BLOCKED Evidence Capture + Remediation Packet (`docs/testing/raw/p7_s01_cutover_readiness_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record first cutover-readiness monitor result as blocked (`Status=FAIL`, `P7-S01 readiness=BLOCKED`, `gate_score=3/4` at `2026-04-21 20:33:32.373259-07:00`) with runtime baseline failure details (`route_decision=defer_no_target`, `unresolved_required=4`, `unresolved_sources=3`, `mismatches=2`) while governance gates remain true. Keep `P7-S01` active and publish deterministic rerun remediation steps (baseline refresh + target/route stabilization + repeat capture) before authorizing P7-S02 authority flips. Two-track disposition: runtime track remains compatibility/rollback baseline pending baseline stabilization; component track remains primary cutover candidate but blocked on baseline gate evidence. P1/P2/P3 impact: no ownership change; evidence + execution-governance refinement only. README/wiki parity: no material repo-state change.

- Program/Phase-7 Activation Packet + Final Pre-Cutover Hardening (`docs/program/PARALLEL-PROGRAM-PLAYBOOK.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/p7_s01_cutover_readiness_monitor.jinja`, `docs/testing/raw/p7_s01_cutover_readiness_checklist.md`, `custom_components/spectra_ls/coordinator.py`): publish post-P6 Plan Delta and activate `P7-S01` (cutover-readiness gate) with explicit two-track disposition, retirement-readiness criteria, and first operator-grade monitor/checklist artifacts; add low-risk component compute tuning by throttling bursty state-change snapshot refresh fanout in coordinator while preserving deterministic explicit refresh paths for service-driven validations. Two-track disposition: runtime track maintained as compatibility/rollback baseline pending bounded cutover flips; component track promoted to primary control-plane readiness with explicit retirement gate workflow. P1/P2/P3 impact: no source-of-truth ownership flip in this slice; governance + performance hardening for cutover execution readiness. README/wiki parity: no material repo-state change.

- Validation/Phase-6 P6-S01 Closeout PASS Evidence + Promotion Sync (`docs/testing/raw/p6_s01_control_center_foundation_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record operator-captured P6-S01 closeout monitor artifact (`2026-04-21 20:17:21.077121-07:00`) with `Status=PASS`, `P6-S01 readiness=READY`, `gate_score=6/6`, safe baseline (`authority_mode=legacy`, `route_decision=route_linkplay_tcp`, clean contract/parity: `missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`, freshness within threshold), and governance gates all true (read-only-first, compatibility, rollback, bounded authority). Two-track disposition: runtime track compatibility-preserved/validated (no ownership cutover), component/docs foundation lane promoted to validated closeout packet. P1/P2/P3 impact: no source-of-truth ownership change; evidence-backed slice promotion only. README/wiki parity: no material repo-state change.

- Validation/Phase-6 P6-S02 Closeout PASS Evidence + Promotion Sync (`docs/testing/raw/p6_s02_control_center_settings_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record operator-captured P6-S02 monitor artifact (`2026-04-21 20:15:35.644673-07:00`) with `Status=PASS`, `P6-S02 readiness=READY`, `gate_score=4/4`, safe baseline (`authority_mode=legacy`, `route_decision=route_linkplay_tcp`, clean contract/parity: `missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`, freshness within threshold), and settings-contract readiness (`schema_version=p6_s02.v1`, `settings_present=true`, `ready_for_customization=true`, `required_keys=8`). Two-track disposition: runtime track compatibility-preserved/validated (no ownership cutover), component/docs settings-contract lane promoted to validated closeout packet. P1/P2/P3 impact: no source-of-truth ownership change; evidence-backed slice promotion only. README/wiki parity: no material repo-state change.

- Validation/Phase-6 P6-S02 Settings-Contract Monitor + Checklist (`docs/testing/raw/p6_s02_control_center_settings_monitor.jinja`, `docs/testing/raw/p6_s02_control_center_settings_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add deterministic operator-grade validation artifacts for the control-center settings contract lane (`set_control_center_settings` + options-flow parity) with bounded gates for runtime safety baseline, settings-surface readiness, unresolved scene-binding visibility, read-only-first posture, and closeout evidence capture. Two-track disposition: runtime track compatibility-preserved (legacy authority baseline unchanged), component/docs track active evidence-capture hardening for P6-S02 closeout progression. P1/P2/P3 impact: no source-of-truth ownership change; validation/evidence tooling hardening only. README/wiki parity: no material repo-state change.

- Validation/Phase-6 P6-S03 Execution Skeleton Promotion Sync (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): promote P6-S03 from `Active` to `Validated` using bounded execution proof captured in the linked P6-S04 closeout packet (`2026-04-21 20:07:51.471030-07:00`), including deterministic dry-run semantics (`last_attempt.status=dry_run_ok`, `input_event=encoder_press`, `mapped_action=play_pause`, `last_attempt.dry_run=true`) under preserved safety baseline (`authority_mode=legacy`, clean contract/parity/freshness). Two-track disposition: runtime track compatibility-preserved/validated (no ownership cutover), component execution-lane skeleton validated for bounded control-input attempts. P1/P2/P3 impact: no source-of-truth ownership change; evidence-backed slice promotion only. README/wiki parity: no material repo-state change.

- Validation/Phase-6 P6-S04 Closeout PASS Evidence + Promotion Sync (`docs/testing/raw/p6_s04_control_input_execution_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record operator-captured P6-S04 monitor artifact (`2026-04-21 20:07:51.471030-07:00`) with `Status=PASS`, `P6-S04 readiness=READY`, `gate_score=5/5`, and deterministic execution semantics (`last_attempt.status=dry_run_ok`, `input_event=encoder_press`, `mapped_action=play_pause`, `last_attempt.dry_run=true`) under safe baseline (`authority_mode=legacy`, `route_decision=route_linkplay_tcp`, clean contract/parity: `missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`, fresh snapshot). Two-track disposition: runtime track compatibility-preserved/validated (no ownership cutover), component/docs execution-validation track promoted to validated closeout packet. P1/P2/P3 impact: no source-of-truth ownership change; evidence-backed slice promotion only. README/wiki parity: no material repo-state change.

- Governance/Diagnosis Policy Hardening (`.github/copilot-instructions.md`): add an explicit mandatory dual-path diagnosis rule for analysis/bugfix work — every issue triage must evaluate both runtime path (`packages/` + `esphome/`) and custom-component path (`custom_components/spectra_ls`), and must record a two-track disposition (`implemented`, `shimmed`, or `deferred with rationale`) even when only one path is changed. This prevents single-track debugging blind spots during parallel migration. P1/P2/P3 impact: no source-of-truth ownership change; process-rigor hardening only. README/wiki/roadmap parity: no material repo-state change.

- HA/MA REST JSON-Shape Hardening Follow-up (`packages/ma_control_hub/rest.inc`): adjust `MA Active Player` invalid-ID fallback to remain on `players/get` with a sanitized fallback player-id (active-target-derived, then sentinel) instead of switching command shape, preventing intermittent `homeassistant.components.rest.util` warnings (`JSON result was not a dictionary or list with 0th element a dictionary`) caused by response-shape drift during transient ID churn. Two-track disposition: runtime track hardened; custom-component track unchanged (no HA REST sensor dependency in `custom_components/spectra_ls`, coordinator/registry path remains read-only diagnostics surface). P1/P2/P3 impact: no source-of-truth ownership change; parser-shape resilience hardening only. README/wiki/roadmap parity: no material repo-state change.

- HA/MA Warning-Noise Hardening (`packages/spectra_ls_hw_status.yaml`, `packages/ma_control_hub/rest.inc`): suppress repeated `Already running` churn for `automation.spectra_ls_hw_status_refresh_now_playing` by switching to restart-mode execution, and guard `players/get` REST payload construction so transient invalid `sensor.ma_active_player_id_resolved` states no longer issue malformed MA calls that surface as cryptic `Error fetching data: unknown failed with unknown`. P1/P2/P3 impact: no source-of-truth ownership change; runtime warning-noise and transient-state resilience hardening only. README/wiki/roadmap parity: no material repo-state change.

- ESPHome/Audio Ingest + Warning-Noise Hardening (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`): reduce startup/reconnect warning churn by hardening HA numeric ingest for active-position/duration against transient non-numeric payloads (`unknown`/`unavailable`/`none`) and by rate-limiting repeated invalid control-host warning logs during short-lived target/host settling windows. P1/P2/P3 impact: no source-of-truth ownership change; runtime resilience/log-quality hardening only. README/wiki/roadmap parity: no material repo-state change.

- HA/MA Control-Hub Runtime Warning Hardening (`packages/ma_control_hub/template.inc`, `packages/ma_control_hub/automation.inc`): fix template render crashes caused by unguarded `| first` on empty room selections in `MA Active Control Path` and `MA Active Control Capable`, and guard startup `homeassistant.update_entity` call so missing `sensor.spectra_ls_rooms_raw` no longer emits forced-update warnings during package load timing windows. P1/P2/P3 impact: no source-of-truth ownership change; runtime diagnostics stability hardening only. README/wiki/roadmap parity: no material repo-state change.

- Validation/Phase-6 P6-S04 Control-Input Execution Monitor + Checklist (`docs/testing/raw/p6_s04_control_input_execution_monitor.jinja`, `docs/testing/raw/p6_s04_control_input_execution_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `README.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/wiki/Custom-Component-Setup-Roadmap-Stub.md`): add deterministic operator-grade validation artifacts for the new `spectra_ls.execute_control_center_input` lane with dry-run/read-only enforcement gates, execution-attempt diagnostics classification, and bounded closeout evidence fields for PASS/WARN/FAIL decisions. P1/P2/P3 impact: no source-of-truth ownership change; validation/evidence tooling hardening only. README/wiki/roadmap parity: completed in same change set.

- Custom Component/Phase-6 P6-S02 Control-Center Settings Contract Foundation (`custom_components/spectra_ls/config_flow.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `custom_components/spectra_ls/diagnostics.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/__init__.py`, `README.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/wiki/Custom-Component-Setup-Roadmap-Stub.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): begin first functional Phase-6 implementation by introducing an integration-level Control Center settings contract for button/encoder/scene customization with read-only-safe defaults and diagnostics visibility, while preserving legacy authority boundaries (`authority_mode=legacy`) and backward compatibility of existing parity surfaces. P1/P2/P3 impact: no source-of-truth ownership change; additive P6 configuration-layer capability only. README/wiki/roadmap parity: updated in the same change set.

- Validation/Phase-6 P6-S01 Baseline PASS Evidence Capture (`docs/testing/raw/p6_s01_control_center_foundation_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record first operator-captured P6-S01 monitor artifact (`2026-04-21 19:18:22.634239-07:00`) with `Status=PASS`, `P6-S01 readiness=READY`, `gate_score=6/6`, safe runtime baseline (`authority_mode=legacy`, `route_decision=route_linkplay_tcp`, `contract_valid=true`, `missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`, fresh snapshot), and all foundation governance gates true. Slice remains `Active` while bounded foundation tasks continue toward closeout packet completion. P1/P2/P3 impact: no source-of-truth ownership change; evidence progression only. README parity: no material repo-state change.

- Roadmap/Phase-6 P6-S01 Control-Center Foundation Activation (`docs/testing/raw/p6_s01_control_center_foundation_checklist.md`, `docs/testing/raw/p6_s01_control_center_foundation_monitor.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): activate the first Phase-6 slice for sidebar control-center foundation work with a deterministic operator checklist + readiness monitor template, explicit read-only-first launch posture, and bounded rollback/compatibility gates linked from both phase ledgers. P1/P2/P3 impact: no source-of-truth ownership change; post-Phase-5 planning/execution governance progression only. README parity: no material repo-state change.

- Validation/Phase-5 P5-S04 Phase Exit Closeout + Promotion (`docs/testing/raw/p5_s04_phase_exit_closeout_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record operator-captured P5-S04 monitor evidence (`2026-04-21 19:04:03.253466-07:00`) with `Status=PASS`, `Phase-5 closeout readiness=READY`, `gate_score=7/7`, safe authority baseline (`authority_mode=legacy`), clean parity/contracts (`missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`), and all governance gates true; promote `P5-S04` from `Active` to `Validated` in phase ledgers and mark Phase-5 closeout packet as accepted with P6 planning handoff readiness. P1/P2/P3 impact: no source-of-truth ownership change; evidence-backed phase closeout governance only. README parity: no material repo-state change.

- Validation/Phase-5 P5-S04 Phase-Exit Functionality Monitor Template (`docs/testing/raw/p5_s04_phase_exit_functionality_monitor.jinja`, `docs/testing/raw/p5_s04_phase_exit_closeout_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): publish a deterministic copy/paste monitor template for Phase-5 closeout gate classification (authority/parity/freshness + explicit phase-exit governance gates) and wire it into active P5-S04 checklist/roadmap execution references. P1/P2/P3 impact: no source-of-truth ownership change; diagnostics/evidence tooling hardening only. README parity: no material repo-state change.

- Roadmap/Phase-5 P5-S04 Phase-Exit Slice Activation (`docs/testing/raw/p5_s04_phase_exit_closeout_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): activate a dedicated Phase-5 closeout slice to consolidate cross-domain evidence from P5-S01/P5-S02/P5-S03, enforce final phase-exit gates (authority baseline, parity stability, rollback posture, and retirement-readiness disposition), and document the explicit handoff gate to Phase-6 control-center foundation work. P1/P2/P3 impact: no source-of-truth ownership change; governance/evidence progression only. README parity: no material repo-state change.

- ESPHome/Lighting Hold Debug Telemetry Retirement (`esphome/spectra_ls_system/packages/spectra-ls-lighting.yaml`): remove temporary diagnostics entities (`Lighting Hold Active`, `Lighting Hold Remaining Ms`) after timing validation evidence confirmed 3s hold behavior is stable. This restores a cleaner runtime entity surface while preserving the underlying lighting hold logic. P1/P2/P3 impact: no source-of-truth ownership change; diagnostics-only cleanup. README parity: no material repo-state change.

- ESPHome/Lighting Hold Debug Telemetry (`esphome/spectra_ls_system/packages/spectra-ls-lighting.yaml`): add diagnostics-category sensors to expose hold timing behavior in HA Developer Tools (`binary_sensor` hold-active state and `sensor` hold-remaining-ms) so post-adjust UI timing can be validated without log parsing. No control-path behavior or authority ownership changes. P1/P2/P3 impact: no source-of-truth ownership change; diagnostics visibility enhancement only. README parity: no material repo-state change.

- ESPHome/OLED Lighting Hold Render Continuity Fix (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`): prevent the observed post-adjustment visual artifact where the OLED could briefly blank before transitioning to Now Playing by treating the active lighting hold window (`lighting_return_to_audio_ms`) as an explicit lighting-render window in display-state computation and lighting-screen rendering guards. This keeps UI continuity after the last lighting adjustment input while preserving normal handoff to Now Playing when hold expires. P1/P2/P3 impact: no source-of-truth ownership change; display-state timing correctness hardening only. README parity: no material repo-state change.

- Validation/Phase-5 P5-S03 Run-1 Complete PASS Closeout + Promotion (`docs/testing/raw/p5_s03_lighting_orchestration_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record completed bounded lighting run-window evidence with all three captures in-place — pre-window PASS/READY (`2026-04-21 17:41:12.841574-07:00`), in-window PASS/READY (`2026-04-21 17:44:00.229390-07:00`), and post-window PASS/READY (`2026-04-21 17:44:45.600796-07:00`) — including explicit safe post-window authority disposition (`authority_mode=legacy`), clean contract/parity (`missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`), and no stop-condition triggers. Promote `P5-S03` from `Active` to `Validated` in Phase-5 ledgers. P1/P2/P3 impact: no source-of-truth ownership change; evidence-backed slice closeout/progression only. README parity: no material repo-state change.

- Validation/Phase-5 P5-S03 Run-1 Pre-Window PASS Evidence Capture (`docs/testing/raw/p5_s03_lighting_orchestration_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record first live lighting-domain monitor artifact (`2026-04-21 17:41:12.841574-07:00`) with `Status=PASS`, `Lighting readiness=READY`, `authority_mode=legacy`, `route_decision=route_linkplay_tcp`, clean contract/parity (`missing_required=0`, `unresolved_required=0`, `unresolved_sources=0`, `mismatches=0`), and healthy selector posture (`room=Living Room`, `target=All`, options ready). Control snapshot age remains informational only per corrected monitor semantics. P1/P2/P3 impact: no source-of-truth ownership change; bounded evidence capture progression only. README parity: no material repo-state change.

- Validation/Phase-5 P5-S03 Freshness Semantics Correction (`docs/testing/raw/p5_s03_lighting_functionality_monitor.jinja`, `docs/testing/raw/p5_s03_lighting_orchestration_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): fix a false-warning path where lighting verdicts were hard-gated by shared shadow snapshot `captured_at` age even when lighting selectors/contracts were healthy, which could misclassify bounded lighting interactions as stale/non-ready. Freshness is now rendered as control-plane context only (informational) and no longer blocks `Status`/`Lighting readiness` classification for P5-S03 gate-prep evidence. P1/P2/P3 impact: no source-of-truth ownership change; diagnostics semantics correction only. README parity: no material repo-state change.

- Validation/Phase-5 P5-S03 Run-1 Evidence Packet Wiring (`docs/testing/raw/p5_s03_lighting_orchestration_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add an operator-ready first-run packet and monitor-to-record field mapping for the active lighting slice so the first bounded P5-S03 evidence capture can be executed and classified deterministically from `p5_s03_lighting_functionality_monitor.jinja`. P1/P2/P3 impact: no source-of-truth ownership change; this is execution-readiness/evidence-capture hardening only. README parity: no material repo-state change.

- Validation/Phase-5 P5-S03 Lighting Functionality Monitor Template (`docs/testing/raw/p5_s03_lighting_functionality_monitor.jinja`, `docs/testing/raw/p5_s03_lighting_orchestration_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): publish a deterministic raw monitor template for lighting-domain gate-prep evidence (authority/route/contract/parity/freshness + room/target selector/options/entity mapping health) and wire it into the active P5-S03 run-window checklist as the primary live capture surface. P1/P2/P3 impact: no source-of-truth ownership change; this is diagnostics/evidence tooling for the active lighting slice. README parity: no material repo-state change.

- Roadmap/Phase-5 P5-S02 Promotion Decision + P5-S03 Activation (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`, `docs/testing/raw/p5_s03_lighting_orchestration_run_window_checklist.md`): record explicit status-lane promotion of `P5-S02` from `Active` to `Validated` based on consolidated Run-1/Run-2 closeout packet evidence, and activate `P5-S03` (lighting orchestration domain) as the next domain-isolated gate-prep slice with published operator run-window checklist. P1/P2/P3 impact: no source-of-truth ownership change; this is phased status progression with legacy authority boundaries retained by default. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Closeout Packet Preparation (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`): publish a consolidated closeout packet candidate that references bounded Run-1 and Run-2 PASS/READY evidence, complete trial-audit payloads, and explicit legacy post-window authority disposition, while keeping slice status `Active` until final promotion decision is explicitly recorded. P1/P2/P3 impact: no source-of-truth ownership change; this is evidence-packaging and closeout-readiness documentation hardening only. README parity: no material repo-state change.

- Roadmap/Phase-5 P5-S03 Lighting Slice Activation Packet (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/p5_s03_lighting_orchestration_run_window_checklist.md`): add a domain-isolated next-slice card and operator run-window checklist for optional lighting-orchestration cutover validation after P5-S02 closeout, including strict isolation/rollback/evidence gates and fail-closed stop conditions. P1/P2/P3 impact: no source-of-truth ownership change enacted by this planning publication. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Run-2 PASS/READY Evidence Capture (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`): record operator-captured Run-2 monitor artifact (`2026-04-21 17:25:39.987075-07:00`) with `authority_mode=legacy`, `route_decision=route_linkplay_tcp`, contract clean (`missing_required=0`, `unresolved_required=0`), metadata gate `PASS` (`9/9`), and metadata trial audit `status=dry_run_ok`, `window_id=p5s02-2026-04-21-run2`, `audit_payload_state=COMPLETE`, `trial_gate_verdict=PASS`, `eligible_for_closeout=true`, `missing_audit_fields=0`. P1/P2/P3 impact: no source-of-truth ownership change; this extends P5-S02 bounded-window evidence depth with fresh strict-comparator-grade run data while metadata ownership remains legacy. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Run-2 Targeting Correction (Discovery-First) (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`): remove install-specific `expected_target=media_player.spectra_ls_2` from Run-2 strict packet guidance and replace with dynamic monitor-derived targeting (`route_trace.active_target` from pre-window snapshot) plus optional omission when active target churn is expected. P1/P2/P3 impact: no source-of-truth ownership change; this restores anonymized/discovery-first execution posture while preserving strict fail-closed comparator semantics. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Run-2 Strict Evidence Packet (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`): publish the immediate next bounded-window execution packet after M1 promotion-gate satisfaction, including strict expected-target/route comparator guidance, deterministic run-id/correlation capture, and explicit pass criteria for the next fresh metadata trial record while preserving `legacy` authority baseline policy. P1/P2/P3 impact: no source-of-truth ownership change; this is execution-discipline hardening for continued P5-S02 gate-prep evidence. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02-M1 Fresh COMPLETE Dry-Run Evidence Capture (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`): record operator-captured monitor evidence (`2026-04-21 17:18:18.431382-07:00`) showing `Status=PASS`, `Metadata readiness=READY`, `authority_mode=legacy`, `route_linkplay_tcp`, contract clean (`missing_required=0`, `unresolved_required=0`), metadata gate `9/9`, and metadata-trial audit `dry_run_ok` with `M1 audit payload completeness=COMPLETE`, `trial_gate_verdict=PASS`, `eligible_for_closeout=true`. P1/P2/P3 impact: no source-of-truth ownership change; this satisfies the documented fresh dry-run promotion evidence gate for P5-S02-M1 while metadata ownership remains legacy. README parity: no material repo-state change.

- Repo/Main Legacy Path Retirement (`esphome/control-py/**`, `esphome/control-board-esp32-tcp.yaml`, `.gitignore`, `.github/copilot-instructions.md`, `.github/BRANCH-OPERATIONS.md`, `.github/SHARED-CONTRACT-CHECKLIST.md`, `docs/README.md`): remove legacy `control-py` and `control-board-esp32-tcp.yaml` from `main` Git tracking and add explicit ignore guards so these paths stay out of GitHub on `main`. Backup path used before untracking: `/mnt/homeassistant/_backup_2026-04-21/control-py-main-removal/`. Restore command: `cp -a /mnt/homeassistant/_backup_2026-04-21/control-py-main-removal/control-py /mnt/homeassistant/esphome/ && cp -a /mnt/homeassistant/_backup_2026-04-21/control-py-main-removal/control-board-esp32-tcp.yaml /mnt/homeassistant/esphome/`. P1/P2/P3 impact: no runtime source-of-truth change (active path remains `esphome/spectra_ls_system/**`); governance/process/docs updated to stop enforcing control-py parity on `main`. README parity: material repo-state change captured in docs index and workspace instructions.

- HA + ESPHome/LinkPlay Parser + Mixed-Protocol Hardening (`packages/ma_control_hub/template.inc`, `esphome/spectra_ls_system/packages/spectra-ls-system.yaml`, `esphome/control-py/packages/spectra-ls-system.yaml`): fix a remaining Jinja block-closure error in `MA Active Control Capable` (`missing endif`) that could still break template parsing, and add adaptive LinkPlay status polling fallback between HTTPS and HTTP in both active and control-py runtime tracks so mixed Arylic/Up2Stream transports can self-correct when a target only responds on one scheme. P1/P2/P3 impact: no source-of-truth ownership change enacted; parser stability + transport resilience hardening only. README parity: no material repo-state change.

- HA/Config Parse Recovery — `template.inc` Stray Jinja Token Fix (`packages/ma_control_hub/template.inc`): remove an accidental unmatched `{% endif %}` inside `binary_sensor.ma_no_control_capable_hosts` attributes that caused Home Assistant YAML parsing to fail (`found character '%' that cannot start any token`). This is a syntax recovery fix only; runtime contract behavior for host-capability diagnostics is unchanged. P1/P2/P3 impact: no source-of-truth ownership change enacted; parser-stability hotfix only. README parity: no material repo-state change.

- Custom Component/Phase-5 P5-S02 One-Shot Sequence Service (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/services.yaml`, `docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`, `docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add `spectra_ls.run_p5_s02_sequence` to execute authority baseline set + registry/contracts/route/metadata refresh + bounded metadata trial in one deterministic call, reducing operator sequencing drift while preserving fail-closed trial semantics and legacy authority boundaries. P1/P2/P3 impact: no source-of-truth ownership change enacted; P5 execution ergonomics/consistency hardening only. README parity: no material repo-state change.

- Custom Component/Phase-5 P5-S02 Metadata Trial Gate Semantics Hardening (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add canonical trial preflight semantics to `metadata_trial_last_attempt` with explicit `blocking_reasons`, `trial_gate_verdict`, and `eligible_for_closeout` fields so dry-run window outcomes classify deterministically without inferring from status text. P1/P2/P3 impact: no source-of-truth ownership change enacted; P5 execution-contract clarity hardening only. README parity: no material repo-state change.

- HA/Current-Cycle User Feedback — No Control-Capable Hosts Notification (`packages/ma_control_hub/template.inc`, `packages/ma_control_hub/automation.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): add an operator-facing feedback path that raises a persistent Home Assistant notification when an active target is selected but no control-capable host route is available, with auto-dismiss when control-capable host context recovers. This provides immediate basic user guidance instead of silent no-op behavior in host-degraded states. P1/P2/P3 impact: no source-of-truth ownership change enacted; runtime feedback/diagnostics UX hardening only. README parity: no material repo-state change.

- Docs/Deferred H1 Report-Log-Heal Scaffold Publication (`docs/features/H1-report-log-heal-scaffold.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/README.md`): publish a detailed implementation-ready H1 blueprint for diagnostics-driven reporting, structured logging, and bounded low-risk auto-heal policy design (deferred execution track) with explicit safety gates, schema contracts, rollout phases, and stop conditions so future implementation can proceed without re-discovery. P1/P2/P3 impact: no source-of-truth ownership change enacted; planning/documentation acceleration only. README parity: material docs-index update included in this change set.

- Validation/Phase-5 P5-S02 Checklist Source-of-Truth Alignment (`docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`): update operator fill hints/examples to prefer payload-native audit fields from `write_controls.metadata_trial_last_attempt` (`audit_payload_state`, `missing_audit_fields`) over display-derived labels, ensuring evidence capture remains deterministic if monitor formatting changes. P1/P2/P3 impact: no source-of-truth ownership change enacted; documentation-consistency hardening only. README parity: no material repo-state change.

- Custom Component/Phase-5 P5-S02 Native Trial-Audit Completeness Contract (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): emit canonical metadata-trial audit completeness fields directly from coordinator payload (`audit_payload_complete`, `audit_payload_state`, `missing_audit_fields`) so run-window evidence uses a single source-of-truth for `COMPLETE/PARTIAL/N/A` interpretation instead of template-only inference. P1/P2/P3 impact: no source-of-truth ownership change enacted; diagnostics contract hardening only. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Gold-Standard COMPLETE Audit Example (`docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`): add a concrete copy/paste run-window evidence block that demonstrates a fully populated metadata trial audit (`status`, `window_id`, requested/effective mode, dry-run flag, reason, correlation id, `requested_at`, `completed_at`, and `M1 audit payload completeness=COMPLETE`) to reduce operator interpretation drift for closeout-grade captures. P1/P2/P3 impact: no source-of-truth ownership change enacted; documentation/evidence-quality hardening only. README parity: no material repo-state change.

## 2026-04-20

- Custom Component/Phase-6 P6-S03 Control-Input Execution Skeleton (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/__init__.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `README.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/wiki/Custom-Component-Setup-Roadmap-Stub.md`): implement the first bounded control-input execution lane for Spectra Control Center mappings with dry-run-first semantics, read-only-mode enforcement, and explicit action-attempt diagnostics so button/encoder bindings can be exercised safely before any authority expansion. P1/P2/P3 impact: no source-of-truth ownership change; additive execution diagnostics and safety controls only. README/wiki parity: completed in same change set.

- Validation/Phase-5 P5-S02 Checklist Audit-Completeness Alignment (`docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`): align run-window evidence template/examples with monitor-side M1 audit completeness outputs by adding explicit capture fields for trial audit status/completeness and requested/completed timestamps, plus closeout interpretation guidance (`COMPLETE` expected when a trial was executed). P1/P2/P3 impact: no source-of-truth ownership change enacted; documentation/evidence-contract alignment only. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Monitor Trial-Audit Completeness Hardening (`docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`): surface missing M1 audit contract fields (`requested_at`, `completed_at`) and emit a deterministic audit completeness verdict (`N/A`, `COMPLETE`, `PARTIAL`) so dry-run/write-trial evidence can be assessed without cross-referencing coordinator internals. P1/P2/P3 impact: no source-of-truth ownership change enacted; diagnostics-only evidence-quality hardening. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Run-3 Evidence Consolidation (`docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record bounded run-window progression showing (a) legacy baseline PASS/CAUTION with fail-closed dry-run trial block when metadata lane is not ready, (b) replayed PASS/READY (`9/9`) under fresh active-title metadata conditions, and (c) expected component-mode CAUTION/WARN policy posture (`authority_mode_not_legacy`) while metadata ownership remains legacy. P1/P2/P3 impact: no source-of-truth ownership change enacted; evidence + policy interpretation sync only. README parity: no material repo-state change.

- ESPHome/OLED Idle Chrome Fail-Closed (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`): suppress lingering now-playing chrome (volume meter/progress) when no title/artist/album payload is present and no live transport-playing signal is active. This prevents idle startup tails from keeping the display visually “alive” via transport overlays after metadata/title fallback has been blanked. P1/P2/P3 impact: no source-of-truth ownership change; display-layer idle truthfulness hardening only. README parity: no material repo-state change.

- ESPHome/OLED Startup No-Text Top-Band Suppression (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`): when now-playing render is entered without a resolved title, suppress the filled top title band so startup/idle transitions cannot show a solid header stripe with no text. This keeps the OLED fail-closed for no-metadata/no-title windows while preserving normal title-band rendering when a real title is present. P1/P2/P3 impact: no source-of-truth ownership change; display-layer startup polish only. README parity: no material repo-state change.

- ESPHome/OLED False `PLAYING` Idle Header Fail-Closed Fix (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`): prevent now-playing fallback title from rendering `PLAYING` when playback is only inferred from stale/recent activity signals. The fallback header now requires concrete live transport-playing evidence (HA state playing with fresh activity or active Arylic playing flags) before displaying `PLAYING`; otherwise the title stays blank in idle/no-track windows. P1/P2/P3 impact: no source-of-truth ownership change; display-layer truthfulness hardening only. README parity: no material repo-state change.

- Governance/Build→Upload Enforcement (`.github/copilot-instructions.md`): apply explicit operator directive that successful ESPHome builds in active sessions are deployment-required by default (build must be followed by OTA upload + upload proof) unless the user explicitly requests `build-only`/`no upload`. P1/P2/P3 impact: no source-of-truth ownership change; process enforcement hardening only. README parity: no material repo-state change.

- ESPHome/Control-Target Prompt Retirement (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `esphome/spectra_ls_system/packages/spectra-ls-ui.yaml`, `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/spectra_ls_system/substitutions.yaml`): remove legacy control-target prompt UX/flow from active runtime (first-boot popup gating, Gear menu control-select entry, and dedicated CONTROL TARGETS render path), fail-open audio control sends based on host resolution only, and keep metadata popup behavior for non-prompt actions. P1/P2/P3 impact: no source-of-truth ownership change; runtime UX/control-path simplification only. README parity: no material repo-state change.

- ESPHome/OLED First-Boot Idle Title Fail-Closed Hardening (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`): tighten generic source/device placeholder suppression for now-playing fallback rendering (including normalized `Spectra L/S` variants) and enforce blank title behavior when playback is not active so first-boot idle windows do not display `SPECTRA LS` at the top line. P1/P2/P3 impact: no source-of-truth ownership change; display-layer idle semantics hardening only. README parity: no material repo-state change.

- Governance/Path-Direction Guardrail (`.github/copilot-instructions.md`, `docs/README.md`): add an explicit active-path note that default main-track ESPHome runtime edits must target `esphome/spectra_ls_system/**` (entrypoint `esphome/spectra_ls_system.yaml`), while `esphome/control-py/**` is treated as the stabilized alternate line and must only be edited when the request explicitly targets that path or requires a documented shared-contract/parity update. P1/P2/P3 impact: no source-of-truth ownership change; process-direction hardening only. README parity: no material repo-state change.

- ESPHome/OLED Active-Path Scope Correction + Generic Source Placeholder Suppression (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/control-py/spectra-ls-peripherals.yaml`): align runtime edits to the active `spectra_ls_system` OLED peripherals path, suppress generic source placeholders (`NET`/`Network` and `SPECTRA LS`) from now-playing OLED source/title fallback context, and remove the out-of-scope `control-py` placeholder suppression touch so stabilized `control-py` stays unchanged for this track. P1/P2/P3 impact: no source-of-truth ownership change; display-layer scope/placeholder hardening only. README parity: no material repo-state change.

- ESPHome/OLED Source Placeholder Suppression (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, `esphome/control-py/spectra-ls-peripherals.yaml`): suppress generic `NET` source placeholders in now-playing OLED rendering so the source field can remain blank instead of displaying `NET`/`SRC` when no meaningful source metadata is available. Includes cache-path sanitization for `playing_no_meta` fallbacks to prevent stale `NET` values from persisting across refresh ticks. P1/P2/P3 impact: no source-of-truth ownership change; display-layer semantics hardening only. README parity: no material repo-state change.

- HA + Custom Component/Metadata Gate-Prep Dual-Track Hardening (`packages/ma_control_hub/template.inc`, `custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): close residual dual-path confusion by (1) requiring mapped room-meta fallback entities in legacy `ma_active_meta_entity` to satisfy active/recent freshness criteria before selection, and (2) surfacing explicit component metadata gate scoring/blocking context (`gate_score`, `gate_max`, `blocking_reasons`) so authority drift or stale paused-now-playing conditions are immediately attributable in P5-S02 monitor output. P1/P2/P3 impact: no source-of-truth ownership change; runtime/component validation truthfulness hardening only. README parity: no material repo-state change.

- HA/Paused-Stale Metadata Candidate Guard Hardening (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): fix remaining stale metadata hold path by requiring paused metadata candidates to carry recent playback-progress evidence before they can participate in `ma_meta_candidates`, `ma_meta_resolver`, and preferred fallback selection for `now_playing_entity`. This closes the residual path where stale paused entities (for example lingering AppleTV/WiiM metadata) could still win candidate confidence despite missing fresh position updates. P1/P2/P3 impact: no source-of-truth ownership change; legacy runtime truthfulness hardening only. README parity: no material repo-state change.

- Custom Component/Metadata Stale-Paused Gate Parity Hardening (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`, `docs/testing/raw/stale_meta_root_cause_diagnostic.jinja`): align component metadata-prep diagnostics with runtime stale-hold semantics by adding explicit now-playing fresh-playback checks and a paused-without-fresh-signal gate that prevents stale paused metadata from presenting as handoff-ready. Also dedupe stale root-cause diagnostic candidate rows when `ma_active_meta_entity` and `now_playing_entity` resolve to the same entity. P1/P2/P3 impact: no source-of-truth ownership change; lock-step diagnostics/validation hardening only. README parity: no material repo-state change.

- HA/Metadata Resolver Stale-Hold Root-Cause Fix (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`, `docs/testing/raw/stale_meta_root_cause_diagnostic.jinja`): prevent stale now-playing lock by requiring `ma_meta_resolver` candidates to be active-or-recent (not score-only), removing `pref_has_meta`-only fallback promotion in `sensor.now_playing_entity`, and tightening the kitchen-meta override to active-state/recent-stream evidence rather than generic non-unknown state. Also harden stale diagnostic template winner-row selection to avoid empty-sequence errors. P1/P2/P3 impact: no ownership/source-of-truth change; legacy metadata truthfulness hardening only. README parity: no material repo-state change.

- Validation/Stale-Metadata Root-Cause Diagnostic Template (`docs/testing/raw/stale_meta_root_cause_diagnostic.jinja`): add a copy/paste HA template that identifies which metadata entity is currently winning (`ma_active_meta_entity` vs `now_playing_entity` vs active target), computes freshness/age from position-update timestamps, and emits an explicit stale-hold reason summary so operators can see why old AppleTV metadata remains surfaced. P1/P2/P3 impact: no source-of-truth ownership change; diagnostics-only visibility enhancement. README parity: no material repo-state change.

- Custom Component/Metadata Ownership Boundary Visibility Hardening (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`): make metadata cutover posture explicit in component diagnostics by surfacing ownership fields (`metadata_authority_owner`, `metadata_cutover_active`, `cutover_block_reason`) and gating metadata-prep readiness with no-authority-expansion semantics (`legacy` baseline required for closeout-ready metadata gate-prep). This prevents component-mode routing trials from being misread as metadata ownership cutover and keeps P5-S02 fail-closed semantics explicit. P1/P2/P3 impact: no source-of-truth ownership change enacted; diagnostics contract hardening only. README parity: no material repo-state change.

- Governance/Parallel Lock-Step Bugfix Rule (`.github/copilot-instructions.md`): add an explicit requirement that bugfixes touching behavior-visible contracts keep runtime (`packages/` + `esphome/`) and custom-component (`custom_components/spectra_ls`) tracks in lock-step when feasible; when strict lock-step is not feasible in the same slice, required disposition is immediate explicit shim/defer rationale + changelog note in the same change set. P1/P2/P3 impact: no source-of-truth ownership change; process-governance hardening only. README parity: no material repo-state change.

- HA/Now-Playing State Truthfulness Hardening (`packages/ma_control_hub/template.inc`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): fix stale AppleTV/metadata hold scenarios by removing metadata-recent/title-present heuristics that could incorrectly promote paused/stopped feeds to `playing`. `sensor.ma_active_state` and `sensor.now_playing_state` now prioritize explicit playback states (`playing/paused/stopped/idle/...`) from active target/MA player/meta surfaces and keep paused/stopped semantics intact even when metadata continues broadcasting. P1/P2/P3 impact: no ownership/source-of-truth change; runtime state-classification correctness fix only. README parity: no material repo-state change.

- Runtime/Audio Volume Write Unblock + Control-Host Fallback Hardening (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, `packages/ma_control_hub/template.inc`): fix volume knob no-op scenarios by allowing bounded volume sends while prompt state is active when playback is clearly active and hosts are available, and by strengthening active-target host fallback so valid target IPs can still resolve control hosts during transient control-path/capability ambiguity. This preserves fail-closed behavior when no hosts are resolvable. P1/P2/P3 impact: no ownership/source-of-truth change; runtime control reliability hardening only. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Monitor Readiness Consistency Hardening (`docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`): align readiness classification with authority safety posture so `authority_mode != legacy` cannot present as `Metadata readiness=READY`; authority drift now reports `CAUTION` while preserving explicit FAIL semantics for missing/unresolved required contracts. P1/P2/P3 impact: no ownership/source-of-truth change; diagnostics-only consistency fix. README parity: no material repo-state change.

- Custom Component/Phase-5 Contract Validity Hardening for Control-Host Loss (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): tighten contract validation so required surfaces must be both present and resolved (not `unknown`/`unavailable`/`none`), closing the gap where metadata could appear healthy while control-host state had effectively dropped. Adds explicit unresolved-required reporting to P5-S02 monitor output. P1/P2/P3 impact: no ownership/source-of-truth change; this is fail-closed diagnostics hardening. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Monitor Fallback Hardening (`docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`): fix monitor target-entity selection to avoid false primary binding when shadow alias exists without useful attributes, add deterministic fallback selection based on available `route_trace`/`captured_at` attributes, and include `metadata_write_trial` in suggested refresh actions so first-pass dry-run evidence capture is not skipped. P1/P2/P3 impact: no source-of-truth ownership change. README parity: no material repo-state change.

- Custom Component/Phase-5 P5-S02-M1 Metadata Trial Contract Implementation (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): implement a dry-run-first metadata trial service contract (`metadata_write_trial`) with explicit audit payload fields (`window_id`, requested/effective mode, requested/completed timestamps, status/reason, correlation id), fail-closed gating, and shadow-snapshot visibility under write controls so mechanism-definition execution can be validated deterministically before any metadata authority takeover attempts. Includes monitor-template exposure of trial audit fields for copy/paste evidence capture. P1/P2/P3 impact: no source-of-truth ownership change enacted by this implementation pass; authority remains legacy by default. README parity: no material repo-state change.

- Roadmap/Phase-5 P5-S02 Mechanism-Definition Slice Skeleton (`docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add an execution-ready mechanism-definition skeleton for metadata authority cutover planning, including proposed service contract shape, strict preflight/stop gates, rollback/disarm proof requirements, and evidence fields for trial readiness decisions. P1/P2/P3 impact: no source-of-truth ownership change enacted by this documentation update. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Post-Window Proof Capture (`docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record final post-window monitor artifact (`2026-04-20 18:08:32.691994-07:00`) showing `authority_mode=legacy` with persistent metadata `PASS/READY` and clean contract/parity signals, and mark the bounded Run-1 evidence record as closeout-eligible while keeping P5-S02 slice status Active (gate-prep) pending mechanism-definition gates. P1/P2/P3 impact: no source-of-truth ownership change enacted by this documentation update. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Post-Window Proof Prep (`docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`, `docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`): add an explicit post-window completion capture section and one-paste Example D block so the final authority-disposition proof can be recorded deterministically and the active P5-S02 run can close without ad hoc formatting. P1/P2/P3 impact: no source-of-truth ownership change enacted by this documentation update. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Operator Runtime Evidence Capture (`docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record operator-captured metadata monitor output showing gate-prep baseline `PASS/READY` with safe authority posture (`legacy`), deterministic route (`route_linkplay_tcp`), clean contract/metadata checks (`missing_required=0`), and clean parity/freshness signals; keep slice in Active gate-prep pending post-window evidence/disposition capture. P1/P2/P3 impact: no source-of-truth ownership change enacted by this documentation update. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 First Live-Evidence Capture Block (`docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`): add an explicit Run-1 evidence record block with direct field mapping from the metadata monitor output so operators can capture the first live P5-S02 gate-prep run deterministically without ad hoc formatting. P1/P2/P3 impact: no source-of-truth ownership change enacted by this documentation update. README parity: no material repo-state change.

- Validation/Phase-5 P5-S02 Metadata Monitor + Checklist Hardening (`docs/testing/raw/p5_s02_metadata_functionality_monitor.jinja`, `docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`): add a dedicated copy/paste metadata-domain monitor template and wire it into the active P5-S02 run-window checklist with a concrete quick-start capture flow and closeout-ready evidence example, so metadata gate-prep execution uses deterministic live signals instead of scaffold text only. P1/P2/P3 impact: no source-of-truth ownership change enacted by this documentation update. README parity: no material repo-state change.

- Roadmap/Post-Phase Control Center Track (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `README.md`): add an explicit post-Phase-5 destination track for a full Home Assistant sidebar **Spectra Control Center** (setup, tuning, defaults, overrides, mapped-environment visibility, and operator diagnostics) while preserving current execution priority on active P5-S02 metadata gate-prep. P1/P2/P3 impact: no source-of-truth ownership change enacted by this documentation update.

- Roadmap/Phase-5 P5-S02 Activation + Process Depth Upgrade (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`): advance P5-S02 from draft scaffold to **Active (gate-prep)** and add explicit beyond-mirroring implementation workflow (capability contract inventory, metadata authority mechanism definition, bounded write-path trial protocol, rollback and evidence closure criteria) so next-slice execution is concrete and not limited to legacy-behavior parity mirroring. P1/P2/P3 impact: no source-of-truth ownership change enacted by this documentation update. README parity: no material repo-state change.

- Roadmap/Phase-5 P5-S02 Metadata Slice Scaffold (`docs/testing/raw/p5_s02_metadata_cutover_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): publish a bounded metadata-domain follow-on slice card/checklist after P5-S01 validation, explicitly scoped as gate-prep/readiness-driven (no implied metadata write-authority takeover service) with domain isolation, rollback-first posture, and evidence requirements before any Active cutover execution. P1/P2/P3 impact: no source-of-truth ownership change. README parity: no material repo-state change.

- Roadmap/Phase-5 P5-S01 Closeout Validation (`docs/testing/raw/p5_s01_routing_cutover_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): capture post-window rollback evidence (`authority_mode=legacy`, healthy route/contract/parity/freshness) after prior runtime `VERIFIED` proof and advance P5-S01 from Active to Validated (routing-domain slice), while keeping broader Phase-5 multi-domain cutover work pending subsequent slice activation. P1/P2/P3 impact: no source-of-truth ownership change. README parity: no material repo-state change.

- Roadmap/Phase-5 P5-S01 Activation Progress (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): promote P5-S01 (routing domain) from `Draft` planning posture to `Active` execution posture with concrete runtime evidence (`Status=PASS`, `Write proof=VERIFIED`, `last_attempt.status=noop_already_selected`, deterministic `route_linkplay_tcp`, clean contract/parity/freshness), while explicitly retaining non-closeout state pending bounded-window completion and post-window rollback evidence. P1/P2/P3 impact: no source-of-truth ownership change. README parity: no material repo-state change.

- Validation/Phase-5 VERIFIED Runtime Evidence Capture (`docs/testing/raw/p5_s01_routing_cutover_run_window_checklist.md`): add a concrete operator-captured P5-S01 monitor artifact showing `Status=PASS` with `Write proof=VERIFIED` (`last_attempt.status=noop_already_selected`) and clean route/contract/parity/freshness checks, so closeout interpretation is anchored to a real run-window record rather than synthetic examples only. P1/P2/P3 impact: no source-of-truth ownership change. README parity: no material repo-state change.

- Validation/Phase-5 Monitor Readability + De-dup Fix (`docs/testing/raw/p5_s01_component_functionality_monitor.jinja`): harden the P5-S01 component monitor with an explicit write-proof classification (`READY` / `VERIFIED` / `BLOCKED`) derived from `write_controls.last_attempt.status`, and remove accidental full-file duplicate content so operators get a single deterministic output block during cutover checks. P1/P2/P3 impact: no source-of-truth ownership change. README parity: no material repo-state change.

- Validation/Phase-5 Component Functionality Monitor Template (`docs/testing/raw/p5_s01_component_functionality_monitor.jinja`, `docs/testing/raw/p5_s01_routing_cutover_run_window_checklist.md`): add a single copy/paste raw monitor template for real-time P5-S01 component feedback (authority mode, last write attempt status/reason, route decision, contract validity, parity drift, and snapshot freshness) and link it from the run-window checklist for fast operator verification while interacting with pots/encoders. P1/P2/P3 impact: no source-of-truth ownership change. README parity: no material repo-state change.

- Validation/Phase-5 Mechanism Clarification (`docs/testing/raw/p5_s01_routing_cutover_run_window_checklist.md`): add an explicit plain-language mechanism section clarifying that P5-S01 does **not** delete or move legacy control-hub logic; it uses a single-writer authority switch (`spectra_ls.set_write_authority`) with bounded routing-only trial services and rollback-to-legacy end-of-window policy. P1/P2/P3 impact: no source-of-truth ownership change. README parity: no material repo-state change.

- Validation/Phase-5 Evidence Template Explicit Examples (`docs/testing/raw/p5_s01_routing_cutover_run_window_checklist.md`): add fully filled, audit-style examples for both closeout-eligible `PASS` and fail-closed `FAIL` (with mandatory rollback) so operators can copy structure and classify outcomes deterministically during P5-S01 routing run windows. P1/P2/P3 impact: no source-of-truth ownership change. README parity: no material repo-state change.

- Validation/Phase-5 Run Window Evidence Record Template (`docs/testing/raw/p5_s01_routing_cutover_run_window_checklist.md`): add a copy/paste structured operator evidence block for one-record-per-run-window capture (`preflight`, `pre-arm`, `in-window`, `stop-conditions`, `rollback`, `post-window`, and final verdict) so P5-S01 trial documentation is deterministic and closeout decisions are audit-friendly. P1/P2/P3 impact: no source-of-truth ownership change. README parity: no material repo-state change.

- Validation/Phase-5 Run Window Checklist (`docs/testing/raw/p5_s01_routing_cutover_run_window_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): publish an execution-ready **P5-S01 routing cutover** operator checklist covering preflight gate proof, explicit arm/disarm sequencing, bounded observation capture, fail-closed stop conditions, rollback proof, and closure evidence requirements; link checklist from both roadmap docs for deterministic run-window execution. P1/P2/P3 impact: no source-of-truth ownership change introduced by this documentation publication. README parity: no material repo-state change.

- Roadmap/Phase-5 Starter Slice Card (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): publish bounded **P5-S01 routing-domain** starter slice as `Draft` with explicit in/out scope, two-track disposition (runtime rollback shim + component trial path), activation gates (authority/parity/isolation/rollback/evidence), fail-closed stop conditions, and P1/P2/P3 impact check. No runtime or source-of-truth ownership change is enacted by this documentation slice publication. README parity: no material repo-state change.

- Roadmap/Phase-5 Entry Criteria Delta Note (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add explicit post-F4 handoff gating for Phase 5 cutover execution (authority baseline, parity hold, single-domain isolation, rollback proof, and fresh-evidence requirement), and keep Phase 5 status as `Planned` until gates are satisfied in a scoped domain slice. No runtime/source-of-truth authority change introduced by this documentation delta. README parity: no material repo-state change.

- Validation/F4-S03 Closeout Seal + Phase-4 Diagnostics Slice Completion (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): close F4-S03 with runtime **PASS** evidence (`gate_score=8/8`, `authority_mode=legacy`, `no_authority_expansion=true`, `route_decision=route_linkplay_tcp`, `active_target=media_player.spectra_ls_2`) and seal the bounded Phase-4 diagnostics slice set (`F4-S01/F4-S02/F4-S03`) as validated while preserving legacy write authority boundaries. Includes explicit P1/P2/P3 impact check (no source-of-truth or ownership changes). README parity: no material repo-state change.

- HA/Lighting Startup Warning Suppression (`packages/spectra_ls_lighting_hub.yaml`): prevent one-shot `input_select` warning (`Current option: Unknown no longer valid`) during room-option bootstrap by applying a transitional options bridge (`[current] + safe_options`) when a reset is needed, selecting a valid room, then pruning back to canonical options. No contract/authority changes; runtime behavior remains discovery-first. README parity: no material repo-state change.

- Custom Component/F4-S02 Builder Regression Fix (`custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): fix accidental function-split regression where `_build_action_catalog_validation` returned `None` (due misplaced F4-S03 builder insertion), which forced persistent `ready_for_f4_s02=false` and blocked F4-S03 closeout dependency gate despite healthy route/target context. Restored complete F4-S02 payload builder flow and kept F4-S03 dependency guards intact. README parity: no material repo-state change.

- Validation/F4-S03 Dependency-WARN Guidance Hardening (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/f4_s03_crossfade_balance_validation.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden F4-S03 diagnostics for no-target/deferred-route captures by surfacing explicit dependency context (`active_target_resolved`, `route_decision`, `ready_for_f4_s02`) and adding deterministic rerun guidance (set/confirm active target, rerun F4-S01 → F4-S02 → F4-S03 in legacy mode) when the only failing gate is F4-S02 readiness. README parity: no material repo-state change.

- Custom Component/F4-S03 None-Payload Crash Guard (`custom_components/spectra_ls/coordinator.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): fix runtime crash path where `crossfade_balance_validation` attempted `.get(...)` on a `None` `action_catalog_validation` object during snapshot builds (service and state-change callback paths), by normalizing upstream F4 payloads to mapping-safe defaults and hardening event callback snapshot refresh to fail-safe logging rather than uncaught callback exceptions. README parity: no material repo-state change.

- Custom Component/F4 Sequence Hardening Pass-2 (`custom_components/spectra_ls/__init__.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): harden `run_f4_s01_sequence`, `run_f4_s02_sequence`, and `run_f4_s03_sequence` with explicit stage-level execution wrappers and surfaced failure stage context (instead of opaque unknown-action failures), preserving diagnostics-only behavior and legacy authority boundaries. README parity: no material repo-state change.

- Custom Component/F4-S03 Crossfade-Balance Diagnostics Scaffold (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): implement Phase 4 Slice-03 diagnostics-only service/payload path (`validate_crossfade_balance`, `run_f4_s03_sequence`) with normalized slider-domain schema, mode-profile contract visibility, sample no-op mix plan diagnostics, and no-authority-expansion checks while preserving legacy runtime write ownership. README parity: no material repo-state change.

- Validation/F4-S03 Raw Template Scaffold (`docs/testing/raw/f4_s03_crossfade_balance_validation.jinja`, `docs/testing/raw/f4_s03_crossfade_balance_entry_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a deterministic copy/paste raw validator for Phase 4 Slice-03 crossfade/balance contract readiness with freshness guards, authority safety checks, and explicit pre-implementation handling when F4-S03 payload/service surfaces are not yet present; update F4-S03 checkpoint docs to include the runtime evidence artifact path. README parity: no material repo-state change.

- Validation/F4-S03 Entry Checklist Publication (`docs/testing/raw/f4_s03_crossfade_balance_entry_checklist.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a deterministic diagnostics-first F4-S03 kickoff checklist covering preflight gates, scope guardrails, closeout evidence requirements, and operator expectation that diagnostics sequence actions are UI-quiet with confirmation in template/entity outputs. README parity: no material repo-state change.

- Validation/F4-S02 Closeout Seal + Action Feedback Clarity (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/f4_s02_action_catalog_validation.jinja`): close F4-S02 using runtime `PASS 7/7` evidence in legacy authority mode (`ready_for_f4_s02=true`, `f4_s01_ready_reference=true`, `dry_run_only=true`, `no_authority_expansion=true`) and clarify operator expectations that `run_f4_s01_sequence`/`run_f4_s02_sequence` are silent diagnostics-refresh actions whose confirmation is observed in template/entity outputs rather than UI toast feedback. README parity: no material repo-state change.

- Validation/F4-S01 Closeout Seal + Legacy-Mode Runbook Clarification (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/f4_s01_capability_profile_validation.jinja`): close F4-S01 with runtime `PASS 6/6` evidence in legacy authority mode (`authority_mode=legacy`, `ready_for_f4_s01=true`, `route=route_linkplay_tcp`, metadata prep ready), and add explicit operator-facing guidance for running `spectra_ls.run_f4_s01_sequence` with `mode: legacy` before closeout checks. README parity: no material repo-state change.

- Validation/F4-S02 Runtime WARN Evidence Capture (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/f4_s02_action_catalog_validation.jinja`): archive a concrete F4-S02 runtime artifact with `WARN 6/7` where only `capability_profile_ready=false` blocked closeout, and add deterministic rerun guidance to execute legacy-mode F4-S01 first and then rerun F4-S02 sequence for closure-grade evidence. README parity: no material repo-state change.

- Validation/F4-S01 Runtime Evidence Capture (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/testing/raw/f4_s01_capability_profile_validation.jinja`): archive a concrete F4-S01 runtime artifact with `WARN` (`gate_score=5/6`) where all capability/profile gates were healthy except `no_authority_expansion` (`authority_mode=component`), and tighten template recommendation text to explicitly require rerun with `run_f4_s01_sequence(mode='legacy')` for diagnostics-only closeout eligibility. README parity: no material repo-state change.

- Validation/F4-S01 Runtime Evidence Blocker Note (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): record explicit evidence gap for F4-S01 closeout where runtime PASS/WARN/FAIL template output is not yet captured in tracked artifacts/logs for this slice window; keep F4-S01 open and evidence-first (no inferred closeout). README parity: no material repo-state change.

- Custom Component/F4-S02 Programmable Action Safety Skeleton (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `docs/testing/raw/f4_s02_action_catalog_validation.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add diagnostics-only action-catalog safety scaffolding with explicit arm/confirm/cooldown/audit schema visibility, no-authority-expansion checks, and one-shot validation services while preserving legacy ownership boundaries. README parity: no material repo-state change.

- Validation/P3 Final Closeout Seal (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): publish final evidence-backed Phase-3 seal with soak completion (`3` successful cycles in `4` attempts, `distinct_pass_targets=2`), closure-gate semantics alignment (`soak_evidence` is closure-grade; runtime is health-check-only), and metadata candidate payload parity proof (`PASS 5/5`, consistent rows/summary/best payload). P3-S01/P3-S02/P3-S03 are now explicitly sealed and Phase-4 continuation is confirmed on active F4-S01 lane. README parity: no material repo-state change.

- Validation/P3 Candidate Payload Parity Template (`docs/testing/raw/p3_candidate_payload_parity_validation.jinja`): add a raw copy/paste operator template that checks consistency between `sensor.ma_meta_candidates` state count, `candidate_rows_json`, `candidate_summary_json`, and `best_candidate_json`, with deterministic PASS/WARN/FAIL diagnostics for row/summary/best mismatches and empty-entity false-positive regressions. README parity: no material repo-state change.

- ESPHome/Active Entrypoint Stale-Reference Cleanup (`esphome/spectra_ls_system.yaml`): remove deprecated `control-board-peripherals` include comment and retire outdated “no-rings vs rings” checklist wording from the active runtime entrypoint, keeping package guidance aligned to current `spectra-ls-peripherals.yaml` source-of-truth naming. README parity: no material repo-state change.

- Validation/P3 Closure Gate Operator-Clarity Hardening (`docs/testing/raw/p3_s01_s02_closure_gate_check.jinja`): improve runtime vs soak-evidence readability by rendering soak-only gates as `n/a (runtime mode)` outside `soak_evidence`, add explicit inline mode guidance/output so runtime checks cannot be mistaken for closure-grade evidence, and add a compact “how to run soak_evidence mode” section with required fields sourced from `script.spectra_p3_soak_one_shot` notifications/logging. README parity: no material repo-state change.

## 2026-04-19

- Validation/P3 Closure Gate Runtime-Mode Semantics Hardening (`docs/testing/raw/p3_s01_s02_closure_gate_check.jinja`): make closeout gating explicitly mode-aware so runtime evaluation no longer hard-fails solely on missing soak counters (`observed_soak_attempts=0`) while still enforcing strict soak-cycle/attempt/distinct-target/provenance gates in `soak_evidence` mode; runtime-only outputs are now explicitly non-closeout (`WARN` floor) to prevent accidental closure without evidence-backed soak capture. README parity: no material repo-state change.

- HA/Now-Playing Title Correctness Hardening (`packages/ma_control_hub/template.inc`): remove source/app/friendly-name fallback from `sensor.now_playing_entity` title resolver so non-track labels (for example device/source placeholders) no longer appear as fake now-playing titles during metadata startup gaps; source visibility remains available via `sensor.now_playing_source`. README parity: no material repo-state change.

- HA/MA Meta Candidate Contract Fix (`packages/ma_control_hub/template.inc`): resolve a Jinja accumulation scope bug in `sensor.ma_meta_candidates` where `candidate_rows_json` (and MA-ID enrichment inputs) were built with loop-local list reassignment, yielding `state > 0` while `candidate_summary_json`/`best_candidate_json` stayed empty (`best_entity=''`, `best_score=0`). Switched to namespace-backed accumulation so row payload, summary entities, and best-candidate outputs remain contract-consistent for diagnostics and metadata-prep gates. README parity: no material repo-state change.

- Validation/P3-H3 Closure Gate Fail-Closed Hardening (`docs/testing/raw/p3_s01_s02_closure_gate_check.jinja`, `docs/testing/raw/p3_s01_s02_soak_protocol.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): enforce strict closure semantics by changing defaults to runtime-safe fail values and requiring soak-evidence provenance (`observed_evidence_source`, `observed_evidence_reference`, `observed_evidence_collected_at <=24h`) before soak-mode closure can PASS/WARN; missing provenance now hard-FAILs to prevent optimistic/manual-only closeout toggles. README parity: no material repo-state change.

- Validation/P3-H2 Closure Gate Soak-Parity Fix (`docs/testing/raw/p3_s01_s02_closure_gate_check.jinja`, `docs/testing/raw/p3_s01_s02_soak_protocol.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): eliminate soak-mode false WARN outcomes caused by runtime parity drift outside soak windows by adding explicit operator evidence input `observed_parity_ok` and using it when `evaluation_mode=soak_evidence` (runtime parity is still reported in breakdown for diagnostics). README parity: no material repo-state change.

- Validation/P3-H1 Hardening Second Pass (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/p3_s01_guarded_write_validation.jinja`, `docs/testing/raw/p3_s02_selection_handoff_validation.jinja`, `docs/testing/raw/p3_s03_metadata_prep_validation.jinja`, `docs/testing/raw/p3_s01_s02_closure_gate_check.jinja`, `docs/testing/raw/p3_s01_s02_soak_protocol.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): strengthen Phase-3 reliability with stricter metadata-candidate readiness semantics (reject empty best-candidate payloads), broader coordinator listener coverage for helper/metadata entities, closure-gate soak-attempt consistency checks, and snapshot-freshness guards across P3 validation templates to reduce stale/false-positive closeout outcomes. README parity: no material repo-state change.

- Custom Component/F4-S01 Capability+Profile Diagnostics Slice (`custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `docs/testing/raw/f4_s01_capability_profile_validation.jinja`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): implement Phase-4 Slice-01 diagnostics-only scaffolding with capability-matrix summary, profile-schema contract surface, no-authority-expansion guard visibility, one-shot validation sequence services, and deterministic operator template/checkpoint docs without introducing control ownership cutover writes. README parity: no material repo-state change.

- Validation/P3-S03 Final Runtime Proof Snapshot (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): archive final operator evidence for metadata-prep closeout (`PASS`, `7/7`, `ready_for_metadata_handoff=true`) with resolved metadata probes (`active_meta_entity`, `now_playing_entity/state/title`) and zero missing metadata contracts; explicitly retain compatibility-mode boundary and note that non-cutover runtime route/selection context (`defer_not_capable`, legacy authority, S02 FAIL) is expected and non-blocking for diagnostics-only S03 closure. README parity: no material repo-state change.

- Validation/P3-S03 Closeout + Phase-4 Planning (`docs/testing/raw/p3_s03_metadata_prep_validation.jinja`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): close P3-S03 diagnostics slice using runtime evidence (`PASS 7/7`, `ready_for_metadata_handoff=true`, missing-required=`0`) while keeping metadata ownership explicitly in legacy compatibility mode, and publish a bounded Phase-4 cutover slice plan (`F4-S01` capability matrix + profile scaffolding, `F4-S02` programmable action catalog safety skeleton, `F4-S03` crossfade/balance service contract + diagnostics-only probe). Also improve S03 template metadata value-probe rendering with live-entity fallback to avoid misleading `missing` display when checks are resolved true. README parity: no material repo-state change.

- Validation/P3-S03 Template Listener Fallback (`docs/testing/raw/p3_s03_metadata_prep_validation.jinja`): add direct rendered listener-anchor lines using explicit `states('entity_id')` and `state_attr('entity_id', ...)` calls for both shadow sensor variants and metadata contract entities, ensuring Home Assistant Template UI registers concrete dependencies even when intermediate-variable anchors are ignored. README parity: no material repo-state change.

- Validation/P3-S03 Template Listener Hardening (`docs/testing/raw/p3_s03_metadata_prep_validation.jinja`): switch listener anchors to explicit `states.sensor.*` object-path references and include a lightweight watcher checksum in rendered output so Home Assistant’s dependency tracker reliably detects concrete entity subscriptions in Developer Tools Template mode. README parity: no material repo-state change.

- Validation/P3-S03 Template Event Tracking Fix (`docs/testing/raw/p3_s03_metadata_prep_validation.jinja`): add explicit static entity/attribute watchers for both `shadow_*` and `spectra_ls_shadow_*` sensor variants so Home Assistant template dependency tracking reliably subscribes to runtime updates instead of warning that the template has no event listeners. README parity: no material repo-state change.

- Custom Component/P3-S03 Metadata Prep (Diagnostics-Only) (`custom_components/spectra_ls/*`, `docs/testing/raw/p3_s03_metadata_prep_validation.jinja`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add metadata-preparation validation surfaces and one-shot sequence service for Phase 3 Slice-03 without enabling metadata ownership cutover, including explicit checks for legacy metadata contract entities and candidate payload readiness. README parity: no material repo-state change.

- Validation/P3 Closure Gate False-Negative Fix (`docs/testing/raw/p3_s01_s02_closure_gate_check.jinja`, `docs/testing/raw/p3_s01_s02_soak_protocol.md`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`): add explicit `evaluation_mode` support so closeout checks can run against recorded soak evidence (`soak_evidence`) instead of transient live runtime context (`runtime`) that may legitimately return `legacy` authority or `defer_not_capable` after the soak window; prevents post-soak false FAIL outcomes while keeping runtime-strict mode available for live checks. README parity: no material repo-state change.

- Validation/P3-S01+S02 Closeout Decision (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): apply explicit single-capable-topology waiver for the distinct PASS-target gate (based on sustained 3 PASS cycles with clean route/contract/parity/handoff compatibility signals) and advance P3-S01/P3-S02 from Active/Pending to validated closeout state; P3-S03 remains next in-sequence. README parity: no material repo-state change.

- Validation/P3 Closure Gate Tooling (`docs/testing/raw/p3_s01_s02_closure_gate_check.jinja`, `docs/testing/raw/p3_s01_s02_soak_protocol.md`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add a single deterministic closeout template that combines S01 guard status, S02 handoff status, parity/contract checks, and explicit soak evidence inputs (including single-capable-topology waiver handling) so P3-S01/S02 closure decisions are evidence-driven and reproducible without broad multi-template interpretation drift. README parity: no material repo-state change.

- ESPHome/Audio Warning Noise Reduction (`esphome/control-py/packages/spectra-ls-audio-tcp.yaml`, mirrored in `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`): remove unused Home Assistant numeric mirror sensor for `sensor.ma_active_volume` (`id: ha_audio_volume`) to eliminate repeated ESPHome conversion warnings when MA volume is transiently `unknown` during startup/reconnect windows; no control-path behavior changes. README parity: no material repo-state change.

- Validation/P3 Soak Runner Topology Default (`scripts.yaml`): align `script.spectra_p3_soak_one_shot` default distinct-pass-target gate to single-active-target topology (`required_distinct_pass_targets=1`) so successful 3/3 single-target control-path validation no longer reports false "incomplete" outcomes by default; strict multi-target validation remains available by explicitly setting the gate to `2+`. README parity: no material repo-state change.

- Validation/P3-S02 Soak Runtime Evidence (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): capture automated one-shot soak result (`script.spectra_p3_soak_one_shot`) with **3 successful cycles in 5 attempts**, preserving zero contract/parity drift (`contract_valid=true`, `missing_scripts=0`, `missing_automation_ids=0`, `unresolved_sources=0`, `mismatches=0`) and deterministic eligible-route PASS cycles on `route_linkplay_tcp`; note that non-capable target hops were soft-skipped (`defer_not_capable`) and the explicit “≥2 distinct PASS targets” closure gate remains open unless waived for single-capable-topology conditions. README parity: no material repo-state change.

- Validation/P3 Stage-Report Ownership Snapshot (`docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): add an explicit stage-report note clarifying current ownership boundaries (legacy `.inc` runtime remains source-of-truth for broad selection/control orchestration while `custom_components/spectra_ls` owns diagnostics/one-shot orchestration/guarded write-trial framework), with readiness callouts for next-step soak execution vs. closure readiness. README parity: no material repo-state change.

- Validation/P3 Soak Protocol (`docs/testing/raw/p3_s01_s02_soak_protocol.md`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`): add a no-cowboy small soak runbook for repeating P3-S01 + P3-S02 one-shot validation across multiple active-target switches, and codify evidence-based closure gates (consecutive PASS cycles, zero parity/missing-compat regressions) before graduating P3-S02 from Active/Pending. README parity: no material repo-state change.

- Validation/P3-S02 Runtime Proof (`docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`): capture live operator evidence for `p3_s02_selection_handoff_validation.jinja` showing `PASS` in `component` authority mode with deterministic `route_linkplay_tcp`, valid contracts, helper/options readiness (`target_in_options=true`), and zero missing compatibility scripts/automation IDs. README parity: no material repo-state change.

- Custom Component/P3-S02 One-Shot Validation Slice (`custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `docs/testing/raw/p3_s02_selection_handoff_validation.jinja`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`): add a guarded one-shot P3-S02 validation sequence that performs authority/registry/contract/route refresh and emits explicit selection-handoff readiness diagnostics (legacy helper/options/scripting surfaces) with a raw template for PASS/WARN/FAIL operator validation. README parity: no material repo-state change.

- Custom Component/P3 Operator UX (`custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/services.yaml`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`): add one-shot Phase 3-S01 sequence service (`spectra_ls.run_p3_s01_sequence`) to run authority set + rebuild + contract validation + route-trace refresh + guarded write trial in a single action call, reducing manual multi-step operator execution overhead. README parity: no material repo-state change.

- Custom Component/P3 Validation Correctness Fix (`custom_components/spectra_ls/coordinator.py`, `docs/testing/raw/p3_s01_guarded_write_validation.jinja`): fix false FAIL outcomes where `contract_validation` could disappear on subsequent snapshot refreshes after a successful write trial; coordinator now emits contract-validation state consistently and P3 template degrades to WARN when contract-validation payload is missing rather than misclassifying healthy guarded write outcomes as hard FAIL. README parity: no material repo-state change.

- Validation/Template Hardening (`docs/testing/raw/p2_negative_case_regression.jinja`, `docs/testing/raw/p3_s01_guarded_write_validation.jinja`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`): clarify that service checkmarks indicate execution (not semantic pass), strengthen negative-case interpretation when route remains healthy, and add a dedicated P3-S01 guarded-write validation template focused on authority mode, last write attempt status, and guard outcomes. README parity: no material repo-state change.

- Custom Component/Phase 3 Slice-01 Guard Framework (`custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/services.yaml`, `custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `custom_components/spectra_ls/manifest.json`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`, `docs/architecture/CONTROL-HUB-ARCHITECTURE.md`): implement single-writer authority controls (`legacy` default, `component` opt-in), guarded manual route-write trial service, and anti-loop safeguards (debounce, reentrancy, route-decision eligibility, correlation-id tracing) with write-control diagnostics surfaced in shadow attributes. README parity: no material repo-state change.

- Custom Component/Phase 3 Slice-01 Start (`custom_components/spectra_ls/*`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`): begin guarded routing write-path work with single-writer authority controls, loop/flap prevention guards, and rollback-safe defaults while preserving legacy source-of-truth compatibility during activation staging. README parity: no material repo-state change.

- Validation/Roadmap Progression (`docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`): record fresh live P2 runtime proof artifact (`PASS 8/8`, deterministic `route_linkplay_tcp`, contract valid with zero missing required entities, zero unresolved/mismatch drift, fresh snapshot) and advance `P3-S01` status to **Active** for guarded routing write-path work. README parity: no material repo-state change.

- Program Governance/No-Assumption Hardening (`.github/copilot-instructions.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`, `docs/notes/NOTES-engineering-rigor.md`): codify evidence-first implementation policy (no cowboy/no assumptions), require explicit contract inventory + tough-spot disclosure before migration write-path changes, add a P1/P2 validation snapshot, and expand Phase 3+ into explicit guarded slices (`P3-S01` routing write path, `P3-S02` selection handoff, `P3-S03` metadata prep) with stricter acceptance gates. README parity: no material repo-state change.

- Docs/Roadmap Quality Cleanup (`docs/roadmap/v-next-NOTES.md`): resolve residual roadmap markdown structure debt (heading/list spacing and indentation) so validation/lint checks are clean before Phase 3 execution work. README parity: no material repo-state change.

- Custom Component/Phase 2 Closure Hardening (`custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `docs/testing/raw/p2_registry_router_verification.jinja`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/testing/raw/p2_negative_case_regression.jinja`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`): expose registry/route-trace/contract-validation diagnostics on shadow entities, add snapshot freshness gating to P2 verification, and add an explicit negative-case regression template for alarm-path validation before Phase 3 planning. README parity: no material repo-state change.

- Docs/DevTools Copy-Paste UX (`esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/testing/raw/p2_registry_router_verification.jinja`): add raw no-fence Jinja template path for Phase 2 verification so operators can copy/paste directly into Home Assistant Template editor without markdown code-fence cleanup. README parity: no material repo-state change.

- Docs/Validation Template Robustness — Shadow Entity ID Auto-Detection (`esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`): fix Phase 2 validation false-fail after reboot by supporting both HA entity-id styles (`sensor.shadow_*` from has-entity-name registry behavior and legacy `sensor.spectra_ls_shadow_*` assumptions), and add explicit legacy upstream readiness context so startup `unknown` states are diagnosed as settling vs. true parity failure. README parity: no material repo-state change.

- Custom Component/Phase 2 Slice-02 Deterministic Validation Hardening (`custom_components/spectra_ls/registry.py`, `custom_components/spectra_ls/router.py`, `custom_components/spectra_ls/coordinator.py`, `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`): strengthen read-only registry/router determinism with dual-shape rooms/targets payload parsing and resolved-path route tracing, and add a dedicated P2 verification DevTools workbench for contract validation + route-trace checks. README parity: no material repo-state change.

- Custom Component/Official HA Brand Folder Compliance (`custom_components/spectra_ls/brand/icon.png`, `custom_components/spectra_ls/brand/logo.png`, `.github/copilot-instructions.md`, `custom_components/spectra_ls/manifest.json`): align integration icon process to Home Assistant 2026.3+ documented behavior by shipping local brand assets under `brand/` (supported by the Brands Proxy API), add explicit docs-first branding workflow rules to workspace instructions, and bump integration version to force metadata refresh on reload. README parity: no material repo-state change.

- Custom Component/Phase 2 Slice-01 Registry + Router Scaffold (`custom_components/spectra_ls/__init__.py`, `custom_components/spectra_ls/coordinator.py`, `custom_components/spectra_ls/diagnostics.py`, `custom_components/spectra_ls/const.py`, `custom_components/spectra_ls/registry.py`, `custom_components/spectra_ls/router.py`, `custom_components/spectra_ls/services.yaml`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`): start Phase 2 with discovery-first normalized target registry and read-only route-trace foundation (`linkplay_tcp` classification only), plus maintenance services (`rebuild_registry`, `validate_contracts`, `dump_route_trace`) for deterministic diagnostics without introducing control write-path behavior. README parity: no material repo-state change.

- Custom Component/Icon Availability Fix (`custom_components/spectra_ls/manifest.json`): add explicit manifest icon fallback (`mdi:audio-video`) and bump integration version so Home Assistant refreshes integration metadata and stops showing “icon not available” when brand assets are not resolved.

- Custom Component/Validation UX Polish (`custom_components/spectra_ls/icon.png`, `custom_components/spectra_ls/logo.png`, `docs/testing/DEVTOOLS-TEMPLATES.local.md`): add placeholder integration image assets for Devices & Services branding and add a persistent P1-S01 shadow-parity Developer Tools template block for repeatable validation/triage across load/parity/attributes/icon checks.

- Custom Component/Icon + Install Flow Clarification (`custom_components/spectra_ls/sensor.py`, `custom_components/spectra_ls/binary_sensor.py`, `docs/wiki/Install-on-Your-Own-HA.md`): add placeholder icon (`mdi:audio-video`) for Spectra LS shadow parity entities and document required Home Assistant reboot before adding the new custom integration so discovery reflects freshly deployed `custom_components` files.

- Custom Component/Phase 1 Slice-01 Shadow Parity (`custom_components/spectra_ls/*`, `.gitignore`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`, `docs/roadmap/v-next-NOTES.md`): scaffold `spectra_ls` integration baseline, unignore the dedicated tracked component path, and add read-only routing parity surfaces for `ma_active_target`, `ma_active_control_path`, `ma_active_control_capable`, and `ma_control_hosts` with diagnostics-first mismatch visibility and no control write-path side effects. README parity: no material repo-state change.

- Docs/Wiki Control Surface Inventory (`docs/wiki/Control-Surface-Inputs-and-Expanders.md`, `docs/wiki/Home.md`, `docs/wiki/_Sidebar.md`, `docs/wiki/README.md`): add a dedicated operator-facing control reference for buttons, selectors, encoders, sliders/pots, expander bus addresses, and RP→ESP event-path mapping; wire the page into wiki navigation. README parity: no material repo-state change.

- Docs/Wiki Content Expansion + Marker Cleanup (`docs/wiki/Home.md`, `docs/wiki/Getting-Started.md`, `docs/wiki/Install-on-Your-Own-HA.md`, `docs/wiki/User-Setup-Deploy-and-HA-Integration.md`, `docs/wiki/Operations-Runbooks.md`, `docs/wiki/Discussions-and-Projects-Workflow.md`, `docs/wiki/README.md`): remove temporary wiki sync marker and expand wiki stubs into fuller operator-facing runbooks, install guidance, workflow criteria, and troubleshooting references.

- Docs/Wiki Sync Retest Trigger (`docs/wiki/Home.md`): publish a tiny wiki-source update after manual wiki initialization to force a new `Wiki Sync` run and verify end-to-end automation.

- Docs/Wiki Initialization Clarification (`docs/wiki/README.md`): add explicit requirement to initialize the wiki repository by creating/saving the first wiki Home page before automated git sync can clone `<repo>.wiki.git`.

- Tooling/Wiki Sync Reliability + Node24 Readiness (`.github/workflows/wiki-sync.yml`, `docs/wiki/README.md`): upgrade checkout action to Node24-ready version, add explicit Node24 opt-in env, and add preflight diagnostics for wiki repository reachability/auth so `exit 128` failures report actionable causes (wiki disabled/uninitialized or PAT scope/permission mismatch).

- Docs/Wiki Sync Smoke Test (`docs/wiki/Home.md`): add a small visible sync-marker line to confirm GitHub Actions wiki publishing is functioning after repository secret setup.

- Docs/Wiki PAT Navigation Fix (`docs/wiki/README.md`): correct fine-grained PAT instructions to a valid profile-settings route and add direct URL fallback so users can reliably reach token creation UI.

- Docs/README Inspiration Line Expansion (`README.md`): update front-page inspiration sentence to describe Spectra L/S as a minimal home DJ-mixer style control surface coupled with lighting, automation, and human-centric interaction.

- Docs/README Hardware-First Context (`README.md`): clarify on the front page that Spectra L/S is a hardware-first stack that requires the physical controller path first, then uses ESPHome + Home Assistant software orchestration; add explicit MCU role list (ESP32-S3 + RP2040) so new users understand installation context.

- Docs/README Front-Pitch Cleanup + Install Checklist Stub (`README.md`, `docs/wiki/Install-on-Your-Own-HA.md`, `docs/wiki/Home.md`, `docs/wiki/_Sidebar.md`, `docs/wiki/Welcome-README-and-Bug-Workflow.md`): simplify front-page elevator pitch to be strongly user-focused (minimal hardware/repo-detail emphasis), and add a strict copy/paste style “Install on your own HA” checklist with runtime-first steps plus placeholder track for upcoming custom-component setup flow.

- Docs/Wiki User-Onboarding + Bug Workflow + README Positioning (`README.md`, `.github/copilot-instructions.md`, `docs/wiki/*`, `.github/ISSUE_TEMPLATE/bug_report.yml`): add first-pass Welcome/README-and-bug-submission wiki workflow, add user-focused setup/deploy/integration stub with explicit manual prerequisites (tokens/API/manual setup), add wiki content policy for what should/should-not live in wiki, add custom-component setup roadmap stub tied to current roadmap phases, enforce mandatory wiki-parity update step in workspace instructions, and update README positioning section to “Why this is missing from Home Assistant” with physical-first framing and inspiration links.

- Governance/Wiki Automation + PM Operating Model (`.github/workflows/wiki-sync.yml`, `docs/wiki/*`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/governance/LABEL-TAXONOMY.md`, `CONTRIBUTING.md`, `docs/README.md`): add production-ready GitHub Wiki sync workflow from `docs/wiki/`, expand wiki information architecture using mature OSS patterns (home/sidebar/runbooks/architecture/triage/release flow), document fine-grained PAT setup for wiki publishing, and define Discussions + Projects operating model mapped to all repository scope paths for triage and delivery governance.

- Docs/Product Positioning Expansion (`README.md`, `docs/roadmap/v-next-NOTES.md`, `docs/roadmap/CUSTOM-COMPONENT-ROADMAP.md`): position Spectra L/S as the analog, tactile control surface for Home Assistant beyond audio/lighting (while keeping heavy focus on those domains), with explicit mappable input philosophy for broader home control actions.

- Governance/GitHub Maturity Baseline (`CONTRIBUTING.md`, `CODEOWNERS`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `.github/ISSUE_TEMPLATE/*`, `.github/pull_request_template.md`, `docs/governance/GITHUB-MASTERWORK-BLUEPRINT.md`, `docs/governance/LABEL-TAXONOMY.md`, `docs/README.md`, `.github/copilot-instructions.md`): establish a mature, Immich-inspired repository governance foundation with structured issue intake, PR quality gates, ownership mapping, security reporting policy, contribution standards, and a documented rollout blueprint for “masterwork” GitHub implementation quality.

- Docs/Legend + Wiring Protocol Upgrade (`docs/circuitpy/RP-LEGEND.md`, `docs/hardware/WIRING-LAYOUT-PROTOCOL.md`, `docs/README.md`, `README.md`, `.github/copilot-instructions.md`, optional wiki sync scaffolding): expand the docs legend into a directive wiring/layout protocol reference with deep links to component-level details, add a docs index entry for the new protocol document, enforce docs/README parity directives in workspace instructions, and simplify frontpage README to user-focused overview with basic documentation links.

- Docs/Repository Documentation Reorganization (`docs/**`, `README.md`, `.github/copilot-instructions.md`): move project documentation/notes into `docs/` (except top-level `README.md`), remap key workflow/reference paths, and keep frontpage README end-user-focused while relocating developer onboarding to docs.

- Docs/Developer Instructions (`DEVELOPER-INSTRUCTIONS.md`, `README.md`): move contributor/developer hygiene guidance off frontpage README into a dedicated developer instruction document, and add a formal preflight checklist plus implementation workflow for instrumentation, documentation parity, and code-change verification.

- Docs/README Workflow Pointer (`README.md`): add a contributor-visible development hygiene pointer to `.github/copilot-instructions.md` so code-hygiene and note-hygiene requirements are discoverable without opening workspace instruction internals first.

- Workflow/Hygiene Policy (`.github/copilot-instructions.md`): add an explicit code-hygiene + note-hygiene standard so future edits keep concise in-code intent comments near non-obvious logic while maintaining synchronized architecture/contract documentation in repo docs.

- Docs/Retroactive Runtime Audit (`esphome/spectra_ls_system/CODEBASE-RUNTIME-ARCHITECTURE.md`, `packages/ma_control_hub/CONTROL-HUB-ARCHITECTURE.md`, `esphome/spectra_ls_system/DEAD-PATHS-CLEANUP.md`, `.github/copilot-instructions.md`, `README.md`, `esphome/spectra_ls_system/CUSTOM-COMPONENT-ROADMAP.md`, `esphome/spectra_ls_system/v-next-NOTES.md`): add baseline architecture/feature documentation for active ESPHome runtime + MA control hub, publish dead-path/cleanup candidate matrix with risk tiers, and require ongoing functionality/feature documentation updates in future slices.

- Docs/Parallel Program Operating System (`esphome/spectra_ls_system/PARALLEL-PROGRAM-PLAYBOOK.md`, `.github/copilot-instructions.md`, `esphome/spectra_ls_system/CUSTOM-COMPONENT-ROADMAP.md`, `esphome/spectra_ls_system/v-next-NOTES.md`, `README.md`): add strict-but-flexible guardrails for running runtime + custom-component tracks in parallel, including slice templates, parity checkpoints, anti-detail-trap workflow, and mandatory roadmap/v-next/README synchronization protocol.

- Docs/Implementation Draft (`esphome/spectra_ls_system/CUSTOM-COMPONENT-ROADMAP.md`, `esphome/spectra_ls_system/v-next-NOTES.md`): add a concrete Phase-1 Slice-01 implementation draft (shadow-mode parity surfaces, acceptance criteria, file skeleton, and track-A/track-B disposition template) to make first-step execution deterministic.

- Workflow/Docs Parity Gate (`.github/copilot-instructions.md`): add an explicit README update step to the mandatory workflow so repo-state changes include README parity checks/updates before completion.

- Docs/Program Governance (`.github/copilot-instructions.md`, `esphome/spectra_ls_system/CUSTOM-COMPONENT-ROADMAP.md`, `esphome/spectra_ls_system/v-next-NOTES.md`): formalize a required parallel-development program for the new `custom_components/spectra_ls` track alongside the current HA package + ESPHome runtime, define phased migration/cutover gates, and add v-next alignment checkpoints so roadmap, contracts, and implementation status remain synchronized.

## 2026-04-18

- Workflow/Quality Gate Hardening (`.github/copilot-instructions.md`): add mandatory verification gates for ESPHome changes — compile/build must succeed **before** commit/push, OTA upload must complete before closing deployment tasks, and responses must include explicit evidence (`build result`, `OTA successful`, `HEAD==origin`) rather than assumptions.

- ESPHome/Lighting Compile Hotfix (`esphome/spectra_ls_system/packages/spectra-ls-lighting.yaml`): fix slider lambda scope error by recomputing `lighting_menu_active` within the value-processing lambda (where deferred-vs-immediate brightness apply is decided), restoring successful firmware compilation for the room-menu brightness UX refactor.

- ESPHome/Lighting Menu UX Refactor (`esphome/spectra_ls_system/packages/spectra-ls-lighting.yaml`, `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`): make lighting slider apply **room brightness directly** while browsing Lighting Rooms/Targets lists (no need to enter `All -> Lighting Values` first), and replace legacy `Lighting Values` labeling with reusable friendly labels (`Brightness <friendly name>`, `Hue <friendly name>`, `Sat <friendly name>`) across lighting-adjust menu rendering paths.

- Workflow/Git Push Cadence Policy (`.github/copilot-instructions.md`): add mandatory active-session checkpoint push cadence (at least every 10 minutes or on each completed logical slice, whichever comes first) so rollback points remain recent during iterative work.

- Repo Ignore-Scope Lockdown (`.gitignore`): stop re-including `blueprints/` and `custom_components/` and explicitly ignore both paths so environment-specific/HA-HACS artifacts are not tracked or uploaded to GitHub from this project repository.

- HA/Global Target Exclusion Filter (`packages/spectra_ls_lighting_hub.yaml`, `packages/ma_control_hub/script.inc`, `packages/ma_control_hub/template.inc`): add centralized `no-spectra` label exclusion so entities tagged with this label are never included in Spectra lighting catalogs, MA discovered/known target option lists, or control-target prompt target catalogs.

- ESPHome/Menu Boot First-Press Intercept Follow-up (`esphome/spectra_ls_system/packages/spectra-ls-ui.yaml`): fix remaining boot-only two-press path where auto control-target prompt could still consume first Select when menu was already active. Auto prompt is now dismissed/snoozed without consuming submenu entry; manual prompt behavior remains unchanged.

- ESPHome/OLED Word-Wrap Tail Fix (`esphome/spectra_ls_system/components/sls_oled_text_layout.h`): replace character-slice fallback behavior for non-fitting multi-word labels with word-preserving fallback + ellipsis on line 2, preventing orphan single-character second lines (for example `Conditione` / `r`) while keeping centered two-line rendering deterministic.

- ESPHome/Menu First-Press Boot Intercept Fix (`esphome/spectra_ls_system/packages/spectra-ls-ui.yaml`): resolve boot-only double-press submenu entry by prioritizing non-menu Select handling before auto control-target prompt consumption; first Select now enters submenu deterministically, while manual control-target prompt behavior remains intact.

- ESPHome/Menu UX One-Press Entry Fix (`esphome/spectra_ls_system/packages/spectra-ls-ui.yaml`): remove two-step submenu entry behavior by routing a non-menu Select press directly into a submenu (`Lighting Rooms` when in lighting screen, `Gear` when in audio screen) instead of consuming an initial press to land on root-menu state.

- ESPHome/Menu Directionality Root-Cause Correction (`esphome/spectra_ls_system/substitutions.yaml`): after full-stack sign trace (RP2040 encoder generation → UART packet decode → unified menu mapper), confirm RP path publishes raw encoder deltas without inversion and restore canonical top-level policy to `menu_encoder_nav_sign: "-1"` so left-turn no longer maps to downward movement.

- ESPHome/OLED Progress Bootstrap Hardening (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`): add local HA progress timestamp bootstrap when playback is active and duration is known but `ha_audio_pos_last_ms` is still `0` (for example no initial HA position-change event yet). This allows now-playing progress computation to arm and advance from cached/local elapsed time instead of staying permanently invalid.

- ESPHome/Menu Directionality Fix (`esphome/spectra_ls_system/substitutions.yaml`): set unified menu navigation direction policy to raw encoder sign (`menu_encoder_nav_sign: "1"`) so left-turn index motion is no longer inverted into downward movement.

- ESPHome/OLED Progress Bootstrap Fix (`esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`): initialize `ha_audio_pos_last_ms` on first received HA position sample (even when delta is flat) so now-playing progress logic can arm and project forward instead of remaining permanently invalid when position starts at `0`.

- ESPHome/OLED Progress Hold Fix (`esphome/spectra_ls_system/spectra-ls-peripherals.yaml`): fix static progress-bar behavior when HA position updates remain numerically flat (for example repeated `0`) by projecting from cached displayed position for the same track window while playback is active. This keeps OLED progress advancing deterministically from local elapsed time instead of repeatedly resetting to the non-advancing feed sample.

- ESPHome/Audio Transport Reliability Hardening (`esphome/spectra_ls_system/components/arylic_tcp.h`): make TCP send path resilient to transient network jitter by adding one immediate retry attempt per payload and deferring global backoff until repeated consecutive failures (instead of tripping backoff on a single timeout). This reduces brittle command drops during fast volume sweeps when occasional `connect() timeout` events occur.

- Tooling/OTA Safety Hardening (`bin/esphome_spectra_upload_local.sh`): uploader now rebuilds staged firmware by default before OTA (with `--no-build` escape hatch and `--clean-stage` passthrough), preventing silent stale-artifact uploads that can leave runtime behavior on old transport code despite recent source edits.

- ESPHome/Audio Transport Root-Cause Fix (`esphome/spectra_ls_system/components/arylic_tcp.h`): remove dequeue-time payload mutation for queued `VOL` sends (previously replacing popped payload with `recent->last_payload`) and replace it with deterministic superseded-item drop logic for volume commands only. This eliminates target/TX divergence (for example `Pot send ... target=N` while TX emits a different `VOL:M`) caused by mutable latest-payload rewrite race behavior.

- ESPHome/Audio Transport Anti-Replay Guard (`esphome/spectra_ls_system/components/arylic_tcp.h`): add queue-age protection for volume passthrough commands (`VOL`) so stale queued volume payloads are dropped instead of being transmitted late, and add explicit per-volume enqueue logging to make delayed-source attribution deterministic during ghost-volume investigations. **Shared-contract note:** applied to `spectra_ls_system` (`main`) now for live issue containment; `menu-only` (`esphome/control-py`) parity update is intentionally deferred and tracked as branch divergence until validated migration.

- ESPHome/Audio Tuning Rollback Test (`esphome/spectra_ls_system/substitutions.yaml`): re-apply recommended **Profile B+** control/tcp audio tunings (pot cadence + TCP pacing/coalescing/backoff) after user-local edits introduced lag/replay behavior, restoring known-good baseline values for A/B validation.

- ESPHome/Menu Navigation Direction Contract: Add shared top-level menu navigation helper (`components/sls_menu_nav.h`) and route menu-encoder delta handling through a global direction mapping (`menu_encoder_nav_sign`) in `spectra-ls-ui.yaml`, so clockwise/counterclockwise behavior is configured once and applied consistently across all menu levels/prompt lists.

- HA/Command-Line Resilience Fix: In `packages/spectra_ls_hw_status.yaml`, harden the `Spectra LS HW Status` command to guard for missing script/input files and emit a valid fallback JSON payload when unavailable, preventing recurring `command_line` return-code errors while preserving dependent attribute contracts.

- ESPHome/OLED Universal Title-Style Split Tuning: In `esphome/spectra_ls_system/components/sls_oled_text_layout.h`, refine two-line split scoring with capitalization-aware and connector-word penalties so labels avoid awkward breaks (for example ending line 1 with minor words like `of`, `the`, `in`, `to`) while preserving pixel-fit and center alignment across all OLED views.

- ESPHome/OLED Universal Label Layout Reset: In `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, remove the incremental line/centering helper stack and replace it with a single universal pixel-width-based centered wrap/fit method (shared by menu and lighting paths) so all label rendering uses the same deterministic split/truncate rules and avoids right-edge clipping across fonts.

- ESPHome/OLED Pixel-Width Header Wrap Fix: In `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, replace lighting-header-only character-count wrapping with font-pixel-width-aware split/fit logic and per-line width guards so long labels (including wider glyph endings) cannot clip at the right edge in centered two-line rendering.

- ESPHome/OLED Edge-Clipping Fix: In `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, tighten lighting-header wrap thresholds using display-width-based character limits to prevent right-edge clipping of long labels in both active and slider-only lighting screens.

- ESPHome/OLED Lighting Header Wrap Fix: In `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, update the active non-slider lighting header path to use centered two-line wrapping for long target labels and shift mode/brightness HUD elements down to avoid overlap.

- ESPHome/Universal Fallback Targets: In `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, replace room-specific fallback target construction with a universal target fallback set so menu behavior remains portable when HA option feeds are temporarily unavailable.

- ESPHome/OLED Text Fit Improvement: In `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, add centered two-line word-wrap rendering for long menu/prompt/lighting labels (instead of hard cutoff/ellipsis-only rendering) while keeping current font sizes unchanged; list row heights now expand only when wrapping is actually needed to preserve normal menu density for short labels.

- ESPHome/NP Responsiveness + Noise Tuning: In `esphome/spectra_ls_system/substitutions.yaml`, speed metadata refresh cadence (`ha_audio_refresh_ms` `6000→2000`, `meta_refresh_ms` `5000→1500`, `meta_refresh_tick_ms` `500→250`) to reduce new-track/play-start detection lag; also reduce periodic heartbeat status logging (`heartbeat_interval_ms` `60000→300000`). In `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, slow HA-facing control number republish cadence (`EQ Low/Mid/High`, `Arylic Volume Set` `60s→300s`). In `esphome/spectra_ls_system/packages/spectra-ls-diagnostics.yaml`, move passive diagnostics publishing from `60s→300s` (including CPU/heap/PSRAM/virtual diagnostics) to cut recurring `[S]` state spam.

- ESPHome/Runtime Noise Reduction (quiet by default): In `esphome/spectra_ls_system.yaml`, disable `web_server` state logging (`log: false`) to stop high-churn `[S][...]` state-stream output; in `esphome/spectra_ls_system/packages/spectra-ls-diagnostics.yaml`, disable CPU debug logging by default and raise passive diagnostic refresh intervals to 60s; in `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, raise HA-facing EQ/volume template number refresh intervals to 60s to reduce routine status chatter.

- ESPHome/Diagnostics Cleanup: Remove one-cycle `disp_diag` transition logger from `esphome/spectra_ls_system/spectra-ls-peripherals.yaml` after root-cause playback predicate hardening, reducing log noise without changing display-state behavior.

- ESPHome/HA Position Activity Debounce: In `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, only refresh `ha_audio_pos_last_ms` when `ha_audio_position` actually moves (>=0.05s delta), preventing unchanged paused-position updates from being misread as active playback evidence.

- ESPHome/Now-Playing Idle-Recency Clamp (follow-up): Refine `playing_effective` in `esphome/spectra_ls_system/spectra-ls-peripherals.yaml` so Arylic/LR recency and activity windows only contribute when corresponding sources are non-idle, and only refresh `last_playing_true_ms` under active-source conditions. This prevents idle metadata timestamp churn from holding `play=1`/NP state with empty content.

- ESPHome/Now-Playing Evidence Gate Fix (diagnostic-driven): Use one-cycle `disp_diag` findings to harden `playing_effective` in `esphome/spectra_ls_system/spectra-ls-peripherals.yaml` so HA `state=playing` only refreshes playback-hold timestamps when recent HA playback evidence exists (recent meta or position updates). This prevents stale upstream `playing` flags from pinning the idle display on Now Playing with empty content.

- ESPHome/Display Diagnostics (One-Cycle Instrumentation): Add always-on, rate-limited transition logging in `esphome/spectra_ls_system/spectra-ls-peripherals.yaml` (`compute_display_state`) for `display_state`, `no_audio_activity`, `menu_active`, `lighting_active`, and `screen_mode` so stuck-idle branch selection can be traced from a single reboot cycle.

- ESPHome/Now-Playing Predicate Hardening: In `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, tighten `playing_effective` recovery so Arylic/LR recency no longer revives playback when source is idle, and make the clear-down condition depend on active-source/activity signals rather than raw recency timestamps. This prevents idle no-content systems from being pinned on the Now Playing screen.

- ESPHome/UI Display-State Routing Fix: In `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, update `compute_display_state` decision ordering so `no_audio_activity` resolves directly to `STATE_BLANK` when menu and lighting-activity windows are not active, preventing idle home/target text screens from persisting when nothing is playing.

- ESPHome/UI Idle Blanking Root-Cause Fix: In `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, stop treating passive volume-pot polling as synthetic user activity by only updating `last_input_ms` when the mapped pot target actually changes; this prevents idle timers from being continuously reset by analog polling noise and allows proper blanking when nothing is playing.

## 2026-04-17

- ESPHome/UI Idle-Blank Safety Fix: In `esphome/spectra_ls_system/spectra-ls-peripherals.yaml`, change the unknown display-state fallback renderer to fail closed (blank screen) instead of drawing `${friendly_name}`/status text; in `packages/spectra-ls-system.yaml`, initialize `display_state` to blank by default so post-flash startup/compute race windows cannot surface the stale top-line `SPECTRA LS` header when nothing is playing.

- HA/MA Control-Hub Consolidation (template.inc, batch 11): Final 5+ dedupe sweep by adding shared scalar resolver attributes for now-playing (`resolved_position`, `resolved_duration`, `resolved_volume`, `resolved_friendly`) and MA-active (`resolved_duration`, `resolved_position`, `resolved_volume`) surfaces, then rewiring the corresponding sensors to consume shared attributes instead of repeating inline target/meta fallback blocks.

- HA/MA Control-Hub Consolidation (template.inc, batch 10): Execute a 5+ slice maintainability pass by (1) adding shared `resolved_app` on `sensor.ma_active_player_id_resolved` and rewiring `MA Active App`, and (2) normalizing remaining control-path `rooms_json` string parsing callsites to the dual-mode contract (trim+sentinel guard, `[`/`{` support, mapping extraction) across host/path/target/capability/ambiguity readers.

- HA/MA Control-Hub Dedupe (template.inc, batch 9): Add shared `resolved_artist` and `resolved_album` attributes on `sensor.now_playing_entity` and `sensor.ma_active_player_id_resolved`, then rewire `Now Playing Artist/Album` and `MA Active Artist/Album` sensors to consume those shared attributes; preserves existing fallback precedence while removing duplicated inline artist/album resolution blocks.

- HA/MA Control-Hub Dedupe (template.inc, batch 8): Add shared `resolved_title` attributes on `sensor.now_playing_entity` and `sensor.ma_active_player_id_resolved`, then rewire `Now Playing Title` and `MA Active Title` sensors to consume those shared attributes; preserves existing URL/queue suppression and fallback ordering while removing duplicated inline title-resolution blocks.

- HA/MA Control-Hub Dedupe (template.inc, batch 7): Add shared resolved source attributes on `sensor.now_playing_entity` and `sensor.ma_active_player_id_resolved` (candidate filtering + queue/placeholder guards + media_player friendly-name normalization), then rewire `Now Playing Source` and `MA Active Source` to consume those shared resolved values instead of duplicating inline candidate-reduction loops.

- HA/MA Control-Hub Dedupe (template.inc, batch 6): Add shared `player_json` + `player_fields_json` attributes on `sensor.now_playing_entity` and rewire `Now Playing Title/Artist/Album/Source/State` to consume the shared fields payload instead of repeating per-sensor MA player lookup loops; also align `packages/ma_control_hub/script.inc` string JSON guard to accept both `[` and `{` payload starts for mapping-compatible room parser behavior.

- HA/MA Control-Hub Dedupe (template.inc, batch 5): Add shared `player_fields_json` projection on `sensor.ma_active_player_id_resolved` and rewire `MA Active Title/Artist/Album/App/Source` to consume that shared payload instead of repeating per-sensor `player_json` parse/field extraction blocks; also normalize `packages/ma_control_hub/script.inc` room JSON string parse branch to use trimmed payload + sentinel guard consistency.

- DevTools + HA/MA Control-Hub Quality (batch 4): In `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` Iteration Workbench 1A and command-effect templates, replace manual `expected_value` dependency with action-aligned effect expectation (`expected_effect_value`) derived from a single probe action value input (with safe default to entity `min`) to eliminate false WARN deltas from mismatched expected/probe payloads; in `packages/ma_control_hub/template.inc`, add shared `player_json` lookup payload on `sensor.ma_active_player_id_resolved` and reuse it across `MA Active Title/Artist/Album/App/Source` to dedupe repeated `lookup_id/short_id/players/ns.p` loops without behavior drift.

- HA/MA Control-Hub Correctness + Quality (template.inc, batch 3): Fix high-risk self-reference in `sensor.spectra_ls_rooms_json` state parser to source room data from `sensor.spectra_ls_rooms_raw.rooms` (authoritative input) instead of self-reading `sensor.spectra_ls_rooms_json.rooms_json`; additionally normalize `MA Meta Resolver` guarded string parse to use `raw_trim | from_json`, switch `MA Detected Receiver Entity` to normalized `sensor.spectra_ls_rooms_json.rooms_json`, and remove dead locals (`_mp`, `appletv`) that were not consumed.

- HA/MA Control-Hub Code Quality (template.inc, batch 2): Continue parser-contract cleanup in `packages/ma_control_hub/template.inc` by (1) routing additional room consumers (`MA Detected Receiver Entity`, `MA Active Meta Entity`) to normalized `sensor.spectra_ls_rooms_json.rooms_json` payloads instead of re-reading raw rooms attributes, and (2) standardizing guarded string JSON parsing in touched branches to parse `raw_trim` values.

- HA/MA Control-Hub Code Quality (template.inc): Refactor `sensor.ma_meta_candidates` attribute derivation in `packages/ma_control_hub/template.inc` to use a shared parsed summary payload (`candidate_summary_json`) for entities/names/labels/scores/best-candidate outputs, reducing duplicated parser branches and improving maintainability; also normalize guarded JSON parse inputs to use trimmed payloads in touched parser branches.

- Docs/DevTools Batched Refactor (3 slices): Update `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` to (1) route Iteration Workbench 1A and Template #2 room-map parsing through normalized `sensor.spectra_ls_rooms_json.rooms_json` payloads instead of re-parsing `sensor.spectra_ls_rooms_raw` inline, and (2) replace broad `states.number` scans in Templates #3 and #5 with explicit Spectra audio control candidate lists to reduce listener noise and render churn.

- HA/MA Meta Candidates Refactor: Consolidate repeated candidate scan loops in `packages/ma_control_hub/template.inc` by introducing a shared candidate-row payload attribute for `sensor.ma_meta_candidates` and deriving summary attributes from that shared structure, reducing drift and repeated high-cost template passes.

- Docs/DevTools 1A Noise Reduction: Tune `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` Iteration Workbench (Template 1A) command-readiness scan to use a targeted audio-control number candidate list instead of broad `states.number` domain enumeration, reducing irrelevant listener wakeups while keeping expected Spectra control-surface probes intact.

- HA/MA Meta Candidates Dedupe: Reduce duplicated scoring loops in `packages/ma_control_hub/template.inc` by introducing a shared `best_candidate_json` attribute for `sensor.ma_meta_candidates` and routing `best_entity`/`best_score` through that shared payload.

- HA/MA Script Parser Unification: Update `packages/ma_control_hub/script.inc` (`ma_update_target_options`) to consume normalized `sensor.spectra_ls_rooms_json` payload (`rooms_json`) for room-target option derivation, aligning script-side parsing with template-side helper refactors and reducing contract drift.

- HA/MA Parser Helper Refactor: Reduce duplicated `rooms` parser blocks in `packages/ma_control_hub/template.inc` by routing key control-path sensors (`ma_control_hosts`, `ma_active_control_path`, `ma_control_targets` + attributes, `ma_active_target_by_host`, `ma_active_control_capable`, `ma_control_ambiguous`) through normalized `sensor.spectra_ls_rooms_json` payload ingestion.

- HA/MA Refactor (Meta Resolver): Reduce parser drift in `packages/ma_control_hub/template.inc` by introducing a shared `best_candidate_json` attribute in `sensor.ma_meta_resolver` and routing both `best_entity` and `best_score` through it instead of maintaining duplicate `candidates_json` parse/scan blocks.

- Docs/DevTools Iteration Workbench: Add a unified single-paste `1→4` diagnostics template (`1A`) to `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` that consolidates health, routing path, command readiness, and command-effect checks into one iterative operator workflow with compact roll-up status and inline probe payload suggestions.

- HA/Now-Playing Source Stability (ma_control_hub): Harden `packages/ma_control_hub/template.inc` source resolvers (`Now Playing Source`, `MA Active Source`) to select the first valid non-placeholder, non-queue candidate in deterministic order (with media_player entity-id friendly-name resolution and final friendly-name fallback), preventing intermittent blank `src` output while target/host/meta are otherwise healthy.

- ESPHome/Runtime Cleanup (Code-Side): Remove unreferenced high-frequency internal template sensors from `esphome/spectra_ls_system/packages/spectra-ls-lighting.yaml` (`lighting_hue_state`, `lighting_sat_state`, `lighting_brightness_state`, `selected_room_state`, `selected_target_state`) to eliminate unnecessary 500ms polling work with no behavioral contract impact.

- Docs/DevTools Phase-4 Template Modernization: Update `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` Template #2 and Template #14 to use the full dual-mode payload contract (native sequence/mapping + guarded string JSON parse) with trimmed lowercase sentinel checks (`raw_trim_l`) for deterministic parser behavior.

- Docs/DevTools Iteration-4 Accuracy Fix: Update `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` Template #16 to detect virtual battery status using HA `sensor`-domain entities (not `text_sensor` domain) and soften verdict semantics so idle/no-recent-probe activity reports `WARN` rather than hard `FAIL` when core virtual entities are present.

- Docs/DevTools Hotfix: Repair Jinja syntax corruption in `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` Template #16 (`Iteration 4 Diagnostics Chatter Check`) by removing stray `endif/endfor` mismatches and leaked lines from adjacent templates, restoring valid render behavior in HA Developer Tools.

- Docs/DevTools Diagnostics Hardening: Update `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` Iteration 4 chatter template virtual-entity discovery to tolerate prefixed `friendly_name` variants (substring match) and reorder tail templates into strict `13 → 14 → 15 → 16` numeric sequence for predictable navigation.

- Docs/DevTools Ordering: Reorder `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` template sections into strict numeric sequence so the tail block is now `13 → 14 → 15 → 16` (eliminates out-of-order `16/14/15/13` layout).

- Diagnostics/Template Fix (Iteration 4): Harden `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` Template #16 entity resolution to use `friendly_name` matching with explicit entity-id fallbacks, preventing false FAIL reports where virtual diagnostics exist but `name`-based lookup returns empty IDs.

- ESPHome/Diagnostics Iteration 4: Reduce virtual-input diagnostics chatter in `esphome/spectra_ls_system/packages/spectra-ls-diagnostics.yaml` by slowing passive publish cadence for virtual mode/control/status entities (no behavior-path changes), and add an Iteration 4 DT verification template in `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` focused on interaction visibility with lower idle spam.

- HA/MA Parser Guard Determinism (Iteration 2): Tighten complex-payload string parser guards in `packages/ma_control_hub/template.inc` and `packages/ma_control_hub/script.inc` to evaluate sentinel checks against trimmed lowercase payloads, reducing whitespace/case-induced drift while preserving dual-mode sequence/mapping behavior.

- HA/MA Full Parser Modernization (Iteration): Expand `packages/ma_control_hub/template.inc` rooms/candidate readers to fully honor the dual-mode contract across remaining call sites (native sequence, native mapping with canonical key extraction, and guarded string JSON parse) and add an iteration DT validation template in `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` to verify parser-shape compatibility after each modernization pass.

- HA/MA Contract Hardening: Update `packages/ma_control_hub/template.inc` and `packages/ma_control_hub/script.inc` rooms/candidate readers to a dual-mode parser contract (native sequence payloads accepted directly; string JSON payloads parsed only after trim+guard checks), preventing audio routing/meta/control surfaces from collapsing when Home Assistant exposes attribute payloads as non-string objects.

- HA/Lighting Native Payload Parse Fix: Update `packages/spectra_ls_lighting_hub.yaml` catalog consumers to accept both string JSON arrays and native sequence payloads from `items_json`, preventing false-empty parses when Home Assistant surfaces `state_attr(..., 'items_json')` as a non-string object.

- HA/Lighting JSON Parse Hardening: Make `packages/spectra_ls_lighting_hub.yaml` catalog/options JSON parsing whitespace-tolerant (`| trim` before `[` checks and parse) across template sensors and sync automations, preventing valid JSON payloads with leading newline/space from being treated as empty and collapsing selectors to `No Rooms` / `All`.

- HA/Lighting Catalog Fallback Hardening: Add area-registry (`areas()` + `area_entities()`) fallback population path inside `sensor.control_board_eligible_light_catalog` so room/target helper surfaces recover even when primary `states.light`/`area_id(...)` pass yields empty results. This prevents persistent `No Rooms`/`All` selector collapse after reload when catalog bootstrap is unexpectedly empty.

- HA/Lighting Catalog Payload Overflow Fix: Move `sensor.control_board_eligible_light_catalog` payload from sensor state into attribute-backed JSON (`items_json`) and switch dependent templates to read attribute-first with state fallback. This prevents long JSON truncation/parse collapse that manifested as `control_board_room_options = ["No Rooms"]` and `control_board_target_options = ["All"]` despite healthy entities.

- HA/Lighting Catalog-First Target Resolution Fix: Refactor `packages/spectra_ls_lighting_hub.yaml` room/target contract surfaces to resolve from a shared eligible-light catalog built from `states.light` + `area_id(...)`, then drive `room_options`, `target_options`, `room_area_id`, `target_entity_id`, `room_hs`, and `room_on` from that catalog. This removes dependence on direct per-template `area_entities(...)` scans and restores per-room target population where selectors previously degraded to `All`-only.

- HA/Lighting Area-Selection Scope Fix: Correct Jinja loop-scope behavior in `packages/spectra_ls_lighting_hub.yaml` by replacing plain `area_id_selected` loop assignments with `namespace(area_id=...)` in room-dependent target/HS/on templates. This restores per-room light discovery so populated rooms no longer show `All`-only targets.

- HA/Lighting Template Runtime Fix: Correct malformed Jinja block closures in `packages/spectra_ls_lighting_hub.yaml` (`Control Board Target Options` and `Control Board Target Entity ID`) where `endif` incorrectly closed `for ent in lights` loops; this was driving `sensor.control_board_room_options`/`target_options` contract dependents into `unavailable` and collapsing room UI back to static fallback labels.

- Diagnostics Hardening: Update `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` Template #6 (Lighting Rename Validation) to treat placeholder-only room options (`No Rooms`/`Unknown`) as **FAIL** and `All`-only target options as degraded (**WARN**), preventing false PASS reports when room population is actually broken.

- HA/Lighting Room Source-of-Truth Fix: Switch `packages/spectra_ls_lighting_hub.yaml` room/target derivation to HA area registry as primary contract (`areas()` + `area_entities()`), including `room_options`, `room_area_id`, `target_options`, `target_entity_id`, `room_hs`, and `room_on`, decoupling visible room population from eligible-light catalog filtering.

- HA/Lighting Fallback Policy Correction: Remove `Unassigned` all-lights fallback from `sensor.control_board_eligible_light_catalog` and switch empty room fallback to `No Rooms`; add placeholder-only guard in room sync to preserve existing concrete room options. This prevents repeated `Unassigned + Back` loops from masking real area contract issues.

- HA/Lighting Startup Stabilization: Remove transient availability gating from `sensor.control_board_eligible_light_catalog` and add anti-downgrade guard in room sync so `input_select.control_board_room` is not overwritten by `['Unassigned']` when concrete room options already exist; addresses recurring `Unassigned + Back` regressions after reload/boot churn.

- Hotfix: Correct `packages/spectra_ls_lighting_hub.yaml` Jinja block terminator in `sensor.control_board_eligible_light_catalog` (`endif` vs accidental `endfor`) to restore template loading; this unblocks room/target option generation that had been failing at HA config parse time.

- HA/Lighting Regression Correction: Restore `packages/spectra_ls_lighting_hub.yaml` eligible-light catalog to area-first discovery (`areas()` + `area_entities()`), with all-lights fallback only when area-derived results are empty; fixes unintended "Unassigned-only" room output introduced by prior all-lights-first refactor.

- MA Control Targets Boot Fallback: Harden `packages/ma_control_hub/template.inc` so `sensor.ma_control_targets` falls back to `input_select.ma_active_target` options when `sensor.spectra_ls_rooms_raw` is not yet populated at boot/reload, preventing blank control-target prompt/options during startup race windows.

- HA/Lighting Selectability Regression Fix: Update `packages/spectra_ls_lighting_hub.yaml` sync automations to stop preserving stale room/target option values in `input_select` options, and force-reset to valid parsed options when contracts repopulate; resolves "Select Control Target" lock state with no selectable targets.

- HA/Lighting Room Options Root-Cause Fix: Rework `sensor.control_board_eligible_light_catalog` in `packages/spectra_ls_lighting_hub.yaml` to derive candidates from all `light.*` entities (including unassigned-area lights) instead of area-only enumeration, preventing `['Unknown']` option collapse that forced ESP OLED room-menu fallback labels (`Room 1/2/3/4`).

- Diagnostics/Template Fix: Correct `Source Truth Test` false-negative behavior in `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` by replacing brittle core sentinel failure logic (`sensor.time`/`sun.sun`) with a core entity-count heuristic and by switching source probes to realistic exposed entities; prevents misleading "HA degraded" conclusions when control-hub contracts are the actual fault domain.

- HA/Lighting Control-Hub Recovery: Broaden `sensor.control_board_eligible_light_catalog` eligibility in `packages/spectra_ls_lighting_hub.yaml` to include on/off and legacy light entities (while still excluding speaker-like pseudo-lights), preventing false-empty room/target catalogs that caused ESP menus to fall back to generic room labels.

- ESPHome/Source Status UX Hardening: Normalize source placeholders and surface explicit fallback status (`HA not accessible`) in OLED source resolution when all source feeds are empty/unavailable, removing ambiguous blank/unknown source states.

- ESPHome/HA Option Intake Resilience: Add dual-source room/target option feeds (`input_select` attribute options + catalog sensors) and prefer non-empty parsed lists for OLED menu rendering and UI count refresh paths, preventing transient helper-state gaps from collapsing lighting menus to static fallback room labels.

- ESPHome/Now-Playing Architecture Hardening: Remove source-only activity from playback truth in `spectra-ls-peripherals.yaml` (no longer promotes passive source labels into active playback), tighten display hold to real playing/recent-playing signals, and clear source fallback caches when hold is inactive so idle state resolves to true blank.

- ESPHome/Option Parser Contract Hardening: Apply placeholder-value filtering (`unknown`, `unavailable`, `none`, `null`, `idle`) to duplicated dynamic option parsers in `spectra-ls-lighting.yaml` so HA transient states cannot contaminate room/target selector mapping paths.

- ESPHome/Lighting Menu Resilience Fix: Sanitize dynamic option parsing in `spectra-ls-peripherals.yaml` so placeholder HA values (`unknown`, `unavailable`, `none`, `null`, `idle`) are dropped instead of rendered as real menu entries; Lighting Rooms now correctly falls back to configured room labels when HA options are temporarily unavailable.

- ESPHome/OLED Idle-State Fix: Prevent stale source labels (for example `YOUTUBE`) from holding Now Playing when no active playback exists by tightening display-active gating to playing/recent-media signals and clearing source-fallback fields when media display hold is inactive.

- ESPHome/Display Contract Refactor: Move display-state IDs to shared substitutions and replace local enum literals in `spectra-ls-peripherals.yaml` state computation/render paths, keeping display mode semantics centralized and reusable across package evolution.

- ESPHome/Nav Constants Refactor: Add shared menu-count/fallback-count substitutions and replace active hardcoded menu counts/branch sizes in UI, hardware nav handlers, and OLED menu rendering paths to reduce cross-file drift and make menu evolution centrally tunable.

- HA/Lighting Architecture Refactor: Introduce a reusable `sensor.control_board_eligible_light_catalog` contract in `packages/spectra_ls_lighting_hub.yaml` and route room options, target options, room area resolution, target entity resolution, room HS, and room on/off state through that single eligibility source to prevent layered filtering drift.

- Workflow/Instruction Update: Extend `.github/copilot-instructions.md` with an explicit architecture-first directive for this workspace: treat bug reports as signals to improve reusable design and avoid "just make it work" patch layering.

- HA/Lighting Target Hygiene: Harden `packages/spectra_ls_lighting_hub.yaml` target derivation to exclude speaker-like pseudo-light entities (name/entity contains `speaker`) while retaining domain-light routing, preventing entries like `Kitchen speakers` from appearing in lighting target menus.

- HA/Lighting Contract Fix: Enforce strict light-only target options in `packages/spectra_ls_lighting_hub.yaml` by removing stale-current option carryover from target sync, preventing non-light entries (for example speaker labels) from persisting in `input_select.control_board_target`.

- ESPHome/UI UX Fix: Add direct one-click entry from non-menu lighting screen into Lighting Rooms menu in `spectra-ls-ui.yaml` so opening lighting menu no longer requires an extra select press.

- ESPHome/UI Navigation Fix: Eliminate boot-time ghost root-menu rendering that was visually presenting menu tiles without an active menu state, causing dead-end-feeling navigation and hidden menu semantics.

- ESPHome/UI Menu Fix: Force boot/root menu rendering to the icon-tile root level so fallback/boot states no longer show literal `Lighting / Audio / Gear` list text; those labels are internal root-category semantics behind icon navigation.

- ESPHome/UI Regression Fix: Remove implicit top-level menu re-entry after lighting-adjust hold and playback-stop transitions so the UI no longer auto-shows literal `Lighting / Audio / Gear` tiles unless the menu is explicitly opened by user input.

- ESPHome/UI Architecture: Replace hardcoded menu-level magic numbers in active lighting/UI control paths with named menu-level constants from substitutions to reduce transition fragility and improve v-next maintainability.

- ESPHome/Lighting Architecture: Centralize lighting value-adjust transition flow into a single script path (`start_lighting_value_adjust`) to eliminate duplicated menu-to-adjust state writes and reduce transition drift risk as v-next menu behavior expands.

- ESPHome/Lighting UX Flow: Rework lighting value adjustment behavior so Hue/Saturation (and lighting adjust paths) do not get pre-empted by active playback; after adjustment activity ends, UI returns to the same lighting menu context, holds for 5000ms, then gracefully transitions to Home (no active playback) or Now Playing (when active).

- ESPHome/Lighting Hardening: Make UI auto-routing hardware-mode-aware in `spectra-ls-ui.yaml` so audio-driven auto-flip behavior no longer overrides explicit lighting selector modes (`hardware_mode` 0/1), improving hardware-first determinism for v-next.

- ESPHome/Lighting Hardening: Add structural guardrails for v-next stability by removing hardcoded lighting hold timing (new substitution), sanitizing slider selection index handling in room/target menu contexts, and gating HA light service dispatch when neither target entity nor area routing is available.

- ESPHome/Lighting Audit Fix: Correct display-state gating so lighting render windows honor `hue_ring_active_until_ms` (not only brightness/audio windows), preventing Hue/Saturation lighting views from dropping back to the main/menu screen during active adjustments.

- ESPHome/Lighting Audit Fix: Harden slider input routing in `spectra-ls-lighting.yaml` so slider movement exits stale non-lighting menu states and applies as lighting input, while still preserving room/target selection behavior when explicitly in Room/Target menu levels.

- ESPHome/Lighting Audit Fix: Add a consistent temporary lighting hold window in `focus_lighting` so encoder/button/slider interactions all keep `screen_mode=0` long enough to prevent immediate auto-flip back to audio while playback is active.

- Docs/README: Tighten `Digital Audio Ingest + Final DAC Path` intro wording and add an explicit plain-language positioning line: “In plain terms: a DAC for your home.”

- Docs/README: Re-group Inputs feature copy into explicit **Lighting Controls** and **Audio Controls** blocks, and replace the awkward controls-example heading with a concise **Controls** section label for cleaner launch-page flow.

- Docs/README: Final editorial launch-page pass — enforce one-time `Spectra Level / Source (Spectra L/S)` introduction and use `Spectra L/S` thereafter, while tightening opening narrative flow for a cohesive front-to-back product voice.

- Docs/README: Rework non-spec sections into product-outcome language (what users feel and can do) by removing file-path/architecture-heavy prose from system interaction and feature sections; keep detailed technical capability data centered in specs/cutsheet areas.

- Docs/README: Replace implementation-heavy input inventory wording with outcome-focused product copy; explicitly call out direct lighting Brightness/Hue/Saturation control over rooms or individual lights and keep the Inputs section centered on what users can do (not internal mechanics).

- Docs/README + v-next NOTES: Add promotional hands-on input examples (including full transport controls: Play/Pause, Next, Back/Previous), strengthen product-forward copy tone, and add a formal v-next crossfade/balance slider requirement: in multi-room mode, slider shifts volume balance between rooms; in single-room mode, slider controls speaker balance.

- Docs/README: Replace dry input inventory copy with concrete control behavior notes for the lighting slider (brightness mapping + debounced sends), room switching (cycle + input_select sync), and the implemented multi-room transition/crossfade path via `script.control_board_set_light_dynamic` transition support.

- Docs/README: Add an explicit `HD DAC Capability Cutsheet` block (wireless stack, multi-room behavior, music-service list, and local-source support) and add plain-language whole-home multi-speaker synchronization copy (same-track room lock + independent room playback) without protocol-insider wording.

- Docs/README: Normalize shorthand usage to `Spectra L/S` (no standalone `L/S`), change EQ phrasing to `Physical 3-Band EQ`, move Seesaw under an explicit Inputs section, and expand physical-control listing to call out switches/buttons/knobs/sliders/dials.

- Docs/README: Add explicit direct analog-input control wording for Volume + 3-band EQ, document automatic Light/Sound interaction behavior when Home Assistant already knows targets, and include UP2STREAM HD DAC audio-side capability notes (dual-band Wi-Fi, AirPlay 2, Spotify Connect, TIDAL Connect, aptX HD Bluetooth 5.0, no-amp module class) sourced from published product metadata.

- Docs/README: Expand front-page intended-use messaging to explicitly call out direct analog control for Volume + 3-band EQ (bass/mid/treble), home dance-party usage, v-next multiple switches/dials expansion, and responsive screen/menu navigation feedback.

- Docs/README: Link `Home Assistant` at its first paragraph mention and de-link later duplicate `Home Assistant` link usage so external-linking follows first-mention-only style.

- Docs/README: Reframe the Digital Audio Ingest section around intended day-to-day use (coffee-table/home digital-to-analog control hub, couch/desk operation) and remove HDMI/ARC hyperlinking in that section while keeping capability statements intact.

- Docs/README: Remove the literal “audio nerd” phrasing in the Digital Audio Ingest + Final DAC Path section and replace the multi-bullet codec list with a single concise `Supports: FLAC, MP3, AAC, AAC+, ALAC, APE, WAV.` line.

- Docs/README: Set front-page title and first project mention to **Spectra Level / Source** (with immediate **L/S** shorthand), normalize remaining visible **Spectra LS** wording to **L/S**, and remove the dedicated external-links block including the dead `n4archive` reference in favor of inline linked hardware/ecosystem mentions.

- Docs/README: Convert front-page hardware/audio ecosystem mentions to inline manufacturer/project URLs (MCUs, input ICs, control interface ecosystems, HDMI/ARC reference, ESS ES9038Q2M, and OLED controller references) so readers can jump directly from each mention to its canonical project/vendor page.

- Docs/README: Normalize product naming in front-page audio section to introduce **Level / Source** once and use **L/S** shorthand thereafter while preserving published ARC/DAC capability details.

- Docs/README: Strengthen front-page hardware/audio positioning to describe Spectra Level / Source as a fully integrated high-fidelity source-to-output system (not a DAC-only claim), while retaining the explicit ARC ingest → ESS ES9038Q2M conversion path and published 192kHz/24-bit + codec capability references.

- Docs/Hardware Notes: Document HDMI ARC-capable source ingest path (HDMI input → ARC digital audio split/extract → final ESS ES9038Q2M DAC stage) including 192kHz/24-bit target capability and codec support references (`FLAC`, `MP3`, `AAC`, `AAC+`, `ALAC`, `APE`, `WAV`) in project notes/README.

- ESPHome/Diagnostics: Add a temporary virtual-input harness in `esphome/spectra_ls_system/packages/spectra-ls-diagnostics.yaml` with mode/control injectors, mode-nav test buttons, and a one-shot battery script so selector/control-class/menu flows can be validated in HA without physical RP2040 wiring changes.
- RP2040/Phase B: Fix v-next selector/control-class event fidelity by emitting IDs `120` and `121` as analog packets (index payload preserved) instead of button packets (boolean collapse), updated in both live `CIRCUITPY/code.py` and mirror `esphome/circuitpy/code.py`.
- ESPHome/Phase C: Add initial `120`–`124` event consumers in `esphome/spectra_ls_system/packages/spectra-ls-hardware.yaml` and new shared state globals in `packages/spectra-ls-system.yaml` (`hardware_mode`, `control_class`, `menu_override_active`) with deterministic mode routing and clear-on-hardware-change menu override behavior.

- Tooling/Build: Harden `bin/esphome_spectra_build_local.sh` + `bin/esphome_spectra_upload_local.sh` for fast iteration with incremental stage sync, stable local artifact copies, build summary output, optional forced clean stage, and upload helper auto-use of latest staged config.
- ESPHome/OTA: Migrate `esphome/spectra_ls_system.yaml` from legacy `web_server.ota: true` to OTA platform syntax (`ota: - platform: web_server`) for ESPHome 2026.4.0+ compatibility while preserving web UI upload support.
- Tooling/Build: Add local workstation automation scripts under `bin/` for one-command Spectra LS build kickoff (staging copy + path rewrite + compile artifact reporting) with optional OTA upload, so fast Ryzen-side builds avoid HA VM/Celeron compile bottlenecks without modifying tracked runtime YAML.
- ESPHome/Web UI: Enable `web_server` OTA exposure in `esphome/spectra_ls_system.yaml` (`web_server.ota: true`) so the built-in device web UI renders the firmware upload form (POST `/update`) for direct binary uploads.
- UI/Code Cleanup: Deduplicate OLED display lambda option parsing helpers in `esphome/spectra_ls_system/spectra-ls-peripherals.yaml` by reusing a single shared `trim_ascii`/`parse_options` implementation for both control-target prompt and dynamic options lists (no behavior change).
- UI/Code Cleanup: Remove redundant/unreachable second `STATE_NOW_PLAYING` render branch in `esphome/spectra_ls_system/spectra-ls-peripherals.yaml` display lambda and drop unused local state variable, reducing ambiguity in display-state flow.
- UI/Splash Stability: Fix startup splash sequence restart in `esphome/spectra_ls_system/spectra-ls-peripherals.yaml` by preventing delayed `on_boot` splash timer reinitialization after the display loop has already started splash timing.
- UI/Splash Readability: Keep the “SPECTRA” title line visible throughout splash stage 2 (subtitle reveal), removing the abrupt title disappearance while preserving existing splash timing.
- HA/MA Boot Auto-Select Fix: Update `packages/ma_control_hub/script.inc` so startup default target selection includes discovered targets (not only static room targets), preventing `none` from persisting when discoverable control-capable targets exist.
- HA/MA Startup UX: Add fallback auto-pick of first active (playing/paused) allowed target when detected receiver is not ready yet, reducing blank-screen/prompt-on-boot behavior while discovery data settles.
- Docs/Architecture Notes: Expand `esphome/spectra_ls_system/v-next-NOTES.md` with a dedicated control-path/hardware-family roadmap section, including current as-wired implementation snapshot and phased family/codepath expansion plan.
- HA/MA Override Schema: Extend room/target override contract to carry `hardware_family`, `control_path`, `control_capable`, and `capabilities` metadata so overrides can fully represent control routing intent for current and future device families.
- HA/MA Capability Routing: Gate control-host resolution on `control_capable` + routed `control_path` (currently `linkplay_tcp`) so direct POT adjustments are sent only to explicitly control-capable targets.
- HA/MA Routing Policy: Add discovery-first control-path routing in `packages/ma_control_hub/template.inc` with explicit per-target tagging (`linkplay_tcp` now, extensible for future non-LinkPlay paths) and expose active control path as a template sensor.
- HA/MA Fallback Safety: Add `input_boolean.ma_control_fallback_enabled` (default `off`) in `packages/ma_control_hub/input_boolean.inc` and gate static host override/fallback routing branches behind this tunable for clean no-override testing by default.
- HA/MA Target Discovery: Tighten discovered target inclusion in `packages/ma_control_hub/script.inc` to include only linkplay-compatible TCP targets by default, while preserving extensibility for future routing classes.
- Project Guidance: Update `.github/copilot-instructions.md` and `esphome/spectra_ls_system/v-next-NOTES.md` to codify discovery-first onboarding, default-off fallback policy, and per-device control-path decision requirements.
- HA/MA Auto-Discovery: Enhance `packages/ma_control_hub/script.inc` and `packages/ma_control_hub/template.inc` so MA-discovered players with valid entity IDs and IP addresses are treated as first-class targets/options and TCP host candidates, reducing dependency on static per-room host mappings for plug-in onboarding.
- HA/MA Routing: In `sensor.ma_control_hosts`, prefer active target entity `ip_address` when available before static fallback mappings, enabling zero-touch control host resolution for newly discovered compatible players.
- ESPHome/Config Hygiene: Clarify Arylic endpoint section in `esphome/spectra_ls_system/substitutions.yaml` to document HA runtime authority (`sensor.ma_control_hosts` / `sensor.ma_control_port`) and switch bootstrap endpoint defaults to neutral non-site-specific values instead of repo-pinned local hosts.
- ESPHome/Config Hygiene: Reorganize `esphome/spectra_ls_system/substitutions.yaml` tunables into distinct domain sections (system cadence, display/menu UI, audio UX/prompting, lighting UX, and control-loop/log cadence) to remove mixed random grouping and improve operator readability.
- Docs: Update `esphome/spectra_ls_system/SUBSTITUTIONS-TUNING-LEGEND.md` with a substitutions layout map so tuning guidance matches the new sectioned structure.
- ESPHome/Tuning Policy: Adopt B+ as default in `esphome/spectra_ls_system/substitutions.yaml` based on observed low-latency/high-quality network behavior with occasional jitter spikes; reserve B++ as DJ-candidate profile pending extended soak validation.
- ESPHome/Tuning Surface: Add next-batch substitution tunables for non-transport smoothing in `esphome/spectra_ls_system/substitutions.yaml` (control-target evaluation cadence, MA target sync interval, meta-cycle settle delay, input/log throttle intervals) and wire these into `packages/spectra-ls-audio-tcp.yaml` to remove hardcoded timing constants.
- Docs: Update `esphome/spectra_ls_system/SUBSTITUTIONS-TUNING-LEGEND.md` with network-quality guidance, B+ default positioning, and B++ DJ-candidate profile notes.
- ESPHome/Audio UX: Add reboot ambiguity grace handling in `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml` so a valid MA target + control host can establish lock during early boot even when transient ambiguity sensors report true, preventing repeated post-reboot control-target prompts.
- ESPHome/Tuning: Apply a tighter B++ responsiveness profile in `esphome/spectra_ls_system/substitutions.yaml` (pot intervals/settle and Arylic TCP pacing/burst/worker tuning) for reduced perceived control lag while preserving transport stability margins.
- ESPHome/Substitutions: Prune non-spectra dead keys from `esphome/spectra_ls_system/substitutions.yaml` (legacy BLE presence + lighting-controller carryovers and other unreferenced runtime leftovers) and expand section comments into operator-oriented guidance (purpose, gains, risks, and tuning intent).
- ESPHome/Audio TCP: Add substitution-driven tunables for Arylic TCP worker/log behavior (queue length, worker stack/priority, and TX log throttle) and wire them through build flags into `components/arylic_tcp.h` so they can be tuned without C++ edits.
- ESPHome/Audio TCP: Wire Arylic TCP pacing constants to compile-time substitution-driven build flags from `esphome/spectra_ls_system/substitutions.yaml` (including newly exposed burst-guard knobs), so tuning values are actually applied without editing `arylic_tcp.h` defaults.
- Docs: Add `esphome/spectra_ls_system/SUBSTITUTIONS-TUNING-LEGEND.md` with baseline profile sets (Baseline/Safe/Performance/Aggressive), parameter descriptions, and staged test guidance for per-network tuning.
- ESPHome/Audio TCP: Consolidate duplicated script-level EQ gate/send logic by introducing shared `arylic_set_eq_channel` in `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml`, with existing `arylic_set_bass/mid/treble` IDs retained as thin compatibility wrappers.
- ESPHome/Audio TCP: Consolidate duplicated EQ pot update/defer logic in `esphome/spectra_ls_system/packages/spectra-ls-audio-tcp.yaml` into a shared channel-aware script path to reduce repeated hot-path code while preserving per-channel behavior and deferred-send semantics.
- Docs: Update `README.md` with explicit external reference links (ESPHome, Home Assistant, Music Assistant, and Arylic/LinkPlay API references) for easier operator onboarding.
- ESPHome/Audio TCP: Add reversible Arylic TCP burst guard in `esphome/spectra_ls_system/components/arylic_tcp.h` to pace sustained command bursts per host/port via a short sliding-window delay (delay/coalesce behavior, no protocol changes) to reduce control-lane chatter while preserving responsiveness.
- ESPHome: Update `esphome/spectra_ls_system/substitutions.yaml` `friendly_name` to remove reserved URL path separator (`/`) and avoid 2026.7.0 validation failure.
- RP2040/Architecture: Pass 3B.1 removes dead legacy inline autocalibration/tracking functions and globals from `code.py` now that `sls_calibration_runtime.py` is authoritative, reducing monolith drift and keeping loop behavior manager-driven.
- RP2040/Docs+Architecture: Add per-file RP ownership/instruction contracts at top of all RP firmware source files (`boot.py`, `code.py`, `sls_*.py`) and add a detailed RP legend document on-device (`CIRCUITPY`) with mirrored repo copy; begin Pass 3B by extracting autocalibration/tracking state machine responsibilities toward a dedicated runtime module to reduce monolith drift risk.
- RP2040/Architecture: Pass 3 introduces reusable runtime helper functions for button-edge emit/log flow and precomputed analog channel profiles to reduce repeated hot-path lookups/branches while preserving packet format and behavioral semantics.
- RP2040/Architecture: Pass 2 modularization extracts analog/calibration helper functions from `code.py` into `sls_analog.py` and `sls_calibration.py` (live `CIRCUITPY` + repo mirror) while preserving runtime loop behavior and packet contract.
- RP2040/Architecture: Begin v-next-safe modularization of firmware by extracting config/protocol/input-decode helpers from monolithic `code.py` into dedicated modules in live `CIRCUITPY/` with synchronized mirror under `esphome/circuitpy/`; preserve existing packet contract and runtime behavior.
- RP2040/Phase B: Add hardware input-capture contract in `CIRCUITPY/code.py` + mirror `esphome/circuitpy/code.py` for v-next reserved events (`120`–`124`) including mode selector, control-class selector, and mode-navigation momentary event emits; preserve existing event IDs and behavior while adding parallel v-next event path.
- Productization: de-track `spectra_ls_primary_tcp_host.yaml` and `spectra_ls_room_tcp_host.yaml` from git/GitHub while keeping local files in place; host defaults now use secrets-based values and these per-user endpoint files are ignored to prevent personal config pollution.
- Docs/Policy: add explicit universal-product directive in `.github/copilot-instructions.md` forbidding tracked per-user host/IP config artifacts.
- Docs/Policy: Add a required pre-flight checklist to `.github/copilot-instructions.md` for any file move/delete operation; session must show backup path, planned `CHANGELOG.md` line, and restore command/path before execution.
- Docs/Policy: Strengthen `.github/copilot-instructions.md` with mandatory file move/delete governance — deletes now require non-repo backup + changelog entry + restore path, and moves/renames must be changelogged with source/destination paths.
- Repo/ESPHome: Rename `esphome/secrets.example.yaml` to `esphome/secrets.example` so the secrets template is not treated as an ESPHome dashboard project.
- ESPHome: Phase 16 rename step on `spectra_ls_system` path — rename final active package includes from `control-board-audio-tcp.yaml` and `control-board-lighting.yaml` to `spectra-ls-audio-tcp.yaml` and `spectra-ls-lighting.yaml`, and repoint `esphome/spectra_ls_system.yaml` include map/comments.
- HA: Harden `sensor.now_playing_state` in `packages/ma_control_hub/template.inc` to avoid propagating transient `unknown/unavailable` from `sensor.now_playing_entity`; fallback now resolves to `sensor.ma_active_state` (or `idle`) for stable rename-step validation and routing diagnostics.
- Docs: Normalize section numbering/order in `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` so rename validation checks run in ascending sequence (9 → 10 → 11).
- ESPHome: Phase 15 rename step on `spectra_ls_system` path — rename system package include from `packages/control-board-system.yaml` to `packages/spectra-ls-system.yaml` and repoint `esphome/spectra_ls_system.yaml` include.
- ESPHome: Phase 14 rename step on `spectra_ls_system` path — rename UI package include from `packages/control-board-ui.yaml` to `packages/spectra-ls-ui.yaml` and repoint `esphome/spectra_ls_system.yaml` include.
- Docs: Add a dedicated `ma_control_hub` package-loader regression template to `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` for fast PASS/WARN/FAIL checks in HA Developer Tools after reload/restart.
- Repo: Add regression guardrails for `ma_control_hub` split package layout by ignoring `packages/ma_control_hub/*.yaml` (allow only `.inc`) and adding `.github/validate-ma-control-hub-layout.sh` to fail fast on duplicate fragments or missing host include files.
- HA: Fix `ma_control_hub` package loader conflicts by removing duplicate `packages/ma_control_hub/*.yaml` split fragments (keep `.inc` only) so `!include_dir_named packages` does not auto-load sub-fragments as standalone packages.
- HA: Restore missing `/config/spectra_ls_primary_tcp_host.yaml` and `/config/spectra_ls_room_tcp_host.yaml` include files used by `packages/ma_control_hub/input_text.inc` to resolve startup config load errors.
- ESPHome: Phase 13 rename step on `spectra_ls_system` path — rename hardware package include from `packages/control-board-hardware.yaml` to `packages/spectra-ls-hardware.yaml` and repoint `esphome/spectra_ls_system.yaml` include.
- ESPHome: Phase 12 rename step on `spectra_ls_system` path — rename active peripherals include from `control-board-peripherals-no-rings.yaml` to `spectra-ls-peripherals.yaml` and repoint `esphome/spectra_ls_system.yaml` include.
- Repo: Stop syncing archived `previous/` trees to GitHub by ignoring `esphome/control-py/previous/` and `esphome/spectra_ls_system/previous/`.
- ESPHome: Move legacy/stale package files out of active paths into `previous/` (non-runtime archive storage).
- Repo: Sync `esphome/spectra_ls_system/` content to `main` so GitHub reflects the active spectra_ls_system project tree.

## 2026-04-16

- ESPHome: Phase 11 rename step on tracked main path — rename archived rings peripherals file from `esphome/control-py/previous/control-board-peripherals.yaml` to `esphome/control-py/previous/spectra-ls-peripherals-rings-legacy.yaml` (archive-only, not in active includes).
- ESPHome: Phase 10 rename step on tracked main path — rename legacy non-TCP audio package file from `esphome/control-py/packages/control-board-audio.yaml` to `esphome/control-py/packages/spectra-ls-audio-legacy.yaml` (legacy/stale, TCP package remains authoritative).
- ESPHome: Phase 9 rename step on tracked main path — rename legacy headless package file from `control-board-headless.yaml` to `spectra-ls-headless.yaml` (no active include references).
- ESPHome: Phase 8 rename step on tracked main path — rename active peripherals include from `spectra-ls-peripherals-no-rings.yaml` to `spectra-ls-peripherals.yaml` and repoint `esphome/control-board-esp32-tcp.yaml` include.
- Docs: Update rename validation wording in `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` from “Peripherals No-Rings” to generic “Peripherals” for the new filename.
- ESPHome: Phase 7 rename step on tracked main path — rename no-rings peripherals file to `spectra-ls-peripherals-no-rings.yaml` and repoint `esphome/control-board-esp32-tcp.yaml` include.
- Docs: Add targeted post-rename HA reload validation template (peripherals no-rings step) to `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` for per-step spectra rename checks.
- ESPHome: Phase 6 rename step on tracked main path — rename hardware package to `spectra-ls-hardware.yaml` and repoint `esphome/control-board-esp32-tcp.yaml` include.
- Docs: Add targeted post-rename HA reload validation template (hardware package step) to `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` for per-step spectra rename checks.
- ESPHome: Phase 5 rename step on tracked main path — rename UI package to `spectra-ls-ui.yaml` and repoint `esphome/control-board-esp32-tcp.yaml` include.
- Docs: Add targeted post-rename HA reload validation template (UI package step) to `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` for per-step spectra rename checks.
- ESPHome: Phase 4 rename step on tracked main path — rename system package to `spectra-ls-system.yaml` and repoint `esphome/control-board-esp32-tcp.yaml` include.
- Docs: Add targeted post-rename HA reload validation template (system package step) to `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` for per-step spectra rename checks.
- ESPHome: Phase 3 rename step on tracked main path — rename audio TCP package to `spectra-ls-audio-tcp.yaml` and repoint `esphome/control-board-esp32-tcp.yaml` include.
- Docs: Add targeted post-rename HA reload validation template (audio package step) to `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` for per-step spectra rename checks.
- Docs: Add targeted post-rename HA reload validation template (lighting package step) to `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` for per-step spectra rename checks.
- ESPHome: Phase 2 rename step on tracked main path — rename lighting package to `spectra-ls-lighting.yaml` and repoint `esphome/control-board-esp32-tcp.yaml` include.
- Docs: Publish `esphome/spectra_ls_system/DEVTOOLS-TEMPLATES.local.md` to `main` and add README high-level system-test overview for Templates 1–5 smoke/diagnostic workflow.
- HA: Prevent `ma_control_hub` split fragments from being misloaded as standalone packages by renaming `packages/ma_control_hub/*.yaml` to `*.inc` and updating `packages/ma_control_hub.yaml` includes.
- HA: Restore `packages/ma_control_hub.yaml` and all split files from `menu-only` branch for parity, and revert temporary absolute include-root rewrite.
- HA: Fix `packages/ma_control_hub.yaml` include roots to `/config/packages/ma_control_hub/*` so YAML reload resolves split package files correctly.
- ESPHome: Phase 1 rename step on tracked main path — rename active diagnostics package to `spectra-ls-diagnostics.yaml` and repoint `esphome/control-board-esp32-tcp.yaml` include.
- Docs: README description now calls out room/lighting/audio auto-discovery and minimal user configuration expectations.
- Repo/Docs: Stop tracking root `configuration.yaml`; switch to placeholder-based integration via `SPECTRA-HA-CONFIG-PLACEHOLDERS.md` for adding Spectra-specific lines into an existing Home Assistant config.
- Docs: Add bold README project-state banner (active heavy development + publish date) and a current hardware reference section covering MCUs, expanders, control interfaces, and recommended OLED screen profile.
- Docs: Declare `esphome/spectra_ls_system/v-next-NOTES.md` as the `main` branch direction source and require `README.md` alignment to that hardware-first roadmap.
- Docs: Update README elevator pitch to plain-English whole-home audio + lighting focus and add explicit audio clients/player types section (Music Assistant, HA media_player, WiiM integration, Arylic/LinkPlay, AirPlay/Apple TV, Plex optional).
- Docs: Rewrite README project-intent intro to emphasize whole-home audio + lighting control and simplify system interaction language.
- Docs: Add mandatory shared-contract go/no-go checklist (`.github/SHARED-CONTRACT-CHECKLIST.md`) and wire it into branch runbook + Copilot directives.
- Docs: Add top-level repository `README.md` with project intent, architecture interaction summary, key features, and detailed deploy instructions.
- ESPHome: Reduce diagnostics and control-number publish cadence to 30s (`EQ Low/Mid/High`, `Arylic Volume Set`, `OLED Contrast`, heap/PSRAM diagnostics) and simplify CPU reporting to a single `CPU Usage` sensor (disable per-core sensors and top-task logs).
- ESPHome: Reduce number state publish spam by throttling template number update intervals (`EQ Low/Mid/High`, `Arylic Volume Set`) from 500ms to 5s and `OLED Contrast` from 1s to 10s.
- ESPHome: Canonicalize secrets structure to `esphome/secrets.yaml` and remove duplicate per-project `secrets.yaml` under `esphome/spectra_ls_system/`.
- Repo: Add tracked `esphome/secrets.example.yaml` template while keeping real secret files ignored.
- Repo: Establish concurrent dual-branch operations with dedicated `menu-only` worktree under `.worktrees/menu-only`.
- Docs: Add filesystem safety gate to prevent destructive live-path moves/deletes without reversible snapshot and verification.
- Repo: Tighten HA tracking scope to Spectra project docs/packages/ESPHome paths and stop tracking generic HA root runtime files.

## 2026-04-15

- Repo: Enforce RP2040 source-of-truth policy (live `CIRCUITPY/` + single repo mirror `esphome/circuitpy/`) and remove stale duplicate `boot.py`/`code.py` copies from other ESPHome paths.
- Docs: Tighten Copilot instructions for senior/public-product standards, latest HA/ESPHome API diligence, and mandatory dual-sync RP2040 edits (live `CIRCUITPY/` + `esphome/circuitpy/`).
- Repo: Migrate to top-level `/mnt/homeassistant` Git root and import `esphome` history under `esphome/` with preserved commit history.
- Repo: Track core HA root files on `main` (`configuration.yaml`, `automations.yaml`, `scripts.yaml`, `scenes.yaml`, and `packages/`).
- Docs: Add dual-branch shared-contract merge gate (`main` + `menu-only`) with required paired update or explicit divergence note.

## 2026-04-14

- HA: Explicitly enable the template integration to ensure package template sensors load.
- HA: Explicitly enable the command_line integration for Spectra LS HW status polling.
- HA: Remove unsupported from_json default parameter in Spectra LS rooms template to ensure sensor loads.
- HA: Reduce false control-target ambiguity when now-playing clearly matches the active target.
- ESPHome: Add v.next self-contained `spectra_ls_system/` project and repoint `spectra_ls_system.yaml` to use it (substitutions, packages, components, and assets).
- ESPHome: Rename v.next peripherals file to `spectra-ls-peripherals.yaml` (drop legacy naming).

## 2026-04-13

- ESPHome: Repoint Control Board v2 entrypoint to control-py-scoped substitutions and components so the config is self-contained when duplicating control-py.
- ESPHome: Consolidate control-board dependencies into control-py (components and fonts), keeping root entrypoint in place.
- ESPHome: Fix rp2040_uart header path after moving components into control-py.
- ESPHome: Use MA active sensors directly for now-playing metadata to restore artist/source fields.
- HA: Add unified now-playing meta resolver with normalized scoring across MA/HA sources.
- HA: Harden meta resolver Plex allowlist defaults and debug candidate output handling.
- HA: Split ma_control_hub into logical include files for maintainability.
- HA: Gate kitchen meta override behind MA meta confidence so strong detections win.
- HA: Prefer active meta when it is playing/paused or fresh to avoid stale now-playing selection.
- HA: Use media_player is_playing/is_paused/play_state attributes to determine active meta and reduce stale picks.
- ESPHome: Add animated boot splash for Spectra L \ S lettering.
- Docs: Update Copilot instructions to avoid editing rings peripherals file.
- ESPHome: Add audio control gate logging + fallback to ma_control_host when ma_control_hosts is empty (volume/EQ TCP sends).
- ESPHome: Keep last valid control host(s) when active target is valid (avoid transient clears on HA flaps).
- ESPHome: Apply manual_ip from substitutions so Control Board stays on the IoT subnet.
- ESPHome: Temporarily pin `wifi.use_address` to 192.168.30.246 for Control Board v2 (OTA/API target only).
- ESPHome: Switch Control Board v2 Wi-Fi secrets to non-IoT keys and remove temporary use_address pin.
- Repo: Restore tracking of `esphome/circuitpy/` (authoritative RP2040 firmware mirror), while ignoring `control-py/previous/circuitpy/`.
- Repo: Track HA package `packages/ma_control_hub.yaml`.

## 2026-04-12

- ESPHome: Stop forced HA update_entity calls for MA control host sensors to avoid template AssertionError spam.
- HA: Prefer active target for now-playing entity when target is playing with metadata to avoid stale AirPlay meta on OLED.
- ESPHome: Add control-target prompt grace window around playback/ambiguity transitions to prevent popups during track switches.
- ESPHome: Add Gear menu (Meta Select + Control Select) and manual Control Target prompt entry.
- ESPHome: Label control-target popup “Control Targets”.
- RP2040/Docs: Add dedicated Select button on PCF8575 (event 37) and mirror select input to menus.
- ESPHome: Harden control-target prompt parsing (escaped JSON), add prompt cancel/empty handling, and align menu activation index.
- ESPHome: Debounce control-target prompts, add Control Target popup label, and hold last valid TCP hosts during transient HA updates.
- ESPHome: Reduce Control Target popup highlight top overlap (2px padding).
- HA/ESPHome: Harden lighting target label → entity mapping and debounce slider brightness sends to avoid bursts.
- ESPHome: Lighting slider no longer pushes brightness while in menu mode; brightness mode auto-exits after inactivity and on menu entry.
- ESPHome: Deduplicate lighting HS/color apply logic (shared script).
- ESPHome: Remove unused lighting HS wrapper script (use shared apply path).
- ESPHome: Add control-target prompt cooldown to avoid repeated prompts on volume touches when hosts/ambiguity are unstable.
- ESPHome: Keep control-target selection sticky until the play/control source changes; suppress re-prompts while locked.
- ESPHome: Apply sticky control-target lock guard across EQ/volume audio control paths.
- ESPHome: Keep sticky control-target lock from clearing on empty/unknown source keys (only clear on confirmed source change).
- ESPHome: Ignore invalid control host updates while sticky lock is active to prevent prompt flapping.
- ESPHome: Adopt first valid source key into sticky lock to prevent initial lock drop.
- ESPHome: Fix EQ pot rounding so full range reaches -10..10.
- ESPHome: Block control-target prompt immediately on selection to avoid double-press.
- ESPHome: Centralize control-target prompt gating and keep lock sticky until user explicitly reopens the selector.
- Docs: Added detailed plan for ADS7830 + 5‑position control‑target rotary switch reorg (no implementation yet).

## 2026-04-07

- HA: Refactored MA meta/control hub for dynamic meta detection, receiver matching, overrides, and low‑confidence handling.
- HA: Meta candidates now filtered to active (playing/paused) sources and respect Plex allow‑lists plus legacy local session filter.
- HA: Added MA/HA labeling for meta candidates (`MA:` / `HA:`) to disambiguate origins.
- HA: Active target options now include the detected receiver when not already present.
- HA: Added universal TV/HDMI → Spectra auto‑switch package (`spectra_ls_tv_source_auto.yaml`).
- ESPHome: Added Meta menu (low‑confidence chooser) and HA override calls for manual selection.
- ESPHome: Meta menu header renamed to “Meta Player”.
- ESPHome: Suppressed EQ overlay during boot to avoid brief EQ flash.
- ESPHome: UI render path no longer mutates menu timing; last input timestamp now initialized at boot.
- ESPHome: Moved stale `control-board-peripherals.yaml` to `previous/` to keep it out of active builds.
- ESPHome: Tightened now-playing gating so stale metadata doesn’t revive the screen when nothing is playing.
- Docs: Updated control‑board notes to use “Meta player” terminology for override behavior.

## 2026-04-09

- HA: MA Active Friendly now prefers host-mapped/target entity (removes meta-driven label flips).
- ESPHome: Volume pot now mirrors EQ handling (interval/settle/jump deferral + 50ms deferred send loop).

## 2026-04-10

- ESPHome: Centralized Control Board v2 substitutions into `substitutions.yaml` and updated control-board configs to include it.
- ESPHome: Adjusted splash screen layout (bottom-justified subtitle) and reduced splash font sizes to fit OLED.
- ESPHome: Restored `presence.yaml` to its standalone substitutions (kept separate project unchanged).
- ESPHome: Removed HA volume fallback in OLED render path (TCP volume only).
- ESPHome: Nudged splash title down 5px for improved vertical balance.
- ESPHome: Suppressed EQ overlay immediately after splash unless EQ changes post-splash.
- ESPHome: Fixed splash EQ gating compile error (avoid id() shadowing).
- ESPHome: Updated boot splash stages (Stage 1: SPECTRA + "L \ S"; Stage 2: "Level \ Source"; SPECTRA hides after 1s in Stage 2).
- ESPHome: Added missing `eq_overlay_ignore_until_ms` global for display state gating.
- ESPHome: Fixed duplicate `now` declarations in audio TCP lambdas that caused build failures.
- Repo: Added workspace-level Copilot instructions for Home Assistant + ESPHome guidance.
- Repo: Expanded Copilot instructions with additional directives and reference links.
- Repo: Documented CIRCUITPY RP2040 firmware paths in Copilot instructions references.
- Docs: Corrected Control Board v2 notes (RP2040 hardware, TCP-only control path, and ADC channel mapping) and added explicit update gate.
- HA: Treat AirPlay/Apple TV sources as control-target ambiguous to force prompt selection.
- HA: Harden MA selection templates (guard room JSON parsing; define primary Spectra entity in now-playing templates).
- ESPHome: Treat AirPlay/Apple TV sources as ambiguous locally and clear stale control hosts on invalid updates to ensure popup triggers.
- HA: Add Apple TV entity allowlist for ambiguity (ensures prompt even when app/source is non-AirPlay).
- ESPHome: Reset control target prompt after popup expiry so repeated hardware interactions re-trigger the prompt.
- ESPHome: Keep control target prompt visible until ambiguity clears (no timeout when selection required).
- ESPHome: Trigger target prompt immediately when ambiguity turns on (no waiting for hardware input).
- ESPHome/HA: Add control-target selection list wiring (entities + menu selection via encoder).
- HA: Autodetect receiver now prefers a playing room entity from `spectra_ls_rooms_raw` before heuristic scoring.
- HA: Auto-select bypasses idle delay when a detected receiver is actively playing.
- HA: Added UART-over-TCP hardware play-state polling across rooms (`spectra_ls_hw_status`) using `PINFGET`.
- HA: Detected receiver now prefers hardware playing entity when available.
- HA: HW status polling is now on-demand (now-playing triggers + 15s active refresh) instead of constant 5s polling.
- HA: HW polling now treats AirPlay as playing when progress advances (mode-based + cached deltas).
- Docs: Updated Control Board v2 wiring/input ID map to match current ESPHome button/encoder/analog IDs.
- Docs: Updated RP2040 PCF8575 pin map and button list to match current CircuitPython firmware.
- Docs: Reordered control-py notes to surface pin maps near the top and refreshed deploy steps for current firmware.
- Docs: Re-read and refreshed control-board-2 notes with current TCP control path and reality check.
- ESPHome: Added debug logging for progress-bar drops and HA position/duration updates to trace meta gating.
- Hybrid: Move position smoothing to ESPHome (local timer between HA updates) and keep HA position sensors lightweight.
- Cleanup: Remove temporary HA/meta progress debug logging after hybrid stabilization.
- ESPHome: Decouple progress bar source from meta slots (prefer HA progress when active; fall back to arylic).
- ESPHome: CPU usage component refactor (single-source header, logging controls + tag) and diagnostic logging toggle.
- Audit: Confirmed `control-board-peripherals-no-rings.yaml` in `esphome/control-py/` is complete (not empty).
- ESPHome: Fix control-target prompt rendering and selection handling in UI/menu paths.
- HA/ESPHome: Remove Apple TV ambiguity detection (AirPlay-only prompt).

## 2026-04-11

- ESPHome: Fix OLED interval lambda missing time base (`now`) compilation error.
- ESPHome: Fix control-target prompt encoder navigation (use labels list for count).
- ESPHome: Use menu encoder center press as Select input.
- RP2040/Docs: Remove bass/treble encoder references (EQ is pot-only).
- ESPHome: Auto-dismiss control-target prompt once control hosts are valid and ambiguity clears; track manual prompts to avoid auto-close.

## 2026-04-06

- ESPHome/RP2040: removed calibration UI/autocal/tracking flows; RP2040 continues to apply `/calibration.json`.
- ESPHome: simplified menu to Lighting/Audio only; TCP audio package is authoritative (non-TCP marked legacy).
- Docs: consolidated to a single control-board-2 notes file.
- ESPHome: EQ display now renders 3-band vertical bars (L/M/H) to match the three physical EQ pots.
- ESPHome: EQ overlay layout refined (slimmer rows, adjusted label spacing/positioning).
- RP2040: EQ pots now snap near edges to guarantee full min/max sweep.
- RP2040: enabled EQ pot auto-capture (tracking) to save min/max to `/calibration.json`.
- ESPHome: EQ bars no longer render fully filled at 0; draw outline with center fill only.
- ESPHome: fixed EQ endpoint rounding so 0% maps to full negative dB (no truncation).
- ESPHome: stabilized Now Playing against brief HA/meta dropouts (no blanking flicker).
- ESPHome: EQ overlay bars thickened (3px slider tick, taller container).
- ESPHome: volume pot pickup/catch to prevent HA/MA volume replay overriding the knob.
- ESPHome: extended Now Playing hold window to avoid periodic blanking when HA state flaps.
- HA: MA auto-select now ignores missing media_player entities to avoid forced update failures.
- HA: delay MA target refresh on startup and skip auto-select until MA players are available.
- ESPHome: Settings menu brightness slider now points at OLED contrast control.
- HA: auto-select can switch immediately on boot/invalid target while retaining debounce for normal changes.
- HA: Control Board view now targets live MA selector and OLED contrast entity.
- ESPHome: expose OLED contrast number to HA for brightness control.
- HA: MA auto-select now reacts to player/target changes and polls faster to cut startup lag.
- RP2040: add idle noise gate for volume pot to prevent phantom volume drift.
- HA: persist and restore last valid MA target to prevent target loss and volume dropouts.
- RP2040: require multiple consistent volume pot readings before sending changes.
- ESPHome/HA: volume controls are now read-only (ignore HA/MA volume writes; hide HA slider).
- ESPHome: removed volume pot catch/hold logic (input handling is RP2040-only).
- HA: disabled MA balance volume writes to keep volume read-only.
- HA: added volume_set watcher to log and notify any HA volume writes to MA targets.
- HA: volume_set watcher now logs HA context (user_id/parent_id/origin) for faster source tracing.
- HA: paused MA auto-select time-pattern loop to stop 5s focus flapping during volume debug.
- HA: auto-select no longer switches away when current target is active (reduces target churn).
- HA: auto-select action now uses script.turn_on to fix unknown action error.
- HA: added watcher for MA volume state changes (with context) to trace non-HA volume shifts.
- HA: now-playing/meta entity pinned to active target to prevent cross-room stale metadata.
- HA: now-playing entity selection prefers the player with real metadata and most recent updates (reduces AirPlay/Spectra blanks).
- HA: MA Active Title now falls back to Now Playing Title/Source/Friendly to avoid OLED blanks when MA metadata is empty.
- HA: Spectra LS meta entity now matches its playback entity to keep MA Active metadata populated.
- HA: Replaced AirPlay-specific meta heuristics with dynamic meta detection and receiver matching.
- HA: Refactored meta/control architecture with dynamic meta detection, receiver detection, meta override, and low-confidence indicator; removed debug volume watcher automations.

## 2026-03-25

- ESPHome: Added EQ full-screen slider view (9-band, 75% adjacent) and mirrored it to both `control-board-peripherals-no-rings.yaml` and `control-board-peripherals.yaml`.
- ESPHome: Added MA control host fallback (`sensor.ma_control_host`) when `ma_control_hosts` is empty; refreshes both sensors periodically.
- ESPHome: Arylic TCP send coalescing to reduce burst flooding while keeping pot responsiveness (latest payload wins, skip immediate duplicates).

## 2026-03-24

- HA: Split MA control hub into focused package files for maintainability.
- ESPHome: Increased Arylic TCP timeout and added ADS read safeguards/recovery in RP2040 input path.
- ESPHome: Added EQ screen overlay features and display state improvements to prevent Now Playing blanking.
